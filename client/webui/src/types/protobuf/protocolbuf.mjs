/*eslint-disable block-scoped-var, id-length, no-control-regex, no-magic-numbers, no-mixed-operators, no-prototype-builtins, no-redeclare, no-shadow, no-var, sort-vars, default-case, jsdoc/require-param*/
import $protobuf from "protobufjs/minimal.js";

// Common aliases
const $Reader = $protobuf.Reader, $Writer = $protobuf.Writer, $util = $protobuf.util;
const $Object = $util.global.Object, $undefined = $util.global.undefined, $Error = $util.global.Error, $TypeError = $util.global.TypeError, $Array = $util.global.Array, $String = $util.global.String, $Boolean = $util.global.Boolean, $parseInt = $util.global.parseInt, $BigInt = $util.global.BigInt, $Number = $util.global.Number, $isFinite = $util.global.isFinite;

// Exported root namespace
const $root = $protobuf.roots["default"] || ($protobuf.roots["default"] = {});

/**
 * PB_Attr enum.
 * @name PB_Attr
 * @enum {number}
 * @property {number} ATTR_MAX_NONE=0 ATTR_MAX_NONE value
 * @property {number} ATTR_MAX_MANA=-2 ATTR_MAX_MANA value
 * @property {number} ATTR_MAX_HEALTH=-1 ATTR_MAX_HEALTH value
 * @property {number} ATTR_HEALTH=1 ATTR_HEALTH value
 * @property {number} ATTR_MANA=2 ATTR_MANA value
 * @property {number} ATTR_STRENGTH=3 ATTR_STRENGTH value
 * @property {number} ATTR_AGILITY=4 ATTR_AGILITY value
 * @property {number} ATTR_INTELLIGENCE=5 ATTR_INTELLIGENCE value
 * @property {number} ATTR_VITALITY=6 ATTR_VITALITY value
 * @property {number} ATTR_LUCK=7 ATTR_LUCK value
 * @property {number} ATTR_ARMOR=8 ATTR_ARMOR value
 * @property {number} ATTR_CRIT_CHANCE=100 ATTR_CRIT_CHANCE value
 * @property {number} ATTR_CRIT_DAMAGE=101 ATTR_CRIT_DAMAGE value
 * @property {number} ATTR_ATTACK_SPEED=102 ATTR_ATTACK_SPEED value
 * @property {number} ATTR_DODGE_CHANCE=103 ATTR_DODGE_CHANCE value
 * @property {number} ATTR_BLOCK_CHANCE=104 ATTR_BLOCK_CHANCE value
 * @property {number} ATTR_DAMAGE_MULTIPLIER=105 ATTR_DAMAGE_MULTIPLIER value
 */
export const PB_Attr = $root.PB_Attr = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "ATTR_MAX_NONE"] = 0;
    values[valuesById[-2] = "ATTR_MAX_MANA"] = -2;
    values[valuesById[-1] = "ATTR_MAX_HEALTH"] = -1;
    values[valuesById[1] = "ATTR_HEALTH"] = 1;
    values[valuesById[2] = "ATTR_MANA"] = 2;
    values[valuesById[3] = "ATTR_STRENGTH"] = 3;
    values[valuesById[4] = "ATTR_AGILITY"] = 4;
    values[valuesById[5] = "ATTR_INTELLIGENCE"] = 5;
    values[valuesById[6] = "ATTR_VITALITY"] = 6;
    values[valuesById[7] = "ATTR_LUCK"] = 7;
    values[valuesById[8] = "ATTR_ARMOR"] = 8;
    values[valuesById[100] = "ATTR_CRIT_CHANCE"] = 100;
    values[valuesById[101] = "ATTR_CRIT_DAMAGE"] = 101;
    values[valuesById[102] = "ATTR_ATTACK_SPEED"] = 102;
    values[valuesById[103] = "ATTR_DODGE_CHANCE"] = 103;
    values[valuesById[104] = "ATTR_BLOCK_CHANCE"] = 104;
    values[valuesById[105] = "ATTR_DAMAGE_MULTIPLIER"] = 105;
    return values;
})();

/**
 * BattleEventType enum.
 * @name BattleEventType
 * @enum {number}
 * @property {number} BATTLE_EVENT_TYPE_NONE=0 BATTLE_EVENT_TYPE_NONE value
 * @property {number} BATTLE_EVENT_TYPE_BATTLE=1 BATTLE_EVENT_TYPE_BATTLE value
 * @property {number} BATTLE_EVENT_TYPE_TURN=10 BATTLE_EVENT_TYPE_TURN value
 * @property {number} BATTLE_EVENT_TYPE_ABILITY_ACTIVATE=20 BATTLE_EVENT_TYPE_ABILITY_ACTIVATE value
 * @property {number} BATTLE_EVENT_TYPE_DAMAGE=30 BATTLE_EVENT_TYPE_DAMAGE value
 * @property {number} BATTLE_EVENT_TYPE_HEAL=31 BATTLE_EVENT_TYPE_HEAL value
 * @property {number} BATTLE_EVENT_TYPE_EFFECT_APPLIED=40 BATTLE_EVENT_TYPE_EFFECT_APPLIED value
 * @property {number} BATTLE_EVENT_TYPE_EFFECT_REMOVED=41 BATTLE_EVENT_TYPE_EFFECT_REMOVED value
 * @property {number} BATTLE_EVENT_TYPE_EFFECT_TICK=42 BATTLE_EVENT_TYPE_EFFECT_TICK value
 * @property {number} BATTLE_EVENT_TYPE_DEATH=50 BATTLE_EVENT_TYPE_DEATH value
 * @property {number} BATTLE_EVENT_TYPE_STATUS_CHANGE=60 BATTLE_EVENT_TYPE_STATUS_CHANGE value
 * @property {number} BATTLE_EVENT_TYPE_ATTR_CHANGE=61 BATTLE_EVENT_TYPE_ATTR_CHANGE value
 * @property {number} BATTLE_EVENT_TYPE_CUSTOM=100 BATTLE_EVENT_TYPE_CUSTOM value
 */
export const BattleEventType = $root.BattleEventType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "BATTLE_EVENT_TYPE_NONE"] = 0;
    values[valuesById[1] = "BATTLE_EVENT_TYPE_BATTLE"] = 1;
    values[valuesById[10] = "BATTLE_EVENT_TYPE_TURN"] = 10;
    values[valuesById[20] = "BATTLE_EVENT_TYPE_ABILITY_ACTIVATE"] = 20;
    values[valuesById[30] = "BATTLE_EVENT_TYPE_DAMAGE"] = 30;
    values[valuesById[31] = "BATTLE_EVENT_TYPE_HEAL"] = 31;
    values[valuesById[40] = "BATTLE_EVENT_TYPE_EFFECT_APPLIED"] = 40;
    values[valuesById[41] = "BATTLE_EVENT_TYPE_EFFECT_REMOVED"] = 41;
    values[valuesById[42] = "BATTLE_EVENT_TYPE_EFFECT_TICK"] = 42;
    values[valuesById[50] = "BATTLE_EVENT_TYPE_DEATH"] = 50;
    values[valuesById[60] = "BATTLE_EVENT_TYPE_STATUS_CHANGE"] = 60;
    values[valuesById[61] = "BATTLE_EVENT_TYPE_ATTR_CHANGE"] = 61;
    values[valuesById[100] = "BATTLE_EVENT_TYPE_CUSTOM"] = 100;
    return values;
})();

/**
 * PB_BattleMode enum.
 * @name PB_BattleMode
 * @enum {number}
 * @property {number} BATTLE_MODE_NONE=0 BATTLE_MODE_NONE value
 * @property {number} BATTLE_MODE_TURN_BASED=1 BATTLE_MODE_TURN_BASED value
 * @property {number} BATTLE_MODE_REALTIME=2 BATTLE_MODE_REALTIME value
 */
export const PB_BattleMode = $root.PB_BattleMode = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "BATTLE_MODE_NONE"] = 0;
    values[valuesById[1] = "BATTLE_MODE_TURN_BASED"] = 1;
    values[valuesById[2] = "BATTLE_MODE_REALTIME"] = 2;
    return values;
})();

/**
 * PB_PlayerType enum.
 * @name PB_PlayerType
 * @enum {number}
 * @property {number} PLAYER_TYPE_NONE=0 PLAYER_TYPE_NONE value
 * @property {number} PLAYER_TYPE_HERO=1 PLAYER_TYPE_HERO value
 * @property {number} PLAYER_TYPE_MONSTER=2 PLAYER_TYPE_MONSTER value
 */
export const PB_PlayerType = $root.PB_PlayerType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "PLAYER_TYPE_NONE"] = 0;
    values[valuesById[1] = "PLAYER_TYPE_HERO"] = 1;
    values[valuesById[2] = "PLAYER_TYPE_MONSTER"] = 2;
    return values;
})();

/**
 * PB_BattlePhase enum.
 * @name PB_BattlePhase
 * @enum {number}
 * @property {number} BATTLE_PHASE_NONE=0 BATTLE_PHASE_NONE value
 * @property {number} BATTLE_PHASE_PREPARING=1 BATTLE_PHASE_PREPARING value
 * @property {number} BATTLE_PHASE_STARTED=2 BATTLE_PHASE_STARTED value
 * @property {number} BATTLE_PHASE_ENDING=3 BATTLE_PHASE_ENDING value
 * @property {number} BATTLE_PHASE_FINISHED=4 BATTLE_PHASE_FINISHED value
 */
export const PB_BattlePhase = $root.PB_BattlePhase = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "BATTLE_PHASE_NONE"] = 0;
    values[valuesById[1] = "BATTLE_PHASE_PREPARING"] = 1;
    values[valuesById[2] = "BATTLE_PHASE_STARTED"] = 2;
    values[valuesById[3] = "BATTLE_PHASE_ENDING"] = 3;
    values[valuesById[4] = "BATTLE_PHASE_FINISHED"] = 4;
    return values;
})();

/**
 * PB_TeamSide enum.
 * @name PB_TeamSide
 * @enum {number}
 * @property {number} TEAM_SIDE_NONE=0 TEAM_SIDE_NONE value
 * @property {number} TEAM_SIDE_ATTACKER=1 TEAM_SIDE_ATTACKER value
 * @property {number} TEAM_SIDE_DEFENDER=2 TEAM_SIDE_DEFENDER value
 * @property {number} TEAM_SIDE_NEUTRAL=3 TEAM_SIDE_NEUTRAL value
 */
export const PB_TeamSide = $root.PB_TeamSide = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "TEAM_SIDE_NONE"] = 0;
    values[valuesById[1] = "TEAM_SIDE_ATTACKER"] = 1;
    values[valuesById[2] = "TEAM_SIDE_DEFENDER"] = 2;
    values[valuesById[3] = "TEAM_SIDE_NEUTRAL"] = 3;
    return values;
})();

/**
 * PB_VictoryCondition enum.
 * @name PB_VictoryCondition
 * @enum {number}
 * @property {number} VICTORY_COND_NONE=0 VICTORY_COND_NONE value
 * @property {number} VICTORY_COND_DEFENDER_DEATH=1 VICTORY_COND_DEFENDER_DEATH value
 * @property {number} VICTORY_COND_ATTACKER_DEATH=2 VICTORY_COND_ATTACKER_DEATH value
 * @property {number} VICTORY_COND_ELIMINATE_ALL=3 VICTORY_COND_ELIMINATE_ALL value
 * @property {number} VICTORY_COND_SURVIVE_TIME=4 VICTORY_COND_SURVIVE_TIME value
 * @property {number} VICTORY_COND_KILL_TARGET=5 VICTORY_COND_KILL_TARGET value
 * @property {number} VICTORY_COND_SCORE_LIMIT=6 VICTORY_COND_SCORE_LIMIT value
 * @property {number} VICTORY_COND_CUSTOM=99 VICTORY_COND_CUSTOM value
 */
export const PB_VictoryCondition = $root.PB_VictoryCondition = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "VICTORY_COND_NONE"] = 0;
    values[valuesById[1] = "VICTORY_COND_DEFENDER_DEATH"] = 1;
    values[valuesById[2] = "VICTORY_COND_ATTACKER_DEATH"] = 2;
    values[valuesById[3] = "VICTORY_COND_ELIMINATE_ALL"] = 3;
    values[valuesById[4] = "VICTORY_COND_SURVIVE_TIME"] = 4;
    values[valuesById[5] = "VICTORY_COND_KILL_TARGET"] = 5;
    values[valuesById[6] = "VICTORY_COND_SCORE_LIMIT"] = 6;
    values[valuesById[99] = "VICTORY_COND_CUSTOM"] = 99;
    return values;
})();

/**
 * PB_BattleResultType enum.
 * @name PB_BattleResultType
 * @enum {number}
 * @property {number} BATTLE_RESULT_NONE=0 BATTLE_RESULT_NONE value
 * @property {number} BATTLE_RESULT_VICTORY=1 BATTLE_RESULT_VICTORY value
 * @property {number} BATTLE_RESULT_DEFEAT=2 BATTLE_RESULT_DEFEAT value
 * @property {number} BATTLE_RESULT_TIME_OUT=3 BATTLE_RESULT_TIME_OUT value
 * @property {number} BATTLE_RESULT_DRAW=4 BATTLE_RESULT_DRAW value
 * @property {number} BATTLE_RESULT_CANCELLED=5 BATTLE_RESULT_CANCELLED value
 */
export const PB_BattleResultType = $root.PB_BattleResultType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "BATTLE_RESULT_NONE"] = 0;
    values[valuesById[1] = "BATTLE_RESULT_VICTORY"] = 1;
    values[valuesById[2] = "BATTLE_RESULT_DEFEAT"] = 2;
    values[valuesById[3] = "BATTLE_RESULT_TIME_OUT"] = 3;
    values[valuesById[4] = "BATTLE_RESULT_DRAW"] = 4;
    values[valuesById[5] = "BATTLE_RESULT_CANCELLED"] = 5;
    return values;
})();

/**
 * PB_AbilityTargetType enum.
 * @name PB_AbilityTargetType
 * @enum {number}
 * @property {number} ABILITY_TARGET_NONE=0 ABILITY_TARGET_NONE value
 * @property {number} ABILITY_TARGET_SELF=1 ABILITY_TARGET_SELF value
 * @property {number} ABILITY_TARGET_ENEMY_SINGLE=2 ABILITY_TARGET_ENEMY_SINGLE value
 * @property {number} ABILITY_TARGET_FRIEND_SINGLE=3 ABILITY_TARGET_FRIEND_SINGLE value
 * @property {number} ABILITY_TARGET_ENEMY_AOE=4 ABILITY_TARGET_ENEMY_AOE value
 * @property {number} ABILITY_TARGET_FRIEND_AOE=5 ABILITY_TARGET_FRIEND_AOE value
 * @property {number} ABILITY_TARGET_ALL=6 ABILITY_TARGET_ALL value
 * @property {number} ABILITY_TARGET_LOCATION_AOE=7 ABILITY_TARGET_LOCATION_AOE value
 */
export const PB_AbilityTargetType = $root.PB_AbilityTargetType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "ABILITY_TARGET_NONE"] = 0;
    values[valuesById[1] = "ABILITY_TARGET_SELF"] = 1;
    values[valuesById[2] = "ABILITY_TARGET_ENEMY_SINGLE"] = 2;
    values[valuesById[3] = "ABILITY_TARGET_FRIEND_SINGLE"] = 3;
    values[valuesById[4] = "ABILITY_TARGET_ENEMY_AOE"] = 4;
    values[valuesById[5] = "ABILITY_TARGET_FRIEND_AOE"] = 5;
    values[valuesById[6] = "ABILITY_TARGET_ALL"] = 6;
    values[valuesById[7] = "ABILITY_TARGET_LOCATION_AOE"] = 7;
    return values;
})();

/**
 * PB_AbilityType enum.
 * @name PB_AbilityType
 * @enum {number}
 * @property {number} ABILITY_TYPE_NONE=0 ABILITY_TYPE_NONE value
 * @property {number} ABILITY_TYPE_PHYSICAL_ATTACK=1 ABILITY_TYPE_PHYSICAL_ATTACK value
 * @property {number} ABILITY_TYPE_MAGIC_ATTACK=2 ABILITY_TYPE_MAGIC_ATTACK value
 * @property {number} ABILITY_TYPE_LIFESTEAL_ATTACK=3 ABILITY_TYPE_LIFESTEAL_ATTACK value
 * @property {number} ABILITY_TYPE_HEAL=4 ABILITY_TYPE_HEAL value
 * @property {number} ABILITY_TYPE_SELF_BUFF=5 ABILITY_TYPE_SELF_BUFF value
 * @property {number} ABILITY_TYPE_TARGET_BUFF=6 ABILITY_TYPE_TARGET_BUFF value
 * @property {number} ABILITY_TYPE_MAGIC_ATTACK_WITH_BUFF=7 ABILITY_TYPE_MAGIC_ATTACK_WITH_BUFF value
 * @property {number} ABILITY_TYPE_CUSTOM=99 ABILITY_TYPE_CUSTOM value
 */
export const PB_AbilityType = $root.PB_AbilityType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "ABILITY_TYPE_NONE"] = 0;
    values[valuesById[1] = "ABILITY_TYPE_PHYSICAL_ATTACK"] = 1;
    values[valuesById[2] = "ABILITY_TYPE_MAGIC_ATTACK"] = 2;
    values[valuesById[3] = "ABILITY_TYPE_LIFESTEAL_ATTACK"] = 3;
    values[valuesById[4] = "ABILITY_TYPE_HEAL"] = 4;
    values[valuesById[5] = "ABILITY_TYPE_SELF_BUFF"] = 5;
    values[valuesById[6] = "ABILITY_TYPE_TARGET_BUFF"] = 6;
    values[valuesById[7] = "ABILITY_TYPE_MAGIC_ATTACK_WITH_BUFF"] = 7;
    values[valuesById[99] = "ABILITY_TYPE_CUSTOM"] = 99;
    return values;
})();

/**
 * PB_DamageType enum.
 * @name PB_DamageType
 * @enum {number}
 * @property {number} DAMAGE_TYPE_NONE=0 DAMAGE_TYPE_NONE value
 * @property {number} DAMAGE_TYPE_ATTACK=1 DAMAGE_TYPE_ATTACK value
 * @property {number} DAMAGE_TYPE_SKILL=2 DAMAGE_TYPE_SKILL value
 * @property {number} DAMAGE_TYPE_BUFF=3 DAMAGE_TYPE_BUFF value
 */
export const PB_DamageType = $root.PB_DamageType = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "DAMAGE_TYPE_NONE"] = 0;
    values[valuesById[1] = "DAMAGE_TYPE_ATTACK"] = 1;
    values[valuesById[2] = "DAMAGE_TYPE_SKILL"] = 2;
    values[valuesById[3] = "DAMAGE_TYPE_BUFF"] = 3;
    return values;
})();

/**
 * PB_DurationPolicy enum.
 * @name PB_DurationPolicy
 * @enum {number}
 * @property {number} DURATION_POLICY_NONE=0 DURATION_POLICY_NONE value
 * @property {number} DURATION_POLICY_INSTANT=1 DURATION_POLICY_INSTANT value
 * @property {number} DURATION_POLICY_DURATION=2 DURATION_POLICY_DURATION value
 * @property {number} DURATION_POLICY_INFINITE=3 DURATION_POLICY_INFINITE value
 */
export const PB_DurationPolicy = $root.PB_DurationPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "DURATION_POLICY_NONE"] = 0;
    values[valuesById[1] = "DURATION_POLICY_INSTANT"] = 1;
    values[valuesById[2] = "DURATION_POLICY_DURATION"] = 2;
    values[valuesById[3] = "DURATION_POLICY_INFINITE"] = 3;
    return values;
})();

/**
 * PB_StackingPolicy enum.
 * @name PB_StackingPolicy
 * @enum {number}
 * @property {number} STACKING_POLICY_NONE=0 STACKING_POLICY_NONE value
 * @property {number} STACKING_POLICY_AGGREGATE_BY_SOURCE=1 STACKING_POLICY_AGGREGATE_BY_SOURCE value
 * @property {number} STACKING_POLICY_AGGREGATE_BY_TARGET=2 STACKING_POLICY_AGGREGATE_BY_TARGET value
 */
export const PB_StackingPolicy = $root.PB_StackingPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "STACKING_POLICY_NONE"] = 0;
    values[valuesById[1] = "STACKING_POLICY_AGGREGATE_BY_SOURCE"] = 1;
    values[valuesById[2] = "STACKING_POLICY_AGGREGATE_BY_TARGET"] = 2;
    return values;
})();

/**
 * PB_StackDurationRefreshPolicy enum.
 * @name PB_StackDurationRefreshPolicy
 * @enum {number}
 * @property {number} STACK_DURATION_REFRESH_POLICY_NONE=0 STACK_DURATION_REFRESH_POLICY_NONE value
 * @property {number} STACK_DURATION_REFRESH_POLICY_REFRESH_ON_APPLICATION=1 STACK_DURATION_REFRESH_POLICY_REFRESH_ON_APPLICATION value
 * @property {number} STACK_DURATION_REFRESH_POLICY_NEVER_REFRESH=2 STACK_DURATION_REFRESH_POLICY_NEVER_REFRESH value
 * @property {number} STACK_DURATION_REFRESH_POLICY_REFRESH_ON_MAX_STACK=3 STACK_DURATION_REFRESH_POLICY_REFRESH_ON_MAX_STACK value
 */
export const PB_StackDurationRefreshPolicy = $root.PB_StackDurationRefreshPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "STACK_DURATION_REFRESH_POLICY_NONE"] = 0;
    values[valuesById[1] = "STACK_DURATION_REFRESH_POLICY_REFRESH_ON_APPLICATION"] = 1;
    values[valuesById[2] = "STACK_DURATION_REFRESH_POLICY_NEVER_REFRESH"] = 2;
    values[valuesById[3] = "STACK_DURATION_REFRESH_POLICY_REFRESH_ON_MAX_STACK"] = 3;
    return values;
})();

/**
 * PB_PeriodicPolicy enum.
 * @name PB_PeriodicPolicy
 * @enum {number}
 * @property {number} PERIODIC_POLICY_NONE=0 PERIODIC_POLICY_NONE value
 * @property {number} PERIODIC_POLICY_EXECUTE_ON_APPLICATION=1 PERIODIC_POLICY_EXECUTE_ON_APPLICATION value
 * @property {number} PERIODIC_POLICY_EXECUTE_ON_INTERVAL=2 PERIODIC_POLICY_EXECUTE_ON_INTERVAL value
 */
export const PB_PeriodicPolicy = $root.PB_PeriodicPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "PERIODIC_POLICY_NONE"] = 0;
    values[valuesById[1] = "PERIODIC_POLICY_EXECUTE_ON_APPLICATION"] = 1;
    values[valuesById[2] = "PERIODIC_POLICY_EXECUTE_ON_INTERVAL"] = 2;
    return values;
})();

/**
 * PB_ModifierOperation enum.
 * @name PB_ModifierOperation
 * @enum {number}
 * @property {number} MODIFIER_OPERATION_NONE=0 MODIFIER_OPERATION_NONE value
 * @property {number} MODIFIER_OPERATION_ADD=1 MODIFIER_OPERATION_ADD value
 * @property {number} MODIFIER_OPERATION_MULTIPLY=2 MODIFIER_OPERATION_MULTIPLY value
 * @property {number} MODIFIER_OPERATION_OVERRIDE=3 MODIFIER_OPERATION_OVERRIDE value
 */
export const PB_ModifierOperation = $root.PB_ModifierOperation = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "MODIFIER_OPERATION_NONE"] = 0;
    values[valuesById[1] = "MODIFIER_OPERATION_ADD"] = 1;
    values[valuesById[2] = "MODIFIER_OPERATION_MULTIPLY"] = 2;
    values[valuesById[3] = "MODIFIER_OPERATION_OVERRIDE"] = 3;
    return values;
})();

/**
 * PB_AbilityActivationPolicy enum.
 * @name PB_AbilityActivationPolicy
 * @enum {number}
 * @property {number} ABILITY_ACTIVATION_POLICY_NONE=0 ABILITY_ACTIVATION_POLICY_NONE value
 * @property {number} ABILITY_ACTIVATION_POLICY_ON_INPUT_TRIGGERED=1 ABILITY_ACTIVATION_POLICY_ON_INPUT_TRIGGERED value
 * @property {number} ABILITY_ACTIVATION_POLICY_WHILE_INPUT_ACTIVE=2 ABILITY_ACTIVATION_POLICY_WHILE_INPUT_ACTIVE value
 * @property {number} ABILITY_ACTIVATION_POLICY_ON_SPAWN=3 ABILITY_ACTIVATION_POLICY_ON_SPAWN value
 */
export const PB_AbilityActivationPolicy = $root.PB_AbilityActivationPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "ABILITY_ACTIVATION_POLICY_NONE"] = 0;
    values[valuesById[1] = "ABILITY_ACTIVATION_POLICY_ON_INPUT_TRIGGERED"] = 1;
    values[valuesById[2] = "ABILITY_ACTIVATION_POLICY_WHILE_INPUT_ACTIVE"] = 2;
    values[valuesById[3] = "ABILITY_ACTIVATION_POLICY_ON_SPAWN"] = 3;
    return values;
})();

/**
 * PB_AbilityNetPolicy enum.
 * @name PB_AbilityNetPolicy
 * @enum {number}
 * @property {number} ABILITY_NET_POLICY_NONE=0 ABILITY_NET_POLICY_NONE value
 * @property {number} ABILITY_NET_POLICY_LOCAL_PREDICTED=1 ABILITY_NET_POLICY_LOCAL_PREDICTED value
 * @property {number} ABILITY_NET_POLICY_LOCAL_ONLY=2 ABILITY_NET_POLICY_LOCAL_ONLY value
 * @property {number} ABILITY_NET_POLICY_SERVER_INITIATED=3 ABILITY_NET_POLICY_SERVER_INITIATED value
 * @property {number} ABILITY_NET_POLICY_SERVER_ONLY=4 ABILITY_NET_POLICY_SERVER_ONLY value
 */
export const PB_AbilityNetPolicy = $root.PB_AbilityNetPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "ABILITY_NET_POLICY_NONE"] = 0;
    values[valuesById[1] = "ABILITY_NET_POLICY_LOCAL_PREDICTED"] = 1;
    values[valuesById[2] = "ABILITY_NET_POLICY_LOCAL_ONLY"] = 2;
    values[valuesById[3] = "ABILITY_NET_POLICY_SERVER_INITIATED"] = 3;
    values[valuesById[4] = "ABILITY_NET_POLICY_SERVER_ONLY"] = 4;
    return values;
})();

/**
 * PB_CancelAbilityPolicy enum.
 * @name PB_CancelAbilityPolicy
 * @enum {number}
 * @property {number} CANCEL_ABILITY_POLICY_NONE=0 CANCEL_ABILITY_POLICY_NONE value
 * @property {number} CANCEL_ABILITY_POLICY_CANCEL_ABILITY=1 CANCEL_ABILITY_POLICY_CANCEL_ABILITY value
 * @property {number} CANCEL_ABILITY_POLICY_IGNORE_ABILITY=2 CANCEL_ABILITY_POLICY_IGNORE_ABILITY value
 */
export const PB_CancelAbilityPolicy = $root.PB_CancelAbilityPolicy = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "CANCEL_ABILITY_POLICY_NONE"] = 0;
    values[valuesById[1] = "CANCEL_ABILITY_POLICY_CANCEL_ABILITY"] = 1;
    values[valuesById[2] = "CANCEL_ABILITY_POLICY_IGNORE_ABILITY"] = 2;
    return values;
})();

export const PB_AbilitySpec = $root.PB_AbilitySpec = (() => {

    /**
     * Properties of a PB_AbilitySpec.
     * @typedef {Object} PB_AbilitySpec.$Properties
     * @property {number|null} [ability_id] PB_AbilitySpec ability_id
     * @property {number|null} [level] PB_AbilitySpec level
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_AbilitySpec.
     * @exports IPB_AbilitySpec
     * @interface IPB_AbilitySpec
     * @augments PB_AbilitySpec.$Properties
     * @deprecated Use PB_AbilitySpec.$Properties instead.
     */

    /**
     * Shape of a PB_AbilitySpec.
     * @typedef {PB_AbilitySpec.$Properties} PB_AbilitySpec.$Shape
     */

    /**
     * Constructs a new PB_AbilitySpec.
     * @exports PB_AbilitySpec
     * @classdesc Represents a PB_AbilitySpec.
     * @constructor
     * @param {PB_AbilitySpec.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_AbilitySpec = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_AbilitySpec ability_id.
     * @member {number|null|undefined} ability_id
     * @memberof PB_AbilitySpec
     * @instance
     */
    PB_AbilitySpec.prototype.ability_id = null;

    /**
     * PB_AbilitySpec level.
     * @member {number|null|undefined} level
     * @memberof PB_AbilitySpec
     * @instance
     */
    PB_AbilitySpec.prototype.level = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_AbilitySpec.prototype, "_ability_id", {
        get: $util.oneOfGetter($oneOfFields = ["ability_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_AbilitySpec.prototype, "_level", {
        get: $util.oneOfGetter($oneOfFields = ["level"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_AbilitySpec instance using the specified properties.
     * @function create
     * @memberof PB_AbilitySpec
     * @static
     * @param {PB_AbilitySpec.$Properties=} [properties] Properties to set
     * @returns {PB_AbilitySpec} PB_AbilitySpec instance
     * @type {{
     *   (properties: PB_AbilitySpec.$Shape): PB_AbilitySpec & PB_AbilitySpec.$Shape;
     *   (properties?: PB_AbilitySpec.$Properties): PB_AbilitySpec;
     * }}
     */
    PB_AbilitySpec.create = function(properties) {
        return new PB_AbilitySpec(properties);
    };

    /**
     * Encodes the specified PB_AbilitySpec message. Does not implicitly {@link PB_AbilitySpec.verify|verify} messages.
     * @function encode
     * @memberof PB_AbilitySpec
     * @static
     * @param {PB_AbilitySpec.$Properties} message PB_AbilitySpec message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AbilitySpec.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ability_id != null && $Object.hasOwnProperty.call(message, "ability_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ability_id);
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_AbilitySpec message, length delimited. Does not implicitly {@link PB_AbilitySpec.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_AbilitySpec
     * @static
     * @param {PB_AbilitySpec.$Properties} message PB_AbilitySpec message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AbilitySpec.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_AbilitySpec message from the specified reader or buffer.
     * @function decode
     * @memberof PB_AbilitySpec
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_AbilitySpec & PB_AbilitySpec.$Shape} PB_AbilitySpec
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AbilitySpec.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_AbilitySpec();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.ability_id = reader.int32();
                    message._ability_id = "ability_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.level = reader.int32();
                    message._level = "level";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_AbilitySpec message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_AbilitySpec
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_AbilitySpec & PB_AbilitySpec.$Shape} PB_AbilitySpec
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AbilitySpec.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_AbilitySpec message.
     * @function verify
     * @memberof PB_AbilitySpec
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_AbilitySpec.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.ability_id != null && $Object.hasOwnProperty.call(message, "ability_id")) {
            properties._ability_id = 1;
            if (!$util.isInteger(message.ability_id))
                return "ability_id: integer expected";
        }
        if (message.level != null && $Object.hasOwnProperty.call(message, "level")) {
            properties._level = 1;
            if (!$util.isInteger(message.level))
                return "level: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_AbilitySpec message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_AbilitySpec
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_AbilitySpec} PB_AbilitySpec
     */
    PB_AbilitySpec.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_AbilitySpec)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_AbilitySpec: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_AbilitySpec();
        if (object.ability_id != null)
            message.ability_id = object.ability_id | 0;
        if (object.level != null)
            message.level = object.level | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_AbilitySpec message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_AbilitySpec
     * @static
     * @param {PB_AbilitySpec} message PB_AbilitySpec
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_AbilitySpec.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.ability_id != null && $Object.hasOwnProperty.call(message, "ability_id"))
            object.ability_id = message.ability_id;
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            object.level = message.level;
        return object;
    };

    /**
     * Converts this PB_AbilitySpec to JSON.
     * @function toJSON
     * @memberof PB_AbilitySpec
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_AbilitySpec.prototype.toJSON = function() {
        return PB_AbilitySpec.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_AbilitySpec
     * @function getTypeUrl
     * @memberof PB_AbilitySpec
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_AbilitySpec.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_AbilitySpec";
    };

    return PB_AbilitySpec;
})();

export const PB_BattleParticipant = $root.PB_BattleParticipant = (() => {

    /**
     * Properties of a PB_BattleParticipant.
     * @typedef {Object} PB_BattleParticipant.$Properties
     * @property {number|null} [player_id] PB_BattleParticipant player_id
     * @property {PB_PlayerType|null} [player_type] PB_BattleParticipant player_type
     * @property {string|null} [name] PB_BattleParticipant name
     * @property {number|null} [level] PB_BattleParticipant level
     * @property {number|null} [team_id] PB_BattleParticipant team_id
     * @property {string|null} [team_name] PB_BattleParticipant team_name
     * @property {PB_TeamSide|null} [side] PB_BattleParticipant side
     * @property {Array.<PB_AttrPair.$Properties>|null} [initial_attrs] PB_BattleParticipant initial_attrs
     * @property {Array.<PB_AbilitySpec.$Properties>|null} [abilities] PB_BattleParticipant abilities
     * @property {Array.<string>|null} [init_tags] PB_BattleParticipant init_tags
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_BattleParticipant.
     * @exports IPB_BattleParticipant
     * @interface IPB_BattleParticipant
     * @augments PB_BattleParticipant.$Properties
     * @deprecated Use PB_BattleParticipant.$Properties instead.
     */

    /**
     * Shape of a PB_BattleParticipant.
     * @typedef {PB_BattleParticipant.$Properties} PB_BattleParticipant.$Shape
     */

    /**
     * Constructs a new PB_BattleParticipant.
     * @exports PB_BattleParticipant
     * @classdesc Represents a PB_BattleParticipant.
     * @constructor
     * @param {PB_BattleParticipant.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_BattleParticipant = function (properties) {
        this.initial_attrs = [];
        this.abilities = [];
        this.init_tags = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_BattleParticipant player_id.
     * @member {number|null|undefined} player_id
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.player_id = null;

    /**
     * PB_BattleParticipant player_type.
     * @member {PB_PlayerType|null|undefined} player_type
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.player_type = null;

    /**
     * PB_BattleParticipant name.
     * @member {string|null|undefined} name
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.name = null;

    /**
     * PB_BattleParticipant level.
     * @member {number|null|undefined} level
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.level = null;

    /**
     * PB_BattleParticipant team_id.
     * @member {number|null|undefined} team_id
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.team_id = null;

    /**
     * PB_BattleParticipant team_name.
     * @member {string|null|undefined} team_name
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.team_name = null;

    /**
     * PB_BattleParticipant side.
     * @member {PB_TeamSide|null|undefined} side
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.side = null;

    /**
     * PB_BattleParticipant initial_attrs.
     * @member {Array.<PB_AttrPair.$Properties>} initial_attrs
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.initial_attrs = $util.emptyArray;

    /**
     * PB_BattleParticipant abilities.
     * @member {Array.<PB_AbilitySpec.$Properties>} abilities
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.abilities = $util.emptyArray;

    /**
     * PB_BattleParticipant init_tags.
     * @member {Array.<string>} init_tags
     * @memberof PB_BattleParticipant
     * @instance
     */
    PB_BattleParticipant.prototype.init_tags = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_player_id", {
        get: $util.oneOfGetter($oneOfFields = ["player_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_player_type", {
        get: $util.oneOfGetter($oneOfFields = ["player_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_level", {
        get: $util.oneOfGetter($oneOfFields = ["level"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_team_id", {
        get: $util.oneOfGetter($oneOfFields = ["team_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_team_name", {
        get: $util.oneOfGetter($oneOfFields = ["team_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleParticipant.prototype, "_side", {
        get: $util.oneOfGetter($oneOfFields = ["side"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_BattleParticipant instance using the specified properties.
     * @function create
     * @memberof PB_BattleParticipant
     * @static
     * @param {PB_BattleParticipant.$Properties=} [properties] Properties to set
     * @returns {PB_BattleParticipant} PB_BattleParticipant instance
     * @type {{
     *   (properties: PB_BattleParticipant.$Shape): PB_BattleParticipant & PB_BattleParticipant.$Shape;
     *   (properties?: PB_BattleParticipant.$Properties): PB_BattleParticipant;
     * }}
     */
    PB_BattleParticipant.create = function(properties) {
        return new PB_BattleParticipant(properties);
    };

    /**
     * Encodes the specified PB_BattleParticipant message. Does not implicitly {@link PB_BattleParticipant.verify|verify} messages.
     * @function encode
     * @memberof PB_BattleParticipant
     * @static
     * @param {PB_BattleParticipant.$Properties} message PB_BattleParticipant message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleParticipant.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.player_id);
        if (message.player_type != null && $Object.hasOwnProperty.call(message, "player_type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.player_type);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.level);
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.team_id);
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.team_name);
        if (message.side != null && $Object.hasOwnProperty.call(message, "side"))
            writer.uint32(/* id 7, wireType 0 =*/56).int32(message.side);
        if (message.initial_attrs != null && message.initial_attrs.length)
            for (let i = 0; i < message.initial_attrs.length; ++i)
                $root.PB_AttrPair.encode(message.initial_attrs[i], writer.uint32(/* id 8, wireType 2 =*/66).fork(), _depth + 1).ldelim();
        if (message.abilities != null && message.abilities.length)
            for (let i = 0; i < message.abilities.length; ++i)
                $root.PB_AbilitySpec.encode(message.abilities[i], writer.uint32(/* id 9, wireType 2 =*/74).fork(), _depth + 1).ldelim();
        if (message.init_tags != null && message.init_tags.length)
            for (let i = 0; i < message.init_tags.length; ++i)
                writer.uint32(/* id 10, wireType 2 =*/82).string(message.init_tags[i]);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_BattleParticipant message, length delimited. Does not implicitly {@link PB_BattleParticipant.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_BattleParticipant
     * @static
     * @param {PB_BattleParticipant.$Properties} message PB_BattleParticipant message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleParticipant.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_BattleParticipant message from the specified reader or buffer.
     * @function decode
     * @memberof PB_BattleParticipant
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_BattleParticipant & PB_BattleParticipant.$Shape} PB_BattleParticipant
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleParticipant.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_BattleParticipant(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.player_id = reader.int32();
                    message._player_id = "player_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.player_type = reader.int32();
                    message._player_type = "player_type";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.level = reader.int32();
                    message._level = "level";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.team_id = reader.int32();
                    message._team_id = "team_id";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    message.team_name = reader.stringVerify();
                    message._team_name = "team_name";
                    continue;
                }
            case 7: {
                    if (wireType !== 0)
                        break;
                    message.side = reader.int32();
                    message._side = "side";
                    continue;
                }
            case 8: {
                    if (wireType !== 2)
                        break;
                    if (!(message.initial_attrs && message.initial_attrs.length))
                        message.initial_attrs = [];
                    message.initial_attrs.push($root.PB_AttrPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 9: {
                    if (wireType !== 2)
                        break;
                    if (!(message.abilities && message.abilities.length))
                        message.abilities = [];
                    message.abilities.push($root.PB_AbilitySpec.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 10: {
                    if (wireType !== 2)
                        break;
                    if (!(message.init_tags && message.init_tags.length))
                        message.init_tags = [];
                    message.init_tags.push(reader.stringVerify());
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_BattleParticipant message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_BattleParticipant
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_BattleParticipant & PB_BattleParticipant.$Shape} PB_BattleParticipant
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleParticipant.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_BattleParticipant message.
     * @function verify
     * @memberof PB_BattleParticipant
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_BattleParticipant.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id")) {
            properties._player_id = 1;
            if (!$util.isInteger(message.player_id))
                return "player_id: integer expected";
        }
        if (message.player_type != null && $Object.hasOwnProperty.call(message, "player_type")) {
            properties._player_type = 1;
            if (typeof message.player_type !== "number" || (message.player_type | 0) !== message.player_type)
                return "player_type: enum value expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.level != null && $Object.hasOwnProperty.call(message, "level")) {
            properties._level = 1;
            if (!$util.isInteger(message.level))
                return "level: integer expected";
        }
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id")) {
            properties._team_id = 1;
            if (!$util.isInteger(message.team_id))
                return "team_id: integer expected";
        }
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name")) {
            properties._team_name = 1;
            if (!$util.isString(message.team_name))
                return "team_name: string expected";
        }
        if (message.side != null && $Object.hasOwnProperty.call(message, "side")) {
            properties._side = 1;
            if (typeof message.side !== "number" || (message.side | 0) !== message.side)
                return "side: enum value expected";
        }
        if (message.initial_attrs != null && $Object.hasOwnProperty.call(message, "initial_attrs")) {
            if (!$Array.isArray(message.initial_attrs))
                return "initial_attrs: array expected";
            for (let i = 0; i < message.initial_attrs.length; ++i) {
                let error = $root.PB_AttrPair.verify(message.initial_attrs[i], _depth + 1);
                if (error)
                    return "initial_attrs." + error;
            }
        }
        if (message.abilities != null && $Object.hasOwnProperty.call(message, "abilities")) {
            if (!$Array.isArray(message.abilities))
                return "abilities: array expected";
            for (let i = 0; i < message.abilities.length; ++i) {
                let error = $root.PB_AbilitySpec.verify(message.abilities[i], _depth + 1);
                if (error)
                    return "abilities." + error;
            }
        }
        if (message.init_tags != null && $Object.hasOwnProperty.call(message, "init_tags")) {
            if (!$Array.isArray(message.init_tags))
                return "init_tags: array expected";
            for (let i = 0; i < message.init_tags.length; ++i)
                if (!$util.isString(message.init_tags[i]))
                    return "init_tags: string[] expected";
        }
        return null;
    };

    /**
     * Creates a PB_BattleParticipant message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_BattleParticipant
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_BattleParticipant} PB_BattleParticipant
     */
    PB_BattleParticipant.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_BattleParticipant)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_BattleParticipant: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_BattleParticipant();
        if (object.player_id != null)
            message.player_id = object.player_id | 0;
        switch (object.player_type) {
        case "PLAYER_TYPE_NONE":
        case 0:
            message.player_type = 0;
            break;
        case "PLAYER_TYPE_HERO":
        case 1:
            message.player_type = 1;
            break;
        case "PLAYER_TYPE_MONSTER":
        case 2:
            message.player_type = 2;
            break;
        default:
            if (typeof object.player_type === "number" && (object.player_type | 0) === object.player_type)
                message.player_type = object.player_type;
        }
        if (object.name != null)
            message.name = $String(object.name);
        if (object.level != null)
            message.level = object.level | 0;
        if (object.team_id != null)
            message.team_id = object.team_id | 0;
        if (object.team_name != null)
            message.team_name = $String(object.team_name);
        switch (object.side) {
        case "TEAM_SIDE_NONE":
        case 0:
            message.side = 0;
            break;
        case "TEAM_SIDE_ATTACKER":
        case 1:
            message.side = 1;
            break;
        case "TEAM_SIDE_DEFENDER":
        case 2:
            message.side = 2;
            break;
        case "TEAM_SIDE_NEUTRAL":
        case 3:
            message.side = 3;
            break;
        default:
            if (typeof object.side === "number" && (object.side | 0) === object.side)
                message.side = object.side;
        }
        if (object.initial_attrs) {
            if (!$Array.isArray(object.initial_attrs))
                throw $TypeError(".PB_BattleParticipant.initial_attrs: array expected");
            message.initial_attrs = $Array(object.initial_attrs.length);
            for (let i = 0; i < object.initial_attrs.length; ++i) {
                if (!$util.isObject(object.initial_attrs[i]))
                    throw $TypeError(".PB_BattleParticipant.initial_attrs: object expected");
                message.initial_attrs[i] = $root.PB_AttrPair.fromObject(object.initial_attrs[i], _depth + 1);
            }
        }
        if (object.abilities) {
            if (!$Array.isArray(object.abilities))
                throw $TypeError(".PB_BattleParticipant.abilities: array expected");
            message.abilities = $Array(object.abilities.length);
            for (let i = 0; i < object.abilities.length; ++i) {
                if (!$util.isObject(object.abilities[i]))
                    throw $TypeError(".PB_BattleParticipant.abilities: object expected");
                message.abilities[i] = $root.PB_AbilitySpec.fromObject(object.abilities[i], _depth + 1);
            }
        }
        if (object.init_tags) {
            if (!$Array.isArray(object.init_tags))
                throw $TypeError(".PB_BattleParticipant.init_tags: array expected");
            message.init_tags = $Array(object.init_tags.length);
            for (let i = 0; i < object.init_tags.length; ++i)
                message.init_tags[i] = $String(object.init_tags[i]);
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_BattleParticipant message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_BattleParticipant
     * @static
     * @param {PB_BattleParticipant} message PB_BattleParticipant
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_BattleParticipant.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults) {
            object.initial_attrs = [];
            object.abilities = [];
            object.init_tags = [];
        }
        if (message.player_id != null && $Object.hasOwnProperty.call(message, "player_id"))
            object.player_id = message.player_id;
        if (message.player_type != null && $Object.hasOwnProperty.call(message, "player_type"))
            object.player_type = options.enums === $String ? $root.PB_PlayerType[message.player_type] === $undefined ? message.player_type : $root.PB_PlayerType[message.player_type] : message.player_type;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            object.level = message.level;
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id"))
            object.team_id = message.team_id;
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name"))
            object.team_name = message.team_name;
        if (message.side != null && $Object.hasOwnProperty.call(message, "side"))
            object.side = options.enums === $String ? $root.PB_TeamSide[message.side] === $undefined ? message.side : $root.PB_TeamSide[message.side] : message.side;
        if (message.initial_attrs && message.initial_attrs.length) {
            object.initial_attrs = $Array(message.initial_attrs.length);
            for (let j = 0; j < message.initial_attrs.length; ++j)
                object.initial_attrs[j] = $root.PB_AttrPair.toObject(message.initial_attrs[j], options, _depth + 1);
        }
        if (message.abilities && message.abilities.length) {
            object.abilities = $Array(message.abilities.length);
            for (let j = 0; j < message.abilities.length; ++j)
                object.abilities[j] = $root.PB_AbilitySpec.toObject(message.abilities[j], options, _depth + 1);
        }
        if (message.init_tags && message.init_tags.length) {
            object.init_tags = $Array(message.init_tags.length);
            for (let j = 0; j < message.init_tags.length; ++j)
                object.init_tags[j] = message.init_tags[j];
        }
        return object;
    };

    /**
     * Converts this PB_BattleParticipant to JSON.
     * @function toJSON
     * @memberof PB_BattleParticipant
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_BattleParticipant.prototype.toJSON = function() {
        return PB_BattleParticipant.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_BattleParticipant
     * @function getTypeUrl
     * @memberof PB_BattleParticipant
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_BattleParticipant.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_BattleParticipant";
    };

    return PB_BattleParticipant;
})();

export const PB_BattleTeam = $root.PB_BattleTeam = (() => {

    /**
     * Properties of a PB_BattleTeam.
     * @typedef {Object} PB_BattleTeam.$Properties
     * @property {number|null} [team_id] PB_BattleTeam team_id
     * @property {string|null} [team_name] PB_BattleTeam team_name
     * @property {PB_TeamSide|null} [side] PB_BattleTeam side
     * @property {Array.<PB_BattleParticipant.$Properties>|null} [members] PB_BattleTeam members
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_BattleTeam.
     * @exports IPB_BattleTeam
     * @interface IPB_BattleTeam
     * @augments PB_BattleTeam.$Properties
     * @deprecated Use PB_BattleTeam.$Properties instead.
     */

    /**
     * Shape of a PB_BattleTeam.
     * @typedef {PB_BattleTeam.$Properties} PB_BattleTeam.$Shape
     */

    /**
     * Constructs a new PB_BattleTeam.
     * @exports PB_BattleTeam
     * @classdesc Represents a PB_BattleTeam.
     * @constructor
     * @param {PB_BattleTeam.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_BattleTeam = function (properties) {
        this.members = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_BattleTeam team_id.
     * @member {number|null|undefined} team_id
     * @memberof PB_BattleTeam
     * @instance
     */
    PB_BattleTeam.prototype.team_id = null;

    /**
     * PB_BattleTeam team_name.
     * @member {string|null|undefined} team_name
     * @memberof PB_BattleTeam
     * @instance
     */
    PB_BattleTeam.prototype.team_name = null;

    /**
     * PB_BattleTeam side.
     * @member {PB_TeamSide|null|undefined} side
     * @memberof PB_BattleTeam
     * @instance
     */
    PB_BattleTeam.prototype.side = null;

    /**
     * PB_BattleTeam members.
     * @member {Array.<PB_BattleParticipant.$Properties>} members
     * @memberof PB_BattleTeam
     * @instance
     */
    PB_BattleTeam.prototype.members = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleTeam.prototype, "_team_id", {
        get: $util.oneOfGetter($oneOfFields = ["team_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleTeam.prototype, "_team_name", {
        get: $util.oneOfGetter($oneOfFields = ["team_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleTeam.prototype, "_side", {
        get: $util.oneOfGetter($oneOfFields = ["side"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_BattleTeam instance using the specified properties.
     * @function create
     * @memberof PB_BattleTeam
     * @static
     * @param {PB_BattleTeam.$Properties=} [properties] Properties to set
     * @returns {PB_BattleTeam} PB_BattleTeam instance
     * @type {{
     *   (properties: PB_BattleTeam.$Shape): PB_BattleTeam & PB_BattleTeam.$Shape;
     *   (properties?: PB_BattleTeam.$Properties): PB_BattleTeam;
     * }}
     */
    PB_BattleTeam.create = function(properties) {
        return new PB_BattleTeam(properties);
    };

    /**
     * Encodes the specified PB_BattleTeam message. Does not implicitly {@link PB_BattleTeam.verify|verify} messages.
     * @function encode
     * @memberof PB_BattleTeam
     * @static
     * @param {PB_BattleTeam.$Properties} message PB_BattleTeam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleTeam.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.team_id);
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.team_name);
        if (message.side != null && $Object.hasOwnProperty.call(message, "side"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.side);
        if (message.members != null && message.members.length)
            for (let i = 0; i < message.members.length; ++i)
                $root.PB_BattleParticipant.encode(message.members[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_BattleTeam message, length delimited. Does not implicitly {@link PB_BattleTeam.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_BattleTeam
     * @static
     * @param {PB_BattleTeam.$Properties} message PB_BattleTeam message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleTeam.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_BattleTeam message from the specified reader or buffer.
     * @function decode
     * @memberof PB_BattleTeam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_BattleTeam & PB_BattleTeam.$Shape} PB_BattleTeam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleTeam.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_BattleTeam(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.team_id = reader.int32();
                    message._team_id = "team_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.team_name = reader.stringVerify();
                    message._team_name = "team_name";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.side = reader.int32();
                    message._side = "side";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    if (!(message.members && message.members.length))
                        message.members = [];
                    message.members.push($root.PB_BattleParticipant.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_BattleTeam message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_BattleTeam
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_BattleTeam & PB_BattleTeam.$Shape} PB_BattleTeam
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleTeam.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_BattleTeam message.
     * @function verify
     * @memberof PB_BattleTeam
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_BattleTeam.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id")) {
            properties._team_id = 1;
            if (!$util.isInteger(message.team_id))
                return "team_id: integer expected";
        }
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name")) {
            properties._team_name = 1;
            if (!$util.isString(message.team_name))
                return "team_name: string expected";
        }
        if (message.side != null && $Object.hasOwnProperty.call(message, "side")) {
            properties._side = 1;
            if (typeof message.side !== "number" || (message.side | 0) !== message.side)
                return "side: enum value expected";
        }
        if (message.members != null && $Object.hasOwnProperty.call(message, "members")) {
            if (!$Array.isArray(message.members))
                return "members: array expected";
            for (let i = 0; i < message.members.length; ++i) {
                let error = $root.PB_BattleParticipant.verify(message.members[i], _depth + 1);
                if (error)
                    return "members." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_BattleTeam message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_BattleTeam
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_BattleTeam} PB_BattleTeam
     */
    PB_BattleTeam.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_BattleTeam)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_BattleTeam: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_BattleTeam();
        if (object.team_id != null)
            message.team_id = object.team_id | 0;
        if (object.team_name != null)
            message.team_name = $String(object.team_name);
        switch (object.side) {
        case "TEAM_SIDE_NONE":
        case 0:
            message.side = 0;
            break;
        case "TEAM_SIDE_ATTACKER":
        case 1:
            message.side = 1;
            break;
        case "TEAM_SIDE_DEFENDER":
        case 2:
            message.side = 2;
            break;
        case "TEAM_SIDE_NEUTRAL":
        case 3:
            message.side = 3;
            break;
        default:
            if (typeof object.side === "number" && (object.side | 0) === object.side)
                message.side = object.side;
        }
        if (object.members) {
            if (!$Array.isArray(object.members))
                throw $TypeError(".PB_BattleTeam.members: array expected");
            message.members = $Array(object.members.length);
            for (let i = 0; i < object.members.length; ++i) {
                if (!$util.isObject(object.members[i]))
                    throw $TypeError(".PB_BattleTeam.members: object expected");
                message.members[i] = $root.PB_BattleParticipant.fromObject(object.members[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_BattleTeam message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_BattleTeam
     * @static
     * @param {PB_BattleTeam} message PB_BattleTeam
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_BattleTeam.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.members = [];
        if (message.team_id != null && $Object.hasOwnProperty.call(message, "team_id"))
            object.team_id = message.team_id;
        if (message.team_name != null && $Object.hasOwnProperty.call(message, "team_name"))
            object.team_name = message.team_name;
        if (message.side != null && $Object.hasOwnProperty.call(message, "side"))
            object.side = options.enums === $String ? $root.PB_TeamSide[message.side] === $undefined ? message.side : $root.PB_TeamSide[message.side] : message.side;
        if (message.members && message.members.length) {
            object.members = $Array(message.members.length);
            for (let j = 0; j < message.members.length; ++j)
                object.members[j] = $root.PB_BattleParticipant.toObject(message.members[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_BattleTeam to JSON.
     * @function toJSON
     * @memberof PB_BattleTeam
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_BattleTeam.prototype.toJSON = function() {
        return PB_BattleTeam.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_BattleTeam
     * @function getTypeUrl
     * @memberof PB_BattleTeam
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_BattleTeam.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_BattleTeam";
    };

    return PB_BattleTeam;
})();

export const PB_BattleRules = $root.PB_BattleRules = (() => {

    /**
     * Properties of a PB_BattleRules.
     * @typedef {Object} PB_BattleRules.$Properties
     * @property {PB_VictoryCondition|null} [victory_condition] PB_BattleRules victory_condition
     * @property {number|null} [max_turns] PB_BattleRules max_turns
     * @property {number|null} [max_duration] PB_BattleRules max_duration
     * @property {boolean|null} [allow_flee] PB_BattleRules allow_flee
     * @property {number|null} [score_limit] PB_BattleRules score_limit
     * @property {number|null} [target_member_id] PB_BattleRules target_member_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_BattleRules.
     * @exports IPB_BattleRules
     * @interface IPB_BattleRules
     * @augments PB_BattleRules.$Properties
     * @deprecated Use PB_BattleRules.$Properties instead.
     */

    /**
     * Shape of a PB_BattleRules.
     * @typedef {PB_BattleRules.$Properties} PB_BattleRules.$Shape
     */

    /**
     * Constructs a new PB_BattleRules.
     * @exports PB_BattleRules
     * @classdesc Represents a PB_BattleRules.
     * @constructor
     * @param {PB_BattleRules.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_BattleRules = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_BattleRules victory_condition.
     * @member {PB_VictoryCondition|null|undefined} victory_condition
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.victory_condition = null;

    /**
     * PB_BattleRules max_turns.
     * @member {number|null|undefined} max_turns
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.max_turns = null;

    /**
     * PB_BattleRules max_duration.
     * @member {number|null|undefined} max_duration
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.max_duration = null;

    /**
     * PB_BattleRules allow_flee.
     * @member {boolean|null|undefined} allow_flee
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.allow_flee = null;

    /**
     * PB_BattleRules score_limit.
     * @member {number|null|undefined} score_limit
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.score_limit = null;

    /**
     * PB_BattleRules target_member_id.
     * @member {number|null|undefined} target_member_id
     * @memberof PB_BattleRules
     * @instance
     */
    PB_BattleRules.prototype.target_member_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_victory_condition", {
        get: $util.oneOfGetter($oneOfFields = ["victory_condition"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_max_turns", {
        get: $util.oneOfGetter($oneOfFields = ["max_turns"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_max_duration", {
        get: $util.oneOfGetter($oneOfFields = ["max_duration"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_allow_flee", {
        get: $util.oneOfGetter($oneOfFields = ["allow_flee"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_score_limit", {
        get: $util.oneOfGetter($oneOfFields = ["score_limit"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleRules.prototype, "_target_member_id", {
        get: $util.oneOfGetter($oneOfFields = ["target_member_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_BattleRules instance using the specified properties.
     * @function create
     * @memberof PB_BattleRules
     * @static
     * @param {PB_BattleRules.$Properties=} [properties] Properties to set
     * @returns {PB_BattleRules} PB_BattleRules instance
     * @type {{
     *   (properties: PB_BattleRules.$Shape): PB_BattleRules & PB_BattleRules.$Shape;
     *   (properties?: PB_BattleRules.$Properties): PB_BattleRules;
     * }}
     */
    PB_BattleRules.create = function(properties) {
        return new PB_BattleRules(properties);
    };

    /**
     * Encodes the specified PB_BattleRules message. Does not implicitly {@link PB_BattleRules.verify|verify} messages.
     * @function encode
     * @memberof PB_BattleRules
     * @static
     * @param {PB_BattleRules.$Properties} message PB_BattleRules message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleRules.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.victory_condition != null && $Object.hasOwnProperty.call(message, "victory_condition"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.victory_condition);
        if (message.max_turns != null && $Object.hasOwnProperty.call(message, "max_turns"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.max_turns);
        if (message.max_duration != null && $Object.hasOwnProperty.call(message, "max_duration"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.max_duration);
        if (message.allow_flee != null && $Object.hasOwnProperty.call(message, "allow_flee"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.allow_flee);
        if (message.score_limit != null && $Object.hasOwnProperty.call(message, "score_limit"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.score_limit);
        if (message.target_member_id != null && $Object.hasOwnProperty.call(message, "target_member_id"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.target_member_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_BattleRules message, length delimited. Does not implicitly {@link PB_BattleRules.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_BattleRules
     * @static
     * @param {PB_BattleRules.$Properties} message PB_BattleRules message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleRules.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_BattleRules message from the specified reader or buffer.
     * @function decode
     * @memberof PB_BattleRules
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_BattleRules & PB_BattleRules.$Shape} PB_BattleRules
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleRules.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_BattleRules(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.victory_condition = reader.int32();
                    message._victory_condition = "victory_condition";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.max_turns = reader.int32();
                    message._max_turns = "max_turns";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.max_duration = reader.int32();
                    message._max_duration = "max_duration";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.allow_flee = reader.bool();
                    message._allow_flee = "allow_flee";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.score_limit = reader.int32();
                    message._score_limit = "score_limit";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.target_member_id = reader.int32();
                    message._target_member_id = "target_member_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_BattleRules message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_BattleRules
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_BattleRules & PB_BattleRules.$Shape} PB_BattleRules
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleRules.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_BattleRules message.
     * @function verify
     * @memberof PB_BattleRules
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_BattleRules.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.victory_condition != null && $Object.hasOwnProperty.call(message, "victory_condition")) {
            properties._victory_condition = 1;
            if (typeof message.victory_condition !== "number" || (message.victory_condition | 0) !== message.victory_condition)
                return "victory_condition: enum value expected";
        }
        if (message.max_turns != null && $Object.hasOwnProperty.call(message, "max_turns")) {
            properties._max_turns = 1;
            if (!$util.isInteger(message.max_turns))
                return "max_turns: integer expected";
        }
        if (message.max_duration != null && $Object.hasOwnProperty.call(message, "max_duration")) {
            properties._max_duration = 1;
            if (!$util.isInteger(message.max_duration))
                return "max_duration: integer expected";
        }
        if (message.allow_flee != null && $Object.hasOwnProperty.call(message, "allow_flee")) {
            properties._allow_flee = 1;
            if (typeof message.allow_flee !== "boolean")
                return "allow_flee: boolean expected";
        }
        if (message.score_limit != null && $Object.hasOwnProperty.call(message, "score_limit")) {
            properties._score_limit = 1;
            if (!$util.isInteger(message.score_limit))
                return "score_limit: integer expected";
        }
        if (message.target_member_id != null && $Object.hasOwnProperty.call(message, "target_member_id")) {
            properties._target_member_id = 1;
            if (!$util.isInteger(message.target_member_id))
                return "target_member_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_BattleRules message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_BattleRules
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_BattleRules} PB_BattleRules
     */
    PB_BattleRules.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_BattleRules)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_BattleRules: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_BattleRules();
        switch (object.victory_condition) {
        case "VICTORY_COND_NONE":
        case 0:
            message.victory_condition = 0;
            break;
        case "VICTORY_COND_DEFENDER_DEATH":
        case 1:
            message.victory_condition = 1;
            break;
        case "VICTORY_COND_ATTACKER_DEATH":
        case 2:
            message.victory_condition = 2;
            break;
        case "VICTORY_COND_ELIMINATE_ALL":
        case 3:
            message.victory_condition = 3;
            break;
        case "VICTORY_COND_SURVIVE_TIME":
        case 4:
            message.victory_condition = 4;
            break;
        case "VICTORY_COND_KILL_TARGET":
        case 5:
            message.victory_condition = 5;
            break;
        case "VICTORY_COND_SCORE_LIMIT":
        case 6:
            message.victory_condition = 6;
            break;
        case "VICTORY_COND_CUSTOM":
        case 99:
            message.victory_condition = 99;
            break;
        default:
            if (typeof object.victory_condition === "number" && (object.victory_condition | 0) === object.victory_condition)
                message.victory_condition = object.victory_condition;
        }
        if (object.max_turns != null)
            message.max_turns = object.max_turns | 0;
        if (object.max_duration != null)
            message.max_duration = object.max_duration | 0;
        if (object.allow_flee != null)
            message.allow_flee = $Boolean(object.allow_flee);
        if (object.score_limit != null)
            message.score_limit = object.score_limit | 0;
        if (object.target_member_id != null)
            message.target_member_id = object.target_member_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_BattleRules message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_BattleRules
     * @static
     * @param {PB_BattleRules} message PB_BattleRules
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_BattleRules.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.victory_condition != null && $Object.hasOwnProperty.call(message, "victory_condition"))
            object.victory_condition = options.enums === $String ? $root.PB_VictoryCondition[message.victory_condition] === $undefined ? message.victory_condition : $root.PB_VictoryCondition[message.victory_condition] : message.victory_condition;
        if (message.max_turns != null && $Object.hasOwnProperty.call(message, "max_turns"))
            object.max_turns = message.max_turns;
        if (message.max_duration != null && $Object.hasOwnProperty.call(message, "max_duration"))
            object.max_duration = message.max_duration;
        if (message.allow_flee != null && $Object.hasOwnProperty.call(message, "allow_flee"))
            object.allow_flee = message.allow_flee;
        if (message.score_limit != null && $Object.hasOwnProperty.call(message, "score_limit"))
            object.score_limit = message.score_limit;
        if (message.target_member_id != null && $Object.hasOwnProperty.call(message, "target_member_id"))
            object.target_member_id = message.target_member_id;
        return object;
    };

    /**
     * Converts this PB_BattleRules to JSON.
     * @function toJSON
     * @memberof PB_BattleRules
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_BattleRules.prototype.toJSON = function() {
        return PB_BattleRules.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_BattleRules
     * @function getTypeUrl
     * @memberof PB_BattleRules
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_BattleRules.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_BattleRules";
    };

    return PB_BattleRules;
})();

export const PB_BattleReq = $root.PB_BattleReq = (() => {

    /**
     * Properties of a PB_BattleReq.
     * @typedef {Object} PB_BattleReq.$Properties
     * @property {number|null} [battle_id] PB_BattleReq battle_id
     * @property {PB_BattleMode|null} [mode] PB_BattleReq mode
     * @property {number|null} [battle_type] PB_BattleReq battle_type
     * @property {number|null} [random_seed] PB_BattleReq random_seed
     * @property {PB_BattleRules.$Properties|null} [rules] PB_BattleReq rules
     * @property {Array.<PB_BattleTeam.$Properties>|null} [teams] PB_BattleReq teams
     * @property {number|null} [req_type] PB_BattleReq req_type
     * @property {number|null} [req_id] PB_BattleReq req_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_BattleReq.
     * @exports IPB_BattleReq
     * @interface IPB_BattleReq
     * @augments PB_BattleReq.$Properties
     * @deprecated Use PB_BattleReq.$Properties instead.
     */

    /**
     * Shape of a PB_BattleReq.
     * @typedef {PB_BattleReq.$Properties} PB_BattleReq.$Shape
     */

    /**
     * Constructs a new PB_BattleReq.
     * @exports PB_BattleReq
     * @classdesc Represents a PB_BattleReq.
     * @constructor
     * @param {PB_BattleReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_BattleReq = function (properties) {
        this.teams = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_BattleReq battle_id.
     * @member {number|null|undefined} battle_id
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.battle_id = null;

    /**
     * PB_BattleReq mode.
     * @member {PB_BattleMode|null|undefined} mode
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.mode = null;

    /**
     * PB_BattleReq battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.battle_type = null;

    /**
     * PB_BattleReq random_seed.
     * @member {number|null|undefined} random_seed
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.random_seed = null;

    /**
     * PB_BattleReq rules.
     * @member {PB_BattleRules.$Properties|null|undefined} rules
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.rules = null;

    /**
     * PB_BattleReq teams.
     * @member {Array.<PB_BattleTeam.$Properties>} teams
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.teams = $util.emptyArray;

    /**
     * PB_BattleReq req_type.
     * @member {number|null|undefined} req_type
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.req_type = null;

    /**
     * PB_BattleReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof PB_BattleReq
     * @instance
     */
    PB_BattleReq.prototype.req_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_battle_id", {
        get: $util.oneOfGetter($oneOfFields = ["battle_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_mode", {
        get: $util.oneOfGetter($oneOfFields = ["mode"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_random_seed", {
        get: $util.oneOfGetter($oneOfFields = ["random_seed"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_rules", {
        get: $util.oneOfGetter($oneOfFields = ["rules"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_BattleReq instance using the specified properties.
     * @function create
     * @memberof PB_BattleReq
     * @static
     * @param {PB_BattleReq.$Properties=} [properties] Properties to set
     * @returns {PB_BattleReq} PB_BattleReq instance
     * @type {{
     *   (properties: PB_BattleReq.$Shape): PB_BattleReq & PB_BattleReq.$Shape;
     *   (properties?: PB_BattleReq.$Properties): PB_BattleReq;
     * }}
     */
    PB_BattleReq.create = function(properties) {
        return new PB_BattleReq(properties);
    };

    /**
     * Encodes the specified PB_BattleReq message. Does not implicitly {@link PB_BattleReq.verify|verify} messages.
     * @function encode
     * @memberof PB_BattleReq
     * @static
     * @param {PB_BattleReq.$Properties} message PB_BattleReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.battle_id);
        if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.mode);
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.battle_type);
        if (message.random_seed != null && $Object.hasOwnProperty.call(message, "random_seed"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.random_seed);
        if (message.rules != null && $Object.hasOwnProperty.call(message, "rules"))
            $root.PB_BattleRules.encode(message.rules, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
        if (message.teams != null && message.teams.length)
            for (let i = 0; i < message.teams.length; ++i)
                $root.PB_BattleTeam.encode(message.teams[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 7, wireType 0 =*/56).int32(message.req_type);
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.req_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_BattleReq message, length delimited. Does not implicitly {@link PB_BattleReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_BattleReq
     * @static
     * @param {PB_BattleReq.$Properties} message PB_BattleReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_BattleReq message from the specified reader or buffer.
     * @function decode
     * @memberof PB_BattleReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_BattleReq & PB_BattleReq.$Shape} PB_BattleReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_BattleReq(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.battle_id = reader.int32();
                    message._battle_id = "battle_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.mode = reader.int32();
                    message._mode = "mode";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.random_seed = reader.int32();
                    message._random_seed = "random_seed";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    message.rules = $root.PB_BattleRules.decode(reader, reader.uint32(), $undefined, _depth + 1, message.rules);
                    message._rules = "rules";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    if (!(message.teams && message.teams.length))
                        message.teams = [];
                    message.teams.push($root.PB_BattleTeam.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 7: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 8: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_BattleReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_BattleReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_BattleReq & PB_BattleReq.$Shape} PB_BattleReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_BattleReq message.
     * @function verify
     * @memberof PB_BattleReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_BattleReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id")) {
            properties._battle_id = 1;
            if (!$util.isInteger(message.battle_id))
                return "battle_id: integer expected";
        }
        if (message.mode != null && $Object.hasOwnProperty.call(message, "mode")) {
            properties._mode = 1;
            if (typeof message.mode !== "number" || (message.mode | 0) !== message.mode)
                return "mode: enum value expected";
        }
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        if (message.random_seed != null && $Object.hasOwnProperty.call(message, "random_seed")) {
            properties._random_seed = 1;
            if (!$util.isInteger(message.random_seed))
                return "random_seed: integer expected";
        }
        if (message.rules != null && $Object.hasOwnProperty.call(message, "rules")) {
            properties._rules = 1;
            {
                let error = $root.PB_BattleRules.verify(message.rules, _depth + 1);
                if (error)
                    return "rules." + error;
            }
        }
        if (message.teams != null && $Object.hasOwnProperty.call(message, "teams")) {
            if (!$Array.isArray(message.teams))
                return "teams: array expected";
            for (let i = 0; i < message.teams.length; ++i) {
                let error = $root.PB_BattleTeam.verify(message.teams[i], _depth + 1);
                if (error)
                    return "teams." + error;
            }
        }
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_BattleReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_BattleReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_BattleReq} PB_BattleReq
     */
    PB_BattleReq.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_BattleReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_BattleReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_BattleReq();
        if (object.battle_id != null)
            message.battle_id = object.battle_id | 0;
        switch (object.mode) {
        case "BATTLE_MODE_NONE":
        case 0:
            message.mode = 0;
            break;
        case "BATTLE_MODE_TURN_BASED":
        case 1:
            message.mode = 1;
            break;
        case "BATTLE_MODE_REALTIME":
        case 2:
            message.mode = 2;
            break;
        default:
            if (typeof object.mode === "number" && (object.mode | 0) === object.mode)
                message.mode = object.mode;
        }
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        if (object.random_seed != null)
            message.random_seed = object.random_seed | 0;
        if (object.rules != null) {
            if (!$util.isObject(object.rules))
                throw $TypeError(".PB_BattleReq.rules: object expected");
            message.rules = $root.PB_BattleRules.fromObject(object.rules, _depth + 1);
        }
        if (object.teams) {
            if (!$Array.isArray(object.teams))
                throw $TypeError(".PB_BattleReq.teams: array expected");
            message.teams = $Array(object.teams.length);
            for (let i = 0; i < object.teams.length; ++i) {
                if (!$util.isObject(object.teams[i]))
                    throw $TypeError(".PB_BattleReq.teams: object expected");
                message.teams[i] = $root.PB_BattleTeam.fromObject(object.teams[i], _depth + 1);
            }
        }
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_BattleReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_BattleReq
     * @static
     * @param {PB_BattleReq} message PB_BattleReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_BattleReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.teams = [];
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            object.battle_id = message.battle_id;
        if (message.mode != null && $Object.hasOwnProperty.call(message, "mode"))
            object.mode = options.enums === $String ? $root.PB_BattleMode[message.mode] === $undefined ? message.mode : $root.PB_BattleMode[message.mode] : message.mode;
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        if (message.random_seed != null && $Object.hasOwnProperty.call(message, "random_seed"))
            object.random_seed = message.random_seed;
        if (message.rules != null && $Object.hasOwnProperty.call(message, "rules"))
            object.rules = $root.PB_BattleRules.toObject(message.rules, options, _depth + 1);
        if (message.teams && message.teams.length) {
            object.teams = $Array(message.teams.length);
            for (let j = 0; j < message.teams.length; ++j)
                object.teams[j] = $root.PB_BattleTeam.toObject(message.teams[j], options, _depth + 1);
        }
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        return object;
    };

    /**
     * Converts this PB_BattleReq to JSON.
     * @function toJSON
     * @memberof PB_BattleReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_BattleReq.prototype.toJSON = function() {
        return PB_BattleReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_BattleReq
     * @function getTypeUrl
     * @memberof PB_BattleReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_BattleReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_BattleReq";
    };

    return PB_BattleReq;
})();

export const PB_BattleResult = $root.PB_BattleResult = (() => {

    /**
     * Properties of a PB_BattleResult.
     * @typedef {Object} PB_BattleResult.$Properties
     * @property {number|null} [battle_id] PB_BattleResult battle_id
     * @property {PB_BattleResultType|null} [result_type] PB_BattleResult result_type
     * @property {number|null} [winner_team_id] PB_BattleResult winner_team_id
     * @property {number|null} [total_turns] PB_BattleResult total_turns
     * @property {number|null} [duration_ms] PB_BattleResult duration_ms
     * @property {string|null} [replay_text] PB_BattleResult replay_text
     * @property {Array.<PB_BattleParticipant.$Properties>|null} [final_participants] PB_BattleResult final_participants
     * @property {number|null} [req_type] PB_BattleResult req_type
     * @property {number|null} [req_id] PB_BattleResult req_id
     * @property {string|null} [report_key] PB_BattleResult report_key
     * @property {number|null} [battle_type] PB_BattleResult battle_type
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_BattleResult.
     * @exports IPB_BattleResult
     * @interface IPB_BattleResult
     * @augments PB_BattleResult.$Properties
     * @deprecated Use PB_BattleResult.$Properties instead.
     */

    /**
     * Shape of a PB_BattleResult.
     * @typedef {PB_BattleResult.$Properties} PB_BattleResult.$Shape
     */

    /**
     * Constructs a new PB_BattleResult.
     * @exports PB_BattleResult
     * @classdesc Represents a PB_BattleResult.
     * @constructor
     * @param {PB_BattleResult.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_BattleResult = function (properties) {
        this.final_participants = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_BattleResult battle_id.
     * @member {number|null|undefined} battle_id
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.battle_id = null;

    /**
     * PB_BattleResult result_type.
     * @member {PB_BattleResultType|null|undefined} result_type
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.result_type = null;

    /**
     * PB_BattleResult winner_team_id.
     * @member {number|null|undefined} winner_team_id
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.winner_team_id = null;

    /**
     * PB_BattleResult total_turns.
     * @member {number|null|undefined} total_turns
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.total_turns = null;

    /**
     * PB_BattleResult duration_ms.
     * @member {number|null|undefined} duration_ms
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.duration_ms = null;

    /**
     * PB_BattleResult replay_text.
     * @member {string|null|undefined} replay_text
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.replay_text = null;

    /**
     * PB_BattleResult final_participants.
     * @member {Array.<PB_BattleParticipant.$Properties>} final_participants
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.final_participants = $util.emptyArray;

    /**
     * PB_BattleResult req_type.
     * @member {number|null|undefined} req_type
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.req_type = null;

    /**
     * PB_BattleResult req_id.
     * @member {number|null|undefined} req_id
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.req_id = null;

    /**
     * PB_BattleResult report_key.
     * @member {string|null|undefined} report_key
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.report_key = null;

    /**
     * PB_BattleResult battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof PB_BattleResult
     * @instance
     */
    PB_BattleResult.prototype.battle_type = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_battle_id", {
        get: $util.oneOfGetter($oneOfFields = ["battle_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_result_type", {
        get: $util.oneOfGetter($oneOfFields = ["result_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_winner_team_id", {
        get: $util.oneOfGetter($oneOfFields = ["winner_team_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_total_turns", {
        get: $util.oneOfGetter($oneOfFields = ["total_turns"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_duration_ms", {
        get: $util.oneOfGetter($oneOfFields = ["duration_ms"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_replay_text", {
        get: $util.oneOfGetter($oneOfFields = ["replay_text"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_report_key", {
        get: $util.oneOfGetter($oneOfFields = ["report_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_BattleResult.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_BattleResult instance using the specified properties.
     * @function create
     * @memberof PB_BattleResult
     * @static
     * @param {PB_BattleResult.$Properties=} [properties] Properties to set
     * @returns {PB_BattleResult} PB_BattleResult instance
     * @type {{
     *   (properties: PB_BattleResult.$Shape): PB_BattleResult & PB_BattleResult.$Shape;
     *   (properties?: PB_BattleResult.$Properties): PB_BattleResult;
     * }}
     */
    PB_BattleResult.create = function(properties) {
        return new PB_BattleResult(properties);
    };

    /**
     * Encodes the specified PB_BattleResult message. Does not implicitly {@link PB_BattleResult.verify|verify} messages.
     * @function encode
     * @memberof PB_BattleResult
     * @static
     * @param {PB_BattleResult.$Properties} message PB_BattleResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleResult.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.battle_id);
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.result_type);
        if (message.winner_team_id != null && $Object.hasOwnProperty.call(message, "winner_team_id"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.winner_team_id);
        if (message.total_turns != null && $Object.hasOwnProperty.call(message, "total_turns"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.total_turns);
        if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.duration_ms);
        if (message.replay_text != null && $Object.hasOwnProperty.call(message, "replay_text"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.replay_text);
        if (message.final_participants != null && message.final_participants.length)
            for (let i = 0; i < message.final_participants.length; ++i)
                $root.PB_BattleParticipant.encode(message.final_participants[i], writer.uint32(/* id 7, wireType 2 =*/58).fork(), _depth + 1).ldelim();
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.req_type);
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 9, wireType 0 =*/72).int32(message.req_id);
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            writer.uint32(/* id 10, wireType 2 =*/82).string(message.report_key);
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 11, wireType 0 =*/88).int32(message.battle_type);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_BattleResult message, length delimited. Does not implicitly {@link PB_BattleResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_BattleResult
     * @static
     * @param {PB_BattleResult.$Properties} message PB_BattleResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_BattleResult.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_BattleResult message from the specified reader or buffer.
     * @function decode
     * @memberof PB_BattleResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_BattleResult & PB_BattleResult.$Shape} PB_BattleResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleResult.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_BattleResult(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.battle_id = reader.int32();
                    message._battle_id = "battle_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.result_type = reader.int32();
                    message._result_type = "result_type";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.winner_team_id = reader.int32();
                    message._winner_team_id = "winner_team_id";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.total_turns = reader.int32();
                    message._total_turns = "total_turns";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.duration_ms = reader.int32();
                    message._duration_ms = "duration_ms";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    message.replay_text = reader.stringVerify();
                    message._replay_text = "replay_text";
                    continue;
                }
            case 7: {
                    if (wireType !== 2)
                        break;
                    if (!(message.final_participants && message.final_participants.length))
                        message.final_participants = [];
                    message.final_participants.push($root.PB_BattleParticipant.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 8: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 9: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            case 10: {
                    if (wireType !== 2)
                        break;
                    message.report_key = reader.stringVerify();
                    message._report_key = "report_key";
                    continue;
                }
            case 11: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_BattleResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_BattleResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_BattleResult & PB_BattleResult.$Shape} PB_BattleResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_BattleResult.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_BattleResult message.
     * @function verify
     * @memberof PB_BattleResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_BattleResult.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id")) {
            properties._battle_id = 1;
            if (!$util.isInteger(message.battle_id))
                return "battle_id: integer expected";
        }
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type")) {
            properties._result_type = 1;
            if (typeof message.result_type !== "number" || (message.result_type | 0) !== message.result_type)
                return "result_type: enum value expected";
        }
        if (message.winner_team_id != null && $Object.hasOwnProperty.call(message, "winner_team_id")) {
            properties._winner_team_id = 1;
            if (!$util.isInteger(message.winner_team_id))
                return "winner_team_id: integer expected";
        }
        if (message.total_turns != null && $Object.hasOwnProperty.call(message, "total_turns")) {
            properties._total_turns = 1;
            if (!$util.isInteger(message.total_turns))
                return "total_turns: integer expected";
        }
        if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms")) {
            properties._duration_ms = 1;
            if (!$util.isInteger(message.duration_ms))
                return "duration_ms: integer expected";
        }
        if (message.replay_text != null && $Object.hasOwnProperty.call(message, "replay_text")) {
            properties._replay_text = 1;
            if (!$util.isString(message.replay_text))
                return "replay_text: string expected";
        }
        if (message.final_participants != null && $Object.hasOwnProperty.call(message, "final_participants")) {
            if (!$Array.isArray(message.final_participants))
                return "final_participants: array expected";
            for (let i = 0; i < message.final_participants.length; ++i) {
                let error = $root.PB_BattleParticipant.verify(message.final_participants[i], _depth + 1);
                if (error)
                    return "final_participants." + error;
            }
        }
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key")) {
            properties._report_key = 1;
            if (!$util.isString(message.report_key))
                return "report_key: string expected";
        }
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_BattleResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_BattleResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_BattleResult} PB_BattleResult
     */
    PB_BattleResult.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_BattleResult)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_BattleResult: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_BattleResult();
        if (object.battle_id != null)
            message.battle_id = object.battle_id | 0;
        switch (object.result_type) {
        case "BATTLE_RESULT_NONE":
        case 0:
            message.result_type = 0;
            break;
        case "BATTLE_RESULT_VICTORY":
        case 1:
            message.result_type = 1;
            break;
        case "BATTLE_RESULT_DEFEAT":
        case 2:
            message.result_type = 2;
            break;
        case "BATTLE_RESULT_TIME_OUT":
        case 3:
            message.result_type = 3;
            break;
        case "BATTLE_RESULT_DRAW":
        case 4:
            message.result_type = 4;
            break;
        case "BATTLE_RESULT_CANCELLED":
        case 5:
            message.result_type = 5;
            break;
        default:
            if (typeof object.result_type === "number" && (object.result_type | 0) === object.result_type)
                message.result_type = object.result_type;
        }
        if (object.winner_team_id != null)
            message.winner_team_id = object.winner_team_id | 0;
        if (object.total_turns != null)
            message.total_turns = object.total_turns | 0;
        if (object.duration_ms != null)
            message.duration_ms = object.duration_ms | 0;
        if (object.replay_text != null)
            message.replay_text = $String(object.replay_text);
        if (object.final_participants) {
            if (!$Array.isArray(object.final_participants))
                throw $TypeError(".PB_BattleResult.final_participants: array expected");
            message.final_participants = $Array(object.final_participants.length);
            for (let i = 0; i < object.final_participants.length; ++i) {
                if (!$util.isObject(object.final_participants[i]))
                    throw $TypeError(".PB_BattleResult.final_participants: object expected");
                message.final_participants[i] = $root.PB_BattleParticipant.fromObject(object.final_participants[i], _depth + 1);
            }
        }
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        if (object.report_key != null)
            message.report_key = $String(object.report_key);
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_BattleResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_BattleResult
     * @static
     * @param {PB_BattleResult} message PB_BattleResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_BattleResult.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.final_participants = [];
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            object.battle_id = message.battle_id;
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type"))
            object.result_type = options.enums === $String ? $root.PB_BattleResultType[message.result_type] === $undefined ? message.result_type : $root.PB_BattleResultType[message.result_type] : message.result_type;
        if (message.winner_team_id != null && $Object.hasOwnProperty.call(message, "winner_team_id"))
            object.winner_team_id = message.winner_team_id;
        if (message.total_turns != null && $Object.hasOwnProperty.call(message, "total_turns"))
            object.total_turns = message.total_turns;
        if (message.duration_ms != null && $Object.hasOwnProperty.call(message, "duration_ms"))
            object.duration_ms = message.duration_ms;
        if (message.replay_text != null && $Object.hasOwnProperty.call(message, "replay_text"))
            object.replay_text = message.replay_text;
        if (message.final_participants && message.final_participants.length) {
            object.final_participants = $Array(message.final_participants.length);
            for (let j = 0; j < message.final_participants.length; ++j)
                object.final_participants[j] = $root.PB_BattleParticipant.toObject(message.final_participants[j], options, _depth + 1);
        }
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            object.report_key = message.report_key;
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        return object;
    };

    /**
     * Converts this PB_BattleResult to JSON.
     * @function toJSON
     * @memberof PB_BattleResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_BattleResult.prototype.toJSON = function() {
        return PB_BattleResult.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_BattleResult
     * @function getTypeUrl
     * @memberof PB_BattleResult
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_BattleResult.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_BattleResult";
    };

    return PB_BattleResult;
})();

export const PB_ServerGlobalData = $root.PB_ServerGlobalData = (() => {

    /**
     * Properties of a PB_ServerGlobalData.
     * @typedef {Object} PB_ServerGlobalData.$Properties
     * @property {number|null} [server_begin_timestamp] PB_ServerGlobalData server_begin_timestamp
     * @property {number|null} [next_month_timestamp] PB_ServerGlobalData next_month_timestamp
     * @property {number|null} [next_week_timestamp] PB_ServerGlobalData next_week_timestamp
     * @property {number|null} [last_update_dayid] PB_ServerGlobalData last_update_dayid
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ServerGlobalData.
     * @exports IPB_ServerGlobalData
     * @interface IPB_ServerGlobalData
     * @augments PB_ServerGlobalData.$Properties
     * @deprecated Use PB_ServerGlobalData.$Properties instead.
     */

    /**
     * Shape of a PB_ServerGlobalData.
     * @typedef {PB_ServerGlobalData.$Properties} PB_ServerGlobalData.$Shape
     */

    /**
     * Constructs a new PB_ServerGlobalData.
     * @exports PB_ServerGlobalData
     * @classdesc Represents a PB_ServerGlobalData.
     * @constructor
     * @param {PB_ServerGlobalData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ServerGlobalData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ServerGlobalData server_begin_timestamp.
     * @member {number|null|undefined} server_begin_timestamp
     * @memberof PB_ServerGlobalData
     * @instance
     */
    PB_ServerGlobalData.prototype.server_begin_timestamp = null;

    /**
     * PB_ServerGlobalData next_month_timestamp.
     * @member {number|null|undefined} next_month_timestamp
     * @memberof PB_ServerGlobalData
     * @instance
     */
    PB_ServerGlobalData.prototype.next_month_timestamp = null;

    /**
     * PB_ServerGlobalData next_week_timestamp.
     * @member {number|null|undefined} next_week_timestamp
     * @memberof PB_ServerGlobalData
     * @instance
     */
    PB_ServerGlobalData.prototype.next_week_timestamp = null;

    /**
     * PB_ServerGlobalData last_update_dayid.
     * @member {number|null|undefined} last_update_dayid
     * @memberof PB_ServerGlobalData
     * @instance
     */
    PB_ServerGlobalData.prototype.last_update_dayid = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ServerGlobalData.prototype, "_server_begin_timestamp", {
        get: $util.oneOfGetter($oneOfFields = ["server_begin_timestamp"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ServerGlobalData.prototype, "_next_month_timestamp", {
        get: $util.oneOfGetter($oneOfFields = ["next_month_timestamp"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ServerGlobalData.prototype, "_next_week_timestamp", {
        get: $util.oneOfGetter($oneOfFields = ["next_week_timestamp"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ServerGlobalData.prototype, "_last_update_dayid", {
        get: $util.oneOfGetter($oneOfFields = ["last_update_dayid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ServerGlobalData instance using the specified properties.
     * @function create
     * @memberof PB_ServerGlobalData
     * @static
     * @param {PB_ServerGlobalData.$Properties=} [properties] Properties to set
     * @returns {PB_ServerGlobalData} PB_ServerGlobalData instance
     * @type {{
     *   (properties: PB_ServerGlobalData.$Shape): PB_ServerGlobalData & PB_ServerGlobalData.$Shape;
     *   (properties?: PB_ServerGlobalData.$Properties): PB_ServerGlobalData;
     * }}
     */
    PB_ServerGlobalData.create = function(properties) {
        return new PB_ServerGlobalData(properties);
    };

    /**
     * Encodes the specified PB_ServerGlobalData message. Does not implicitly {@link PB_ServerGlobalData.verify|verify} messages.
     * @function encode
     * @memberof PB_ServerGlobalData
     * @static
     * @param {PB_ServerGlobalData.$Properties} message PB_ServerGlobalData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ServerGlobalData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.server_begin_timestamp != null && $Object.hasOwnProperty.call(message, "server_begin_timestamp"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.server_begin_timestamp);
        if (message.next_month_timestamp != null && $Object.hasOwnProperty.call(message, "next_month_timestamp"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.next_month_timestamp);
        if (message.next_week_timestamp != null && $Object.hasOwnProperty.call(message, "next_week_timestamp"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.next_week_timestamp);
        if (message.last_update_dayid != null && $Object.hasOwnProperty.call(message, "last_update_dayid"))
            writer.uint32(/* id 4, wireType 0 =*/32).int64(message.last_update_dayid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ServerGlobalData message, length delimited. Does not implicitly {@link PB_ServerGlobalData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ServerGlobalData
     * @static
     * @param {PB_ServerGlobalData.$Properties} message PB_ServerGlobalData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ServerGlobalData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ServerGlobalData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ServerGlobalData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ServerGlobalData & PB_ServerGlobalData.$Shape} PB_ServerGlobalData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ServerGlobalData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ServerGlobalData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.server_begin_timestamp = reader.int64();
                    message._server_begin_timestamp = "server_begin_timestamp";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.next_month_timestamp = reader.int64();
                    message._next_month_timestamp = "next_month_timestamp";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.next_week_timestamp = reader.int64();
                    message._next_week_timestamp = "next_week_timestamp";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.last_update_dayid = reader.int64();
                    message._last_update_dayid = "last_update_dayid";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ServerGlobalData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ServerGlobalData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ServerGlobalData & PB_ServerGlobalData.$Shape} PB_ServerGlobalData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ServerGlobalData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ServerGlobalData message.
     * @function verify
     * @memberof PB_ServerGlobalData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ServerGlobalData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.server_begin_timestamp != null && $Object.hasOwnProperty.call(message, "server_begin_timestamp")) {
            properties._server_begin_timestamp = 1;
            if (!$util.isInteger(message.server_begin_timestamp) && !(message.server_begin_timestamp && $util.isInteger(message.server_begin_timestamp.low) && $util.isInteger(message.server_begin_timestamp.high)))
                return "server_begin_timestamp: integer|Long expected";
        }
        if (message.next_month_timestamp != null && $Object.hasOwnProperty.call(message, "next_month_timestamp")) {
            properties._next_month_timestamp = 1;
            if (!$util.isInteger(message.next_month_timestamp) && !(message.next_month_timestamp && $util.isInteger(message.next_month_timestamp.low) && $util.isInteger(message.next_month_timestamp.high)))
                return "next_month_timestamp: integer|Long expected";
        }
        if (message.next_week_timestamp != null && $Object.hasOwnProperty.call(message, "next_week_timestamp")) {
            properties._next_week_timestamp = 1;
            if (!$util.isInteger(message.next_week_timestamp) && !(message.next_week_timestamp && $util.isInteger(message.next_week_timestamp.low) && $util.isInteger(message.next_week_timestamp.high)))
                return "next_week_timestamp: integer|Long expected";
        }
        if (message.last_update_dayid != null && $Object.hasOwnProperty.call(message, "last_update_dayid")) {
            properties._last_update_dayid = 1;
            if (!$util.isInteger(message.last_update_dayid) && !(message.last_update_dayid && $util.isInteger(message.last_update_dayid.low) && $util.isInteger(message.last_update_dayid.high)))
                return "last_update_dayid: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a PB_ServerGlobalData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ServerGlobalData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ServerGlobalData} PB_ServerGlobalData
     */
    PB_ServerGlobalData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ServerGlobalData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ServerGlobalData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ServerGlobalData();
        if (object.server_begin_timestamp != null)
            if ($util.Long)
                message.server_begin_timestamp = $util.Long.fromValue(object.server_begin_timestamp, false);
            else if (typeof object.server_begin_timestamp === "string")
                message.server_begin_timestamp = $parseInt(object.server_begin_timestamp, 10);
            else if (typeof object.server_begin_timestamp === "number")
                message.server_begin_timestamp = object.server_begin_timestamp;
            else if (typeof object.server_begin_timestamp === "object")
                message.server_begin_timestamp = new $util.LongBits(object.server_begin_timestamp.low >>> 0, object.server_begin_timestamp.high >>> 0).toNumber();
        if (object.next_month_timestamp != null)
            if ($util.Long)
                message.next_month_timestamp = $util.Long.fromValue(object.next_month_timestamp, false);
            else if (typeof object.next_month_timestamp === "string")
                message.next_month_timestamp = $parseInt(object.next_month_timestamp, 10);
            else if (typeof object.next_month_timestamp === "number")
                message.next_month_timestamp = object.next_month_timestamp;
            else if (typeof object.next_month_timestamp === "object")
                message.next_month_timestamp = new $util.LongBits(object.next_month_timestamp.low >>> 0, object.next_month_timestamp.high >>> 0).toNumber();
        if (object.next_week_timestamp != null)
            if ($util.Long)
                message.next_week_timestamp = $util.Long.fromValue(object.next_week_timestamp, false);
            else if (typeof object.next_week_timestamp === "string")
                message.next_week_timestamp = $parseInt(object.next_week_timestamp, 10);
            else if (typeof object.next_week_timestamp === "number")
                message.next_week_timestamp = object.next_week_timestamp;
            else if (typeof object.next_week_timestamp === "object")
                message.next_week_timestamp = new $util.LongBits(object.next_week_timestamp.low >>> 0, object.next_week_timestamp.high >>> 0).toNumber();
        if (object.last_update_dayid != null)
            if ($util.Long)
                message.last_update_dayid = $util.Long.fromValue(object.last_update_dayid, false);
            else if (typeof object.last_update_dayid === "string")
                message.last_update_dayid = $parseInt(object.last_update_dayid, 10);
            else if (typeof object.last_update_dayid === "number")
                message.last_update_dayid = object.last_update_dayid;
            else if (typeof object.last_update_dayid === "object")
                message.last_update_dayid = new $util.LongBits(object.last_update_dayid.low >>> 0, object.last_update_dayid.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a PB_ServerGlobalData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ServerGlobalData
     * @static
     * @param {PB_ServerGlobalData} message PB_ServerGlobalData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ServerGlobalData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.server_begin_timestamp != null && $Object.hasOwnProperty.call(message, "server_begin_timestamp"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.server_begin_timestamp = typeof message.server_begin_timestamp === "number" ? $BigInt(message.server_begin_timestamp) : $util.Long.fromBits(message.server_begin_timestamp.low >>> 0, message.server_begin_timestamp.high >>> 0, false).toBigInt();
            else if (typeof message.server_begin_timestamp === "number")
                object.server_begin_timestamp = options.longs === $String ? $String(message.server_begin_timestamp) : message.server_begin_timestamp;
            else
                object.server_begin_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.server_begin_timestamp) : options.longs === $Number ? new $util.LongBits(message.server_begin_timestamp.low >>> 0, message.server_begin_timestamp.high >>> 0).toNumber() : message.server_begin_timestamp;
        if (message.next_month_timestamp != null && $Object.hasOwnProperty.call(message, "next_month_timestamp"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.next_month_timestamp = typeof message.next_month_timestamp === "number" ? $BigInt(message.next_month_timestamp) : $util.Long.fromBits(message.next_month_timestamp.low >>> 0, message.next_month_timestamp.high >>> 0, false).toBigInt();
            else if (typeof message.next_month_timestamp === "number")
                object.next_month_timestamp = options.longs === $String ? $String(message.next_month_timestamp) : message.next_month_timestamp;
            else
                object.next_month_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.next_month_timestamp) : options.longs === $Number ? new $util.LongBits(message.next_month_timestamp.low >>> 0, message.next_month_timestamp.high >>> 0).toNumber() : message.next_month_timestamp;
        if (message.next_week_timestamp != null && $Object.hasOwnProperty.call(message, "next_week_timestamp"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.next_week_timestamp = typeof message.next_week_timestamp === "number" ? $BigInt(message.next_week_timestamp) : $util.Long.fromBits(message.next_week_timestamp.low >>> 0, message.next_week_timestamp.high >>> 0, false).toBigInt();
            else if (typeof message.next_week_timestamp === "number")
                object.next_week_timestamp = options.longs === $String ? $String(message.next_week_timestamp) : message.next_week_timestamp;
            else
                object.next_week_timestamp = options.longs === $String ? $util.Long.prototype.toString.call(message.next_week_timestamp) : options.longs === $Number ? new $util.LongBits(message.next_week_timestamp.low >>> 0, message.next_week_timestamp.high >>> 0).toNumber() : message.next_week_timestamp;
        if (message.last_update_dayid != null && $Object.hasOwnProperty.call(message, "last_update_dayid"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.last_update_dayid = typeof message.last_update_dayid === "number" ? $BigInt(message.last_update_dayid) : $util.Long.fromBits(message.last_update_dayid.low >>> 0, message.last_update_dayid.high >>> 0, false).toBigInt();
            else if (typeof message.last_update_dayid === "number")
                object.last_update_dayid = options.longs === $String ? $String(message.last_update_dayid) : message.last_update_dayid;
            else
                object.last_update_dayid = options.longs === $String ? $util.Long.prototype.toString.call(message.last_update_dayid) : options.longs === $Number ? new $util.LongBits(message.last_update_dayid.low >>> 0, message.last_update_dayid.high >>> 0).toNumber() : message.last_update_dayid;
        return object;
    };

    /**
     * Converts this PB_ServerGlobalData to JSON.
     * @function toJSON
     * @memberof PB_ServerGlobalData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ServerGlobalData.prototype.toJSON = function() {
        return PB_ServerGlobalData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ServerGlobalData
     * @function getTypeUrl
     * @memberof PB_ServerGlobalData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ServerGlobalData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ServerGlobalData";
    };

    return PB_ServerGlobalData;
})();

export const PB_ChatData = $root.PB_ChatData = (() => {

    /**
     * Properties of a PB_ChatData.
     * @typedef {Object} PB_ChatData.$Properties
     * @property {number|null} [id] PB_ChatData id
     * @property {string|null} [chat_msg] PB_ChatData chat_msg
     * @property {string|null} [user_name] PB_ChatData user_name
     * @property {string|null} [name] PB_ChatData name
     * @property {number|null} [uid] PB_ChatData uid
     * @property {number|null} [time] PB_ChatData time
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ChatData.
     * @exports IPB_ChatData
     * @interface IPB_ChatData
     * @augments PB_ChatData.$Properties
     * @deprecated Use PB_ChatData.$Properties instead.
     */

    /**
     * Shape of a PB_ChatData.
     * @typedef {PB_ChatData.$Properties} PB_ChatData.$Shape
     */

    /**
     * Constructs a new PB_ChatData.
     * @exports PB_ChatData
     * @classdesc Represents a PB_ChatData.
     * @constructor
     * @param {PB_ChatData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ChatData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ChatData id.
     * @member {number|null|undefined} id
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.id = null;

    /**
     * PB_ChatData chat_msg.
     * @member {string|null|undefined} chat_msg
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.chat_msg = null;

    /**
     * PB_ChatData user_name.
     * @member {string|null|undefined} user_name
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.user_name = null;

    /**
     * PB_ChatData name.
     * @member {string|null|undefined} name
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.name = null;

    /**
     * PB_ChatData uid.
     * @member {number|null|undefined} uid
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.uid = null;

    /**
     * PB_ChatData time.
     * @member {number|null|undefined} time
     * @memberof PB_ChatData
     * @instance
     */
    PB_ChatData.prototype.time = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_chat_msg", {
        get: $util.oneOfGetter($oneOfFields = ["chat_msg"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ChatData.prototype, "_time", {
        get: $util.oneOfGetter($oneOfFields = ["time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ChatData instance using the specified properties.
     * @function create
     * @memberof PB_ChatData
     * @static
     * @param {PB_ChatData.$Properties=} [properties] Properties to set
     * @returns {PB_ChatData} PB_ChatData instance
     * @type {{
     *   (properties: PB_ChatData.$Shape): PB_ChatData & PB_ChatData.$Shape;
     *   (properties?: PB_ChatData.$Properties): PB_ChatData;
     * }}
     */
    PB_ChatData.create = function(properties) {
        return new PB_ChatData(properties);
    };

    /**
     * Encodes the specified PB_ChatData message. Does not implicitly {@link PB_ChatData.verify|verify} messages.
     * @function encode
     * @memberof PB_ChatData
     * @static
     * @param {PB_ChatData.$Properties} message PB_ChatData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ChatData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).uint64(message.id);
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.chat_msg);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.name);
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.uid);
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 6, wireType 0 =*/48).uint64(message.time);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ChatData message, length delimited. Does not implicitly {@link PB_ChatData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ChatData
     * @static
     * @param {PB_ChatData.$Properties} message PB_ChatData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ChatData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ChatData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ChatData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ChatData & PB_ChatData.$Shape} PB_ChatData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ChatData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ChatData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.uint64();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.chat_msg = reader.stringVerify();
                    message._chat_msg = "chat_msg";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.time = reader.uint64();
                    message._time = "time";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ChatData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ChatData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ChatData & PB_ChatData.$Shape} PB_ChatData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ChatData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ChatData message.
     * @function verify
     * @memberof PB_ChatData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ChatData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id) && !(message.id && $util.isInteger(message.id.low) && $util.isInteger(message.id.high)))
                return "id: integer|Long expected";
        }
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg")) {
            properties._chat_msg = 1;
            if (!$util.isString(message.chat_msg))
                return "chat_msg: string expected";
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.time != null && $Object.hasOwnProperty.call(message, "time")) {
            properties._time = 1;
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a PB_ChatData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ChatData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ChatData} PB_ChatData
     */
    PB_ChatData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ChatData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ChatData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ChatData();
        if (object.id != null)
            if ($util.Long)
                message.id = $util.Long.fromValue(object.id, true);
            else if (typeof object.id === "string")
                message.id = $parseInt(object.id, 10);
            else if (typeof object.id === "number")
                message.id = object.id;
            else if (typeof object.id === "object")
                message.id = new $util.LongBits(object.id.low >>> 0, object.id.high >>> 0).toNumber(true);
        if (object.chat_msg != null)
            message.chat_msg = $String(object.chat_msg);
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        if (object.name != null)
            message.name = $String(object.name);
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.time != null)
            if ($util.Long)
                message.time = $util.Long.fromValue(object.time, true);
            else if (typeof object.time === "string")
                message.time = $parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber(true);
        return message;
    };

    /**
     * Creates a plain object from a PB_ChatData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ChatData
     * @static
     * @param {PB_ChatData} message PB_ChatData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ChatData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.id = typeof message.id === "number" ? $BigInt(message.id) : $util.Long.fromBits(message.id.low >>> 0, message.id.high >>> 0, true).toBigInt();
            else if (typeof message.id === "number")
                object.id = options.longs === $String ? $String(message.id) : message.id;
            else
                object.id = options.longs === $String ? $util.Long.prototype.toString.call(message.id) : options.longs === $Number ? new $util.LongBits(message.id.low >>> 0, message.id.high >>> 0).toNumber(true) : message.id;
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg"))
            object.chat_msg = message.chat_msg;
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, true).toBigInt();
            else if (typeof message.time === "number")
                object.time = options.longs === $String ? $String(message.time) : message.time;
            else
                object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber(true) : message.time;
        return object;
    };

    /**
     * Converts this PB_ChatData to JSON.
     * @function toJSON
     * @memberof PB_ChatData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ChatData.prototype.toJSON = function() {
        return PB_ChatData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ChatData
     * @function getTypeUrl
     * @memberof PB_ChatData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ChatData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ChatData";
    };

    return PB_ChatData;
})();

export const PB_Item = $root.PB_Item = (() => {

    /**
     * Properties of a PB_Item.
     * @typedef {Object} PB_Item.$Properties
     * @property {number|null} [id] PB_Item id
     * @property {number|null} [num] PB_Item num
     * @property {Object.<string,number>|null} [stat_tags] PB_Item stat_tags
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_Item.
     * @exports IPB_Item
     * @interface IPB_Item
     * @augments PB_Item.$Properties
     * @deprecated Use PB_Item.$Properties instead.
     */

    /**
     * Shape of a PB_Item.
     * @typedef {PB_Item.$Properties} PB_Item.$Shape
     */

    /**
     * Constructs a new PB_Item.
     * @exports PB_Item
     * @classdesc Represents a PB_Item.
     * @constructor
     * @param {PB_Item.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_Item = function (properties) {
        this.stat_tags = {};
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_Item id.
     * @member {number|null|undefined} id
     * @memberof PB_Item
     * @instance
     */
    PB_Item.prototype.id = null;

    /**
     * PB_Item num.
     * @member {number|null|undefined} num
     * @memberof PB_Item
     * @instance
     */
    PB_Item.prototype.num = null;

    /**
     * PB_Item stat_tags.
     * @member {Object.<string,number>} stat_tags
     * @memberof PB_Item
     * @instance
     */
    PB_Item.prototype.stat_tags = $util.emptyObject;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_Item.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_Item.prototype, "_num", {
        get: $util.oneOfGetter($oneOfFields = ["num"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_Item instance using the specified properties.
     * @function create
     * @memberof PB_Item
     * @static
     * @param {PB_Item.$Properties=} [properties] Properties to set
     * @returns {PB_Item} PB_Item instance
     * @type {{
     *   (properties: PB_Item.$Shape): PB_Item & PB_Item.$Shape;
     *   (properties?: PB_Item.$Properties): PB_Item;
     * }}
     */
    PB_Item.create = function(properties) {
        return new PB_Item(properties);
    };

    /**
     * Encodes the specified PB_Item message. Does not implicitly {@link PB_Item.verify|verify} messages.
     * @function encode
     * @memberof PB_Item
     * @static
     * @param {PB_Item.$Properties} message PB_Item message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_Item.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.num);
        if (message.stat_tags != null && $Object.hasOwnProperty.call(message, "stat_tags"))
            for (let keys = $Object.keys(message.stat_tags), i = 0; i < keys.length; ++i)
                writer.uint32(/* id 3, wireType 2 =*/26).fork().uint32(/* id 1, wireType 2 =*/10).string(keys[i]).uint32(/* id 2, wireType 0 =*/16).int32(message.stat_tags[keys[i]]).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_Item message, length delimited. Does not implicitly {@link PB_Item.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_Item
     * @static
     * @param {PB_Item.$Properties} message PB_Item message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_Item.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_Item message from the specified reader or buffer.
     * @function decode
     * @memberof PB_Item
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_Item & PB_Item.$Shape} PB_Item
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_Item.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_Item(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.num = reader.int32();
                    message._num = "num";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    if (message.stat_tags === $util.emptyObject)
                        message.stat_tags = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = "";
                    value = 0;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 2)
                                break;
                            key = reader.stringVerify();
                            continue;
                        case 2:
                            if (wireType !== 0)
                                break;
                            value = reader.int32();
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    if (key === "__proto__")
                        $util.makeProp(message.stat_tags, key);
                    message.stat_tags[key] = value;
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_Item message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_Item
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_Item & PB_Item.$Shape} PB_Item
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_Item.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_Item message.
     * @function verify
     * @memberof PB_Item
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_Item.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.num != null && $Object.hasOwnProperty.call(message, "num")) {
            properties._num = 1;
            if (!$util.isInteger(message.num))
                return "num: integer expected";
        }
        if (message.stat_tags != null && $Object.hasOwnProperty.call(message, "stat_tags")) {
            if (!$util.isObject(message.stat_tags))
                return "stat_tags: object expected";
            let key = $Object.keys(message.stat_tags);
            for (let i = 0; i < key.length; ++i)
                if (!$util.isInteger(message.stat_tags[key[i]]))
                    return "stat_tags: integer{k:string} expected";
        }
        return null;
    };

    /**
     * Creates a PB_Item message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_Item
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_Item} PB_Item
     */
    PB_Item.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_Item)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_Item: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_Item();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.num != null)
            message.num = object.num | 0;
        if (object.stat_tags) {
            if (!$util.isObject(object.stat_tags))
                throw $TypeError(".PB_Item.stat_tags: object expected");
            message.stat_tags = {};
            for (let keys = $Object.keys(object.stat_tags), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.stat_tags, keys[i]);
                message.stat_tags[keys[i]] = object.stat_tags[keys[i]] | 0;
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_Item message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_Item
     * @static
     * @param {PB_Item} message PB_Item
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_Item.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.objects || options.defaults)
            object.stat_tags = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
            object.num = message.num;
        let keys2;
        if (message.stat_tags && (keys2 = $Object.keys(message.stat_tags)).length) {
            object.stat_tags = {};
            for (let j = 0; j < keys2.length; ++j) {
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.stat_tags, keys2[j]);
                object.stat_tags[keys2[j]] = message.stat_tags[keys2[j]];
            }
        }
        return object;
    };

    /**
     * Converts this PB_Item to JSON.
     * @function toJSON
     * @memberof PB_Item
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_Item.prototype.toJSON = function() {
        return PB_Item.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_Item
     * @function getTypeUrl
     * @memberof PB_Item
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_Item.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_Item";
    };

    return PB_Item;
})();

export const PB_RoleBagItem = $root.PB_RoleBagItem = (() => {

    /**
     * Properties of a PB_RoleBagItem.
     * @typedef {Object} PB_RoleBagItem.$Properties
     * @property {number|null} [bag_type] PB_RoleBagItem bag_type
     * @property {Array.<PB_Item.$Properties>|null} [items] PB_RoleBagItem items
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RoleBagItem.
     * @exports IPB_RoleBagItem
     * @interface IPB_RoleBagItem
     * @augments PB_RoleBagItem.$Properties
     * @deprecated Use PB_RoleBagItem.$Properties instead.
     */

    /**
     * Shape of a PB_RoleBagItem.
     * @typedef {PB_RoleBagItem.$Properties} PB_RoleBagItem.$Shape
     */

    /**
     * Constructs a new PB_RoleBagItem.
     * @exports PB_RoleBagItem
     * @classdesc Represents a PB_RoleBagItem.
     * @constructor
     * @param {PB_RoleBagItem.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RoleBagItem = function (properties) {
        this.items = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RoleBagItem bag_type.
     * @member {number|null|undefined} bag_type
     * @memberof PB_RoleBagItem
     * @instance
     */
    PB_RoleBagItem.prototype.bag_type = null;

    /**
     * PB_RoleBagItem items.
     * @member {Array.<PB_Item.$Properties>} items
     * @memberof PB_RoleBagItem
     * @instance
     */
    PB_RoleBagItem.prototype.items = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleBagItem.prototype, "_bag_type", {
        get: $util.oneOfGetter($oneOfFields = ["bag_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_RoleBagItem instance using the specified properties.
     * @function create
     * @memberof PB_RoleBagItem
     * @static
     * @param {PB_RoleBagItem.$Properties=} [properties] Properties to set
     * @returns {PB_RoleBagItem} PB_RoleBagItem instance
     * @type {{
     *   (properties: PB_RoleBagItem.$Shape): PB_RoleBagItem & PB_RoleBagItem.$Shape;
     *   (properties?: PB_RoleBagItem.$Properties): PB_RoleBagItem;
     * }}
     */
    PB_RoleBagItem.create = function(properties) {
        return new PB_RoleBagItem(properties);
    };

    /**
     * Encodes the specified PB_RoleBagItem message. Does not implicitly {@link PB_RoleBagItem.verify|verify} messages.
     * @function encode
     * @memberof PB_RoleBagItem
     * @static
     * @param {PB_RoleBagItem.$Properties} message PB_RoleBagItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBagItem.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.bag_type != null && $Object.hasOwnProperty.call(message, "bag_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.bag_type);
        if (message.items != null && message.items.length)
            for (let i = 0; i < message.items.length; ++i)
                $root.PB_Item.encode(message.items[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RoleBagItem message, length delimited. Does not implicitly {@link PB_RoleBagItem.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RoleBagItem
     * @static
     * @param {PB_RoleBagItem.$Properties} message PB_RoleBagItem message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBagItem.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RoleBagItem message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RoleBagItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RoleBagItem & PB_RoleBagItem.$Shape} PB_RoleBagItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBagItem.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RoleBagItem();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.bag_type = reader.int32();
                    message._bag_type = "bag_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.items && message.items.length))
                        message.items = [];
                    message.items.push($root.PB_Item.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RoleBagItem message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RoleBagItem
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RoleBagItem & PB_RoleBagItem.$Shape} PB_RoleBagItem
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBagItem.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RoleBagItem message.
     * @function verify
     * @memberof PB_RoleBagItem
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RoleBagItem.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.bag_type != null && $Object.hasOwnProperty.call(message, "bag_type")) {
            properties._bag_type = 1;
            if (!$util.isInteger(message.bag_type))
                return "bag_type: integer expected";
        }
        if (message.items != null && $Object.hasOwnProperty.call(message, "items")) {
            if (!$Array.isArray(message.items))
                return "items: array expected";
            for (let i = 0; i < message.items.length; ++i) {
                let error = $root.PB_Item.verify(message.items[i], _depth + 1);
                if (error)
                    return "items." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_RoleBagItem message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RoleBagItem
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RoleBagItem} PB_RoleBagItem
     */
    PB_RoleBagItem.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RoleBagItem)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RoleBagItem: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RoleBagItem();
        if (object.bag_type != null)
            message.bag_type = object.bag_type | 0;
        if (object.items) {
            if (!$Array.isArray(object.items))
                throw $TypeError(".PB_RoleBagItem.items: array expected");
            message.items = $Array(object.items.length);
            for (let i = 0; i < object.items.length; ++i) {
                if (!$util.isObject(object.items[i]))
                    throw $TypeError(".PB_RoleBagItem.items: object expected");
                message.items[i] = $root.PB_Item.fromObject(object.items[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_RoleBagItem message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RoleBagItem
     * @static
     * @param {PB_RoleBagItem} message PB_RoleBagItem
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RoleBagItem.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.items = [];
        if (message.bag_type != null && $Object.hasOwnProperty.call(message, "bag_type"))
            object.bag_type = message.bag_type;
        if (message.items && message.items.length) {
            object.items = $Array(message.items.length);
            for (let j = 0; j < message.items.length; ++j)
                object.items[j] = $root.PB_Item.toObject(message.items[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_RoleBagItem to JSON.
     * @function toJSON
     * @memberof PB_RoleBagItem
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RoleBagItem.prototype.toJSON = function() {
        return PB_RoleBagItem.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RoleBagItem
     * @function getTypeUrl
     * @memberof PB_RoleBagItem
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RoleBagItem.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RoleBagItem";
    };

    return PB_RoleBagItem;
})();

export const PB_RoleBag = $root.PB_RoleBag = (() => {

    /**
     * Properties of a PB_RoleBag.
     * @typedef {Object} PB_RoleBag.$Properties
     * @property {Object.<string,PB_RoleBagItem.$Properties>|null} [bag] PB_RoleBag bag
     * @property {Array.<PB_Item.$Properties>|null} [weare_item] PB_RoleBag weare_item
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RoleBag.
     * @exports IPB_RoleBag
     * @interface IPB_RoleBag
     * @augments PB_RoleBag.$Properties
     * @deprecated Use PB_RoleBag.$Properties instead.
     */

    /**
     * Shape of a PB_RoleBag.
     * @typedef {PB_RoleBag.$Properties} PB_RoleBag.$Shape
     */

    /**
     * Constructs a new PB_RoleBag.
     * @exports PB_RoleBag
     * @classdesc Represents a PB_RoleBag.
     * @constructor
     * @param {PB_RoleBag.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RoleBag = function (properties) {
        this.bag = {};
        this.weare_item = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RoleBag bag.
     * @member {Object.<string,PB_RoleBagItem.$Properties>} bag
     * @memberof PB_RoleBag
     * @instance
     */
    PB_RoleBag.prototype.bag = $util.emptyObject;

    /**
     * PB_RoleBag weare_item.
     * @member {Array.<PB_Item.$Properties>} weare_item
     * @memberof PB_RoleBag
     * @instance
     */
    PB_RoleBag.prototype.weare_item = $util.emptyArray;

    /**
     * Creates a new PB_RoleBag instance using the specified properties.
     * @function create
     * @memberof PB_RoleBag
     * @static
     * @param {PB_RoleBag.$Properties=} [properties] Properties to set
     * @returns {PB_RoleBag} PB_RoleBag instance
     * @type {{
     *   (properties: PB_RoleBag.$Shape): PB_RoleBag & PB_RoleBag.$Shape;
     *   (properties?: PB_RoleBag.$Properties): PB_RoleBag;
     * }}
     */
    PB_RoleBag.create = function(properties) {
        return new PB_RoleBag(properties);
    };

    /**
     * Encodes the specified PB_RoleBag message. Does not implicitly {@link PB_RoleBag.verify|verify} messages.
     * @function encode
     * @memberof PB_RoleBag
     * @static
     * @param {PB_RoleBag.$Properties} message PB_RoleBag message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBag.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.bag != null && $Object.hasOwnProperty.call(message, "bag"))
            for (let keys = $Object.keys(message.bag), i = 0; i < keys.length; ++i) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                $root.PB_RoleBagItem.encode(message.bag[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
            }
        if (message.weare_item != null && message.weare_item.length)
            for (let i = 0; i < message.weare_item.length; ++i)
                $root.PB_Item.encode(message.weare_item[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RoleBag message, length delimited. Does not implicitly {@link PB_RoleBag.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RoleBag
     * @static
     * @param {PB_RoleBag.$Properties} message PB_RoleBag message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBag.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RoleBag message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RoleBag
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RoleBag & PB_RoleBag.$Shape} PB_RoleBag
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBag.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RoleBag(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    if (message.bag === $util.emptyObject)
                        message.bag = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = 0;
                    value = null;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 0)
                                break;
                            key = reader.int32();
                            continue;
                        case 2:
                            if (wireType !== 2)
                                break;
                            value = $root.PB_RoleBagItem.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    message.bag[key] = value || new $root.PB_RoleBagItem();
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.weare_item && message.weare_item.length))
                        message.weare_item = [];
                    message.weare_item.push($root.PB_Item.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RoleBag message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RoleBag
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RoleBag & PB_RoleBag.$Shape} PB_RoleBag
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBag.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RoleBag message.
     * @function verify
     * @memberof PB_RoleBag
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RoleBag.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.bag != null && $Object.hasOwnProperty.call(message, "bag")) {
            if (!$util.isObject(message.bag))
                return "bag: object expected";
            let key = $Object.keys(message.bag);
            for (let i = 0; i < key.length; ++i) {
                if (!$util.key32Re.test(key[i]))
                    return "bag: integer key{k:int32} expected";
                {
                    let error = $root.PB_RoleBagItem.verify(message.bag[key[i]], _depth + 1);
                    if (error)
                        return "bag." + error;
                }
            }
        }
        if (message.weare_item != null && $Object.hasOwnProperty.call(message, "weare_item")) {
            if (!$Array.isArray(message.weare_item))
                return "weare_item: array expected";
            for (let i = 0; i < message.weare_item.length; ++i) {
                let error = $root.PB_Item.verify(message.weare_item[i], _depth + 1);
                if (error)
                    return "weare_item." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_RoleBag message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RoleBag
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RoleBag} PB_RoleBag
     */
    PB_RoleBag.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RoleBag)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RoleBag: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RoleBag();
        if (object.bag) {
            if (!$util.isObject(object.bag))
                throw $TypeError(".PB_RoleBag.bag: object expected");
            message.bag = {};
            for (let keys = $Object.keys(object.bag), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.bag, keys[i]);
                if (!$util.isObject(object.bag[keys[i]]))
                    throw $TypeError(".PB_RoleBag.bag: object expected");
                message.bag[keys[i]] = $root.PB_RoleBagItem.fromObject(object.bag[keys[i]], _depth + 1);
            }
        }
        if (object.weare_item) {
            if (!$Array.isArray(object.weare_item))
                throw $TypeError(".PB_RoleBag.weare_item: array expected");
            message.weare_item = $Array(object.weare_item.length);
            for (let i = 0; i < object.weare_item.length; ++i) {
                if (!$util.isObject(object.weare_item[i]))
                    throw $TypeError(".PB_RoleBag.weare_item: object expected");
                message.weare_item[i] = $root.PB_Item.fromObject(object.weare_item[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_RoleBag message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RoleBag
     * @static
     * @param {PB_RoleBag} message PB_RoleBag
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RoleBag.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.weare_item = [];
        if (options.objects || options.defaults)
            object.bag = {};
        let keys2;
        if (message.bag && (keys2 = $Object.keys(message.bag)).length) {
            object.bag = {};
            for (let j = 0; j < keys2.length; ++j) {
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.bag, keys2[j]);
                object.bag[keys2[j]] = $root.PB_RoleBagItem.toObject(message.bag[keys2[j]], options, _depth + 1);
            }
        }
        if (message.weare_item && message.weare_item.length) {
            object.weare_item = $Array(message.weare_item.length);
            for (let j = 0; j < message.weare_item.length; ++j)
                object.weare_item[j] = $root.PB_Item.toObject(message.weare_item[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_RoleBag to JSON.
     * @function toJSON
     * @memberof PB_RoleBag
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RoleBag.prototype.toJSON = function() {
        return PB_RoleBag.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RoleBag
     * @function getTypeUrl
     * @memberof PB_RoleBag
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RoleBag.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RoleBag";
    };

    return PB_RoleBag;
})();

export const PB_RoleBaseInfo = $root.PB_RoleBaseInfo = (() => {

    /**
     * Properties of a PB_RoleBaseInfo.
     * @typedef {Object} PB_RoleBaseInfo.$Properties
     * @property {number|null} [uid] PB_RoleBaseInfo uid
     * @property {string|null} [name] PB_RoleBaseInfo name
     * @property {string|null} [user_name] PB_RoleBaseInfo user_name
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RoleBaseInfo.
     * @exports IPB_RoleBaseInfo
     * @interface IPB_RoleBaseInfo
     * @augments PB_RoleBaseInfo.$Properties
     * @deprecated Use PB_RoleBaseInfo.$Properties instead.
     */

    /**
     * Shape of a PB_RoleBaseInfo.
     * @typedef {PB_RoleBaseInfo.$Properties} PB_RoleBaseInfo.$Shape
     */

    /**
     * Constructs a new PB_RoleBaseInfo.
     * @exports PB_RoleBaseInfo
     * @classdesc Represents a PB_RoleBaseInfo.
     * @constructor
     * @param {PB_RoleBaseInfo.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RoleBaseInfo = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RoleBaseInfo uid.
     * @member {number|null|undefined} uid
     * @memberof PB_RoleBaseInfo
     * @instance
     */
    PB_RoleBaseInfo.prototype.uid = null;

    /**
     * PB_RoleBaseInfo name.
     * @member {string|null|undefined} name
     * @memberof PB_RoleBaseInfo
     * @instance
     */
    PB_RoleBaseInfo.prototype.name = null;

    /**
     * PB_RoleBaseInfo user_name.
     * @member {string|null|undefined} user_name
     * @memberof PB_RoleBaseInfo
     * @instance
     */
    PB_RoleBaseInfo.prototype.user_name = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleBaseInfo.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleBaseInfo.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleBaseInfo.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_RoleBaseInfo instance using the specified properties.
     * @function create
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {PB_RoleBaseInfo.$Properties=} [properties] Properties to set
     * @returns {PB_RoleBaseInfo} PB_RoleBaseInfo instance
     * @type {{
     *   (properties: PB_RoleBaseInfo.$Shape): PB_RoleBaseInfo & PB_RoleBaseInfo.$Shape;
     *   (properties?: PB_RoleBaseInfo.$Properties): PB_RoleBaseInfo;
     * }}
     */
    PB_RoleBaseInfo.create = function(properties) {
        return new PB_RoleBaseInfo(properties);
    };

    /**
     * Encodes the specified PB_RoleBaseInfo message. Does not implicitly {@link PB_RoleBaseInfo.verify|verify} messages.
     * @function encode
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {PB_RoleBaseInfo.$Properties} message PB_RoleBaseInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBaseInfo.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.uid);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RoleBaseInfo message, length delimited. Does not implicitly {@link PB_RoleBaseInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {PB_RoleBaseInfo.$Properties} message PB_RoleBaseInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleBaseInfo.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RoleBaseInfo message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RoleBaseInfo & PB_RoleBaseInfo.$Shape} PB_RoleBaseInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBaseInfo.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RoleBaseInfo();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RoleBaseInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RoleBaseInfo & PB_RoleBaseInfo.$Shape} PB_RoleBaseInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleBaseInfo.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RoleBaseInfo message.
     * @function verify
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RoleBaseInfo.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        return null;
    };

    /**
     * Creates a PB_RoleBaseInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RoleBaseInfo} PB_RoleBaseInfo
     */
    PB_RoleBaseInfo.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RoleBaseInfo)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RoleBaseInfo: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RoleBaseInfo();
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.name != null)
            message.name = $String(object.name);
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        return message;
    };

    /**
     * Creates a plain object from a PB_RoleBaseInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {PB_RoleBaseInfo} message PB_RoleBaseInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RoleBaseInfo.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        return object;
    };

    /**
     * Converts this PB_RoleBaseInfo to JSON.
     * @function toJSON
     * @memberof PB_RoleBaseInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RoleBaseInfo.prototype.toJSON = function() {
        return PB_RoleBaseInfo.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RoleBaseInfo
     * @function getTypeUrl
     * @memberof PB_RoleBaseInfo
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RoleBaseInfo.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RoleBaseInfo";
    };

    return PB_RoleBaseInfo;
})();

export const PB_RoleData = $root.PB_RoleData = (() => {

    /**
     * Properties of a PB_RoleData.
     * @typedef {Object} PB_RoleData.$Properties
     * @property {string|null} [lua_role_data] PB_RoleData lua_role_data
     * @property {PB_RoleBaseInfo.$Properties|null} [base_info] PB_RoleData base_info
     * @property {PB_RoleBag.$Properties|null} [role_bag] PB_RoleData role_bag
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RoleData.
     * @exports IPB_RoleData
     * @interface IPB_RoleData
     * @augments PB_RoleData.$Properties
     * @deprecated Use PB_RoleData.$Properties instead.
     */

    /**
     * Shape of a PB_RoleData.
     * @typedef {PB_RoleData.$Properties} PB_RoleData.$Shape
     */

    /**
     * Constructs a new PB_RoleData.
     * @exports PB_RoleData
     * @classdesc Represents a PB_RoleData.
     * @constructor
     * @param {PB_RoleData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RoleData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RoleData lua_role_data.
     * @member {string|null|undefined} lua_role_data
     * @memberof PB_RoleData
     * @instance
     */
    PB_RoleData.prototype.lua_role_data = null;

    /**
     * PB_RoleData base_info.
     * @member {PB_RoleBaseInfo.$Properties|null|undefined} base_info
     * @memberof PB_RoleData
     * @instance
     */
    PB_RoleData.prototype.base_info = null;

    /**
     * PB_RoleData role_bag.
     * @member {PB_RoleBag.$Properties|null|undefined} role_bag
     * @memberof PB_RoleData
     * @instance
     */
    PB_RoleData.prototype.role_bag = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleData.prototype, "_lua_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["lua_role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleData.prototype, "_base_info", {
        get: $util.oneOfGetter($oneOfFields = ["base_info"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RoleData.prototype, "_role_bag", {
        get: $util.oneOfGetter($oneOfFields = ["role_bag"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_RoleData instance using the specified properties.
     * @function create
     * @memberof PB_RoleData
     * @static
     * @param {PB_RoleData.$Properties=} [properties] Properties to set
     * @returns {PB_RoleData} PB_RoleData instance
     * @type {{
     *   (properties: PB_RoleData.$Shape): PB_RoleData & PB_RoleData.$Shape;
     *   (properties?: PB_RoleData.$Properties): PB_RoleData;
     * }}
     */
    PB_RoleData.create = function(properties) {
        return new PB_RoleData(properties);
    };

    /**
     * Encodes the specified PB_RoleData message. Does not implicitly {@link PB_RoleData.verify|verify} messages.
     * @function encode
     * @memberof PB_RoleData
     * @static
     * @param {PB_RoleData.$Properties} message PB_RoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.lua_role_data != null && $Object.hasOwnProperty.call(message, "lua_role_data"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.lua_role_data);
        if (message.base_info != null && $Object.hasOwnProperty.call(message, "base_info"))
            $root.PB_RoleBaseInfo.encode(message.base_info, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.role_bag != null && $Object.hasOwnProperty.call(message, "role_bag"))
            $root.PB_RoleBag.encode(message.role_bag, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RoleData message, length delimited. Does not implicitly {@link PB_RoleData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RoleData
     * @static
     * @param {PB_RoleData.$Properties} message PB_RoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RoleData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RoleData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RoleData & PB_RoleData.$Shape} PB_RoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RoleData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.lua_role_data = reader.stringVerify();
                    message._lua_role_data = "lua_role_data";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.base_info = $root.PB_RoleBaseInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.base_info);
                    message._base_info = "base_info";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.role_bag = $root.PB_RoleBag.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_bag);
                    message._role_bag = "role_bag";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RoleData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RoleData & PB_RoleData.$Shape} PB_RoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RoleData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RoleData message.
     * @function verify
     * @memberof PB_RoleData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RoleData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.lua_role_data != null && $Object.hasOwnProperty.call(message, "lua_role_data")) {
            properties._lua_role_data = 1;
            if (!$util.isString(message.lua_role_data))
                return "lua_role_data: string expected";
        }
        if (message.base_info != null && $Object.hasOwnProperty.call(message, "base_info")) {
            properties._base_info = 1;
            {
                let error = $root.PB_RoleBaseInfo.verify(message.base_info, _depth + 1);
                if (error)
                    return "base_info." + error;
            }
        }
        if (message.role_bag != null && $Object.hasOwnProperty.call(message, "role_bag")) {
            properties._role_bag = 1;
            {
                let error = $root.PB_RoleBag.verify(message.role_bag, _depth + 1);
                if (error)
                    return "role_bag." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_RoleData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RoleData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RoleData} PB_RoleData
     */
    PB_RoleData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RoleData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RoleData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RoleData();
        if (object.lua_role_data != null)
            message.lua_role_data = $String(object.lua_role_data);
        if (object.base_info != null) {
            if (!$util.isObject(object.base_info))
                throw $TypeError(".PB_RoleData.base_info: object expected");
            message.base_info = $root.PB_RoleBaseInfo.fromObject(object.base_info, _depth + 1);
        }
        if (object.role_bag != null) {
            if (!$util.isObject(object.role_bag))
                throw $TypeError(".PB_RoleData.role_bag: object expected");
            message.role_bag = $root.PB_RoleBag.fromObject(object.role_bag, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_RoleData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RoleData
     * @static
     * @param {PB_RoleData} message PB_RoleData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RoleData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.lua_role_data != null && $Object.hasOwnProperty.call(message, "lua_role_data"))
            object.lua_role_data = message.lua_role_data;
        if (message.base_info != null && $Object.hasOwnProperty.call(message, "base_info"))
            object.base_info = $root.PB_RoleBaseInfo.toObject(message.base_info, options, _depth + 1);
        if (message.role_bag != null && $Object.hasOwnProperty.call(message, "role_bag"))
            object.role_bag = $root.PB_RoleBag.toObject(message.role_bag, options, _depth + 1);
        return object;
    };

    /**
     * Converts this PB_RoleData to JSON.
     * @function toJSON
     * @memberof PB_RoleData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RoleData.prototype.toJSON = function() {
        return PB_RoleData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RoleData
     * @function getTypeUrl
     * @memberof PB_RoleData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RoleData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RoleData";
    };

    return PB_RoleData;
})();

export const PB_InventorySlot = $root.PB_InventorySlot = (() => {

    /**
     * Properties of a PB_InventorySlot.
     * @typedef {Object} PB_InventorySlot.$Properties
     * @property {number|null} [slot_index] PB_InventorySlot slot_index
     * @property {number|null} [id] PB_InventorySlot id
     * @property {number|null} [num] PB_InventorySlot num
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_InventorySlot.
     * @exports IPB_InventorySlot
     * @interface IPB_InventorySlot
     * @augments PB_InventorySlot.$Properties
     * @deprecated Use PB_InventorySlot.$Properties instead.
     */

    /**
     * Shape of a PB_InventorySlot.
     * @typedef {PB_InventorySlot.$Properties} PB_InventorySlot.$Shape
     */

    /**
     * Constructs a new PB_InventorySlot.
     * @exports PB_InventorySlot
     * @classdesc Represents a PB_InventorySlot.
     * @constructor
     * @param {PB_InventorySlot.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_InventorySlot = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_InventorySlot slot_index.
     * @member {number|null|undefined} slot_index
     * @memberof PB_InventorySlot
     * @instance
     */
    PB_InventorySlot.prototype.slot_index = null;

    /**
     * PB_InventorySlot id.
     * @member {number|null|undefined} id
     * @memberof PB_InventorySlot
     * @instance
     */
    PB_InventorySlot.prototype.id = null;

    /**
     * PB_InventorySlot num.
     * @member {number|null|undefined} num
     * @memberof PB_InventorySlot
     * @instance
     */
    PB_InventorySlot.prototype.num = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_InventorySlot.prototype, "_slot_index", {
        get: $util.oneOfGetter($oneOfFields = ["slot_index"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_InventorySlot.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_InventorySlot.prototype, "_num", {
        get: $util.oneOfGetter($oneOfFields = ["num"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_InventorySlot instance using the specified properties.
     * @function create
     * @memberof PB_InventorySlot
     * @static
     * @param {PB_InventorySlot.$Properties=} [properties] Properties to set
     * @returns {PB_InventorySlot} PB_InventorySlot instance
     * @type {{
     *   (properties: PB_InventorySlot.$Shape): PB_InventorySlot & PB_InventorySlot.$Shape;
     *   (properties?: PB_InventorySlot.$Properties): PB_InventorySlot;
     * }}
     */
    PB_InventorySlot.create = function(properties) {
        return new PB_InventorySlot(properties);
    };

    /**
     * Encodes the specified PB_InventorySlot message. Does not implicitly {@link PB_InventorySlot.verify|verify} messages.
     * @function encode
     * @memberof PB_InventorySlot
     * @static
     * @param {PB_InventorySlot.$Properties} message PB_InventorySlot message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InventorySlot.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.slot_index != null && $Object.hasOwnProperty.call(message, "slot_index"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.slot_index);
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.id);
        if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.num);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_InventorySlot message, length delimited. Does not implicitly {@link PB_InventorySlot.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_InventorySlot
     * @static
     * @param {PB_InventorySlot.$Properties} message PB_InventorySlot message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InventorySlot.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_InventorySlot message from the specified reader or buffer.
     * @function decode
     * @memberof PB_InventorySlot
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_InventorySlot & PB_InventorySlot.$Shape} PB_InventorySlot
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InventorySlot.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_InventorySlot();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.slot_index = reader.int32();
                    message._slot_index = "slot_index";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.num = reader.int32();
                    message._num = "num";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_InventorySlot message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_InventorySlot
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_InventorySlot & PB_InventorySlot.$Shape} PB_InventorySlot
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InventorySlot.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_InventorySlot message.
     * @function verify
     * @memberof PB_InventorySlot
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_InventorySlot.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.slot_index != null && $Object.hasOwnProperty.call(message, "slot_index")) {
            properties._slot_index = 1;
            if (!$util.isInteger(message.slot_index))
                return "slot_index: integer expected";
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.num != null && $Object.hasOwnProperty.call(message, "num")) {
            properties._num = 1;
            if (!$util.isInteger(message.num))
                return "num: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_InventorySlot message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_InventorySlot
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_InventorySlot} PB_InventorySlot
     */
    PB_InventorySlot.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_InventorySlot)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_InventorySlot: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_InventorySlot();
        if (object.slot_index != null)
            message.slot_index = object.slot_index | 0;
        if (object.id != null)
            message.id = object.id | 0;
        if (object.num != null)
            message.num = object.num | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_InventorySlot message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_InventorySlot
     * @static
     * @param {PB_InventorySlot} message PB_InventorySlot
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_InventorySlot.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.slot_index != null && $Object.hasOwnProperty.call(message, "slot_index"))
            object.slot_index = message.slot_index;
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.num != null && $Object.hasOwnProperty.call(message, "num"))
            object.num = message.num;
        return object;
    };

    /**
     * Converts this PB_InventorySlot to JSON.
     * @function toJSON
     * @memberof PB_InventorySlot
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_InventorySlot.prototype.toJSON = function() {
        return PB_InventorySlot.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_InventorySlot
     * @function getTypeUrl
     * @memberof PB_InventorySlot
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_InventorySlot.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_InventorySlot";
    };

    return PB_InventorySlot;
})();

export const PB_InventoryData = $root.PB_InventoryData = (() => {

    /**
     * Properties of a PB_InventoryData.
     * @typedef {Object} PB_InventoryData.$Properties
     * @property {number|null} [max_slots] PB_InventoryData max_slots
     * @property {Array.<PB_InventorySlot.$Properties>|null} [slots] PB_InventoryData slots
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_InventoryData.
     * @exports IPB_InventoryData
     * @interface IPB_InventoryData
     * @augments PB_InventoryData.$Properties
     * @deprecated Use PB_InventoryData.$Properties instead.
     */

    /**
     * Shape of a PB_InventoryData.
     * @typedef {PB_InventoryData.$Properties} PB_InventoryData.$Shape
     */

    /**
     * Constructs a new PB_InventoryData.
     * @exports PB_InventoryData
     * @classdesc Represents a PB_InventoryData.
     * @constructor
     * @param {PB_InventoryData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_InventoryData = function (properties) {
        this.slots = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_InventoryData max_slots.
     * @member {number|null|undefined} max_slots
     * @memberof PB_InventoryData
     * @instance
     */
    PB_InventoryData.prototype.max_slots = null;

    /**
     * PB_InventoryData slots.
     * @member {Array.<PB_InventorySlot.$Properties>} slots
     * @memberof PB_InventoryData
     * @instance
     */
    PB_InventoryData.prototype.slots = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_InventoryData.prototype, "_max_slots", {
        get: $util.oneOfGetter($oneOfFields = ["max_slots"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_InventoryData instance using the specified properties.
     * @function create
     * @memberof PB_InventoryData
     * @static
     * @param {PB_InventoryData.$Properties=} [properties] Properties to set
     * @returns {PB_InventoryData} PB_InventoryData instance
     * @type {{
     *   (properties: PB_InventoryData.$Shape): PB_InventoryData & PB_InventoryData.$Shape;
     *   (properties?: PB_InventoryData.$Properties): PB_InventoryData;
     * }}
     */
    PB_InventoryData.create = function(properties) {
        return new PB_InventoryData(properties);
    };

    /**
     * Encodes the specified PB_InventoryData message. Does not implicitly {@link PB_InventoryData.verify|verify} messages.
     * @function encode
     * @memberof PB_InventoryData
     * @static
     * @param {PB_InventoryData.$Properties} message PB_InventoryData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InventoryData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.max_slots != null && $Object.hasOwnProperty.call(message, "max_slots"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.max_slots);
        if (message.slots != null && message.slots.length)
            for (let i = 0; i < message.slots.length; ++i)
                $root.PB_InventorySlot.encode(message.slots[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_InventoryData message, length delimited. Does not implicitly {@link PB_InventoryData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_InventoryData
     * @static
     * @param {PB_InventoryData.$Properties} message PB_InventoryData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InventoryData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_InventoryData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_InventoryData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_InventoryData & PB_InventoryData.$Shape} PB_InventoryData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InventoryData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_InventoryData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.max_slots = reader.int32();
                    message._max_slots = "max_slots";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.slots && message.slots.length))
                        message.slots = [];
                    message.slots.push($root.PB_InventorySlot.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_InventoryData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_InventoryData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_InventoryData & PB_InventoryData.$Shape} PB_InventoryData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InventoryData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_InventoryData message.
     * @function verify
     * @memberof PB_InventoryData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_InventoryData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.max_slots != null && $Object.hasOwnProperty.call(message, "max_slots")) {
            properties._max_slots = 1;
            if (!$util.isInteger(message.max_slots))
                return "max_slots: integer expected";
        }
        if (message.slots != null && $Object.hasOwnProperty.call(message, "slots")) {
            if (!$Array.isArray(message.slots))
                return "slots: array expected";
            for (let i = 0; i < message.slots.length; ++i) {
                let error = $root.PB_InventorySlot.verify(message.slots[i], _depth + 1);
                if (error)
                    return "slots." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_InventoryData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_InventoryData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_InventoryData} PB_InventoryData
     */
    PB_InventoryData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_InventoryData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_InventoryData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_InventoryData();
        if (object.max_slots != null)
            message.max_slots = object.max_slots | 0;
        if (object.slots) {
            if (!$Array.isArray(object.slots))
                throw $TypeError(".PB_InventoryData.slots: array expected");
            message.slots = $Array(object.slots.length);
            for (let i = 0; i < object.slots.length; ++i) {
                if (!$util.isObject(object.slots[i]))
                    throw $TypeError(".PB_InventoryData.slots: object expected");
                message.slots[i] = $root.PB_InventorySlot.fromObject(object.slots[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_InventoryData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_InventoryData
     * @static
     * @param {PB_InventoryData} message PB_InventoryData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_InventoryData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.slots = [];
        if (message.max_slots != null && $Object.hasOwnProperty.call(message, "max_slots"))
            object.max_slots = message.max_slots;
        if (message.slots && message.slots.length) {
            object.slots = $Array(message.slots.length);
            for (let j = 0; j < message.slots.length; ++j)
                object.slots[j] = $root.PB_InventorySlot.toObject(message.slots[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_InventoryData to JSON.
     * @function toJSON
     * @memberof PB_InventoryData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_InventoryData.prototype.toJSON = function() {
        return PB_InventoryData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_InventoryData
     * @function getTypeUrl
     * @memberof PB_InventoryData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_InventoryData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_InventoryData";
    };

    return PB_InventoryData;
})();

export const PB_AttrPair = $root.PB_AttrPair = (() => {

    /**
     * Properties of a PB_AttrPair.
     * @typedef {Object} PB_AttrPair.$Properties
     * @property {number|null} [attr_id] PB_AttrPair attr_id
     * @property {number|null} [value] PB_AttrPair value
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_AttrPair.
     * @exports IPB_AttrPair
     * @interface IPB_AttrPair
     * @augments PB_AttrPair.$Properties
     * @deprecated Use PB_AttrPair.$Properties instead.
     */

    /**
     * Shape of a PB_AttrPair.
     * @typedef {PB_AttrPair.$Properties} PB_AttrPair.$Shape
     */

    /**
     * Constructs a new PB_AttrPair.
     * @exports PB_AttrPair
     * @classdesc Represents a PB_AttrPair.
     * @constructor
     * @param {PB_AttrPair.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_AttrPair = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_AttrPair attr_id.
     * @member {number|null|undefined} attr_id
     * @memberof PB_AttrPair
     * @instance
     */
    PB_AttrPair.prototype.attr_id = null;

    /**
     * PB_AttrPair value.
     * @member {number|null|undefined} value
     * @memberof PB_AttrPair
     * @instance
     */
    PB_AttrPair.prototype.value = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_AttrPair.prototype, "_attr_id", {
        get: $util.oneOfGetter($oneOfFields = ["attr_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_AttrPair.prototype, "_value", {
        get: $util.oneOfGetter($oneOfFields = ["value"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_AttrPair instance using the specified properties.
     * @function create
     * @memberof PB_AttrPair
     * @static
     * @param {PB_AttrPair.$Properties=} [properties] Properties to set
     * @returns {PB_AttrPair} PB_AttrPair instance
     * @type {{
     *   (properties: PB_AttrPair.$Shape): PB_AttrPair & PB_AttrPair.$Shape;
     *   (properties?: PB_AttrPair.$Properties): PB_AttrPair;
     * }}
     */
    PB_AttrPair.create = function(properties) {
        return new PB_AttrPair(properties);
    };

    /**
     * Encodes the specified PB_AttrPair message. Does not implicitly {@link PB_AttrPair.verify|verify} messages.
     * @function encode
     * @memberof PB_AttrPair
     * @static
     * @param {PB_AttrPair.$Properties} message PB_AttrPair message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AttrPair.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.attr_id != null && $Object.hasOwnProperty.call(message, "attr_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.attr_id);
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            writer.uint32(/* id 2, wireType 1 =*/17).double(message.value);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_AttrPair message, length delimited. Does not implicitly {@link PB_AttrPair.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_AttrPair
     * @static
     * @param {PB_AttrPair.$Properties} message PB_AttrPair message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AttrPair.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_AttrPair message from the specified reader or buffer.
     * @function decode
     * @memberof PB_AttrPair
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_AttrPair & PB_AttrPair.$Shape} PB_AttrPair
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AttrPair.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_AttrPair();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.attr_id = reader.int32();
                    message._attr_id = "attr_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 1)
                        break;
                    message.value = reader.double();
                    message._value = "value";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_AttrPair message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_AttrPair
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_AttrPair & PB_AttrPair.$Shape} PB_AttrPair
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AttrPair.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_AttrPair message.
     * @function verify
     * @memberof PB_AttrPair
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_AttrPair.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.attr_id != null && $Object.hasOwnProperty.call(message, "attr_id")) {
            properties._attr_id = 1;
            if (!$util.isInteger(message.attr_id))
                return "attr_id: integer expected";
        }
        if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
            properties._value = 1;
            if (typeof message.value !== "number")
                return "value: number expected";
        }
        return null;
    };

    /**
     * Creates a PB_AttrPair message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_AttrPair
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_AttrPair} PB_AttrPair
     */
    PB_AttrPair.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_AttrPair)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_AttrPair: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_AttrPair();
        if (object.attr_id != null)
            message.attr_id = object.attr_id | 0;
        if (object.value != null)
            message.value = $Number(object.value);
        return message;
    };

    /**
     * Creates a plain object from a PB_AttrPair message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_AttrPair
     * @static
     * @param {PB_AttrPair} message PB_AttrPair
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_AttrPair.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.attr_id != null && $Object.hasOwnProperty.call(message, "attr_id"))
            object.attr_id = message.attr_id;
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            object.value = options.json && !$isFinite(message.value) ? $String(message.value) : message.value;
        return object;
    };

    /**
     * Converts this PB_AttrPair to JSON.
     * @function toJSON
     * @memberof PB_AttrPair
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_AttrPair.prototype.toJSON = function() {
        return PB_AttrPair.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_AttrPair
     * @function getTypeUrl
     * @memberof PB_AttrPair
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_AttrPair.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_AttrPair";
    };

    return PB_AttrPair;
})();

export const PB_AttrData = $root.PB_AttrData = (() => {

    /**
     * Properties of a PB_AttrData.
     * @typedef {Object} PB_AttrData.$Properties
     * @property {Array.<PB_AttrPair.$Properties>|null} [attrs] PB_AttrData attrs
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_AttrData.
     * @exports IPB_AttrData
     * @interface IPB_AttrData
     * @augments PB_AttrData.$Properties
     * @deprecated Use PB_AttrData.$Properties instead.
     */

    /**
     * Shape of a PB_AttrData.
     * @typedef {PB_AttrData.$Properties} PB_AttrData.$Shape
     */

    /**
     * Constructs a new PB_AttrData.
     * @exports PB_AttrData
     * @classdesc Represents a PB_AttrData.
     * @constructor
     * @param {PB_AttrData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_AttrData = function (properties) {
        this.attrs = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_AttrData attrs.
     * @member {Array.<PB_AttrPair.$Properties>} attrs
     * @memberof PB_AttrData
     * @instance
     */
    PB_AttrData.prototype.attrs = $util.emptyArray;

    /**
     * Creates a new PB_AttrData instance using the specified properties.
     * @function create
     * @memberof PB_AttrData
     * @static
     * @param {PB_AttrData.$Properties=} [properties] Properties to set
     * @returns {PB_AttrData} PB_AttrData instance
     * @type {{
     *   (properties: PB_AttrData.$Shape): PB_AttrData & PB_AttrData.$Shape;
     *   (properties?: PB_AttrData.$Properties): PB_AttrData;
     * }}
     */
    PB_AttrData.create = function(properties) {
        return new PB_AttrData(properties);
    };

    /**
     * Encodes the specified PB_AttrData message. Does not implicitly {@link PB_AttrData.verify|verify} messages.
     * @function encode
     * @memberof PB_AttrData
     * @static
     * @param {PB_AttrData.$Properties} message PB_AttrData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AttrData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.attrs != null && message.attrs.length)
            for (let i = 0; i < message.attrs.length; ++i)
                $root.PB_AttrPair.encode(message.attrs[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_AttrData message, length delimited. Does not implicitly {@link PB_AttrData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_AttrData
     * @static
     * @param {PB_AttrData.$Properties} message PB_AttrData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AttrData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_AttrData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_AttrData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_AttrData & PB_AttrData.$Shape} PB_AttrData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AttrData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_AttrData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    if (!(message.attrs && message.attrs.length))
                        message.attrs = [];
                    message.attrs.push($root.PB_AttrPair.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_AttrData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_AttrData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_AttrData & PB_AttrData.$Shape} PB_AttrData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AttrData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_AttrData message.
     * @function verify
     * @memberof PB_AttrData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_AttrData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.attrs != null && $Object.hasOwnProperty.call(message, "attrs")) {
            if (!$Array.isArray(message.attrs))
                return "attrs: array expected";
            for (let i = 0; i < message.attrs.length; ++i) {
                let error = $root.PB_AttrPair.verify(message.attrs[i], _depth + 1);
                if (error)
                    return "attrs." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_AttrData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_AttrData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_AttrData} PB_AttrData
     */
    PB_AttrData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_AttrData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_AttrData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_AttrData();
        if (object.attrs) {
            if (!$Array.isArray(object.attrs))
                throw $TypeError(".PB_AttrData.attrs: array expected");
            message.attrs = $Array(object.attrs.length);
            for (let i = 0; i < object.attrs.length; ++i) {
                if (!$util.isObject(object.attrs[i]))
                    throw $TypeError(".PB_AttrData.attrs: object expected");
                message.attrs[i] = $root.PB_AttrPair.fromObject(object.attrs[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_AttrData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_AttrData
     * @static
     * @param {PB_AttrData} message PB_AttrData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_AttrData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.attrs = [];
        if (message.attrs && message.attrs.length) {
            object.attrs = $Array(message.attrs.length);
            for (let j = 0; j < message.attrs.length; ++j)
                object.attrs[j] = $root.PB_AttrPair.toObject(message.attrs[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_AttrData to JSON.
     * @function toJSON
     * @memberof PB_AttrData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_AttrData.prototype.toJSON = function() {
        return PB_AttrData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_AttrData
     * @function getTypeUrl
     * @memberof PB_AttrData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_AttrData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_AttrData";
    };

    return PB_AttrData;
})();

export const PB_AbilitiesData = $root.PB_AbilitiesData = (() => {

    /**
     * Properties of a PB_AbilitiesData.
     * @typedef {Object} PB_AbilitiesData.$Properties
     * @property {Array.<number>|null} [ability_ids] PB_AbilitiesData ability_ids
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_AbilitiesData.
     * @exports IPB_AbilitiesData
     * @interface IPB_AbilitiesData
     * @augments PB_AbilitiesData.$Properties
     * @deprecated Use PB_AbilitiesData.$Properties instead.
     */

    /**
     * Shape of a PB_AbilitiesData.
     * @typedef {PB_AbilitiesData.$Properties} PB_AbilitiesData.$Shape
     */

    /**
     * Constructs a new PB_AbilitiesData.
     * @exports PB_AbilitiesData
     * @classdesc Represents a PB_AbilitiesData.
     * @constructor
     * @param {PB_AbilitiesData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_AbilitiesData = function (properties) {
        this.ability_ids = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_AbilitiesData ability_ids.
     * @member {Array.<number>} ability_ids
     * @memberof PB_AbilitiesData
     * @instance
     */
    PB_AbilitiesData.prototype.ability_ids = $util.emptyArray;

    /**
     * Creates a new PB_AbilitiesData instance using the specified properties.
     * @function create
     * @memberof PB_AbilitiesData
     * @static
     * @param {PB_AbilitiesData.$Properties=} [properties] Properties to set
     * @returns {PB_AbilitiesData} PB_AbilitiesData instance
     * @type {{
     *   (properties: PB_AbilitiesData.$Shape): PB_AbilitiesData & PB_AbilitiesData.$Shape;
     *   (properties?: PB_AbilitiesData.$Properties): PB_AbilitiesData;
     * }}
     */
    PB_AbilitiesData.create = function(properties) {
        return new PB_AbilitiesData(properties);
    };

    /**
     * Encodes the specified PB_AbilitiesData message. Does not implicitly {@link PB_AbilitiesData.verify|verify} messages.
     * @function encode
     * @memberof PB_AbilitiesData
     * @static
     * @param {PB_AbilitiesData.$Properties} message PB_AbilitiesData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AbilitiesData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ability_ids != null && message.ability_ids.length)
            writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.ability_ids);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_AbilitiesData message, length delimited. Does not implicitly {@link PB_AbilitiesData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_AbilitiesData
     * @static
     * @param {PB_AbilitiesData.$Properties} message PB_AbilitiesData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_AbilitiesData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_AbilitiesData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_AbilitiesData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_AbilitiesData & PB_AbilitiesData.$Shape} PB_AbilitiesData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AbilitiesData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_AbilitiesData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType === 2) {
                        if (!(message.ability_ids && message.ability_ids.length))
                            message.ability_ids = [];
                        reader.int32s(message.ability_ids);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.ability_ids && message.ability_ids.length))
                        message.ability_ids = [];
                    message.ability_ids.push(reader.int32());
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_AbilitiesData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_AbilitiesData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_AbilitiesData & PB_AbilitiesData.$Shape} PB_AbilitiesData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_AbilitiesData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_AbilitiesData message.
     * @function verify
     * @memberof PB_AbilitiesData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_AbilitiesData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.ability_ids != null && $Object.hasOwnProperty.call(message, "ability_ids")) {
            if (!$Array.isArray(message.ability_ids))
                return "ability_ids: array expected";
            for (let i = 0; i < message.ability_ids.length; ++i)
                if (!$util.isInteger(message.ability_ids[i]))
                    return "ability_ids: integer[] expected";
        }
        return null;
    };

    /**
     * Creates a PB_AbilitiesData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_AbilitiesData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_AbilitiesData} PB_AbilitiesData
     */
    PB_AbilitiesData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_AbilitiesData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_AbilitiesData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_AbilitiesData();
        if (object.ability_ids) {
            if (!$Array.isArray(object.ability_ids))
                throw $TypeError(".PB_AbilitiesData.ability_ids: array expected");
            message.ability_ids = $Array(object.ability_ids.length);
            for (let i = 0; i < object.ability_ids.length; ++i)
                message.ability_ids[i] = object.ability_ids[i] | 0;
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_AbilitiesData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_AbilitiesData
     * @static
     * @param {PB_AbilitiesData} message PB_AbilitiesData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_AbilitiesData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.ability_ids = [];
        if (message.ability_ids && message.ability_ids.length) {
            object.ability_ids = $Array(message.ability_ids.length);
            for (let j = 0; j < message.ability_ids.length; ++j)
                object.ability_ids[j] = message.ability_ids[j];
        }
        return object;
    };

    /**
     * Converts this PB_AbilitiesData to JSON.
     * @function toJSON
     * @memberof PB_AbilitiesData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_AbilitiesData.prototype.toJSON = function() {
        return PB_AbilitiesData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_AbilitiesData
     * @function getTypeUrl
     * @memberof PB_AbilitiesData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_AbilitiesData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_AbilitiesData";
    };

    return PB_AbilitiesData;
})();

export const PB_QuestObjectiveData = $root.PB_QuestObjectiveData = (() => {

    /**
     * Properties of a PB_QuestObjectiveData.
     * @typedef {Object} PB_QuestObjectiveData.$Properties
     * @property {number|null} [current_count] PB_QuestObjectiveData current_count
     * @property {boolean|null} [completed] PB_QuestObjectiveData completed
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_QuestObjectiveData.
     * @exports IPB_QuestObjectiveData
     * @interface IPB_QuestObjectiveData
     * @augments PB_QuestObjectiveData.$Properties
     * @deprecated Use PB_QuestObjectiveData.$Properties instead.
     */

    /**
     * Shape of a PB_QuestObjectiveData.
     * @typedef {PB_QuestObjectiveData.$Properties} PB_QuestObjectiveData.$Shape
     */

    /**
     * Constructs a new PB_QuestObjectiveData.
     * @exports PB_QuestObjectiveData
     * @classdesc Represents a PB_QuestObjectiveData.
     * @constructor
     * @param {PB_QuestObjectiveData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_QuestObjectiveData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_QuestObjectiveData current_count.
     * @member {number|null|undefined} current_count
     * @memberof PB_QuestObjectiveData
     * @instance
     */
    PB_QuestObjectiveData.prototype.current_count = null;

    /**
     * PB_QuestObjectiveData completed.
     * @member {boolean|null|undefined} completed
     * @memberof PB_QuestObjectiveData
     * @instance
     */
    PB_QuestObjectiveData.prototype.completed = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestObjectiveData.prototype, "_current_count", {
        get: $util.oneOfGetter($oneOfFields = ["current_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestObjectiveData.prototype, "_completed", {
        get: $util.oneOfGetter($oneOfFields = ["completed"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_QuestObjectiveData instance using the specified properties.
     * @function create
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {PB_QuestObjectiveData.$Properties=} [properties] Properties to set
     * @returns {PB_QuestObjectiveData} PB_QuestObjectiveData instance
     * @type {{
     *   (properties: PB_QuestObjectiveData.$Shape): PB_QuestObjectiveData & PB_QuestObjectiveData.$Shape;
     *   (properties?: PB_QuestObjectiveData.$Properties): PB_QuestObjectiveData;
     * }}
     */
    PB_QuestObjectiveData.create = function(properties) {
        return new PB_QuestObjectiveData(properties);
    };

    /**
     * Encodes the specified PB_QuestObjectiveData message. Does not implicitly {@link PB_QuestObjectiveData.verify|verify} messages.
     * @function encode
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {PB_QuestObjectiveData.$Properties} message PB_QuestObjectiveData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestObjectiveData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.current_count != null && $Object.hasOwnProperty.call(message, "current_count"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.current_count);
        if (message.completed != null && $Object.hasOwnProperty.call(message, "completed"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.completed);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_QuestObjectiveData message, length delimited. Does not implicitly {@link PB_QuestObjectiveData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {PB_QuestObjectiveData.$Properties} message PB_QuestObjectiveData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestObjectiveData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_QuestObjectiveData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_QuestObjectiveData & PB_QuestObjectiveData.$Shape} PB_QuestObjectiveData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestObjectiveData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_QuestObjectiveData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.current_count = reader.int32();
                    message._current_count = "current_count";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.completed = reader.bool();
                    message._completed = "completed";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_QuestObjectiveData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_QuestObjectiveData & PB_QuestObjectiveData.$Shape} PB_QuestObjectiveData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestObjectiveData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_QuestObjectiveData message.
     * @function verify
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_QuestObjectiveData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.current_count != null && $Object.hasOwnProperty.call(message, "current_count")) {
            properties._current_count = 1;
            if (!$util.isInteger(message.current_count))
                return "current_count: integer expected";
        }
        if (message.completed != null && $Object.hasOwnProperty.call(message, "completed")) {
            properties._completed = 1;
            if (typeof message.completed !== "boolean")
                return "completed: boolean expected";
        }
        return null;
    };

    /**
     * Creates a PB_QuestObjectiveData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_QuestObjectiveData} PB_QuestObjectiveData
     */
    PB_QuestObjectiveData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_QuestObjectiveData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_QuestObjectiveData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_QuestObjectiveData();
        if (object.current_count != null)
            message.current_count = object.current_count | 0;
        if (object.completed != null)
            message.completed = $Boolean(object.completed);
        return message;
    };

    /**
     * Creates a plain object from a PB_QuestObjectiveData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {PB_QuestObjectiveData} message PB_QuestObjectiveData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_QuestObjectiveData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.current_count != null && $Object.hasOwnProperty.call(message, "current_count"))
            object.current_count = message.current_count;
        if (message.completed != null && $Object.hasOwnProperty.call(message, "completed"))
            object.completed = message.completed;
        return object;
    };

    /**
     * Converts this PB_QuestObjectiveData to JSON.
     * @function toJSON
     * @memberof PB_QuestObjectiveData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_QuestObjectiveData.prototype.toJSON = function() {
        return PB_QuestObjectiveData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_QuestObjectiveData
     * @function getTypeUrl
     * @memberof PB_QuestObjectiveData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_QuestObjectiveData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_QuestObjectiveData";
    };

    return PB_QuestObjectiveData;
})();

export const PB_QuestSaveData = $root.PB_QuestSaveData = (() => {

    /**
     * Properties of a PB_QuestSaveData.
     * @typedef {Object} PB_QuestSaveData.$Properties
     * @property {number|null} [status] PB_QuestSaveData status
     * @property {number|null} [start_time] PB_QuestSaveData start_time
     * @property {number|null} [complete_time] PB_QuestSaveData complete_time
     * @property {Array.<PB_QuestObjectiveData.$Properties>|null} [objectives] PB_QuestSaveData objectives
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_QuestSaveData.
     * @exports IPB_QuestSaveData
     * @interface IPB_QuestSaveData
     * @augments PB_QuestSaveData.$Properties
     * @deprecated Use PB_QuestSaveData.$Properties instead.
     */

    /**
     * Shape of a PB_QuestSaveData.
     * @typedef {PB_QuestSaveData.$Properties} PB_QuestSaveData.$Shape
     */

    /**
     * Constructs a new PB_QuestSaveData.
     * @exports PB_QuestSaveData
     * @classdesc Represents a PB_QuestSaveData.
     * @constructor
     * @param {PB_QuestSaveData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_QuestSaveData = function (properties) {
        this.objectives = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_QuestSaveData status.
     * @member {number|null|undefined} status
     * @memberof PB_QuestSaveData
     * @instance
     */
    PB_QuestSaveData.prototype.status = null;

    /**
     * PB_QuestSaveData start_time.
     * @member {number|null|undefined} start_time
     * @memberof PB_QuestSaveData
     * @instance
     */
    PB_QuestSaveData.prototype.start_time = null;

    /**
     * PB_QuestSaveData complete_time.
     * @member {number|null|undefined} complete_time
     * @memberof PB_QuestSaveData
     * @instance
     */
    PB_QuestSaveData.prototype.complete_time = null;

    /**
     * PB_QuestSaveData objectives.
     * @member {Array.<PB_QuestObjectiveData.$Properties>} objectives
     * @memberof PB_QuestSaveData
     * @instance
     */
    PB_QuestSaveData.prototype.objectives = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestSaveData.prototype, "_status", {
        get: $util.oneOfGetter($oneOfFields = ["status"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestSaveData.prototype, "_start_time", {
        get: $util.oneOfGetter($oneOfFields = ["start_time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestSaveData.prototype, "_complete_time", {
        get: $util.oneOfGetter($oneOfFields = ["complete_time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_QuestSaveData instance using the specified properties.
     * @function create
     * @memberof PB_QuestSaveData
     * @static
     * @param {PB_QuestSaveData.$Properties=} [properties] Properties to set
     * @returns {PB_QuestSaveData} PB_QuestSaveData instance
     * @type {{
     *   (properties: PB_QuestSaveData.$Shape): PB_QuestSaveData & PB_QuestSaveData.$Shape;
     *   (properties?: PB_QuestSaveData.$Properties): PB_QuestSaveData;
     * }}
     */
    PB_QuestSaveData.create = function(properties) {
        return new PB_QuestSaveData(properties);
    };

    /**
     * Encodes the specified PB_QuestSaveData message. Does not implicitly {@link PB_QuestSaveData.verify|verify} messages.
     * @function encode
     * @memberof PB_QuestSaveData
     * @static
     * @param {PB_QuestSaveData.$Properties} message PB_QuestSaveData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestSaveData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.status);
        if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.start_time);
        if (message.complete_time != null && $Object.hasOwnProperty.call(message, "complete_time"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.complete_time);
        if (message.objectives != null && message.objectives.length)
            for (let i = 0; i < message.objectives.length; ++i)
                $root.PB_QuestObjectiveData.encode(message.objectives[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_QuestSaveData message, length delimited. Does not implicitly {@link PB_QuestSaveData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_QuestSaveData
     * @static
     * @param {PB_QuestSaveData.$Properties} message PB_QuestSaveData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestSaveData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_QuestSaveData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_QuestSaveData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_QuestSaveData & PB_QuestSaveData.$Shape} PB_QuestSaveData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestSaveData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_QuestSaveData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.status = reader.int32();
                    message._status = "status";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.start_time = reader.int64();
                    message._start_time = "start_time";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.complete_time = reader.int64();
                    message._complete_time = "complete_time";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    if (!(message.objectives && message.objectives.length))
                        message.objectives = [];
                    message.objectives.push($root.PB_QuestObjectiveData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_QuestSaveData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_QuestSaveData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_QuestSaveData & PB_QuestSaveData.$Shape} PB_QuestSaveData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestSaveData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_QuestSaveData message.
     * @function verify
     * @memberof PB_QuestSaveData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_QuestSaveData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
            properties._status = 1;
            if (!$util.isInteger(message.status))
                return "status: integer expected";
        }
        if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time")) {
            properties._start_time = 1;
            if (!$util.isInteger(message.start_time) && !(message.start_time && $util.isInteger(message.start_time.low) && $util.isInteger(message.start_time.high)))
                return "start_time: integer|Long expected";
        }
        if (message.complete_time != null && $Object.hasOwnProperty.call(message, "complete_time")) {
            properties._complete_time = 1;
            if (!$util.isInteger(message.complete_time) && !(message.complete_time && $util.isInteger(message.complete_time.low) && $util.isInteger(message.complete_time.high)))
                return "complete_time: integer|Long expected";
        }
        if (message.objectives != null && $Object.hasOwnProperty.call(message, "objectives")) {
            if (!$Array.isArray(message.objectives))
                return "objectives: array expected";
            for (let i = 0; i < message.objectives.length; ++i) {
                let error = $root.PB_QuestObjectiveData.verify(message.objectives[i], _depth + 1);
                if (error)
                    return "objectives." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_QuestSaveData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_QuestSaveData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_QuestSaveData} PB_QuestSaveData
     */
    PB_QuestSaveData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_QuestSaveData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_QuestSaveData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_QuestSaveData();
        if (object.status != null)
            message.status = object.status | 0;
        if (object.start_time != null)
            if ($util.Long)
                message.start_time = $util.Long.fromValue(object.start_time, false);
            else if (typeof object.start_time === "string")
                message.start_time = $parseInt(object.start_time, 10);
            else if (typeof object.start_time === "number")
                message.start_time = object.start_time;
            else if (typeof object.start_time === "object")
                message.start_time = new $util.LongBits(object.start_time.low >>> 0, object.start_time.high >>> 0).toNumber();
        if (object.complete_time != null)
            if ($util.Long)
                message.complete_time = $util.Long.fromValue(object.complete_time, false);
            else if (typeof object.complete_time === "string")
                message.complete_time = $parseInt(object.complete_time, 10);
            else if (typeof object.complete_time === "number")
                message.complete_time = object.complete_time;
            else if (typeof object.complete_time === "object")
                message.complete_time = new $util.LongBits(object.complete_time.low >>> 0, object.complete_time.high >>> 0).toNumber();
        if (object.objectives) {
            if (!$Array.isArray(object.objectives))
                throw $TypeError(".PB_QuestSaveData.objectives: array expected");
            message.objectives = $Array(object.objectives.length);
            for (let i = 0; i < object.objectives.length; ++i) {
                if (!$util.isObject(object.objectives[i]))
                    throw $TypeError(".PB_QuestSaveData.objectives: object expected");
                message.objectives[i] = $root.PB_QuestObjectiveData.fromObject(object.objectives[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_QuestSaveData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_QuestSaveData
     * @static
     * @param {PB_QuestSaveData} message PB_QuestSaveData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_QuestSaveData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.objectives = [];
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
            object.status = message.status;
        if (message.start_time != null && $Object.hasOwnProperty.call(message, "start_time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.start_time = typeof message.start_time === "number" ? $BigInt(message.start_time) : $util.Long.fromBits(message.start_time.low >>> 0, message.start_time.high >>> 0, false).toBigInt();
            else if (typeof message.start_time === "number")
                object.start_time = options.longs === $String ? $String(message.start_time) : message.start_time;
            else
                object.start_time = options.longs === $String ? $util.Long.prototype.toString.call(message.start_time) : options.longs === $Number ? new $util.LongBits(message.start_time.low >>> 0, message.start_time.high >>> 0).toNumber() : message.start_time;
        if (message.complete_time != null && $Object.hasOwnProperty.call(message, "complete_time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.complete_time = typeof message.complete_time === "number" ? $BigInt(message.complete_time) : $util.Long.fromBits(message.complete_time.low >>> 0, message.complete_time.high >>> 0, false).toBigInt();
            else if (typeof message.complete_time === "number")
                object.complete_time = options.longs === $String ? $String(message.complete_time) : message.complete_time;
            else
                object.complete_time = options.longs === $String ? $util.Long.prototype.toString.call(message.complete_time) : options.longs === $Number ? new $util.LongBits(message.complete_time.low >>> 0, message.complete_time.high >>> 0).toNumber() : message.complete_time;
        if (message.objectives && message.objectives.length) {
            object.objectives = $Array(message.objectives.length);
            for (let j = 0; j < message.objectives.length; ++j)
                object.objectives[j] = $root.PB_QuestObjectiveData.toObject(message.objectives[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_QuestSaveData to JSON.
     * @function toJSON
     * @memberof PB_QuestSaveData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_QuestSaveData.prototype.toJSON = function() {
        return PB_QuestSaveData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_QuestSaveData
     * @function getTypeUrl
     * @memberof PB_QuestSaveData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_QuestSaveData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_QuestSaveData";
    };

    return PB_QuestSaveData;
})();

export const PB_QuestData = $root.PB_QuestData = (() => {

    /**
     * Properties of a PB_QuestData.
     * @typedef {Object} PB_QuestData.$Properties
     * @property {number|null} [max_active] PB_QuestData max_active
     * @property {Object.<string,PB_QuestSaveData.$Properties>|null} [active_quests] PB_QuestData active_quests
     * @property {Array.<number>|null} [completed_quests] PB_QuestData completed_quests
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_QuestData.
     * @exports IPB_QuestData
     * @interface IPB_QuestData
     * @augments PB_QuestData.$Properties
     * @deprecated Use PB_QuestData.$Properties instead.
     */

    /**
     * Shape of a PB_QuestData.
     * @typedef {PB_QuestData.$Properties} PB_QuestData.$Shape
     */

    /**
     * Constructs a new PB_QuestData.
     * @exports PB_QuestData
     * @classdesc Represents a PB_QuestData.
     * @constructor
     * @param {PB_QuestData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_QuestData = function (properties) {
        this.active_quests = {};
        this.completed_quests = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_QuestData max_active.
     * @member {number|null|undefined} max_active
     * @memberof PB_QuestData
     * @instance
     */
    PB_QuestData.prototype.max_active = null;

    /**
     * PB_QuestData active_quests.
     * @member {Object.<string,PB_QuestSaveData.$Properties>} active_quests
     * @memberof PB_QuestData
     * @instance
     */
    PB_QuestData.prototype.active_quests = $util.emptyObject;

    /**
     * PB_QuestData completed_quests.
     * @member {Array.<number>} completed_quests
     * @memberof PB_QuestData
     * @instance
     */
    PB_QuestData.prototype.completed_quests = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_QuestData.prototype, "_max_active", {
        get: $util.oneOfGetter($oneOfFields = ["max_active"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_QuestData instance using the specified properties.
     * @function create
     * @memberof PB_QuestData
     * @static
     * @param {PB_QuestData.$Properties=} [properties] Properties to set
     * @returns {PB_QuestData} PB_QuestData instance
     * @type {{
     *   (properties: PB_QuestData.$Shape): PB_QuestData & PB_QuestData.$Shape;
     *   (properties?: PB_QuestData.$Properties): PB_QuestData;
     * }}
     */
    PB_QuestData.create = function(properties) {
        return new PB_QuestData(properties);
    };

    /**
     * Encodes the specified PB_QuestData message. Does not implicitly {@link PB_QuestData.verify|verify} messages.
     * @function encode
     * @memberof PB_QuestData
     * @static
     * @param {PB_QuestData.$Properties} message PB_QuestData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.max_active != null && $Object.hasOwnProperty.call(message, "max_active"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.max_active);
        if (message.active_quests != null && $Object.hasOwnProperty.call(message, "active_quests"))
            for (let keys = $Object.keys(message.active_quests), i = 0; i < keys.length; ++i) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                $root.PB_QuestSaveData.encode(message.active_quests[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
            }
        if (message.completed_quests != null && message.completed_quests.length)
            writer.uint32(/* id 3, wireType 2 =*/26).int32s(message.completed_quests);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_QuestData message, length delimited. Does not implicitly {@link PB_QuestData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_QuestData
     * @static
     * @param {PB_QuestData.$Properties} message PB_QuestData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_QuestData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_QuestData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_QuestData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_QuestData & PB_QuestData.$Shape} PB_QuestData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_QuestData(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.max_active = reader.int32();
                    message._max_active = "max_active";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (message.active_quests === $util.emptyObject)
                        message.active_quests = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = 0;
                    value = null;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 0)
                                break;
                            key = reader.int32();
                            continue;
                        case 2:
                            if (wireType !== 2)
                                break;
                            value = $root.PB_QuestSaveData.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    message.active_quests[key] = value || new $root.PB_QuestSaveData();
                    continue;
                }
            case 3: {
                    if (wireType === 2) {
                        if (!(message.completed_quests && message.completed_quests.length))
                            message.completed_quests = [];
                        reader.int32s(message.completed_quests);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.completed_quests && message.completed_quests.length))
                        message.completed_quests = [];
                    message.completed_quests.push(reader.int32());
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_QuestData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_QuestData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_QuestData & PB_QuestData.$Shape} PB_QuestData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_QuestData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_QuestData message.
     * @function verify
     * @memberof PB_QuestData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_QuestData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.max_active != null && $Object.hasOwnProperty.call(message, "max_active")) {
            properties._max_active = 1;
            if (!$util.isInteger(message.max_active))
                return "max_active: integer expected";
        }
        if (message.active_quests != null && $Object.hasOwnProperty.call(message, "active_quests")) {
            if (!$util.isObject(message.active_quests))
                return "active_quests: object expected";
            let key = $Object.keys(message.active_quests);
            for (let i = 0; i < key.length; ++i) {
                if (!$util.key32Re.test(key[i]))
                    return "active_quests: integer key{k:int32} expected";
                {
                    let error = $root.PB_QuestSaveData.verify(message.active_quests[key[i]], _depth + 1);
                    if (error)
                        return "active_quests." + error;
                }
            }
        }
        if (message.completed_quests != null && $Object.hasOwnProperty.call(message, "completed_quests")) {
            if (!$Array.isArray(message.completed_quests))
                return "completed_quests: array expected";
            for (let i = 0; i < message.completed_quests.length; ++i)
                if (!$util.isInteger(message.completed_quests[i]))
                    return "completed_quests: integer[] expected";
        }
        return null;
    };

    /**
     * Creates a PB_QuestData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_QuestData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_QuestData} PB_QuestData
     */
    PB_QuestData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_QuestData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_QuestData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_QuestData();
        if (object.max_active != null)
            message.max_active = object.max_active | 0;
        if (object.active_quests) {
            if (!$util.isObject(object.active_quests))
                throw $TypeError(".PB_QuestData.active_quests: object expected");
            message.active_quests = {};
            for (let keys = $Object.keys(object.active_quests), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.active_quests, keys[i]);
                if (!$util.isObject(object.active_quests[keys[i]]))
                    throw $TypeError(".PB_QuestData.active_quests: object expected");
                message.active_quests[keys[i]] = $root.PB_QuestSaveData.fromObject(object.active_quests[keys[i]], _depth + 1);
            }
        }
        if (object.completed_quests) {
            if (!$Array.isArray(object.completed_quests))
                throw $TypeError(".PB_QuestData.completed_quests: array expected");
            message.completed_quests = $Array(object.completed_quests.length);
            for (let i = 0; i < object.completed_quests.length; ++i)
                message.completed_quests[i] = object.completed_quests[i] | 0;
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_QuestData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_QuestData
     * @static
     * @param {PB_QuestData} message PB_QuestData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_QuestData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.completed_quests = [];
        if (options.objects || options.defaults)
            object.active_quests = {};
        if (message.max_active != null && $Object.hasOwnProperty.call(message, "max_active"))
            object.max_active = message.max_active;
        let keys2;
        if (message.active_quests && (keys2 = $Object.keys(message.active_quests)).length) {
            object.active_quests = {};
            for (let j = 0; j < keys2.length; ++j) {
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.active_quests, keys2[j]);
                object.active_quests[keys2[j]] = $root.PB_QuestSaveData.toObject(message.active_quests[keys2[j]], options, _depth + 1);
            }
        }
        if (message.completed_quests && message.completed_quests.length) {
            object.completed_quests = $Array(message.completed_quests.length);
            for (let j = 0; j < message.completed_quests.length; ++j)
                object.completed_quests[j] = message.completed_quests[j];
        }
        return object;
    };

    /**
     * Converts this PB_QuestData to JSON.
     * @function toJSON
     * @memberof PB_QuestData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_QuestData.prototype.toJSON = function() {
        return PB_QuestData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_QuestData
     * @function getTypeUrl
     * @memberof PB_QuestData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_QuestData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_QuestData";
    };

    return PB_QuestData;
})();

export const PB_HeroData = $root.PB_HeroData = (() => {

    /**
     * Properties of a PB_HeroData.
     * @typedef {Object} PB_HeroData.$Properties
     * @property {number|null} [hero_id] PB_HeroData hero_id
     * @property {number|null} [level] PB_HeroData level
     * @property {number|null} [exp] PB_HeroData exp
     * @property {PB_AbilitiesData.$Properties|null} [abilities] PB_HeroData abilities
     * @property {PB_AttrData.$Properties|null} [attrs] PB_HeroData attrs
     * @property {number|null} [combat_power] PB_HeroData combat_power
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_HeroData.
     * @exports IPB_HeroData
     * @interface IPB_HeroData
     * @augments PB_HeroData.$Properties
     * @deprecated Use PB_HeroData.$Properties instead.
     */

    /**
     * Shape of a PB_HeroData.
     * @typedef {PB_HeroData.$Properties} PB_HeroData.$Shape
     */

    /**
     * Constructs a new PB_HeroData.
     * @exports PB_HeroData
     * @classdesc Represents a PB_HeroData.
     * @constructor
     * @param {PB_HeroData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_HeroData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_HeroData hero_id.
     * @member {number|null|undefined} hero_id
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.hero_id = null;

    /**
     * PB_HeroData level.
     * @member {number|null|undefined} level
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.level = null;

    /**
     * PB_HeroData exp.
     * @member {number|null|undefined} exp
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.exp = null;

    /**
     * PB_HeroData abilities.
     * @member {PB_AbilitiesData.$Properties|null|undefined} abilities
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.abilities = null;

    /**
     * PB_HeroData attrs.
     * @member {PB_AttrData.$Properties|null|undefined} attrs
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.attrs = null;

    /**
     * PB_HeroData combat_power.
     * @member {number|null|undefined} combat_power
     * @memberof PB_HeroData
     * @instance
     */
    PB_HeroData.prototype.combat_power = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_hero_id", {
        get: $util.oneOfGetter($oneOfFields = ["hero_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_level", {
        get: $util.oneOfGetter($oneOfFields = ["level"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_exp", {
        get: $util.oneOfGetter($oneOfFields = ["exp"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_abilities", {
        get: $util.oneOfGetter($oneOfFields = ["abilities"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_attrs", {
        get: $util.oneOfGetter($oneOfFields = ["attrs"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_HeroData.prototype, "_combat_power", {
        get: $util.oneOfGetter($oneOfFields = ["combat_power"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_HeroData instance using the specified properties.
     * @function create
     * @memberof PB_HeroData
     * @static
     * @param {PB_HeroData.$Properties=} [properties] Properties to set
     * @returns {PB_HeroData} PB_HeroData instance
     * @type {{
     *   (properties: PB_HeroData.$Shape): PB_HeroData & PB_HeroData.$Shape;
     *   (properties?: PB_HeroData.$Properties): PB_HeroData;
     * }}
     */
    PB_HeroData.create = function(properties) {
        return new PB_HeroData(properties);
    };

    /**
     * Encodes the specified PB_HeroData message. Does not implicitly {@link PB_HeroData.verify|verify} messages.
     * @function encode
     * @memberof PB_HeroData
     * @static
     * @param {PB_HeroData.$Properties} message PB_HeroData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_HeroData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.hero_id != null && $Object.hasOwnProperty.call(message, "hero_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.hero_id);
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.level);
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.exp);
        if (message.abilities != null && $Object.hasOwnProperty.call(message, "abilities"))
            $root.PB_AbilitiesData.encode(message.abilities, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.attrs != null && $Object.hasOwnProperty.call(message, "attrs"))
            $root.PB_AttrData.encode(message.attrs, writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power"))
            writer.uint32(/* id 6, wireType 0 =*/48).int64(message.combat_power);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_HeroData message, length delimited. Does not implicitly {@link PB_HeroData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_HeroData
     * @static
     * @param {PB_HeroData.$Properties} message PB_HeroData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_HeroData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_HeroData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_HeroData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_HeroData & PB_HeroData.$Shape} PB_HeroData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_HeroData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_HeroData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.hero_id = reader.int32();
                    message._hero_id = "hero_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.level = reader.int32();
                    message._level = "level";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.exp = reader.int64();
                    message._exp = "exp";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    message.abilities = $root.PB_AbilitiesData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.abilities);
                    message._abilities = "abilities";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    message.attrs = $root.PB_AttrData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.attrs);
                    message._attrs = "attrs";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.combat_power = reader.int64();
                    message._combat_power = "combat_power";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_HeroData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_HeroData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_HeroData & PB_HeroData.$Shape} PB_HeroData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_HeroData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_HeroData message.
     * @function verify
     * @memberof PB_HeroData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_HeroData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.hero_id != null && $Object.hasOwnProperty.call(message, "hero_id")) {
            properties._hero_id = 1;
            if (!$util.isInteger(message.hero_id))
                return "hero_id: integer expected";
        }
        if (message.level != null && $Object.hasOwnProperty.call(message, "level")) {
            properties._level = 1;
            if (!$util.isInteger(message.level))
                return "level: integer expected";
        }
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp")) {
            properties._exp = 1;
            if (!$util.isInteger(message.exp) && !(message.exp && $util.isInteger(message.exp.low) && $util.isInteger(message.exp.high)))
                return "exp: integer|Long expected";
        }
        if (message.abilities != null && $Object.hasOwnProperty.call(message, "abilities")) {
            properties._abilities = 1;
            {
                let error = $root.PB_AbilitiesData.verify(message.abilities, _depth + 1);
                if (error)
                    return "abilities." + error;
            }
        }
        if (message.attrs != null && $Object.hasOwnProperty.call(message, "attrs")) {
            properties._attrs = 1;
            {
                let error = $root.PB_AttrData.verify(message.attrs, _depth + 1);
                if (error)
                    return "attrs." + error;
            }
        }
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power")) {
            properties._combat_power = 1;
            if (!$util.isInteger(message.combat_power) && !(message.combat_power && $util.isInteger(message.combat_power.low) && $util.isInteger(message.combat_power.high)))
                return "combat_power: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a PB_HeroData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_HeroData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_HeroData} PB_HeroData
     */
    PB_HeroData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_HeroData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_HeroData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_HeroData();
        if (object.hero_id != null)
            message.hero_id = object.hero_id | 0;
        if (object.level != null)
            message.level = object.level | 0;
        if (object.exp != null)
            if ($util.Long)
                message.exp = $util.Long.fromValue(object.exp, false);
            else if (typeof object.exp === "string")
                message.exp = $parseInt(object.exp, 10);
            else if (typeof object.exp === "number")
                message.exp = object.exp;
            else if (typeof object.exp === "object")
                message.exp = new $util.LongBits(object.exp.low >>> 0, object.exp.high >>> 0).toNumber();
        if (object.abilities != null) {
            if (!$util.isObject(object.abilities))
                throw $TypeError(".PB_HeroData.abilities: object expected");
            message.abilities = $root.PB_AbilitiesData.fromObject(object.abilities, _depth + 1);
        }
        if (object.attrs != null) {
            if (!$util.isObject(object.attrs))
                throw $TypeError(".PB_HeroData.attrs: object expected");
            message.attrs = $root.PB_AttrData.fromObject(object.attrs, _depth + 1);
        }
        if (object.combat_power != null)
            if ($util.Long)
                message.combat_power = $util.Long.fromValue(object.combat_power, false);
            else if (typeof object.combat_power === "string")
                message.combat_power = $parseInt(object.combat_power, 10);
            else if (typeof object.combat_power === "number")
                message.combat_power = object.combat_power;
            else if (typeof object.combat_power === "object")
                message.combat_power = new $util.LongBits(object.combat_power.low >>> 0, object.combat_power.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a PB_HeroData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_HeroData
     * @static
     * @param {PB_HeroData} message PB_HeroData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_HeroData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.hero_id != null && $Object.hasOwnProperty.call(message, "hero_id"))
            object.hero_id = message.hero_id;
        if (message.level != null && $Object.hasOwnProperty.call(message, "level"))
            object.level = message.level;
        if (message.exp != null && $Object.hasOwnProperty.call(message, "exp"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.exp = typeof message.exp === "number" ? $BigInt(message.exp) : $util.Long.fromBits(message.exp.low >>> 0, message.exp.high >>> 0, false).toBigInt();
            else if (typeof message.exp === "number")
                object.exp = options.longs === $String ? $String(message.exp) : message.exp;
            else
                object.exp = options.longs === $String ? $util.Long.prototype.toString.call(message.exp) : options.longs === $Number ? new $util.LongBits(message.exp.low >>> 0, message.exp.high >>> 0).toNumber() : message.exp;
        if (message.abilities != null && $Object.hasOwnProperty.call(message, "abilities"))
            object.abilities = $root.PB_AbilitiesData.toObject(message.abilities, options, _depth + 1);
        if (message.attrs != null && $Object.hasOwnProperty.call(message, "attrs"))
            object.attrs = $root.PB_AttrData.toObject(message.attrs, options, _depth + 1);
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.combat_power = typeof message.combat_power === "number" ? $BigInt(message.combat_power) : $util.Long.fromBits(message.combat_power.low >>> 0, message.combat_power.high >>> 0, false).toBigInt();
            else if (typeof message.combat_power === "number")
                object.combat_power = options.longs === $String ? $String(message.combat_power) : message.combat_power;
            else
                object.combat_power = options.longs === $String ? $util.Long.prototype.toString.call(message.combat_power) : options.longs === $Number ? new $util.LongBits(message.combat_power.low >>> 0, message.combat_power.high >>> 0).toNumber() : message.combat_power;
        return object;
    };

    /**
     * Converts this PB_HeroData to JSON.
     * @function toJSON
     * @memberof PB_HeroData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_HeroData.prototype.toJSON = function() {
        return PB_HeroData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_HeroData
     * @function getTypeUrl
     * @memberof PB_HeroData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_HeroData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_HeroData";
    };

    return PB_HeroData;
})();

export const PB_HeroListData = $root.PB_HeroListData = (() => {

    /**
     * Properties of a PB_HeroListData.
     * @typedef {Object} PB_HeroListData.$Properties
     * @property {Array.<PB_HeroData.$Properties>|null} [heroes] PB_HeroListData heroes
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_HeroListData.
     * @exports IPB_HeroListData
     * @interface IPB_HeroListData
     * @augments PB_HeroListData.$Properties
     * @deprecated Use PB_HeroListData.$Properties instead.
     */

    /**
     * Shape of a PB_HeroListData.
     * @typedef {PB_HeroListData.$Properties} PB_HeroListData.$Shape
     */

    /**
     * Constructs a new PB_HeroListData.
     * @exports PB_HeroListData
     * @classdesc Represents a PB_HeroListData.
     * @constructor
     * @param {PB_HeroListData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_HeroListData = function (properties) {
        this.heroes = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_HeroListData heroes.
     * @member {Array.<PB_HeroData.$Properties>} heroes
     * @memberof PB_HeroListData
     * @instance
     */
    PB_HeroListData.prototype.heroes = $util.emptyArray;

    /**
     * Creates a new PB_HeroListData instance using the specified properties.
     * @function create
     * @memberof PB_HeroListData
     * @static
     * @param {PB_HeroListData.$Properties=} [properties] Properties to set
     * @returns {PB_HeroListData} PB_HeroListData instance
     * @type {{
     *   (properties: PB_HeroListData.$Shape): PB_HeroListData & PB_HeroListData.$Shape;
     *   (properties?: PB_HeroListData.$Properties): PB_HeroListData;
     * }}
     */
    PB_HeroListData.create = function(properties) {
        return new PB_HeroListData(properties);
    };

    /**
     * Encodes the specified PB_HeroListData message. Does not implicitly {@link PB_HeroListData.verify|verify} messages.
     * @function encode
     * @memberof PB_HeroListData
     * @static
     * @param {PB_HeroListData.$Properties} message PB_HeroListData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_HeroListData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.heroes != null && message.heroes.length)
            for (let i = 0; i < message.heroes.length; ++i)
                $root.PB_HeroData.encode(message.heroes[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_HeroListData message, length delimited. Does not implicitly {@link PB_HeroListData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_HeroListData
     * @static
     * @param {PB_HeroListData.$Properties} message PB_HeroListData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_HeroListData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_HeroListData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_HeroListData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_HeroListData & PB_HeroListData.$Shape} PB_HeroListData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_HeroListData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_HeroListData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    if (!(message.heroes && message.heroes.length))
                        message.heroes = [];
                    message.heroes.push($root.PB_HeroData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_HeroListData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_HeroListData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_HeroListData & PB_HeroListData.$Shape} PB_HeroListData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_HeroListData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_HeroListData message.
     * @function verify
     * @memberof PB_HeroListData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_HeroListData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.heroes != null && $Object.hasOwnProperty.call(message, "heroes")) {
            if (!$Array.isArray(message.heroes))
                return "heroes: array expected";
            for (let i = 0; i < message.heroes.length; ++i) {
                let error = $root.PB_HeroData.verify(message.heroes[i], _depth + 1);
                if (error)
                    return "heroes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_HeroListData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_HeroListData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_HeroListData} PB_HeroListData
     */
    PB_HeroListData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_HeroListData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_HeroListData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_HeroListData();
        if (object.heroes) {
            if (!$Array.isArray(object.heroes))
                throw $TypeError(".PB_HeroListData.heroes: array expected");
            message.heroes = $Array(object.heroes.length);
            for (let i = 0; i < object.heroes.length; ++i) {
                if (!$util.isObject(object.heroes[i]))
                    throw $TypeError(".PB_HeroListData.heroes: object expected");
                message.heroes[i] = $root.PB_HeroData.fromObject(object.heroes[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_HeroListData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_HeroListData
     * @static
     * @param {PB_HeroListData} message PB_HeroListData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_HeroListData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.heroes = [];
        if (message.heroes && message.heroes.length) {
            object.heroes = $Array(message.heroes.length);
            for (let j = 0; j < message.heroes.length; ++j)
                object.heroes[j] = $root.PB_HeroData.toObject(message.heroes[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_HeroListData to JSON.
     * @function toJSON
     * @memberof PB_HeroListData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_HeroListData.prototype.toJSON = function() {
        return PB_HeroListData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_HeroListData
     * @function getTypeUrl
     * @memberof PB_HeroListData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_HeroListData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_HeroListData";
    };

    return PB_HeroListData;
})();

export const PB_LineupData = $root.PB_LineupData = (() => {

    /**
     * Properties of a PB_LineupData.
     * @typedef {Object} PB_LineupData.$Properties
     * @property {number|null} [battle_type] PB_LineupData battle_type
     * @property {Array.<number>|null} [hero_ids] PB_LineupData hero_ids
     * @property {number|null} [combat_power] PB_LineupData combat_power
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_LineupData.
     * @exports IPB_LineupData
     * @interface IPB_LineupData
     * @augments PB_LineupData.$Properties
     * @deprecated Use PB_LineupData.$Properties instead.
     */

    /**
     * Shape of a PB_LineupData.
     * @typedef {PB_LineupData.$Properties} PB_LineupData.$Shape
     */

    /**
     * Constructs a new PB_LineupData.
     * @exports PB_LineupData
     * @classdesc Represents a PB_LineupData.
     * @constructor
     * @param {PB_LineupData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_LineupData = function (properties) {
        this.hero_ids = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_LineupData battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof PB_LineupData
     * @instance
     */
    PB_LineupData.prototype.battle_type = null;

    /**
     * PB_LineupData hero_ids.
     * @member {Array.<number>} hero_ids
     * @memberof PB_LineupData
     * @instance
     */
    PB_LineupData.prototype.hero_ids = $util.emptyArray;

    /**
     * PB_LineupData combat_power.
     * @member {number|null|undefined} combat_power
     * @memberof PB_LineupData
     * @instance
     */
    PB_LineupData.prototype.combat_power = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_LineupData.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_LineupData.prototype, "_combat_power", {
        get: $util.oneOfGetter($oneOfFields = ["combat_power"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_LineupData instance using the specified properties.
     * @function create
     * @memberof PB_LineupData
     * @static
     * @param {PB_LineupData.$Properties=} [properties] Properties to set
     * @returns {PB_LineupData} PB_LineupData instance
     * @type {{
     *   (properties: PB_LineupData.$Shape): PB_LineupData & PB_LineupData.$Shape;
     *   (properties?: PB_LineupData.$Properties): PB_LineupData;
     * }}
     */
    PB_LineupData.create = function(properties) {
        return new PB_LineupData(properties);
    };

    /**
     * Encodes the specified PB_LineupData message. Does not implicitly {@link PB_LineupData.verify|verify} messages.
     * @function encode
     * @memberof PB_LineupData
     * @static
     * @param {PB_LineupData.$Properties} message PB_LineupData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_LineupData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.battle_type);
        if (message.hero_ids != null && message.hero_ids.length)
            writer.uint32(/* id 2, wireType 2 =*/18).int32s(message.hero_ids);
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.combat_power);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_LineupData message, length delimited. Does not implicitly {@link PB_LineupData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_LineupData
     * @static
     * @param {PB_LineupData.$Properties} message PB_LineupData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_LineupData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_LineupData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_LineupData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_LineupData & PB_LineupData.$Shape} PB_LineupData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_LineupData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_LineupData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            case 2: {
                    if (wireType === 2) {
                        if (!(message.hero_ids && message.hero_ids.length))
                            message.hero_ids = [];
                        reader.int32s(message.hero_ids);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.hero_ids && message.hero_ids.length))
                        message.hero_ids = [];
                    message.hero_ids.push(reader.int32());
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.combat_power = reader.int64();
                    message._combat_power = "combat_power";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_LineupData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_LineupData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_LineupData & PB_LineupData.$Shape} PB_LineupData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_LineupData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_LineupData message.
     * @function verify
     * @memberof PB_LineupData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_LineupData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        if (message.hero_ids != null && $Object.hasOwnProperty.call(message, "hero_ids")) {
            if (!$Array.isArray(message.hero_ids))
                return "hero_ids: array expected";
            for (let i = 0; i < message.hero_ids.length; ++i)
                if (!$util.isInteger(message.hero_ids[i]))
                    return "hero_ids: integer[] expected";
        }
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power")) {
            properties._combat_power = 1;
            if (!$util.isInteger(message.combat_power) && !(message.combat_power && $util.isInteger(message.combat_power.low) && $util.isInteger(message.combat_power.high)))
                return "combat_power: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a PB_LineupData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_LineupData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_LineupData} PB_LineupData
     */
    PB_LineupData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_LineupData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_LineupData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_LineupData();
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        if (object.hero_ids) {
            if (!$Array.isArray(object.hero_ids))
                throw $TypeError(".PB_LineupData.hero_ids: array expected");
            message.hero_ids = $Array(object.hero_ids.length);
            for (let i = 0; i < object.hero_ids.length; ++i)
                message.hero_ids[i] = object.hero_ids[i] | 0;
        }
        if (object.combat_power != null)
            if ($util.Long)
                message.combat_power = $util.Long.fromValue(object.combat_power, false);
            else if (typeof object.combat_power === "string")
                message.combat_power = $parseInt(object.combat_power, 10);
            else if (typeof object.combat_power === "number")
                message.combat_power = object.combat_power;
            else if (typeof object.combat_power === "object")
                message.combat_power = new $util.LongBits(object.combat_power.low >>> 0, object.combat_power.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a PB_LineupData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_LineupData
     * @static
     * @param {PB_LineupData} message PB_LineupData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_LineupData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.hero_ids = [];
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        if (message.hero_ids && message.hero_ids.length) {
            object.hero_ids = $Array(message.hero_ids.length);
            for (let j = 0; j < message.hero_ids.length; ++j)
                object.hero_ids[j] = message.hero_ids[j];
        }
        if (message.combat_power != null && $Object.hasOwnProperty.call(message, "combat_power"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.combat_power = typeof message.combat_power === "number" ? $BigInt(message.combat_power) : $util.Long.fromBits(message.combat_power.low >>> 0, message.combat_power.high >>> 0, false).toBigInt();
            else if (typeof message.combat_power === "number")
                object.combat_power = options.longs === $String ? $String(message.combat_power) : message.combat_power;
            else
                object.combat_power = options.longs === $String ? $util.Long.prototype.toString.call(message.combat_power) : options.longs === $Number ? new $util.LongBits(message.combat_power.low >>> 0, message.combat_power.high >>> 0).toNumber() : message.combat_power;
        return object;
    };

    /**
     * Converts this PB_LineupData to JSON.
     * @function toJSON
     * @memberof PB_LineupData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_LineupData.prototype.toJSON = function() {
        return PB_LineupData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_LineupData
     * @function getTypeUrl
     * @memberof PB_LineupData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_LineupData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_LineupData";
    };

    return PB_LineupData;
})();

export const PB_LineupListData = $root.PB_LineupListData = (() => {

    /**
     * Properties of a PB_LineupListData.
     * @typedef {Object} PB_LineupListData.$Properties
     * @property {Array.<PB_LineupData.$Properties>|null} [lineups] PB_LineupListData lineups
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_LineupListData.
     * @exports IPB_LineupListData
     * @interface IPB_LineupListData
     * @augments PB_LineupListData.$Properties
     * @deprecated Use PB_LineupListData.$Properties instead.
     */

    /**
     * Shape of a PB_LineupListData.
     * @typedef {PB_LineupListData.$Properties} PB_LineupListData.$Shape
     */

    /**
     * Constructs a new PB_LineupListData.
     * @exports PB_LineupListData
     * @classdesc Represents a PB_LineupListData.
     * @constructor
     * @param {PB_LineupListData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_LineupListData = function (properties) {
        this.lineups = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_LineupListData lineups.
     * @member {Array.<PB_LineupData.$Properties>} lineups
     * @memberof PB_LineupListData
     * @instance
     */
    PB_LineupListData.prototype.lineups = $util.emptyArray;

    /**
     * Creates a new PB_LineupListData instance using the specified properties.
     * @function create
     * @memberof PB_LineupListData
     * @static
     * @param {PB_LineupListData.$Properties=} [properties] Properties to set
     * @returns {PB_LineupListData} PB_LineupListData instance
     * @type {{
     *   (properties: PB_LineupListData.$Shape): PB_LineupListData & PB_LineupListData.$Shape;
     *   (properties?: PB_LineupListData.$Properties): PB_LineupListData;
     * }}
     */
    PB_LineupListData.create = function(properties) {
        return new PB_LineupListData(properties);
    };

    /**
     * Encodes the specified PB_LineupListData message. Does not implicitly {@link PB_LineupListData.verify|verify} messages.
     * @function encode
     * @memberof PB_LineupListData
     * @static
     * @param {PB_LineupListData.$Properties} message PB_LineupListData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_LineupListData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.lineups != null && message.lineups.length)
            for (let i = 0; i < message.lineups.length; ++i)
                $root.PB_LineupData.encode(message.lineups[i], writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_LineupListData message, length delimited. Does not implicitly {@link PB_LineupListData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_LineupListData
     * @static
     * @param {PB_LineupListData.$Properties} message PB_LineupListData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_LineupListData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_LineupListData message from the specified reader or buffer.
     * @function decode
     * @memberof PB_LineupListData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_LineupListData & PB_LineupListData.$Shape} PB_LineupListData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_LineupListData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_LineupListData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    if (!(message.lineups && message.lineups.length))
                        message.lineups = [];
                    message.lineups.push($root.PB_LineupData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_LineupListData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_LineupListData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_LineupListData & PB_LineupListData.$Shape} PB_LineupListData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_LineupListData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_LineupListData message.
     * @function verify
     * @memberof PB_LineupListData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_LineupListData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.lineups != null && $Object.hasOwnProperty.call(message, "lineups")) {
            if (!$Array.isArray(message.lineups))
                return "lineups: array expected";
            for (let i = 0; i < message.lineups.length; ++i) {
                let error = $root.PB_LineupData.verify(message.lineups[i], _depth + 1);
                if (error)
                    return "lineups." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_LineupListData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_LineupListData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_LineupListData} PB_LineupListData
     */
    PB_LineupListData.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_LineupListData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_LineupListData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_LineupListData();
        if (object.lineups) {
            if (!$Array.isArray(object.lineups))
                throw $TypeError(".PB_LineupListData.lineups: array expected");
            message.lineups = $Array(object.lineups.length);
            for (let i = 0; i < object.lineups.length; ++i) {
                if (!$util.isObject(object.lineups[i]))
                    throw $TypeError(".PB_LineupListData.lineups: object expected");
                message.lineups[i] = $root.PB_LineupData.fromObject(object.lineups[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_LineupListData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_LineupListData
     * @static
     * @param {PB_LineupListData} message PB_LineupListData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_LineupListData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.lineups = [];
        if (message.lineups && message.lineups.length) {
            object.lineups = $Array(message.lineups.length);
            for (let j = 0; j < message.lineups.length; ++j)
                object.lineups[j] = $root.PB_LineupData.toObject(message.lineups[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_LineupListData to JSON.
     * @function toJSON
     * @memberof PB_LineupListData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_LineupListData.prototype.toJSON = function() {
        return PB_LineupListData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_LineupListData
     * @function getTypeUrl
     * @memberof PB_LineupListData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_LineupListData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_LineupListData";
    };

    return PB_LineupListData;
})();

export const PB_RankNode = $root.PB_RankNode = (() => {

    /**
     * Properties of a PB_RankNode.
     * @typedef {Object} PB_RankNode.$Properties
     * @property {number|null} [rank] PB_RankNode rank
     * @property {number|null} [uid] PB_RankNode uid
     * @property {string|null} [name] PB_RankNode name
     * @property {number|null} [value] PB_RankNode value
     * @property {number|null} [power] PB_RankNode power
     * @property {Array.<PB_HeroData.$Properties>|null} [heroes] PB_RankNode heroes
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RankNode.
     * @exports IPB_RankNode
     * @interface IPB_RankNode
     * @augments PB_RankNode.$Properties
     * @deprecated Use PB_RankNode.$Properties instead.
     */

    /**
     * Shape of a PB_RankNode.
     * @typedef {PB_RankNode.$Properties} PB_RankNode.$Shape
     */

    /**
     * Constructs a new PB_RankNode.
     * @exports PB_RankNode
     * @classdesc Represents a PB_RankNode.
     * @constructor
     * @param {PB_RankNode.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RankNode = function (properties) {
        this.heroes = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RankNode rank.
     * @member {number|null|undefined} rank
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.rank = null;

    /**
     * PB_RankNode uid.
     * @member {number|null|undefined} uid
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.uid = null;

    /**
     * PB_RankNode name.
     * @member {string|null|undefined} name
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.name = null;

    /**
     * PB_RankNode value.
     * @member {number|null|undefined} value
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.value = null;

    /**
     * PB_RankNode power.
     * @member {number|null|undefined} power
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.power = null;

    /**
     * PB_RankNode heroes.
     * @member {Array.<PB_HeroData.$Properties>} heroes
     * @memberof PB_RankNode
     * @instance
     */
    PB_RankNode.prototype.heroes = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankNode.prototype, "_rank", {
        get: $util.oneOfGetter($oneOfFields = ["rank"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankNode.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankNode.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankNode.prototype, "_value", {
        get: $util.oneOfGetter($oneOfFields = ["value"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankNode.prototype, "_power", {
        get: $util.oneOfGetter($oneOfFields = ["power"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_RankNode instance using the specified properties.
     * @function create
     * @memberof PB_RankNode
     * @static
     * @param {PB_RankNode.$Properties=} [properties] Properties to set
     * @returns {PB_RankNode} PB_RankNode instance
     * @type {{
     *   (properties: PB_RankNode.$Shape): PB_RankNode & PB_RankNode.$Shape;
     *   (properties?: PB_RankNode.$Properties): PB_RankNode;
     * }}
     */
    PB_RankNode.create = function(properties) {
        return new PB_RankNode(properties);
    };

    /**
     * Encodes the specified PB_RankNode message. Does not implicitly {@link PB_RankNode.verify|verify} messages.
     * @function encode
     * @memberof PB_RankNode
     * @static
     * @param {PB_RankNode.$Properties} message PB_RankNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RankNode.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.rank != null && $Object.hasOwnProperty.call(message, "rank"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.rank);
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.uid);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.name);
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.value);
        if (message.power != null && $Object.hasOwnProperty.call(message, "power"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.power);
        if (message.heroes != null && message.heroes.length)
            for (let i = 0; i < message.heroes.length; ++i)
                $root.PB_HeroData.encode(message.heroes[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RankNode message, length delimited. Does not implicitly {@link PB_RankNode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RankNode
     * @static
     * @param {PB_RankNode.$Properties} message PB_RankNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RankNode.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RankNode message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RankNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RankNode & PB_RankNode.$Shape} PB_RankNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RankNode.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RankNode();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.rank = reader.int32();
                    message._rank = "rank";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.value = reader.int32();
                    message._value = "value";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.power = reader.int32();
                    message._power = "power";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    if (!(message.heroes && message.heroes.length))
                        message.heroes = [];
                    message.heroes.push($root.PB_HeroData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RankNode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RankNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RankNode & PB_RankNode.$Shape} PB_RankNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RankNode.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RankNode message.
     * @function verify
     * @memberof PB_RankNode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RankNode.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.rank != null && $Object.hasOwnProperty.call(message, "rank")) {
            properties._rank = 1;
            if (!$util.isInteger(message.rank))
                return "rank: integer expected";
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
            properties._value = 1;
            if (!$util.isInteger(message.value))
                return "value: integer expected";
        }
        if (message.power != null && $Object.hasOwnProperty.call(message, "power")) {
            properties._power = 1;
            if (!$util.isInteger(message.power))
                return "power: integer expected";
        }
        if (message.heroes != null && $Object.hasOwnProperty.call(message, "heroes")) {
            if (!$Array.isArray(message.heroes))
                return "heroes: array expected";
            for (let i = 0; i < message.heroes.length; ++i) {
                let error = $root.PB_HeroData.verify(message.heroes[i], _depth + 1);
                if (error)
                    return "heroes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_RankNode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RankNode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RankNode} PB_RankNode
     */
    PB_RankNode.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RankNode)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RankNode: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RankNode();
        if (object.rank != null)
            message.rank = object.rank | 0;
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.name != null)
            message.name = $String(object.name);
        if (object.value != null)
            message.value = object.value | 0;
        if (object.power != null)
            message.power = object.power | 0;
        if (object.heroes) {
            if (!$Array.isArray(object.heroes))
                throw $TypeError(".PB_RankNode.heroes: array expected");
            message.heroes = $Array(object.heroes.length);
            for (let i = 0; i < object.heroes.length; ++i) {
                if (!$util.isObject(object.heroes[i]))
                    throw $TypeError(".PB_RankNode.heroes: object expected");
                message.heroes[i] = $root.PB_HeroData.fromObject(object.heroes[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_RankNode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RankNode
     * @static
     * @param {PB_RankNode} message PB_RankNode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RankNode.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.heroes = [];
        if (message.rank != null && $Object.hasOwnProperty.call(message, "rank"))
            object.rank = message.rank;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            object.value = message.value;
        if (message.power != null && $Object.hasOwnProperty.call(message, "power"))
            object.power = message.power;
        if (message.heroes && message.heroes.length) {
            object.heroes = $Array(message.heroes.length);
            for (let j = 0; j < message.heroes.length; ++j)
                object.heroes[j] = $root.PB_HeroData.toObject(message.heroes[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_RankNode to JSON.
     * @function toJSON
     * @memberof PB_RankNode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RankNode.prototype.toJSON = function() {
        return PB_RankNode.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RankNode
     * @function getTypeUrl
     * @memberof PB_RankNode
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RankNode.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RankNode";
    };

    return PB_RankNode;
})();

export const PB_ArenaRankNode = $root.PB_ArenaRankNode = (() => {

    /**
     * Properties of a PB_ArenaRankNode.
     * @typedef {Object} PB_ArenaRankNode.$Properties
     * @property {PB_RankNode.$Properties|null} [base] PB_ArenaRankNode base
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ArenaRankNode.
     * @exports IPB_ArenaRankNode
     * @interface IPB_ArenaRankNode
     * @augments PB_ArenaRankNode.$Properties
     * @deprecated Use PB_ArenaRankNode.$Properties instead.
     */

    /**
     * Shape of a PB_ArenaRankNode.
     * @typedef {PB_ArenaRankNode.$Properties} PB_ArenaRankNode.$Shape
     */

    /**
     * Constructs a new PB_ArenaRankNode.
     * @exports PB_ArenaRankNode
     * @classdesc Represents a PB_ArenaRankNode.
     * @constructor
     * @param {PB_ArenaRankNode.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ArenaRankNode = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ArenaRankNode base.
     * @member {PB_RankNode.$Properties|null|undefined} base
     * @memberof PB_ArenaRankNode
     * @instance
     */
    PB_ArenaRankNode.prototype.base = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaRankNode.prototype, "_base", {
        get: $util.oneOfGetter($oneOfFields = ["base"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ArenaRankNode instance using the specified properties.
     * @function create
     * @memberof PB_ArenaRankNode
     * @static
     * @param {PB_ArenaRankNode.$Properties=} [properties] Properties to set
     * @returns {PB_ArenaRankNode} PB_ArenaRankNode instance
     * @type {{
     *   (properties: PB_ArenaRankNode.$Shape): PB_ArenaRankNode & PB_ArenaRankNode.$Shape;
     *   (properties?: PB_ArenaRankNode.$Properties): PB_ArenaRankNode;
     * }}
     */
    PB_ArenaRankNode.create = function(properties) {
        return new PB_ArenaRankNode(properties);
    };

    /**
     * Encodes the specified PB_ArenaRankNode message. Does not implicitly {@link PB_ArenaRankNode.verify|verify} messages.
     * @function encode
     * @memberof PB_ArenaRankNode
     * @static
     * @param {PB_ArenaRankNode.$Properties} message PB_ArenaRankNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaRankNode.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.base != null && $Object.hasOwnProperty.call(message, "base"))
            $root.PB_RankNode.encode(message.base, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ArenaRankNode message, length delimited. Does not implicitly {@link PB_ArenaRankNode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ArenaRankNode
     * @static
     * @param {PB_ArenaRankNode.$Properties} message PB_ArenaRankNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaRankNode.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ArenaRankNode message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ArenaRankNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ArenaRankNode & PB_ArenaRankNode.$Shape} PB_ArenaRankNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaRankNode.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ArenaRankNode();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.base = $root.PB_RankNode.decode(reader, reader.uint32(), $undefined, _depth + 1, message.base);
                    message._base = "base";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ArenaRankNode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ArenaRankNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ArenaRankNode & PB_ArenaRankNode.$Shape} PB_ArenaRankNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaRankNode.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ArenaRankNode message.
     * @function verify
     * @memberof PB_ArenaRankNode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ArenaRankNode.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.base != null && $Object.hasOwnProperty.call(message, "base")) {
            properties._base = 1;
            {
                let error = $root.PB_RankNode.verify(message.base, _depth + 1);
                if (error)
                    return "base." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_ArenaRankNode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ArenaRankNode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ArenaRankNode} PB_ArenaRankNode
     */
    PB_ArenaRankNode.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ArenaRankNode)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ArenaRankNode: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ArenaRankNode();
        if (object.base != null) {
            if (!$util.isObject(object.base))
                throw $TypeError(".PB_ArenaRankNode.base: object expected");
            message.base = $root.PB_RankNode.fromObject(object.base, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_ArenaRankNode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ArenaRankNode
     * @static
     * @param {PB_ArenaRankNode} message PB_ArenaRankNode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ArenaRankNode.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.base != null && $Object.hasOwnProperty.call(message, "base"))
            object.base = $root.PB_RankNode.toObject(message.base, options, _depth + 1);
        return object;
    };

    /**
     * Converts this PB_ArenaRankNode to JSON.
     * @function toJSON
     * @memberof PB_ArenaRankNode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ArenaRankNode.prototype.toJSON = function() {
        return PB_ArenaRankNode.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ArenaRankNode
     * @function getTypeUrl
     * @memberof PB_ArenaRankNode
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ArenaRankNode.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ArenaRankNode";
    };

    return PB_ArenaRankNode;
})();

export const PB_MessageHead = $root.PB_MessageHead = (() => {

    /**
     * Properties of a PB_MessageHead.
     * @typedef {Object} PB_MessageHead.$Properties
     * @property {number|null} [msg_type] PB_MessageHead msg_type
     * @property {Array.<number>|null} [param] PB_MessageHead param
     * @property {string|null} [token] PB_MessageHead token
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_MessageHead.
     * @exports IPB_MessageHead
     * @interface IPB_MessageHead
     * @augments PB_MessageHead.$Properties
     * @deprecated Use PB_MessageHead.$Properties instead.
     */

    /**
     * Shape of a PB_MessageHead.
     * @typedef {PB_MessageHead.$Properties} PB_MessageHead.$Shape
     */

    /**
     * Constructs a new PB_MessageHead.
     * @exports PB_MessageHead
     * @classdesc Represents a PB_MessageHead.
     * @constructor
     * @param {PB_MessageHead.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_MessageHead = function (properties) {
        this.param = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_MessageHead msg_type.
     * @member {number|null|undefined} msg_type
     * @memberof PB_MessageHead
     * @instance
     */
    PB_MessageHead.prototype.msg_type = null;

    /**
     * PB_MessageHead param.
     * @member {Array.<number>} param
     * @memberof PB_MessageHead
     * @instance
     */
    PB_MessageHead.prototype.param = $util.emptyArray;

    /**
     * PB_MessageHead token.
     * @member {string|null|undefined} token
     * @memberof PB_MessageHead
     * @instance
     */
    PB_MessageHead.prototype.token = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_MessageHead.prototype, "_msg_type", {
        get: $util.oneOfGetter($oneOfFields = ["msg_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_MessageHead.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_MessageHead instance using the specified properties.
     * @function create
     * @memberof PB_MessageHead
     * @static
     * @param {PB_MessageHead.$Properties=} [properties] Properties to set
     * @returns {PB_MessageHead} PB_MessageHead instance
     * @type {{
     *   (properties: PB_MessageHead.$Shape): PB_MessageHead & PB_MessageHead.$Shape;
     *   (properties?: PB_MessageHead.$Properties): PB_MessageHead;
     * }}
     */
    PB_MessageHead.create = function(properties) {
        return new PB_MessageHead(properties);
    };

    /**
     * Encodes the specified PB_MessageHead message. Does not implicitly {@link PB_MessageHead.verify|verify} messages.
     * @function encode
     * @memberof PB_MessageHead
     * @static
     * @param {PB_MessageHead.$Properties} message PB_MessageHead message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_MessageHead.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.msg_type != null && $Object.hasOwnProperty.call(message, "msg_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.msg_type);
        if (message.param != null && message.param.length)
            writer.uint32(/* id 2, wireType 2 =*/18).int32s(message.param);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_MessageHead message, length delimited. Does not implicitly {@link PB_MessageHead.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_MessageHead
     * @static
     * @param {PB_MessageHead.$Properties} message PB_MessageHead message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_MessageHead.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_MessageHead message from the specified reader or buffer.
     * @function decode
     * @memberof PB_MessageHead
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_MessageHead & PB_MessageHead.$Shape} PB_MessageHead
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_MessageHead.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_MessageHead();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.msg_type = reader.int32();
                    message._msg_type = "msg_type";
                    continue;
                }
            case 2: {
                    if (wireType === 2) {
                        if (!(message.param && message.param.length))
                            message.param = [];
                        reader.int32s(message.param);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.param && message.param.length))
                        message.param = [];
                    message.param.push(reader.int32());
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.token = reader.stringVerify();
                    message._token = "token";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_MessageHead message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_MessageHead
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_MessageHead & PB_MessageHead.$Shape} PB_MessageHead
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_MessageHead.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_MessageHead message.
     * @function verify
     * @memberof PB_MessageHead
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_MessageHead.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.msg_type != null && $Object.hasOwnProperty.call(message, "msg_type")) {
            properties._msg_type = 1;
            if (!$util.isInteger(message.msg_type))
                return "msg_type: integer expected";
        }
        if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
            if (!$Array.isArray(message.param))
                return "param: array expected";
            for (let i = 0; i < message.param.length; ++i)
                if (!$util.isInteger(message.param[i]))
                    return "param: integer[] expected";
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isString(message.token))
                return "token: string expected";
        }
        return null;
    };

    /**
     * Creates a PB_MessageHead message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_MessageHead
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_MessageHead} PB_MessageHead
     */
    PB_MessageHead.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_MessageHead)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_MessageHead: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_MessageHead();
        if (object.msg_type != null)
            message.msg_type = object.msg_type | 0;
        if (object.param) {
            if (!$Array.isArray(object.param))
                throw $TypeError(".PB_MessageHead.param: array expected");
            message.param = $Array(object.param.length);
            for (let i = 0; i < object.param.length; ++i)
                message.param[i] = object.param[i] | 0;
        }
        if (object.token != null)
            message.token = $String(object.token);
        return message;
    };

    /**
     * Creates a plain object from a PB_MessageHead message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_MessageHead
     * @static
     * @param {PB_MessageHead} message PB_MessageHead
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_MessageHead.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.param = [];
        if (message.msg_type != null && $Object.hasOwnProperty.call(message, "msg_type"))
            object.msg_type = message.msg_type;
        if (message.param && message.param.length) {
            object.param = $Array(message.param.length);
            for (let j = 0; j < message.param.length; ++j)
                object.param[j] = message.param[j];
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        return object;
    };

    /**
     * Converts this PB_MessageHead to JSON.
     * @function toJSON
     * @memberof PB_MessageHead
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_MessageHead.prototype.toJSON = function() {
        return PB_MessageHead.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_MessageHead
     * @function getTypeUrl
     * @memberof PB_MessageHead
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_MessageHead.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_MessageHead";
    };

    return PB_MessageHead;
})();

export const P_ServerConnectResult = $root.P_ServerConnectResult = (() => {

    /**
     * Properties of a P_ServerConnectResult.
     * @typedef {Object} P_ServerConnectResult.$Properties
     * @property {boolean|null} [success] P_ServerConnectResult success
     * @property {string|null} [error] P_ServerConnectResult error
     * @property {boolean|null} [disconnect] P_ServerConnectResult disconnect
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ServerConnectResult.
     * @exports IP_ServerConnectResult
     * @interface IP_ServerConnectResult
     * @augments P_ServerConnectResult.$Properties
     * @deprecated Use P_ServerConnectResult.$Properties instead.
     */

    /**
     * Shape of a P_ServerConnectResult.
     * @typedef {P_ServerConnectResult.$Properties} P_ServerConnectResult.$Shape
     */

    /**
     * Constructs a new P_ServerConnectResult.
     * @exports P_ServerConnectResult
     * @classdesc Represents a P_ServerConnectResult.
     * @constructor
     * @param {P_ServerConnectResult.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ServerConnectResult = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ServerConnectResult success.
     * @member {boolean|null|undefined} success
     * @memberof P_ServerConnectResult
     * @instance
     */
    P_ServerConnectResult.prototype.success = null;

    /**
     * P_ServerConnectResult error.
     * @member {string|null|undefined} error
     * @memberof P_ServerConnectResult
     * @instance
     */
    P_ServerConnectResult.prototype.error = null;

    /**
     * P_ServerConnectResult disconnect.
     * @member {boolean|null|undefined} disconnect
     * @memberof P_ServerConnectResult
     * @instance
     */
    P_ServerConnectResult.prototype.disconnect = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerConnectResult.prototype, "_success", {
        get: $util.oneOfGetter($oneOfFields = ["success"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerConnectResult.prototype, "_error", {
        get: $util.oneOfGetter($oneOfFields = ["error"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerConnectResult.prototype, "_disconnect", {
        get: $util.oneOfGetter($oneOfFields = ["disconnect"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ServerConnectResult instance using the specified properties.
     * @function create
     * @memberof P_ServerConnectResult
     * @static
     * @param {P_ServerConnectResult.$Properties=} [properties] Properties to set
     * @returns {P_ServerConnectResult} P_ServerConnectResult instance
     * @type {{
     *   (properties: P_ServerConnectResult.$Shape): P_ServerConnectResult & P_ServerConnectResult.$Shape;
     *   (properties?: P_ServerConnectResult.$Properties): P_ServerConnectResult;
     * }}
     */
    P_ServerConnectResult.create = function(properties) {
        return new P_ServerConnectResult(properties);
    };

    /**
     * Encodes the specified P_ServerConnectResult message. Does not implicitly {@link P_ServerConnectResult.verify|verify} messages.
     * @function encode
     * @memberof P_ServerConnectResult
     * @static
     * @param {P_ServerConnectResult.$Properties} message P_ServerConnectResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerConnectResult.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
            writer.uint32(/* id 1, wireType 0 =*/8).bool(message.success);
        if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.error);
        if (message.disconnect != null && $Object.hasOwnProperty.call(message, "disconnect"))
            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.disconnect);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ServerConnectResult message, length delimited. Does not implicitly {@link P_ServerConnectResult.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ServerConnectResult
     * @static
     * @param {P_ServerConnectResult.$Properties} message P_ServerConnectResult message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerConnectResult.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ServerConnectResult message from the specified reader or buffer.
     * @function decode
     * @memberof P_ServerConnectResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ServerConnectResult & P_ServerConnectResult.$Shape} P_ServerConnectResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerConnectResult.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ServerConnectResult();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.success = reader.bool();
                    message._success = "success";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.error = reader.stringVerify();
                    message._error = "error";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.disconnect = reader.bool();
                    message._disconnect = "disconnect";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ServerConnectResult message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ServerConnectResult
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ServerConnectResult & P_ServerConnectResult.$Shape} P_ServerConnectResult
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerConnectResult.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ServerConnectResult message.
     * @function verify
     * @memberof P_ServerConnectResult
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ServerConnectResult.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.success != null && $Object.hasOwnProperty.call(message, "success")) {
            properties._success = 1;
            if (typeof message.success !== "boolean")
                return "success: boolean expected";
        }
        if (message.error != null && $Object.hasOwnProperty.call(message, "error")) {
            properties._error = 1;
            if (!$util.isString(message.error))
                return "error: string expected";
        }
        if (message.disconnect != null && $Object.hasOwnProperty.call(message, "disconnect")) {
            properties._disconnect = 1;
            if (typeof message.disconnect !== "boolean")
                return "disconnect: boolean expected";
        }
        return null;
    };

    /**
     * Creates a P_ServerConnectResult message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ServerConnectResult
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ServerConnectResult} P_ServerConnectResult
     */
    P_ServerConnectResult.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ServerConnectResult)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ServerConnectResult: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ServerConnectResult();
        if (object.success != null)
            message.success = $Boolean(object.success);
        if (object.error != null)
            message.error = $String(object.error);
        if (object.disconnect != null)
            message.disconnect = $Boolean(object.disconnect);
        return message;
    };

    /**
     * Creates a plain object from a P_ServerConnectResult message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ServerConnectResult
     * @static
     * @param {P_ServerConnectResult} message P_ServerConnectResult
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ServerConnectResult.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.success != null && $Object.hasOwnProperty.call(message, "success"))
            object.success = message.success;
        if (message.error != null && $Object.hasOwnProperty.call(message, "error"))
            object.error = message.error;
        if (message.disconnect != null && $Object.hasOwnProperty.call(message, "disconnect"))
            object.disconnect = message.disconnect;
        return object;
    };

    /**
     * Converts this P_ServerConnectResult to JSON.
     * @function toJSON
     * @memberof P_ServerConnectResult
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ServerConnectResult.prototype.toJSON = function() {
        return P_ServerConnectResult.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ServerConnectResult
     * @function getTypeUrl
     * @memberof P_ServerConnectResult
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ServerConnectResult.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ServerConnectResult";
    };

    return P_ServerConnectResult;
})();

export const P_DBHeartbeat = $root.P_DBHeartbeat = (() => {

    /**
     * Properties of a P_DBHeartbeat.
     * @typedef {Object} P_DBHeartbeat.$Properties
     * @property {number|null} [server_id] P_DBHeartbeat server_id
     * @property {number|null} [time] P_DBHeartbeat time
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_DBHeartbeat.
     * @exports IP_DBHeartbeat
     * @interface IP_DBHeartbeat
     * @augments P_DBHeartbeat.$Properties
     * @deprecated Use P_DBHeartbeat.$Properties instead.
     */

    /**
     * Shape of a P_DBHeartbeat.
     * @typedef {P_DBHeartbeat.$Properties} P_DBHeartbeat.$Shape
     */

    /**
     * Constructs a new P_DBHeartbeat.
     * @exports P_DBHeartbeat
     * @classdesc Represents a P_DBHeartbeat.
     * @constructor
     * @param {P_DBHeartbeat.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_DBHeartbeat = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_DBHeartbeat server_id.
     * @member {number|null|undefined} server_id
     * @memberof P_DBHeartbeat
     * @instance
     */
    P_DBHeartbeat.prototype.server_id = null;

    /**
     * P_DBHeartbeat time.
     * @member {number|null|undefined} time
     * @memberof P_DBHeartbeat
     * @instance
     */
    P_DBHeartbeat.prototype.time = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_DBHeartbeat.prototype, "_server_id", {
        get: $util.oneOfGetter($oneOfFields = ["server_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_DBHeartbeat.prototype, "_time", {
        get: $util.oneOfGetter($oneOfFields = ["time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_DBHeartbeat instance using the specified properties.
     * @function create
     * @memberof P_DBHeartbeat
     * @static
     * @param {P_DBHeartbeat.$Properties=} [properties] Properties to set
     * @returns {P_DBHeartbeat} P_DBHeartbeat instance
     * @type {{
     *   (properties: P_DBHeartbeat.$Shape): P_DBHeartbeat & P_DBHeartbeat.$Shape;
     *   (properties?: P_DBHeartbeat.$Properties): P_DBHeartbeat;
     * }}
     */
    P_DBHeartbeat.create = function(properties) {
        return new P_DBHeartbeat(properties);
    };

    /**
     * Encodes the specified P_DBHeartbeat message. Does not implicitly {@link P_DBHeartbeat.verify|verify} messages.
     * @function encode
     * @memberof P_DBHeartbeat
     * @static
     * @param {P_DBHeartbeat.$Properties} message P_DBHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_DBHeartbeat.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.server_id);
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 2, wireType 0 =*/16).int64(message.time);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_DBHeartbeat message, length delimited. Does not implicitly {@link P_DBHeartbeat.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_DBHeartbeat
     * @static
     * @param {P_DBHeartbeat.$Properties} message P_DBHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_DBHeartbeat.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_DBHeartbeat message from the specified reader or buffer.
     * @function decode
     * @memberof P_DBHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_DBHeartbeat & P_DBHeartbeat.$Shape} P_DBHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_DBHeartbeat.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_DBHeartbeat();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.server_id = reader.int32();
                    message._server_id = "server_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.time = reader.int64();
                    message._time = "time";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_DBHeartbeat message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_DBHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_DBHeartbeat & P_DBHeartbeat.$Shape} P_DBHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_DBHeartbeat.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_DBHeartbeat message.
     * @function verify
     * @memberof P_DBHeartbeat
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_DBHeartbeat.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id")) {
            properties._server_id = 1;
            if (!$util.isInteger(message.server_id))
                return "server_id: integer expected";
        }
        if (message.time != null && $Object.hasOwnProperty.call(message, "time")) {
            properties._time = 1;
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a P_DBHeartbeat message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_DBHeartbeat
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_DBHeartbeat} P_DBHeartbeat
     */
    P_DBHeartbeat.fromObject = function (object, _depth) {
        if (object instanceof $root.P_DBHeartbeat)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_DBHeartbeat: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_DBHeartbeat();
        if (object.server_id != null)
            message.server_id = object.server_id | 0;
        if (object.time != null)
            if ($util.Long)
                message.time = $util.Long.fromValue(object.time, false);
            else if (typeof object.time === "string")
                message.time = $parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a P_DBHeartbeat message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_DBHeartbeat
     * @static
     * @param {P_DBHeartbeat} message P_DBHeartbeat
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_DBHeartbeat.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            object.server_id = message.server_id;
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
            else if (typeof message.time === "number")
                object.time = options.longs === $String ? $String(message.time) : message.time;
            else
                object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
        return object;
    };

    /**
     * Converts this P_DBHeartbeat to JSON.
     * @function toJSON
     * @memberof P_DBHeartbeat
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_DBHeartbeat.prototype.toJSON = function() {
        return P_DBHeartbeat.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_DBHeartbeat
     * @function getTypeUrl
     * @memberof P_DBHeartbeat
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_DBHeartbeat.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_DBHeartbeat";
    };

    return P_DBHeartbeat;
})();

export const P_ServerlDataDb_C = $root.P_ServerlDataDb_C = (() => {

    /**
     * Properties of a P_ServerlDataDb_C.
     * @typedef {Object} P_ServerlDataDb_C.$Properties
     * @property {number|null} [req_type] P_ServerlDataDb_C req_type
     * @property {PB_ServerGlobalData.$Properties|null} [server_global_data] P_ServerlDataDb_C server_global_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ServerlDataDb_C.
     * @exports IP_ServerlDataDb_C
     * @interface IP_ServerlDataDb_C
     * @augments P_ServerlDataDb_C.$Properties
     * @deprecated Use P_ServerlDataDb_C.$Properties instead.
     */

    /**
     * Shape of a P_ServerlDataDb_C.
     * @typedef {P_ServerlDataDb_C.$Properties} P_ServerlDataDb_C.$Shape
     */

    /**
     * Constructs a new P_ServerlDataDb_C.
     * @exports P_ServerlDataDb_C
     * @classdesc Represents a P_ServerlDataDb_C.
     * @constructor
     * @param {P_ServerlDataDb_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ServerlDataDb_C = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ServerlDataDb_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ServerlDataDb_C
     * @instance
     */
    P_ServerlDataDb_C.prototype.req_type = null;

    /**
     * P_ServerlDataDb_C server_global_data.
     * @member {PB_ServerGlobalData.$Properties|null|undefined} server_global_data
     * @memberof P_ServerlDataDb_C
     * @instance
     */
    P_ServerlDataDb_C.prototype.server_global_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerlDataDb_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerlDataDb_C.prototype, "_server_global_data", {
        get: $util.oneOfGetter($oneOfFields = ["server_global_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ServerlDataDb_C instance using the specified properties.
     * @function create
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {P_ServerlDataDb_C.$Properties=} [properties] Properties to set
     * @returns {P_ServerlDataDb_C} P_ServerlDataDb_C instance
     * @type {{
     *   (properties: P_ServerlDataDb_C.$Shape): P_ServerlDataDb_C & P_ServerlDataDb_C.$Shape;
     *   (properties?: P_ServerlDataDb_C.$Properties): P_ServerlDataDb_C;
     * }}
     */
    P_ServerlDataDb_C.create = function(properties) {
        return new P_ServerlDataDb_C(properties);
    };

    /**
     * Encodes the specified P_ServerlDataDb_C message. Does not implicitly {@link P_ServerlDataDb_C.verify|verify} messages.
     * @function encode
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {P_ServerlDataDb_C.$Properties} message P_ServerlDataDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerlDataDb_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data"))
            $root.PB_ServerGlobalData.encode(message.server_global_data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ServerlDataDb_C message, length delimited. Does not implicitly {@link P_ServerlDataDb_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {P_ServerlDataDb_C.$Properties} message P_ServerlDataDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerlDataDb_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ServerlDataDb_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ServerlDataDb_C & P_ServerlDataDb_C.$Shape} P_ServerlDataDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerlDataDb_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ServerlDataDb_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.server_global_data = $root.PB_ServerGlobalData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.server_global_data);
                    message._server_global_data = "server_global_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ServerlDataDb_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ServerlDataDb_C & P_ServerlDataDb_C.$Shape} P_ServerlDataDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerlDataDb_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ServerlDataDb_C message.
     * @function verify
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ServerlDataDb_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data")) {
            properties._server_global_data = 1;
            {
                let error = $root.PB_ServerGlobalData.verify(message.server_global_data, _depth + 1);
                if (error)
                    return "server_global_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_ServerlDataDb_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ServerlDataDb_C} P_ServerlDataDb_C
     */
    P_ServerlDataDb_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ServerlDataDb_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ServerlDataDb_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ServerlDataDb_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.server_global_data != null) {
            if (!$util.isObject(object.server_global_data))
                throw $TypeError(".P_ServerlDataDb_C.server_global_data: object expected");
            message.server_global_data = $root.PB_ServerGlobalData.fromObject(object.server_global_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ServerlDataDb_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {P_ServerlDataDb_C} message P_ServerlDataDb_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ServerlDataDb_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data"))
            object.server_global_data = $root.PB_ServerGlobalData.toObject(message.server_global_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_ServerlDataDb_C to JSON.
     * @function toJSON
     * @memberof P_ServerlDataDb_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ServerlDataDb_C.prototype.toJSON = function() {
        return P_ServerlDataDb_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ServerlDataDb_C
     * @function getTypeUrl
     * @memberof P_ServerlDataDb_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ServerlDataDb_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ServerlDataDb_C";
    };

    return P_ServerlDataDb_C;
})();

export const P_ServerlDataDb_S = $root.P_ServerlDataDb_S = (() => {

    /**
     * Properties of a P_ServerlDataDb_S.
     * @typedef {Object} P_ServerlDataDb_S.$Properties
     * @property {number|null} [req_type] P_ServerlDataDb_S req_type
     * @property {number|null} [save_ret] P_ServerlDataDb_S save_ret
     * @property {PB_ServerGlobalData.$Properties|null} [server_global_data] P_ServerlDataDb_S server_global_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ServerlDataDb_S.
     * @exports IP_ServerlDataDb_S
     * @interface IP_ServerlDataDb_S
     * @augments P_ServerlDataDb_S.$Properties
     * @deprecated Use P_ServerlDataDb_S.$Properties instead.
     */

    /**
     * Shape of a P_ServerlDataDb_S.
     * @typedef {P_ServerlDataDb_S.$Properties} P_ServerlDataDb_S.$Shape
     */

    /**
     * Constructs a new P_ServerlDataDb_S.
     * @exports P_ServerlDataDb_S
     * @classdesc Represents a P_ServerlDataDb_S.
     * @constructor
     * @param {P_ServerlDataDb_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ServerlDataDb_S = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ServerlDataDb_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ServerlDataDb_S
     * @instance
     */
    P_ServerlDataDb_S.prototype.req_type = null;

    /**
     * P_ServerlDataDb_S save_ret.
     * @member {number|null|undefined} save_ret
     * @memberof P_ServerlDataDb_S
     * @instance
     */
    P_ServerlDataDb_S.prototype.save_ret = null;

    /**
     * P_ServerlDataDb_S server_global_data.
     * @member {PB_ServerGlobalData.$Properties|null|undefined} server_global_data
     * @memberof P_ServerlDataDb_S
     * @instance
     */
    P_ServerlDataDb_S.prototype.server_global_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerlDataDb_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerlDataDb_S.prototype, "_save_ret", {
        get: $util.oneOfGetter($oneOfFields = ["save_ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ServerlDataDb_S.prototype, "_server_global_data", {
        get: $util.oneOfGetter($oneOfFields = ["server_global_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ServerlDataDb_S instance using the specified properties.
     * @function create
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {P_ServerlDataDb_S.$Properties=} [properties] Properties to set
     * @returns {P_ServerlDataDb_S} P_ServerlDataDb_S instance
     * @type {{
     *   (properties: P_ServerlDataDb_S.$Shape): P_ServerlDataDb_S & P_ServerlDataDb_S.$Shape;
     *   (properties?: P_ServerlDataDb_S.$Properties): P_ServerlDataDb_S;
     * }}
     */
    P_ServerlDataDb_S.create = function(properties) {
        return new P_ServerlDataDb_S(properties);
    };

    /**
     * Encodes the specified P_ServerlDataDb_S message. Does not implicitly {@link P_ServerlDataDb_S.verify|verify} messages.
     * @function encode
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {P_ServerlDataDb_S.$Properties} message P_ServerlDataDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerlDataDb_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.save_ret);
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data"))
            $root.PB_ServerGlobalData.encode(message.server_global_data, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ServerlDataDb_S message, length delimited. Does not implicitly {@link P_ServerlDataDb_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {P_ServerlDataDb_S.$Properties} message P_ServerlDataDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ServerlDataDb_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ServerlDataDb_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ServerlDataDb_S & P_ServerlDataDb_S.$Shape} P_ServerlDataDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerlDataDb_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ServerlDataDb_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.save_ret = reader.int32();
                    message._save_ret = "save_ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.server_global_data = $root.PB_ServerGlobalData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.server_global_data);
                    message._server_global_data = "server_global_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ServerlDataDb_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ServerlDataDb_S & P_ServerlDataDb_S.$Shape} P_ServerlDataDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ServerlDataDb_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ServerlDataDb_S message.
     * @function verify
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ServerlDataDb_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret")) {
            properties._save_ret = 1;
            if (!$util.isInteger(message.save_ret))
                return "save_ret: integer expected";
        }
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data")) {
            properties._server_global_data = 1;
            {
                let error = $root.PB_ServerGlobalData.verify(message.server_global_data, _depth + 1);
                if (error)
                    return "server_global_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_ServerlDataDb_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ServerlDataDb_S} P_ServerlDataDb_S
     */
    P_ServerlDataDb_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ServerlDataDb_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ServerlDataDb_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ServerlDataDb_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.save_ret != null)
            message.save_ret = object.save_ret | 0;
        if (object.server_global_data != null) {
            if (!$util.isObject(object.server_global_data))
                throw $TypeError(".P_ServerlDataDb_S.server_global_data: object expected");
            message.server_global_data = $root.PB_ServerGlobalData.fromObject(object.server_global_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ServerlDataDb_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {P_ServerlDataDb_S} message P_ServerlDataDb_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ServerlDataDb_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            object.save_ret = message.save_ret;
        if (message.server_global_data != null && $Object.hasOwnProperty.call(message, "server_global_data"))
            object.server_global_data = $root.PB_ServerGlobalData.toObject(message.server_global_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_ServerlDataDb_S to JSON.
     * @function toJSON
     * @memberof P_ServerlDataDb_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ServerlDataDb_S.prototype.toJSON = function() {
        return P_ServerlDataDb_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ServerlDataDb_S
     * @function getTypeUrl
     * @memberof P_ServerlDataDb_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ServerlDataDb_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ServerlDataDb_S";
    };

    return P_ServerlDataDb_S;
})();

export const PB_GlobalDataDbNode = $root.PB_GlobalDataDbNode = (() => {

    /**
     * Properties of a PB_GlobalDataDbNode.
     * @typedef {Object} PB_GlobalDataDbNode.$Properties
     * @property {string|null} [id] PB_GlobalDataDbNode id
     * @property {string|null} [data] PB_GlobalDataDbNode data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_GlobalDataDbNode.
     * @exports IPB_GlobalDataDbNode
     * @interface IPB_GlobalDataDbNode
     * @augments PB_GlobalDataDbNode.$Properties
     * @deprecated Use PB_GlobalDataDbNode.$Properties instead.
     */

    /**
     * Shape of a PB_GlobalDataDbNode.
     * @typedef {PB_GlobalDataDbNode.$Properties} PB_GlobalDataDbNode.$Shape
     */

    /**
     * Constructs a new PB_GlobalDataDbNode.
     * @exports PB_GlobalDataDbNode
     * @classdesc Represents a PB_GlobalDataDbNode.
     * @constructor
     * @param {PB_GlobalDataDbNode.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_GlobalDataDbNode = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_GlobalDataDbNode id.
     * @member {string|null|undefined} id
     * @memberof PB_GlobalDataDbNode
     * @instance
     */
    PB_GlobalDataDbNode.prototype.id = null;

    /**
     * PB_GlobalDataDbNode data.
     * @member {string|null|undefined} data
     * @memberof PB_GlobalDataDbNode
     * @instance
     */
    PB_GlobalDataDbNode.prototype.data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_GlobalDataDbNode.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_GlobalDataDbNode.prototype, "_data", {
        get: $util.oneOfGetter($oneOfFields = ["data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_GlobalDataDbNode instance using the specified properties.
     * @function create
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {PB_GlobalDataDbNode.$Properties=} [properties] Properties to set
     * @returns {PB_GlobalDataDbNode} PB_GlobalDataDbNode instance
     * @type {{
     *   (properties: PB_GlobalDataDbNode.$Shape): PB_GlobalDataDbNode & PB_GlobalDataDbNode.$Shape;
     *   (properties?: PB_GlobalDataDbNode.$Properties): PB_GlobalDataDbNode;
     * }}
     */
    PB_GlobalDataDbNode.create = function(properties) {
        return new PB_GlobalDataDbNode(properties);
    };

    /**
     * Encodes the specified PB_GlobalDataDbNode message. Does not implicitly {@link PB_GlobalDataDbNode.verify|verify} messages.
     * @function encode
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {PB_GlobalDataDbNode.$Properties} message PB_GlobalDataDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_GlobalDataDbNode.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.id);
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.data);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_GlobalDataDbNode message, length delimited. Does not implicitly {@link PB_GlobalDataDbNode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {PB_GlobalDataDbNode.$Properties} message PB_GlobalDataDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_GlobalDataDbNode.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_GlobalDataDbNode message from the specified reader or buffer.
     * @function decode
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_GlobalDataDbNode & PB_GlobalDataDbNode.$Shape} PB_GlobalDataDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_GlobalDataDbNode.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_GlobalDataDbNode();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.id = reader.stringVerify();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.data = reader.stringVerify();
                    message._data = "data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_GlobalDataDbNode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_GlobalDataDbNode & PB_GlobalDataDbNode.$Shape} PB_GlobalDataDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_GlobalDataDbNode.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_GlobalDataDbNode message.
     * @function verify
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_GlobalDataDbNode.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isString(message.id))
                return "id: string expected";
        }
        if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
            properties._data = 1;
            if (!$util.isString(message.data))
                return "data: string expected";
        }
        return null;
    };

    /**
     * Creates a PB_GlobalDataDbNode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_GlobalDataDbNode} PB_GlobalDataDbNode
     */
    PB_GlobalDataDbNode.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_GlobalDataDbNode)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_GlobalDataDbNode: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_GlobalDataDbNode();
        if (object.id != null)
            message.id = $String(object.id);
        if (object.data != null)
            message.data = $String(object.data);
        return message;
    };

    /**
     * Creates a plain object from a PB_GlobalDataDbNode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {PB_GlobalDataDbNode} message PB_GlobalDataDbNode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_GlobalDataDbNode.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            object.data = message.data;
        return object;
    };

    /**
     * Converts this PB_GlobalDataDbNode to JSON.
     * @function toJSON
     * @memberof PB_GlobalDataDbNode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_GlobalDataDbNode.prototype.toJSON = function() {
        return PB_GlobalDataDbNode.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_GlobalDataDbNode
     * @function getTypeUrl
     * @memberof PB_GlobalDataDbNode
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_GlobalDataDbNode.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_GlobalDataDbNode";
    };

    return PB_GlobalDataDbNode;
})();

export const P_GlobalDataDb_C = $root.P_GlobalDataDb_C = (() => {

    /**
     * Properties of a P_GlobalDataDb_C.
     * @typedef {Object} P_GlobalDataDb_C.$Properties
     * @property {number|null} [req_type] P_GlobalDataDb_C req_type
     * @property {Array.<PB_GlobalDataDbNode.$Properties>|null} [datas] P_GlobalDataDb_C datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GlobalDataDb_C.
     * @exports IP_GlobalDataDb_C
     * @interface IP_GlobalDataDb_C
     * @augments P_GlobalDataDb_C.$Properties
     * @deprecated Use P_GlobalDataDb_C.$Properties instead.
     */

    /**
     * Shape of a P_GlobalDataDb_C.
     * @typedef {P_GlobalDataDb_C.$Properties} P_GlobalDataDb_C.$Shape
     */

    /**
     * Constructs a new P_GlobalDataDb_C.
     * @exports P_GlobalDataDb_C
     * @classdesc Represents a P_GlobalDataDb_C.
     * @constructor
     * @param {P_GlobalDataDb_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GlobalDataDb_C = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GlobalDataDb_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_GlobalDataDb_C
     * @instance
     */
    P_GlobalDataDb_C.prototype.req_type = null;

    /**
     * P_GlobalDataDb_C datas.
     * @member {Array.<PB_GlobalDataDbNode.$Properties>} datas
     * @memberof P_GlobalDataDb_C
     * @instance
     */
    P_GlobalDataDb_C.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GlobalDataDb_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GlobalDataDb_C instance using the specified properties.
     * @function create
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {P_GlobalDataDb_C.$Properties=} [properties] Properties to set
     * @returns {P_GlobalDataDb_C} P_GlobalDataDb_C instance
     * @type {{
     *   (properties: P_GlobalDataDb_C.$Shape): P_GlobalDataDb_C & P_GlobalDataDb_C.$Shape;
     *   (properties?: P_GlobalDataDb_C.$Properties): P_GlobalDataDb_C;
     * }}
     */
    P_GlobalDataDb_C.create = function(properties) {
        return new P_GlobalDataDb_C(properties);
    };

    /**
     * Encodes the specified P_GlobalDataDb_C message. Does not implicitly {@link P_GlobalDataDb_C.verify|verify} messages.
     * @function encode
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {P_GlobalDataDb_C.$Properties} message P_GlobalDataDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GlobalDataDb_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_GlobalDataDbNode.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GlobalDataDb_C message, length delimited. Does not implicitly {@link P_GlobalDataDb_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {P_GlobalDataDb_C.$Properties} message P_GlobalDataDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GlobalDataDb_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GlobalDataDb_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GlobalDataDb_C & P_GlobalDataDb_C.$Shape} P_GlobalDataDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GlobalDataDb_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GlobalDataDb_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_GlobalDataDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GlobalDataDb_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GlobalDataDb_C & P_GlobalDataDb_C.$Shape} P_GlobalDataDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GlobalDataDb_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GlobalDataDb_C message.
     * @function verify
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GlobalDataDb_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_GlobalDataDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_GlobalDataDb_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GlobalDataDb_C} P_GlobalDataDb_C
     */
    P_GlobalDataDb_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GlobalDataDb_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GlobalDataDb_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GlobalDataDb_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_GlobalDataDb_C.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_GlobalDataDb_C.datas: object expected");
                message.datas[i] = $root.PB_GlobalDataDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_GlobalDataDb_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {P_GlobalDataDb_C} message P_GlobalDataDb_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GlobalDataDb_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_GlobalDataDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_GlobalDataDb_C to JSON.
     * @function toJSON
     * @memberof P_GlobalDataDb_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GlobalDataDb_C.prototype.toJSON = function() {
        return P_GlobalDataDb_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GlobalDataDb_C
     * @function getTypeUrl
     * @memberof P_GlobalDataDb_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GlobalDataDb_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GlobalDataDb_C";
    };

    return P_GlobalDataDb_C;
})();

export const P_GlobalDataDb_S = $root.P_GlobalDataDb_S = (() => {

    /**
     * Properties of a P_GlobalDataDb_S.
     * @typedef {Object} P_GlobalDataDb_S.$Properties
     * @property {number|null} [req_type] P_GlobalDataDb_S req_type
     * @property {Array.<PB_GlobalDataDbNode.$Properties>|null} [datas] P_GlobalDataDb_S datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GlobalDataDb_S.
     * @exports IP_GlobalDataDb_S
     * @interface IP_GlobalDataDb_S
     * @augments P_GlobalDataDb_S.$Properties
     * @deprecated Use P_GlobalDataDb_S.$Properties instead.
     */

    /**
     * Shape of a P_GlobalDataDb_S.
     * @typedef {P_GlobalDataDb_S.$Properties} P_GlobalDataDb_S.$Shape
     */

    /**
     * Constructs a new P_GlobalDataDb_S.
     * @exports P_GlobalDataDb_S
     * @classdesc Represents a P_GlobalDataDb_S.
     * @constructor
     * @param {P_GlobalDataDb_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GlobalDataDb_S = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GlobalDataDb_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_GlobalDataDb_S
     * @instance
     */
    P_GlobalDataDb_S.prototype.req_type = null;

    /**
     * P_GlobalDataDb_S datas.
     * @member {Array.<PB_GlobalDataDbNode.$Properties>} datas
     * @memberof P_GlobalDataDb_S
     * @instance
     */
    P_GlobalDataDb_S.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GlobalDataDb_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GlobalDataDb_S instance using the specified properties.
     * @function create
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {P_GlobalDataDb_S.$Properties=} [properties] Properties to set
     * @returns {P_GlobalDataDb_S} P_GlobalDataDb_S instance
     * @type {{
     *   (properties: P_GlobalDataDb_S.$Shape): P_GlobalDataDb_S & P_GlobalDataDb_S.$Shape;
     *   (properties?: P_GlobalDataDb_S.$Properties): P_GlobalDataDb_S;
     * }}
     */
    P_GlobalDataDb_S.create = function(properties) {
        return new P_GlobalDataDb_S(properties);
    };

    /**
     * Encodes the specified P_GlobalDataDb_S message. Does not implicitly {@link P_GlobalDataDb_S.verify|verify} messages.
     * @function encode
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {P_GlobalDataDb_S.$Properties} message P_GlobalDataDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GlobalDataDb_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_GlobalDataDbNode.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GlobalDataDb_S message, length delimited. Does not implicitly {@link P_GlobalDataDb_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {P_GlobalDataDb_S.$Properties} message P_GlobalDataDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GlobalDataDb_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GlobalDataDb_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GlobalDataDb_S & P_GlobalDataDb_S.$Shape} P_GlobalDataDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GlobalDataDb_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GlobalDataDb_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_GlobalDataDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GlobalDataDb_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GlobalDataDb_S & P_GlobalDataDb_S.$Shape} P_GlobalDataDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GlobalDataDb_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GlobalDataDb_S message.
     * @function verify
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GlobalDataDb_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_GlobalDataDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_GlobalDataDb_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GlobalDataDb_S} P_GlobalDataDb_S
     */
    P_GlobalDataDb_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GlobalDataDb_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GlobalDataDb_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GlobalDataDb_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_GlobalDataDb_S.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_GlobalDataDb_S.datas: object expected");
                message.datas[i] = $root.PB_GlobalDataDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_GlobalDataDb_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {P_GlobalDataDb_S} message P_GlobalDataDb_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GlobalDataDb_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_GlobalDataDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_GlobalDataDb_S to JSON.
     * @function toJSON
     * @memberof P_GlobalDataDb_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GlobalDataDb_S.prototype.toJSON = function() {
        return P_GlobalDataDb_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GlobalDataDb_S
     * @function getTypeUrl
     * @memberof P_GlobalDataDb_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GlobalDataDb_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GlobalDataDb_S";
    };

    return P_GlobalDataDb_S;
})();

export const PB_ArenaDbNode = $root.PB_ArenaDbNode = (() => {

    /**
     * Properties of a PB_ArenaDbNode.
     * @typedef {Object} PB_ArenaDbNode.$Properties
     * @property {number|null} [uid] PB_ArenaDbNode uid
     * @property {number|null} [score] PB_ArenaDbNode score
     * @property {string|null} [data] PB_ArenaDbNode data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ArenaDbNode.
     * @exports IPB_ArenaDbNode
     * @interface IPB_ArenaDbNode
     * @augments PB_ArenaDbNode.$Properties
     * @deprecated Use PB_ArenaDbNode.$Properties instead.
     */

    /**
     * Shape of a PB_ArenaDbNode.
     * @typedef {PB_ArenaDbNode.$Properties} PB_ArenaDbNode.$Shape
     */

    /**
     * Constructs a new PB_ArenaDbNode.
     * @exports PB_ArenaDbNode
     * @classdesc Represents a PB_ArenaDbNode.
     * @constructor
     * @param {PB_ArenaDbNode.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ArenaDbNode = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ArenaDbNode uid.
     * @member {number|null|undefined} uid
     * @memberof PB_ArenaDbNode
     * @instance
     */
    PB_ArenaDbNode.prototype.uid = null;

    /**
     * PB_ArenaDbNode score.
     * @member {number|null|undefined} score
     * @memberof PB_ArenaDbNode
     * @instance
     */
    PB_ArenaDbNode.prototype.score = null;

    /**
     * PB_ArenaDbNode data.
     * @member {string|null|undefined} data
     * @memberof PB_ArenaDbNode
     * @instance
     */
    PB_ArenaDbNode.prototype.data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaDbNode.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaDbNode.prototype, "_score", {
        get: $util.oneOfGetter($oneOfFields = ["score"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaDbNode.prototype, "_data", {
        get: $util.oneOfGetter($oneOfFields = ["data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ArenaDbNode instance using the specified properties.
     * @function create
     * @memberof PB_ArenaDbNode
     * @static
     * @param {PB_ArenaDbNode.$Properties=} [properties] Properties to set
     * @returns {PB_ArenaDbNode} PB_ArenaDbNode instance
     * @type {{
     *   (properties: PB_ArenaDbNode.$Shape): PB_ArenaDbNode & PB_ArenaDbNode.$Shape;
     *   (properties?: PB_ArenaDbNode.$Properties): PB_ArenaDbNode;
     * }}
     */
    PB_ArenaDbNode.create = function(properties) {
        return new PB_ArenaDbNode(properties);
    };

    /**
     * Encodes the specified PB_ArenaDbNode message. Does not implicitly {@link PB_ArenaDbNode.verify|verify} messages.
     * @function encode
     * @memberof PB_ArenaDbNode
     * @static
     * @param {PB_ArenaDbNode.$Properties} message PB_ArenaDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaDbNode.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.uid);
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.score);
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.data);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ArenaDbNode message, length delimited. Does not implicitly {@link PB_ArenaDbNode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ArenaDbNode
     * @static
     * @param {PB_ArenaDbNode.$Properties} message PB_ArenaDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaDbNode.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ArenaDbNode message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ArenaDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ArenaDbNode & PB_ArenaDbNode.$Shape} PB_ArenaDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaDbNode.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ArenaDbNode();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int64();
                    message._uid = "uid";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.score = reader.int32();
                    message._score = "score";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.data = reader.stringVerify();
                    message._data = "data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ArenaDbNode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ArenaDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ArenaDbNode & PB_ArenaDbNode.$Shape} PB_ArenaDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaDbNode.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ArenaDbNode message.
     * @function verify
     * @memberof PB_ArenaDbNode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ArenaDbNode.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                return "uid: integer|Long expected";
        }
        if (message.score != null && $Object.hasOwnProperty.call(message, "score")) {
            properties._score = 1;
            if (!$util.isInteger(message.score))
                return "score: integer expected";
        }
        if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
            properties._data = 1;
            if (!$util.isString(message.data))
                return "data: string expected";
        }
        return null;
    };

    /**
     * Creates a PB_ArenaDbNode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ArenaDbNode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ArenaDbNode} PB_ArenaDbNode
     */
    PB_ArenaDbNode.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ArenaDbNode)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ArenaDbNode: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ArenaDbNode();
        if (object.uid != null)
            if ($util.Long)
                message.uid = $util.Long.fromValue(object.uid, false);
            else if (typeof object.uid === "string")
                message.uid = $parseInt(object.uid, 10);
            else if (typeof object.uid === "number")
                message.uid = object.uid;
            else if (typeof object.uid === "object")
                message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
        if (object.score != null)
            message.score = object.score | 0;
        if (object.data != null)
            message.data = $String(object.data);
        return message;
    };

    /**
     * Creates a plain object from a PB_ArenaDbNode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ArenaDbNode
     * @static
     * @param {PB_ArenaDbNode} message PB_ArenaDbNode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ArenaDbNode.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.uid = typeof message.uid === "number" ? $BigInt(message.uid) : $util.Long.fromBits(message.uid.low >>> 0, message.uid.high >>> 0, false).toBigInt();
            else if (typeof message.uid === "number")
                object.uid = options.longs === $String ? $String(message.uid) : message.uid;
            else
                object.uid = options.longs === $String ? $util.Long.prototype.toString.call(message.uid) : options.longs === $Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            object.score = message.score;
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            object.data = message.data;
        return object;
    };

    /**
     * Converts this PB_ArenaDbNode to JSON.
     * @function toJSON
     * @memberof PB_ArenaDbNode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ArenaDbNode.prototype.toJSON = function() {
        return PB_ArenaDbNode.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ArenaDbNode
     * @function getTypeUrl
     * @memberof PB_ArenaDbNode
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ArenaDbNode.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ArenaDbNode";
    };

    return PB_ArenaDbNode;
})();

export const P_ArenaDb_C = $root.P_ArenaDb_C = (() => {

    /**
     * Properties of a P_ArenaDb_C.
     * @typedef {Object} P_ArenaDb_C.$Properties
     * @property {number|null} [req_type] P_ArenaDb_C req_type
     * @property {Array.<PB_ArenaDbNode.$Properties>|null} [datas] P_ArenaDb_C datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ArenaDb_C.
     * @exports IP_ArenaDb_C
     * @interface IP_ArenaDb_C
     * @augments P_ArenaDb_C.$Properties
     * @deprecated Use P_ArenaDb_C.$Properties instead.
     */

    /**
     * Shape of a P_ArenaDb_C.
     * @typedef {P_ArenaDb_C.$Properties} P_ArenaDb_C.$Shape
     */

    /**
     * Constructs a new P_ArenaDb_C.
     * @exports P_ArenaDb_C
     * @classdesc Represents a P_ArenaDb_C.
     * @constructor
     * @param {P_ArenaDb_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ArenaDb_C = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ArenaDb_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ArenaDb_C
     * @instance
     */
    P_ArenaDb_C.prototype.req_type = null;

    /**
     * P_ArenaDb_C datas.
     * @member {Array.<PB_ArenaDbNode.$Properties>} datas
     * @memberof P_ArenaDb_C
     * @instance
     */
    P_ArenaDb_C.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaDb_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ArenaDb_C instance using the specified properties.
     * @function create
     * @memberof P_ArenaDb_C
     * @static
     * @param {P_ArenaDb_C.$Properties=} [properties] Properties to set
     * @returns {P_ArenaDb_C} P_ArenaDb_C instance
     * @type {{
     *   (properties: P_ArenaDb_C.$Shape): P_ArenaDb_C & P_ArenaDb_C.$Shape;
     *   (properties?: P_ArenaDb_C.$Properties): P_ArenaDb_C;
     * }}
     */
    P_ArenaDb_C.create = function(properties) {
        return new P_ArenaDb_C(properties);
    };

    /**
     * Encodes the specified P_ArenaDb_C message. Does not implicitly {@link P_ArenaDb_C.verify|verify} messages.
     * @function encode
     * @memberof P_ArenaDb_C
     * @static
     * @param {P_ArenaDb_C.$Properties} message P_ArenaDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaDb_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_ArenaDbNode.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ArenaDb_C message, length delimited. Does not implicitly {@link P_ArenaDb_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ArenaDb_C
     * @static
     * @param {P_ArenaDb_C.$Properties} message P_ArenaDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaDb_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ArenaDb_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_ArenaDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ArenaDb_C & P_ArenaDb_C.$Shape} P_ArenaDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaDb_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ArenaDb_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_ArenaDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ArenaDb_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ArenaDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ArenaDb_C & P_ArenaDb_C.$Shape} P_ArenaDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaDb_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ArenaDb_C message.
     * @function verify
     * @memberof P_ArenaDb_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ArenaDb_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_ArenaDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_ArenaDb_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ArenaDb_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ArenaDb_C} P_ArenaDb_C
     */
    P_ArenaDb_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ArenaDb_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ArenaDb_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ArenaDb_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_ArenaDb_C.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_ArenaDb_C.datas: object expected");
                message.datas[i] = $root.PB_ArenaDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ArenaDb_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ArenaDb_C
     * @static
     * @param {P_ArenaDb_C} message P_ArenaDb_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ArenaDb_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_ArenaDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_ArenaDb_C to JSON.
     * @function toJSON
     * @memberof P_ArenaDb_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ArenaDb_C.prototype.toJSON = function() {
        return P_ArenaDb_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ArenaDb_C
     * @function getTypeUrl
     * @memberof P_ArenaDb_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ArenaDb_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ArenaDb_C";
    };

    return P_ArenaDb_C;
})();

export const P_ArenaDb_S = $root.P_ArenaDb_S = (() => {

    /**
     * Properties of a P_ArenaDb_S.
     * @typedef {Object} P_ArenaDb_S.$Properties
     * @property {number|null} [req_type] P_ArenaDb_S req_type
     * @property {number|null} [save_ret] P_ArenaDb_S save_ret
     * @property {Array.<PB_ArenaDbNode.$Properties>|null} [datas] P_ArenaDb_S datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ArenaDb_S.
     * @exports IP_ArenaDb_S
     * @interface IP_ArenaDb_S
     * @augments P_ArenaDb_S.$Properties
     * @deprecated Use P_ArenaDb_S.$Properties instead.
     */

    /**
     * Shape of a P_ArenaDb_S.
     * @typedef {P_ArenaDb_S.$Properties} P_ArenaDb_S.$Shape
     */

    /**
     * Constructs a new P_ArenaDb_S.
     * @exports P_ArenaDb_S
     * @classdesc Represents a P_ArenaDb_S.
     * @constructor
     * @param {P_ArenaDb_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ArenaDb_S = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ArenaDb_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ArenaDb_S
     * @instance
     */
    P_ArenaDb_S.prototype.req_type = null;

    /**
     * P_ArenaDb_S save_ret.
     * @member {number|null|undefined} save_ret
     * @memberof P_ArenaDb_S
     * @instance
     */
    P_ArenaDb_S.prototype.save_ret = null;

    /**
     * P_ArenaDb_S datas.
     * @member {Array.<PB_ArenaDbNode.$Properties>} datas
     * @memberof P_ArenaDb_S
     * @instance
     */
    P_ArenaDb_S.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaDb_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaDb_S.prototype, "_save_ret", {
        get: $util.oneOfGetter($oneOfFields = ["save_ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ArenaDb_S instance using the specified properties.
     * @function create
     * @memberof P_ArenaDb_S
     * @static
     * @param {P_ArenaDb_S.$Properties=} [properties] Properties to set
     * @returns {P_ArenaDb_S} P_ArenaDb_S instance
     * @type {{
     *   (properties: P_ArenaDb_S.$Shape): P_ArenaDb_S & P_ArenaDb_S.$Shape;
     *   (properties?: P_ArenaDb_S.$Properties): P_ArenaDb_S;
     * }}
     */
    P_ArenaDb_S.create = function(properties) {
        return new P_ArenaDb_S(properties);
    };

    /**
     * Encodes the specified P_ArenaDb_S message. Does not implicitly {@link P_ArenaDb_S.verify|verify} messages.
     * @function encode
     * @memberof P_ArenaDb_S
     * @static
     * @param {P_ArenaDb_S.$Properties} message P_ArenaDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaDb_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.save_ret);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_ArenaDbNode.encode(message.datas[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ArenaDb_S message, length delimited. Does not implicitly {@link P_ArenaDb_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ArenaDb_S
     * @static
     * @param {P_ArenaDb_S.$Properties} message P_ArenaDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaDb_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ArenaDb_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_ArenaDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ArenaDb_S & P_ArenaDb_S.$Shape} P_ArenaDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaDb_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ArenaDb_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.save_ret = reader.int32();
                    message._save_ret = "save_ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_ArenaDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ArenaDb_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ArenaDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ArenaDb_S & P_ArenaDb_S.$Shape} P_ArenaDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaDb_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ArenaDb_S message.
     * @function verify
     * @memberof P_ArenaDb_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ArenaDb_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret")) {
            properties._save_ret = 1;
            if (!$util.isInteger(message.save_ret))
                return "save_ret: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_ArenaDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_ArenaDb_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ArenaDb_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ArenaDb_S} P_ArenaDb_S
     */
    P_ArenaDb_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ArenaDb_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ArenaDb_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ArenaDb_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.save_ret != null)
            message.save_ret = object.save_ret | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_ArenaDb_S.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_ArenaDb_S.datas: object expected");
                message.datas[i] = $root.PB_ArenaDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ArenaDb_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ArenaDb_S
     * @static
     * @param {P_ArenaDb_S} message P_ArenaDb_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ArenaDb_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            object.save_ret = message.save_ret;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_ArenaDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_ArenaDb_S to JSON.
     * @function toJSON
     * @memberof P_ArenaDb_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ArenaDb_S.prototype.toJSON = function() {
        return P_ArenaDb_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ArenaDb_S
     * @function getTypeUrl
     * @memberof P_ArenaDb_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ArenaDb_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ArenaDb_S";
    };

    return P_ArenaDb_S;
})();

export const PB_RankDbNode = $root.PB_RankDbNode = (() => {

    /**
     * Properties of a PB_RankDbNode.
     * @typedef {Object} PB_RankDbNode.$Properties
     * @property {number|null} [uid] PB_RankDbNode uid
     * @property {string|null} [name] PB_RankDbNode name
     * @property {number|null} [value] PB_RankDbNode value
     * @property {string|null} [data] PB_RankDbNode data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_RankDbNode.
     * @exports IPB_RankDbNode
     * @interface IPB_RankDbNode
     * @augments PB_RankDbNode.$Properties
     * @deprecated Use PB_RankDbNode.$Properties instead.
     */

    /**
     * Shape of a PB_RankDbNode.
     * @typedef {PB_RankDbNode.$Properties} PB_RankDbNode.$Shape
     */

    /**
     * Constructs a new PB_RankDbNode.
     * @exports PB_RankDbNode
     * @classdesc Represents a PB_RankDbNode.
     * @constructor
     * @param {PB_RankDbNode.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_RankDbNode = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_RankDbNode uid.
     * @member {number|null|undefined} uid
     * @memberof PB_RankDbNode
     * @instance
     */
    PB_RankDbNode.prototype.uid = null;

    /**
     * PB_RankDbNode name.
     * @member {string|null|undefined} name
     * @memberof PB_RankDbNode
     * @instance
     */
    PB_RankDbNode.prototype.name = null;

    /**
     * PB_RankDbNode value.
     * @member {number|null|undefined} value
     * @memberof PB_RankDbNode
     * @instance
     */
    PB_RankDbNode.prototype.value = null;

    /**
     * PB_RankDbNode data.
     * @member {string|null|undefined} data
     * @memberof PB_RankDbNode
     * @instance
     */
    PB_RankDbNode.prototype.data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankDbNode.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankDbNode.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankDbNode.prototype, "_value", {
        get: $util.oneOfGetter($oneOfFields = ["value"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_RankDbNode.prototype, "_data", {
        get: $util.oneOfGetter($oneOfFields = ["data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_RankDbNode instance using the specified properties.
     * @function create
     * @memberof PB_RankDbNode
     * @static
     * @param {PB_RankDbNode.$Properties=} [properties] Properties to set
     * @returns {PB_RankDbNode} PB_RankDbNode instance
     * @type {{
     *   (properties: PB_RankDbNode.$Shape): PB_RankDbNode & PB_RankDbNode.$Shape;
     *   (properties?: PB_RankDbNode.$Properties): PB_RankDbNode;
     * }}
     */
    PB_RankDbNode.create = function(properties) {
        return new PB_RankDbNode(properties);
    };

    /**
     * Encodes the specified PB_RankDbNode message. Does not implicitly {@link PB_RankDbNode.verify|verify} messages.
     * @function encode
     * @memberof PB_RankDbNode
     * @static
     * @param {PB_RankDbNode.$Properties} message PB_RankDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RankDbNode.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.uid);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            writer.uint32(/* id 3, wireType 0 =*/24).int64(message.value);
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.data);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_RankDbNode message, length delimited. Does not implicitly {@link PB_RankDbNode.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_RankDbNode
     * @static
     * @param {PB_RankDbNode.$Properties} message PB_RankDbNode message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_RankDbNode.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_RankDbNode message from the specified reader or buffer.
     * @function decode
     * @memberof PB_RankDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_RankDbNode & PB_RankDbNode.$Shape} PB_RankDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RankDbNode.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_RankDbNode();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int64();
                    message._uid = "uid";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.value = reader.int64();
                    message._value = "value";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    message.data = reader.stringVerify();
                    message._data = "data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_RankDbNode message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_RankDbNode
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_RankDbNode & PB_RankDbNode.$Shape} PB_RankDbNode
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_RankDbNode.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_RankDbNode message.
     * @function verify
     * @memberof PB_RankDbNode
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_RankDbNode.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid) && !(message.uid && $util.isInteger(message.uid.low) && $util.isInteger(message.uid.high)))
                return "uid: integer|Long expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.value != null && $Object.hasOwnProperty.call(message, "value")) {
            properties._value = 1;
            if (!$util.isInteger(message.value) && !(message.value && $util.isInteger(message.value.low) && $util.isInteger(message.value.high)))
                return "value: integer|Long expected";
        }
        if (message.data != null && $Object.hasOwnProperty.call(message, "data")) {
            properties._data = 1;
            if (!$util.isString(message.data))
                return "data: string expected";
        }
        return null;
    };

    /**
     * Creates a PB_RankDbNode message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_RankDbNode
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_RankDbNode} PB_RankDbNode
     */
    PB_RankDbNode.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_RankDbNode)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_RankDbNode: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_RankDbNode();
        if (object.uid != null)
            if ($util.Long)
                message.uid = $util.Long.fromValue(object.uid, false);
            else if (typeof object.uid === "string")
                message.uid = $parseInt(object.uid, 10);
            else if (typeof object.uid === "number")
                message.uid = object.uid;
            else if (typeof object.uid === "object")
                message.uid = new $util.LongBits(object.uid.low >>> 0, object.uid.high >>> 0).toNumber();
        if (object.name != null)
            message.name = $String(object.name);
        if (object.value != null)
            if ($util.Long)
                message.value = $util.Long.fromValue(object.value, false);
            else if (typeof object.value === "string")
                message.value = $parseInt(object.value, 10);
            else if (typeof object.value === "number")
                message.value = object.value;
            else if (typeof object.value === "object")
                message.value = new $util.LongBits(object.value.low >>> 0, object.value.high >>> 0).toNumber();
        if (object.data != null)
            message.data = $String(object.data);
        return message;
    };

    /**
     * Creates a plain object from a PB_RankDbNode message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_RankDbNode
     * @static
     * @param {PB_RankDbNode} message PB_RankDbNode
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_RankDbNode.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.uid = typeof message.uid === "number" ? $BigInt(message.uid) : $util.Long.fromBits(message.uid.low >>> 0, message.uid.high >>> 0, false).toBigInt();
            else if (typeof message.uid === "number")
                object.uid = options.longs === $String ? $String(message.uid) : message.uid;
            else
                object.uid = options.longs === $String ? $util.Long.prototype.toString.call(message.uid) : options.longs === $Number ? new $util.LongBits(message.uid.low >>> 0, message.uid.high >>> 0).toNumber() : message.uid;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.value != null && $Object.hasOwnProperty.call(message, "value"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.value = typeof message.value === "number" ? $BigInt(message.value) : $util.Long.fromBits(message.value.low >>> 0, message.value.high >>> 0, false).toBigInt();
            else if (typeof message.value === "number")
                object.value = options.longs === $String ? $String(message.value) : message.value;
            else
                object.value = options.longs === $String ? $util.Long.prototype.toString.call(message.value) : options.longs === $Number ? new $util.LongBits(message.value.low >>> 0, message.value.high >>> 0).toNumber() : message.value;
        if (message.data != null && $Object.hasOwnProperty.call(message, "data"))
            object.data = message.data;
        return object;
    };

    /**
     * Converts this PB_RankDbNode to JSON.
     * @function toJSON
     * @memberof PB_RankDbNode
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_RankDbNode.prototype.toJSON = function() {
        return PB_RankDbNode.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_RankDbNode
     * @function getTypeUrl
     * @memberof PB_RankDbNode
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_RankDbNode.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_RankDbNode";
    };

    return PB_RankDbNode;
})();

export const P_RankDb_C = $root.P_RankDb_C = (() => {

    /**
     * Properties of a P_RankDb_C.
     * @typedef {Object} P_RankDb_C.$Properties
     * @property {number|null} [req_type] P_RankDb_C req_type
     * @property {number|null} [rank_type] P_RankDb_C rank_type
     * @property {Array.<PB_RankDbNode.$Properties>|null} [datas] P_RankDb_C datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_RankDb_C.
     * @exports IP_RankDb_C
     * @interface IP_RankDb_C
     * @augments P_RankDb_C.$Properties
     * @deprecated Use P_RankDb_C.$Properties instead.
     */

    /**
     * Shape of a P_RankDb_C.
     * @typedef {P_RankDb_C.$Properties} P_RankDb_C.$Shape
     */

    /**
     * Constructs a new P_RankDb_C.
     * @exports P_RankDb_C
     * @classdesc Represents a P_RankDb_C.
     * @constructor
     * @param {P_RankDb_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_RankDb_C = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_RankDb_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_RankDb_C
     * @instance
     */
    P_RankDb_C.prototype.req_type = null;

    /**
     * P_RankDb_C rank_type.
     * @member {number|null|undefined} rank_type
     * @memberof P_RankDb_C
     * @instance
     */
    P_RankDb_C.prototype.rank_type = null;

    /**
     * P_RankDb_C datas.
     * @member {Array.<PB_RankDbNode.$Properties>} datas
     * @memberof P_RankDb_C
     * @instance
     */
    P_RankDb_C.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_RankDb_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_RankDb_C.prototype, "_rank_type", {
        get: $util.oneOfGetter($oneOfFields = ["rank_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_RankDb_C instance using the specified properties.
     * @function create
     * @memberof P_RankDb_C
     * @static
     * @param {P_RankDb_C.$Properties=} [properties] Properties to set
     * @returns {P_RankDb_C} P_RankDb_C instance
     * @type {{
     *   (properties: P_RankDb_C.$Shape): P_RankDb_C & P_RankDb_C.$Shape;
     *   (properties?: P_RankDb_C.$Properties): P_RankDb_C;
     * }}
     */
    P_RankDb_C.create = function(properties) {
        return new P_RankDb_C(properties);
    };

    /**
     * Encodes the specified P_RankDb_C message. Does not implicitly {@link P_RankDb_C.verify|verify} messages.
     * @function encode
     * @memberof P_RankDb_C
     * @static
     * @param {P_RankDb_C.$Properties} message P_RankDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_RankDb_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.rank_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_RankDbNode.encode(message.datas[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_RankDb_C message, length delimited. Does not implicitly {@link P_RankDb_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_RankDb_C
     * @static
     * @param {P_RankDb_C.$Properties} message P_RankDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_RankDb_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_RankDb_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_RankDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_RankDb_C & P_RankDb_C.$Shape} P_RankDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_RankDb_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_RankDb_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.rank_type = reader.int32();
                    message._rank_type = "rank_type";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_RankDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_RankDb_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_RankDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_RankDb_C & P_RankDb_C.$Shape} P_RankDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_RankDb_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_RankDb_C message.
     * @function verify
     * @memberof P_RankDb_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_RankDb_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type")) {
            properties._rank_type = 1;
            if (!$util.isInteger(message.rank_type))
                return "rank_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_RankDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_RankDb_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_RankDb_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_RankDb_C} P_RankDb_C
     */
    P_RankDb_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_RankDb_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_RankDb_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_RankDb_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.rank_type != null)
            message.rank_type = object.rank_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_RankDb_C.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_RankDb_C.datas: object expected");
                message.datas[i] = $root.PB_RankDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_RankDb_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_RankDb_C
     * @static
     * @param {P_RankDb_C} message P_RankDb_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_RankDb_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            object.rank_type = message.rank_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_RankDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_RankDb_C to JSON.
     * @function toJSON
     * @memberof P_RankDb_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_RankDb_C.prototype.toJSON = function() {
        return P_RankDb_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_RankDb_C
     * @function getTypeUrl
     * @memberof P_RankDb_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_RankDb_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_RankDb_C";
    };

    return P_RankDb_C;
})();

export const P_RankDb_S = $root.P_RankDb_S = (() => {

    /**
     * Properties of a P_RankDb_S.
     * @typedef {Object} P_RankDb_S.$Properties
     * @property {number|null} [req_type] P_RankDb_S req_type
     * @property {number|null} [save_ret] P_RankDb_S save_ret
     * @property {number|null} [rank_type] P_RankDb_S rank_type
     * @property {Array.<PB_RankDbNode.$Properties>|null} [datas] P_RankDb_S datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_RankDb_S.
     * @exports IP_RankDb_S
     * @interface IP_RankDb_S
     * @augments P_RankDb_S.$Properties
     * @deprecated Use P_RankDb_S.$Properties instead.
     */

    /**
     * Shape of a P_RankDb_S.
     * @typedef {P_RankDb_S.$Properties} P_RankDb_S.$Shape
     */

    /**
     * Constructs a new P_RankDb_S.
     * @exports P_RankDb_S
     * @classdesc Represents a P_RankDb_S.
     * @constructor
     * @param {P_RankDb_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_RankDb_S = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_RankDb_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_RankDb_S
     * @instance
     */
    P_RankDb_S.prototype.req_type = null;

    /**
     * P_RankDb_S save_ret.
     * @member {number|null|undefined} save_ret
     * @memberof P_RankDb_S
     * @instance
     */
    P_RankDb_S.prototype.save_ret = null;

    /**
     * P_RankDb_S rank_type.
     * @member {number|null|undefined} rank_type
     * @memberof P_RankDb_S
     * @instance
     */
    P_RankDb_S.prototype.rank_type = null;

    /**
     * P_RankDb_S datas.
     * @member {Array.<PB_RankDbNode.$Properties>} datas
     * @memberof P_RankDb_S
     * @instance
     */
    P_RankDb_S.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_RankDb_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_RankDb_S.prototype, "_save_ret", {
        get: $util.oneOfGetter($oneOfFields = ["save_ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_RankDb_S.prototype, "_rank_type", {
        get: $util.oneOfGetter($oneOfFields = ["rank_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_RankDb_S instance using the specified properties.
     * @function create
     * @memberof P_RankDb_S
     * @static
     * @param {P_RankDb_S.$Properties=} [properties] Properties to set
     * @returns {P_RankDb_S} P_RankDb_S instance
     * @type {{
     *   (properties: P_RankDb_S.$Shape): P_RankDb_S & P_RankDb_S.$Shape;
     *   (properties?: P_RankDb_S.$Properties): P_RankDb_S;
     * }}
     */
    P_RankDb_S.create = function(properties) {
        return new P_RankDb_S(properties);
    };

    /**
     * Encodes the specified P_RankDb_S message. Does not implicitly {@link P_RankDb_S.verify|verify} messages.
     * @function encode
     * @memberof P_RankDb_S
     * @static
     * @param {P_RankDb_S.$Properties} message P_RankDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_RankDb_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.save_ret);
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.rank_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.PB_RankDbNode.encode(message.datas[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_RankDb_S message, length delimited. Does not implicitly {@link P_RankDb_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_RankDb_S
     * @static
     * @param {P_RankDb_S.$Properties} message P_RankDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_RankDb_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_RankDb_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_RankDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_RankDb_S & P_RankDb_S.$Shape} P_RankDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_RankDb_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_RankDb_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.save_ret = reader.int32();
                    message._save_ret = "save_ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.rank_type = reader.int32();
                    message._rank_type = "rank_type";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.PB_RankDbNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_RankDb_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_RankDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_RankDb_S & P_RankDb_S.$Shape} P_RankDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_RankDb_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_RankDb_S message.
     * @function verify
     * @memberof P_RankDb_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_RankDb_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret")) {
            properties._save_ret = 1;
            if (!$util.isInteger(message.save_ret))
                return "save_ret: integer expected";
        }
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type")) {
            properties._rank_type = 1;
            if (!$util.isInteger(message.rank_type))
                return "rank_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.PB_RankDbNode.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_RankDb_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_RankDb_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_RankDb_S} P_RankDb_S
     */
    P_RankDb_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_RankDb_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_RankDb_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_RankDb_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.save_ret != null)
            message.save_ret = object.save_ret | 0;
        if (object.rank_type != null)
            message.rank_type = object.rank_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_RankDb_S.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_RankDb_S.datas: object expected");
                message.datas[i] = $root.PB_RankDbNode.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_RankDb_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_RankDb_S
     * @static
     * @param {P_RankDb_S} message P_RankDb_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_RankDb_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            object.save_ret = message.save_ret;
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            object.rank_type = message.rank_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.PB_RankDbNode.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_RankDb_S to JSON.
     * @function toJSON
     * @memberof P_RankDb_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_RankDb_S.prototype.toJSON = function() {
        return P_RankDb_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_RankDb_S
     * @function getTypeUrl
     * @memberof P_RankDb_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_RankDb_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_RankDb_S";
    };

    return P_RankDb_S;
})();

export const P_LlmChatReq = $root.P_LlmChatReq = (() => {

    /**
     * Properties of a P_LlmChatReq.
     * @typedef {Object} P_LlmChatReq.$Properties
     * @property {string|null} [messages_json] P_LlmChatReq messages_json
     * @property {string|null} [tools_json] P_LlmChatReq tools_json
     * @property {number|null} [max_tokens] P_LlmChatReq max_tokens
     * @property {number|null} [temperature] P_LlmChatReq temperature
     * @property {boolean|null} [stream] P_LlmChatReq stream
     * @property {string|null} [model] P_LlmChatReq model
     * @property {string|null} [grammar] P_LlmChatReq grammar
     * @property {string|null} [json_schema] P_LlmChatReq json_schema
     * @property {boolean|null} [enable_thinking] P_LlmChatReq enable_thinking
     * @property {boolean|null} [use_jinja] P_LlmChatReq use_jinja
     * @property {string|null} [session_key] P_LlmChatReq session_key
     * @property {string|null} [media_datas_json] P_LlmChatReq media_datas_json
     * @property {number|null} [req_id] P_LlmChatReq req_id
     * @property {boolean|null} [add_generation_prompt] P_LlmChatReq add_generation_prompt
     * @property {number|null} [response_format_type] P_LlmChatReq response_format_type
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmChatReq.
     * @exports IP_LlmChatReq
     * @interface IP_LlmChatReq
     * @augments P_LlmChatReq.$Properties
     * @deprecated Use P_LlmChatReq.$Properties instead.
     */

    /**
     * Shape of a P_LlmChatReq.
     * @typedef {P_LlmChatReq.$Properties} P_LlmChatReq.$Shape
     */

    /**
     * Constructs a new P_LlmChatReq.
     * @exports P_LlmChatReq
     * @classdesc Represents a P_LlmChatReq.
     * @constructor
     * @param {P_LlmChatReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmChatReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmChatReq messages_json.
     * @member {string|null|undefined} messages_json
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.messages_json = null;

    /**
     * P_LlmChatReq tools_json.
     * @member {string|null|undefined} tools_json
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.tools_json = null;

    /**
     * P_LlmChatReq max_tokens.
     * @member {number|null|undefined} max_tokens
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.max_tokens = null;

    /**
     * P_LlmChatReq temperature.
     * @member {number|null|undefined} temperature
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.temperature = null;

    /**
     * P_LlmChatReq stream.
     * @member {boolean|null|undefined} stream
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.stream = null;

    /**
     * P_LlmChatReq model.
     * @member {string|null|undefined} model
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.model = null;

    /**
     * P_LlmChatReq grammar.
     * @member {string|null|undefined} grammar
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.grammar = null;

    /**
     * P_LlmChatReq json_schema.
     * @member {string|null|undefined} json_schema
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.json_schema = null;

    /**
     * P_LlmChatReq enable_thinking.
     * @member {boolean|null|undefined} enable_thinking
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.enable_thinking = null;

    /**
     * P_LlmChatReq use_jinja.
     * @member {boolean|null|undefined} use_jinja
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.use_jinja = null;

    /**
     * P_LlmChatReq session_key.
     * @member {string|null|undefined} session_key
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.session_key = null;

    /**
     * P_LlmChatReq media_datas_json.
     * @member {string|null|undefined} media_datas_json
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.media_datas_json = null;

    /**
     * P_LlmChatReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.req_id = null;

    /**
     * P_LlmChatReq add_generation_prompt.
     * @member {boolean|null|undefined} add_generation_prompt
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.add_generation_prompt = null;

    /**
     * P_LlmChatReq response_format_type.
     * @member {number|null|undefined} response_format_type
     * @memberof P_LlmChatReq
     * @instance
     */
    P_LlmChatReq.prototype.response_format_type = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_messages_json", {
        get: $util.oneOfGetter($oneOfFields = ["messages_json"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_tools_json", {
        get: $util.oneOfGetter($oneOfFields = ["tools_json"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_max_tokens", {
        get: $util.oneOfGetter($oneOfFields = ["max_tokens"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_temperature", {
        get: $util.oneOfGetter($oneOfFields = ["temperature"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_stream", {
        get: $util.oneOfGetter($oneOfFields = ["stream"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_model", {
        get: $util.oneOfGetter($oneOfFields = ["model"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_grammar", {
        get: $util.oneOfGetter($oneOfFields = ["grammar"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_json_schema", {
        get: $util.oneOfGetter($oneOfFields = ["json_schema"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_enable_thinking", {
        get: $util.oneOfGetter($oneOfFields = ["enable_thinking"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_use_jinja", {
        get: $util.oneOfGetter($oneOfFields = ["use_jinja"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_session_key", {
        get: $util.oneOfGetter($oneOfFields = ["session_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_media_datas_json", {
        get: $util.oneOfGetter($oneOfFields = ["media_datas_json"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_add_generation_prompt", {
        get: $util.oneOfGetter($oneOfFields = ["add_generation_prompt"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatReq.prototype, "_response_format_type", {
        get: $util.oneOfGetter($oneOfFields = ["response_format_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmChatReq instance using the specified properties.
     * @function create
     * @memberof P_LlmChatReq
     * @static
     * @param {P_LlmChatReq.$Properties=} [properties] Properties to set
     * @returns {P_LlmChatReq} P_LlmChatReq instance
     * @type {{
     *   (properties: P_LlmChatReq.$Shape): P_LlmChatReq & P_LlmChatReq.$Shape;
     *   (properties?: P_LlmChatReq.$Properties): P_LlmChatReq;
     * }}
     */
    P_LlmChatReq.create = function(properties) {
        return new P_LlmChatReq(properties);
    };

    /**
     * Encodes the specified P_LlmChatReq message. Does not implicitly {@link P_LlmChatReq.verify|verify} messages.
     * @function encode
     * @memberof P_LlmChatReq
     * @static
     * @param {P_LlmChatReq.$Properties} message P_LlmChatReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.messages_json != null && $Object.hasOwnProperty.call(message, "messages_json"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.messages_json);
        if (message.tools_json != null && $Object.hasOwnProperty.call(message, "tools_json"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.tools_json);
        if (message.max_tokens != null && $Object.hasOwnProperty.call(message, "max_tokens"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.max_tokens);
        if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature"))
            writer.uint32(/* id 4, wireType 5 =*/37).float(message.temperature);
        if (message.stream != null && $Object.hasOwnProperty.call(message, "stream"))
            writer.uint32(/* id 5, wireType 0 =*/40).bool(message.stream);
        if (message.model != null && $Object.hasOwnProperty.call(message, "model"))
            writer.uint32(/* id 6, wireType 2 =*/50).string(message.model);
        if (message.grammar != null && $Object.hasOwnProperty.call(message, "grammar"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.grammar);
        if (message.json_schema != null && $Object.hasOwnProperty.call(message, "json_schema"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.json_schema);
        if (message.enable_thinking != null && $Object.hasOwnProperty.call(message, "enable_thinking"))
            writer.uint32(/* id 9, wireType 0 =*/72).bool(message.enable_thinking);
        if (message.use_jinja != null && $Object.hasOwnProperty.call(message, "use_jinja"))
            writer.uint32(/* id 10, wireType 0 =*/80).bool(message.use_jinja);
        if (message.session_key != null && $Object.hasOwnProperty.call(message, "session_key"))
            writer.uint32(/* id 11, wireType 2 =*/90).string(message.session_key);
        if (message.media_datas_json != null && $Object.hasOwnProperty.call(message, "media_datas_json"))
            writer.uint32(/* id 12, wireType 2 =*/98).string(message.media_datas_json);
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 13, wireType 0 =*/104).int64(message.req_id);
        if (message.add_generation_prompt != null && $Object.hasOwnProperty.call(message, "add_generation_prompt"))
            writer.uint32(/* id 14, wireType 0 =*/112).bool(message.add_generation_prompt);
        if (message.response_format_type != null && $Object.hasOwnProperty.call(message, "response_format_type"))
            writer.uint32(/* id 15, wireType 5 =*/125).float(message.response_format_type);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmChatReq message, length delimited. Does not implicitly {@link P_LlmChatReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmChatReq
     * @static
     * @param {P_LlmChatReq.$Properties} message P_LlmChatReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmChatReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmChatReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmChatReq & P_LlmChatReq.$Shape} P_LlmChatReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmChatReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.messages_json = reader.stringVerify();
                    message._messages_json = "messages_json";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.tools_json = reader.stringVerify();
                    message._tools_json = "tools_json";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.max_tokens = reader.int32();
                    message._max_tokens = "max_tokens";
                    continue;
                }
            case 4: {
                    if (wireType !== 5)
                        break;
                    message.temperature = reader.float();
                    message._temperature = "temperature";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.stream = reader.bool();
                    message._stream = "stream";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    message.model = reader.stringVerify();
                    message._model = "model";
                    continue;
                }
            case 7: {
                    if (wireType !== 2)
                        break;
                    message.grammar = reader.stringVerify();
                    message._grammar = "grammar";
                    continue;
                }
            case 8: {
                    if (wireType !== 2)
                        break;
                    message.json_schema = reader.stringVerify();
                    message._json_schema = "json_schema";
                    continue;
                }
            case 9: {
                    if (wireType !== 0)
                        break;
                    message.enable_thinking = reader.bool();
                    message._enable_thinking = "enable_thinking";
                    continue;
                }
            case 10: {
                    if (wireType !== 0)
                        break;
                    message.use_jinja = reader.bool();
                    message._use_jinja = "use_jinja";
                    continue;
                }
            case 11: {
                    if (wireType !== 2)
                        break;
                    message.session_key = reader.stringVerify();
                    message._session_key = "session_key";
                    continue;
                }
            case 12: {
                    if (wireType !== 2)
                        break;
                    message.media_datas_json = reader.stringVerify();
                    message._media_datas_json = "media_datas_json";
                    continue;
                }
            case 13: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            case 14: {
                    if (wireType !== 0)
                        break;
                    message.add_generation_prompt = reader.bool();
                    message._add_generation_prompt = "add_generation_prompt";
                    continue;
                }
            case 15: {
                    if (wireType !== 5)
                        break;
                    message.response_format_type = reader.float();
                    message._response_format_type = "response_format_type";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmChatReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmChatReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmChatReq & P_LlmChatReq.$Shape} P_LlmChatReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmChatReq message.
     * @function verify
     * @memberof P_LlmChatReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmChatReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.messages_json != null && $Object.hasOwnProperty.call(message, "messages_json")) {
            properties._messages_json = 1;
            if (!$util.isString(message.messages_json))
                return "messages_json: string expected";
        }
        if (message.tools_json != null && $Object.hasOwnProperty.call(message, "tools_json")) {
            properties._tools_json = 1;
            if (!$util.isString(message.tools_json))
                return "tools_json: string expected";
        }
        if (message.max_tokens != null && $Object.hasOwnProperty.call(message, "max_tokens")) {
            properties._max_tokens = 1;
            if (!$util.isInteger(message.max_tokens))
                return "max_tokens: integer expected";
        }
        if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature")) {
            properties._temperature = 1;
            if (typeof message.temperature !== "number")
                return "temperature: number expected";
        }
        if (message.stream != null && $Object.hasOwnProperty.call(message, "stream")) {
            properties._stream = 1;
            if (typeof message.stream !== "boolean")
                return "stream: boolean expected";
        }
        if (message.model != null && $Object.hasOwnProperty.call(message, "model")) {
            properties._model = 1;
            if (!$util.isString(message.model))
                return "model: string expected";
        }
        if (message.grammar != null && $Object.hasOwnProperty.call(message, "grammar")) {
            properties._grammar = 1;
            if (!$util.isString(message.grammar))
                return "grammar: string expected";
        }
        if (message.json_schema != null && $Object.hasOwnProperty.call(message, "json_schema")) {
            properties._json_schema = 1;
            if (!$util.isString(message.json_schema))
                return "json_schema: string expected";
        }
        if (message.enable_thinking != null && $Object.hasOwnProperty.call(message, "enable_thinking")) {
            properties._enable_thinking = 1;
            if (typeof message.enable_thinking !== "boolean")
                return "enable_thinking: boolean expected";
        }
        if (message.use_jinja != null && $Object.hasOwnProperty.call(message, "use_jinja")) {
            properties._use_jinja = 1;
            if (typeof message.use_jinja !== "boolean")
                return "use_jinja: boolean expected";
        }
        if (message.session_key != null && $Object.hasOwnProperty.call(message, "session_key")) {
            properties._session_key = 1;
            if (!$util.isString(message.session_key))
                return "session_key: string expected";
        }
        if (message.media_datas_json != null && $Object.hasOwnProperty.call(message, "media_datas_json")) {
            properties._media_datas_json = 1;
            if (!$util.isString(message.media_datas_json))
                return "media_datas_json: string expected";
        }
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        if (message.add_generation_prompt != null && $Object.hasOwnProperty.call(message, "add_generation_prompt")) {
            properties._add_generation_prompt = 1;
            if (typeof message.add_generation_prompt !== "boolean")
                return "add_generation_prompt: boolean expected";
        }
        if (message.response_format_type != null && $Object.hasOwnProperty.call(message, "response_format_type")) {
            properties._response_format_type = 1;
            if (typeof message.response_format_type !== "number")
                return "response_format_type: number expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmChatReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmChatReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmChatReq} P_LlmChatReq
     */
    P_LlmChatReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmChatReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmChatReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmChatReq();
        if (object.messages_json != null)
            message.messages_json = $String(object.messages_json);
        if (object.tools_json != null)
            message.tools_json = $String(object.tools_json);
        if (object.max_tokens != null)
            message.max_tokens = object.max_tokens | 0;
        if (object.temperature != null)
            message.temperature = $Number(object.temperature);
        if (object.stream != null)
            message.stream = $Boolean(object.stream);
        if (object.model != null)
            message.model = $String(object.model);
        if (object.grammar != null)
            message.grammar = $String(object.grammar);
        if (object.json_schema != null)
            message.json_schema = $String(object.json_schema);
        if (object.enable_thinking != null)
            message.enable_thinking = $Boolean(object.enable_thinking);
        if (object.use_jinja != null)
            message.use_jinja = $Boolean(object.use_jinja);
        if (object.session_key != null)
            message.session_key = $String(object.session_key);
        if (object.media_datas_json != null)
            message.media_datas_json = $String(object.media_datas_json);
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        if (object.add_generation_prompt != null)
            message.add_generation_prompt = $Boolean(object.add_generation_prompt);
        if (object.response_format_type != null)
            message.response_format_type = $Number(object.response_format_type);
        return message;
    };

    /**
     * Creates a plain object from a P_LlmChatReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmChatReq
     * @static
     * @param {P_LlmChatReq} message P_LlmChatReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmChatReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.messages_json != null && $Object.hasOwnProperty.call(message, "messages_json"))
            object.messages_json = message.messages_json;
        if (message.tools_json != null && $Object.hasOwnProperty.call(message, "tools_json"))
            object.tools_json = message.tools_json;
        if (message.max_tokens != null && $Object.hasOwnProperty.call(message, "max_tokens"))
            object.max_tokens = message.max_tokens;
        if (message.temperature != null && $Object.hasOwnProperty.call(message, "temperature"))
            object.temperature = options.json && !$isFinite(message.temperature) ? $String(message.temperature) : message.temperature;
        if (message.stream != null && $Object.hasOwnProperty.call(message, "stream"))
            object.stream = message.stream;
        if (message.model != null && $Object.hasOwnProperty.call(message, "model"))
            object.model = message.model;
        if (message.grammar != null && $Object.hasOwnProperty.call(message, "grammar"))
            object.grammar = message.grammar;
        if (message.json_schema != null && $Object.hasOwnProperty.call(message, "json_schema"))
            object.json_schema = message.json_schema;
        if (message.enable_thinking != null && $Object.hasOwnProperty.call(message, "enable_thinking"))
            object.enable_thinking = message.enable_thinking;
        if (message.use_jinja != null && $Object.hasOwnProperty.call(message, "use_jinja"))
            object.use_jinja = message.use_jinja;
        if (message.session_key != null && $Object.hasOwnProperty.call(message, "session_key"))
            object.session_key = message.session_key;
        if (message.media_datas_json != null && $Object.hasOwnProperty.call(message, "media_datas_json"))
            object.media_datas_json = message.media_datas_json;
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        if (message.add_generation_prompt != null && $Object.hasOwnProperty.call(message, "add_generation_prompt"))
            object.add_generation_prompt = message.add_generation_prompt;
        if (message.response_format_type != null && $Object.hasOwnProperty.call(message, "response_format_type"))
            object.response_format_type = options.json && !$isFinite(message.response_format_type) ? $String(message.response_format_type) : message.response_format_type;
        return object;
    };

    /**
     * Converts this P_LlmChatReq to JSON.
     * @function toJSON
     * @memberof P_LlmChatReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmChatReq.prototype.toJSON = function() {
        return P_LlmChatReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmChatReq
     * @function getTypeUrl
     * @memberof P_LlmChatReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmChatReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmChatReq";
    };

    return P_LlmChatReq;
})();

export const P_LlmChatResponse = $root.P_LlmChatResponse = (() => {

    /**
     * Properties of a P_LlmChatResponse.
     * @typedef {Object} P_LlmChatResponse.$Properties
     * @property {string|null} [content_delta] P_LlmChatResponse content_delta
     * @property {string|null} [reasoning_delta] P_LlmChatResponse reasoning_delta
     * @property {number|null} [index] P_LlmChatResponse index
     * @property {number|null} [req_id] P_LlmChatResponse req_id
     * @property {string|null} [tool_call_json] P_LlmChatResponse tool_call_json
     * @property {number|null} [tool_call_index] P_LlmChatResponse tool_call_index
     * @property {string|null} [tool_call_id] P_LlmChatResponse tool_call_id
     * @property {string|null} [tool_call_name] P_LlmChatResponse tool_call_name
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmChatResponse.
     * @exports IP_LlmChatResponse
     * @interface IP_LlmChatResponse
     * @augments P_LlmChatResponse.$Properties
     * @deprecated Use P_LlmChatResponse.$Properties instead.
     */

    /**
     * Shape of a P_LlmChatResponse.
     * @typedef {P_LlmChatResponse.$Properties} P_LlmChatResponse.$Shape
     */

    /**
     * Constructs a new P_LlmChatResponse.
     * @exports P_LlmChatResponse
     * @classdesc Represents a P_LlmChatResponse.
     * @constructor
     * @param {P_LlmChatResponse.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmChatResponse = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmChatResponse content_delta.
     * @member {string|null|undefined} content_delta
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.content_delta = null;

    /**
     * P_LlmChatResponse reasoning_delta.
     * @member {string|null|undefined} reasoning_delta
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.reasoning_delta = null;

    /**
     * P_LlmChatResponse index.
     * @member {number|null|undefined} index
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.index = null;

    /**
     * P_LlmChatResponse req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.req_id = null;

    /**
     * P_LlmChatResponse tool_call_json.
     * @member {string|null|undefined} tool_call_json
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.tool_call_json = null;

    /**
     * P_LlmChatResponse tool_call_index.
     * @member {number|null|undefined} tool_call_index
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.tool_call_index = null;

    /**
     * P_LlmChatResponse tool_call_id.
     * @member {string|null|undefined} tool_call_id
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.tool_call_id = null;

    /**
     * P_LlmChatResponse tool_call_name.
     * @member {string|null|undefined} tool_call_name
     * @memberof P_LlmChatResponse
     * @instance
     */
    P_LlmChatResponse.prototype.tool_call_name = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_content_delta", {
        get: $util.oneOfGetter($oneOfFields = ["content_delta"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_reasoning_delta", {
        get: $util.oneOfGetter($oneOfFields = ["reasoning_delta"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_index", {
        get: $util.oneOfGetter($oneOfFields = ["index"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_tool_call_json", {
        get: $util.oneOfGetter($oneOfFields = ["tool_call_json"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_tool_call_index", {
        get: $util.oneOfGetter($oneOfFields = ["tool_call_index"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_tool_call_id", {
        get: $util.oneOfGetter($oneOfFields = ["tool_call_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatResponse.prototype, "_tool_call_name", {
        get: $util.oneOfGetter($oneOfFields = ["tool_call_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmChatResponse instance using the specified properties.
     * @function create
     * @memberof P_LlmChatResponse
     * @static
     * @param {P_LlmChatResponse.$Properties=} [properties] Properties to set
     * @returns {P_LlmChatResponse} P_LlmChatResponse instance
     * @type {{
     *   (properties: P_LlmChatResponse.$Shape): P_LlmChatResponse & P_LlmChatResponse.$Shape;
     *   (properties?: P_LlmChatResponse.$Properties): P_LlmChatResponse;
     * }}
     */
    P_LlmChatResponse.create = function(properties) {
        return new P_LlmChatResponse(properties);
    };

    /**
     * Encodes the specified P_LlmChatResponse message. Does not implicitly {@link P_LlmChatResponse.verify|verify} messages.
     * @function encode
     * @memberof P_LlmChatResponse
     * @static
     * @param {P_LlmChatResponse.$Properties} message P_LlmChatResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatResponse.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.content_delta != null && $Object.hasOwnProperty.call(message, "content_delta"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.content_delta);
        if (message.reasoning_delta != null && $Object.hasOwnProperty.call(message, "reasoning_delta"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.reasoning_delta);
        if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.index);
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 4, wireType 0 =*/32).int64(message.req_id);
        if (message.tool_call_json != null && $Object.hasOwnProperty.call(message, "tool_call_json"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.tool_call_json);
        if (message.tool_call_index != null && $Object.hasOwnProperty.call(message, "tool_call_index"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.tool_call_index);
        if (message.tool_call_id != null && $Object.hasOwnProperty.call(message, "tool_call_id"))
            writer.uint32(/* id 7, wireType 2 =*/58).string(message.tool_call_id);
        if (message.tool_call_name != null && $Object.hasOwnProperty.call(message, "tool_call_name"))
            writer.uint32(/* id 8, wireType 2 =*/66).string(message.tool_call_name);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmChatResponse message, length delimited. Does not implicitly {@link P_LlmChatResponse.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmChatResponse
     * @static
     * @param {P_LlmChatResponse.$Properties} message P_LlmChatResponse message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatResponse.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmChatResponse message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmChatResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmChatResponse & P_LlmChatResponse.$Shape} P_LlmChatResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatResponse.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmChatResponse();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.content_delta = reader.stringVerify();
                    message._content_delta = "content_delta";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.reasoning_delta = reader.stringVerify();
                    message._reasoning_delta = "reasoning_delta";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.index = reader.int32();
                    message._index = "index";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    message.tool_call_json = reader.stringVerify();
                    message._tool_call_json = "tool_call_json";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.tool_call_index = reader.int32();
                    message._tool_call_index = "tool_call_index";
                    continue;
                }
            case 7: {
                    if (wireType !== 2)
                        break;
                    message.tool_call_id = reader.stringVerify();
                    message._tool_call_id = "tool_call_id";
                    continue;
                }
            case 8: {
                    if (wireType !== 2)
                        break;
                    message.tool_call_name = reader.stringVerify();
                    message._tool_call_name = "tool_call_name";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmChatResponse message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmChatResponse
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmChatResponse & P_LlmChatResponse.$Shape} P_LlmChatResponse
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatResponse.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmChatResponse message.
     * @function verify
     * @memberof P_LlmChatResponse
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmChatResponse.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.content_delta != null && $Object.hasOwnProperty.call(message, "content_delta")) {
            properties._content_delta = 1;
            if (!$util.isString(message.content_delta))
                return "content_delta: string expected";
        }
        if (message.reasoning_delta != null && $Object.hasOwnProperty.call(message, "reasoning_delta")) {
            properties._reasoning_delta = 1;
            if (!$util.isString(message.reasoning_delta))
                return "reasoning_delta: string expected";
        }
        if (message.index != null && $Object.hasOwnProperty.call(message, "index")) {
            properties._index = 1;
            if (!$util.isInteger(message.index))
                return "index: integer expected";
        }
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        if (message.tool_call_json != null && $Object.hasOwnProperty.call(message, "tool_call_json")) {
            properties._tool_call_json = 1;
            if (!$util.isString(message.tool_call_json))
                return "tool_call_json: string expected";
        }
        if (message.tool_call_index != null && $Object.hasOwnProperty.call(message, "tool_call_index")) {
            properties._tool_call_index = 1;
            if (!$util.isInteger(message.tool_call_index))
                return "tool_call_index: integer expected";
        }
        if (message.tool_call_id != null && $Object.hasOwnProperty.call(message, "tool_call_id")) {
            properties._tool_call_id = 1;
            if (!$util.isString(message.tool_call_id))
                return "tool_call_id: string expected";
        }
        if (message.tool_call_name != null && $Object.hasOwnProperty.call(message, "tool_call_name")) {
            properties._tool_call_name = 1;
            if (!$util.isString(message.tool_call_name))
                return "tool_call_name: string expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmChatResponse message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmChatResponse
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmChatResponse} P_LlmChatResponse
     */
    P_LlmChatResponse.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmChatResponse)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmChatResponse: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmChatResponse();
        if (object.content_delta != null)
            message.content_delta = $String(object.content_delta);
        if (object.reasoning_delta != null)
            message.reasoning_delta = $String(object.reasoning_delta);
        if (object.index != null)
            message.index = object.index | 0;
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        if (object.tool_call_json != null)
            message.tool_call_json = $String(object.tool_call_json);
        if (object.tool_call_index != null)
            message.tool_call_index = object.tool_call_index | 0;
        if (object.tool_call_id != null)
            message.tool_call_id = $String(object.tool_call_id);
        if (object.tool_call_name != null)
            message.tool_call_name = $String(object.tool_call_name);
        return message;
    };

    /**
     * Creates a plain object from a P_LlmChatResponse message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmChatResponse
     * @static
     * @param {P_LlmChatResponse} message P_LlmChatResponse
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmChatResponse.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.content_delta != null && $Object.hasOwnProperty.call(message, "content_delta"))
            object.content_delta = message.content_delta;
        if (message.reasoning_delta != null && $Object.hasOwnProperty.call(message, "reasoning_delta"))
            object.reasoning_delta = message.reasoning_delta;
        if (message.index != null && $Object.hasOwnProperty.call(message, "index"))
            object.index = message.index;
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        if (message.tool_call_json != null && $Object.hasOwnProperty.call(message, "tool_call_json"))
            object.tool_call_json = message.tool_call_json;
        if (message.tool_call_index != null && $Object.hasOwnProperty.call(message, "tool_call_index"))
            object.tool_call_index = message.tool_call_index;
        if (message.tool_call_id != null && $Object.hasOwnProperty.call(message, "tool_call_id"))
            object.tool_call_id = message.tool_call_id;
        if (message.tool_call_name != null && $Object.hasOwnProperty.call(message, "tool_call_name"))
            object.tool_call_name = message.tool_call_name;
        return object;
    };

    /**
     * Converts this P_LlmChatResponse to JSON.
     * @function toJSON
     * @memberof P_LlmChatResponse
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmChatResponse.prototype.toJSON = function() {
        return P_LlmChatResponse.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmChatResponse
     * @function getTypeUrl
     * @memberof P_LlmChatResponse
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmChatResponse.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmChatResponse";
    };

    return P_LlmChatResponse;
})();

export const P_LlmChatDone = $root.P_LlmChatDone = (() => {

    /**
     * Properties of a P_LlmChatDone.
     * @typedef {Object} P_LlmChatDone.$Properties
     * @property {number|null} [req_id] P_LlmChatDone req_id
     * @property {string|null} [finish_reason] P_LlmChatDone finish_reason
     * @property {number|null} [prompt_tokens] P_LlmChatDone prompt_tokens
     * @property {number|null} [completion_tokens] P_LlmChatDone completion_tokens
     * @property {string|null} [full_text] P_LlmChatDone full_text
     * @property {boolean|null} [has_tool_calls] P_LlmChatDone has_tool_calls
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmChatDone.
     * @exports IP_LlmChatDone
     * @interface IP_LlmChatDone
     * @augments P_LlmChatDone.$Properties
     * @deprecated Use P_LlmChatDone.$Properties instead.
     */

    /**
     * Shape of a P_LlmChatDone.
     * @typedef {P_LlmChatDone.$Properties} P_LlmChatDone.$Shape
     */

    /**
     * Constructs a new P_LlmChatDone.
     * @exports P_LlmChatDone
     * @classdesc Represents a P_LlmChatDone.
     * @constructor
     * @param {P_LlmChatDone.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmChatDone = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmChatDone req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.req_id = null;

    /**
     * P_LlmChatDone finish_reason.
     * @member {string|null|undefined} finish_reason
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.finish_reason = null;

    /**
     * P_LlmChatDone prompt_tokens.
     * @member {number|null|undefined} prompt_tokens
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.prompt_tokens = null;

    /**
     * P_LlmChatDone completion_tokens.
     * @member {number|null|undefined} completion_tokens
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.completion_tokens = null;

    /**
     * P_LlmChatDone full_text.
     * @member {string|null|undefined} full_text
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.full_text = null;

    /**
     * P_LlmChatDone has_tool_calls.
     * @member {boolean|null|undefined} has_tool_calls
     * @memberof P_LlmChatDone
     * @instance
     */
    P_LlmChatDone.prototype.has_tool_calls = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_finish_reason", {
        get: $util.oneOfGetter($oneOfFields = ["finish_reason"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_prompt_tokens", {
        get: $util.oneOfGetter($oneOfFields = ["prompt_tokens"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_completion_tokens", {
        get: $util.oneOfGetter($oneOfFields = ["completion_tokens"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_full_text", {
        get: $util.oneOfGetter($oneOfFields = ["full_text"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmChatDone.prototype, "_has_tool_calls", {
        get: $util.oneOfGetter($oneOfFields = ["has_tool_calls"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmChatDone instance using the specified properties.
     * @function create
     * @memberof P_LlmChatDone
     * @static
     * @param {P_LlmChatDone.$Properties=} [properties] Properties to set
     * @returns {P_LlmChatDone} P_LlmChatDone instance
     * @type {{
     *   (properties: P_LlmChatDone.$Shape): P_LlmChatDone & P_LlmChatDone.$Shape;
     *   (properties?: P_LlmChatDone.$Properties): P_LlmChatDone;
     * }}
     */
    P_LlmChatDone.create = function(properties) {
        return new P_LlmChatDone(properties);
    };

    /**
     * Encodes the specified P_LlmChatDone message. Does not implicitly {@link P_LlmChatDone.verify|verify} messages.
     * @function encode
     * @memberof P_LlmChatDone
     * @static
     * @param {P_LlmChatDone.$Properties} message P_LlmChatDone message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatDone.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.req_id);
        if (message.finish_reason != null && $Object.hasOwnProperty.call(message, "finish_reason"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.finish_reason);
        if (message.prompt_tokens != null && $Object.hasOwnProperty.call(message, "prompt_tokens"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.prompt_tokens);
        if (message.completion_tokens != null && $Object.hasOwnProperty.call(message, "completion_tokens"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.completion_tokens);
        if (message.full_text != null && $Object.hasOwnProperty.call(message, "full_text"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.full_text);
        if (message.has_tool_calls != null && $Object.hasOwnProperty.call(message, "has_tool_calls"))
            writer.uint32(/* id 6, wireType 0 =*/48).bool(message.has_tool_calls);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmChatDone message, length delimited. Does not implicitly {@link P_LlmChatDone.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmChatDone
     * @static
     * @param {P_LlmChatDone.$Properties} message P_LlmChatDone message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmChatDone.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmChatDone message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmChatDone
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmChatDone & P_LlmChatDone.$Shape} P_LlmChatDone
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatDone.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmChatDone();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.finish_reason = reader.stringVerify();
                    message._finish_reason = "finish_reason";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.prompt_tokens = reader.int32();
                    message._prompt_tokens = "prompt_tokens";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.completion_tokens = reader.int32();
                    message._completion_tokens = "completion_tokens";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    message.full_text = reader.stringVerify();
                    message._full_text = "full_text";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.has_tool_calls = reader.bool();
                    message._has_tool_calls = "has_tool_calls";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmChatDone message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmChatDone
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmChatDone & P_LlmChatDone.$Shape} P_LlmChatDone
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmChatDone.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmChatDone message.
     * @function verify
     * @memberof P_LlmChatDone
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmChatDone.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        if (message.finish_reason != null && $Object.hasOwnProperty.call(message, "finish_reason")) {
            properties._finish_reason = 1;
            if (!$util.isString(message.finish_reason))
                return "finish_reason: string expected";
        }
        if (message.prompt_tokens != null && $Object.hasOwnProperty.call(message, "prompt_tokens")) {
            properties._prompt_tokens = 1;
            if (!$util.isInteger(message.prompt_tokens))
                return "prompt_tokens: integer expected";
        }
        if (message.completion_tokens != null && $Object.hasOwnProperty.call(message, "completion_tokens")) {
            properties._completion_tokens = 1;
            if (!$util.isInteger(message.completion_tokens))
                return "completion_tokens: integer expected";
        }
        if (message.full_text != null && $Object.hasOwnProperty.call(message, "full_text")) {
            properties._full_text = 1;
            if (!$util.isString(message.full_text))
                return "full_text: string expected";
        }
        if (message.has_tool_calls != null && $Object.hasOwnProperty.call(message, "has_tool_calls")) {
            properties._has_tool_calls = 1;
            if (typeof message.has_tool_calls !== "boolean")
                return "has_tool_calls: boolean expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmChatDone message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmChatDone
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmChatDone} P_LlmChatDone
     */
    P_LlmChatDone.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmChatDone)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmChatDone: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmChatDone();
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        if (object.finish_reason != null)
            message.finish_reason = $String(object.finish_reason);
        if (object.prompt_tokens != null)
            message.prompt_tokens = object.prompt_tokens | 0;
        if (object.completion_tokens != null)
            message.completion_tokens = object.completion_tokens | 0;
        if (object.full_text != null)
            message.full_text = $String(object.full_text);
        if (object.has_tool_calls != null)
            message.has_tool_calls = $Boolean(object.has_tool_calls);
        return message;
    };

    /**
     * Creates a plain object from a P_LlmChatDone message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmChatDone
     * @static
     * @param {P_LlmChatDone} message P_LlmChatDone
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmChatDone.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        if (message.finish_reason != null && $Object.hasOwnProperty.call(message, "finish_reason"))
            object.finish_reason = message.finish_reason;
        if (message.prompt_tokens != null && $Object.hasOwnProperty.call(message, "prompt_tokens"))
            object.prompt_tokens = message.prompt_tokens;
        if (message.completion_tokens != null && $Object.hasOwnProperty.call(message, "completion_tokens"))
            object.completion_tokens = message.completion_tokens;
        if (message.full_text != null && $Object.hasOwnProperty.call(message, "full_text"))
            object.full_text = message.full_text;
        if (message.has_tool_calls != null && $Object.hasOwnProperty.call(message, "has_tool_calls"))
            object.has_tool_calls = message.has_tool_calls;
        return object;
    };

    /**
     * Converts this P_LlmChatDone to JSON.
     * @function toJSON
     * @memberof P_LlmChatDone
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmChatDone.prototype.toJSON = function() {
        return P_LlmChatDone.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmChatDone
     * @function getTypeUrl
     * @memberof P_LlmChatDone
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmChatDone.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmChatDone";
    };

    return P_LlmChatDone;
})();

export const P_LlmModelsReq = $root.P_LlmModelsReq = (() => {

    /**
     * Properties of a P_LlmModelsReq.
     * @typedef {Object} P_LlmModelsReq.$Properties
     * @property {number|null} [req_id] P_LlmModelsReq req_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmModelsReq.
     * @exports IP_LlmModelsReq
     * @interface IP_LlmModelsReq
     * @augments P_LlmModelsReq.$Properties
     * @deprecated Use P_LlmModelsReq.$Properties instead.
     */

    /**
     * Shape of a P_LlmModelsReq.
     * @typedef {P_LlmModelsReq.$Properties} P_LlmModelsReq.$Shape
     */

    /**
     * Constructs a new P_LlmModelsReq.
     * @exports P_LlmModelsReq
     * @classdesc Represents a P_LlmModelsReq.
     * @constructor
     * @param {P_LlmModelsReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmModelsReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmModelsReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmModelsReq
     * @instance
     */
    P_LlmModelsReq.prototype.req_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmModelsReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmModelsReq instance using the specified properties.
     * @function create
     * @memberof P_LlmModelsReq
     * @static
     * @param {P_LlmModelsReq.$Properties=} [properties] Properties to set
     * @returns {P_LlmModelsReq} P_LlmModelsReq instance
     * @type {{
     *   (properties: P_LlmModelsReq.$Shape): P_LlmModelsReq & P_LlmModelsReq.$Shape;
     *   (properties?: P_LlmModelsReq.$Properties): P_LlmModelsReq;
     * }}
     */
    P_LlmModelsReq.create = function(properties) {
        return new P_LlmModelsReq(properties);
    };

    /**
     * Encodes the specified P_LlmModelsReq message. Does not implicitly {@link P_LlmModelsReq.verify|verify} messages.
     * @function encode
     * @memberof P_LlmModelsReq
     * @static
     * @param {P_LlmModelsReq.$Properties} message P_LlmModelsReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmModelsReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.req_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmModelsReq message, length delimited. Does not implicitly {@link P_LlmModelsReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmModelsReq
     * @static
     * @param {P_LlmModelsReq.$Properties} message P_LlmModelsReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmModelsReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmModelsReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmModelsReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmModelsReq & P_LlmModelsReq.$Shape} P_LlmModelsReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmModelsReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmModelsReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmModelsReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmModelsReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmModelsReq & P_LlmModelsReq.$Shape} P_LlmModelsReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmModelsReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmModelsReq message.
     * @function verify
     * @memberof P_LlmModelsReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmModelsReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmModelsReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmModelsReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmModelsReq} P_LlmModelsReq
     */
    P_LlmModelsReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmModelsReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmModelsReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmModelsReq();
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a P_LlmModelsReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmModelsReq
     * @static
     * @param {P_LlmModelsReq} message P_LlmModelsReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmModelsReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        return object;
    };

    /**
     * Converts this P_LlmModelsReq to JSON.
     * @function toJSON
     * @memberof P_LlmModelsReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmModelsReq.prototype.toJSON = function() {
        return P_LlmModelsReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmModelsReq
     * @function getTypeUrl
     * @memberof P_LlmModelsReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmModelsReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmModelsReq";
    };

    return P_LlmModelsReq;
})();

export const P_LlmModelsRes = $root.P_LlmModelsRes = (() => {

    /**
     * Properties of a P_LlmModelsRes.
     * @typedef {Object} P_LlmModelsRes.$Properties
     * @property {number|null} [req_id] P_LlmModelsRes req_id
     * @property {string|null} [models_json] P_LlmModelsRes models_json
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmModelsRes.
     * @exports IP_LlmModelsRes
     * @interface IP_LlmModelsRes
     * @augments P_LlmModelsRes.$Properties
     * @deprecated Use P_LlmModelsRes.$Properties instead.
     */

    /**
     * Shape of a P_LlmModelsRes.
     * @typedef {P_LlmModelsRes.$Properties} P_LlmModelsRes.$Shape
     */

    /**
     * Constructs a new P_LlmModelsRes.
     * @exports P_LlmModelsRes
     * @classdesc Represents a P_LlmModelsRes.
     * @constructor
     * @param {P_LlmModelsRes.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmModelsRes = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmModelsRes req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmModelsRes
     * @instance
     */
    P_LlmModelsRes.prototype.req_id = null;

    /**
     * P_LlmModelsRes models_json.
     * @member {string|null|undefined} models_json
     * @memberof P_LlmModelsRes
     * @instance
     */
    P_LlmModelsRes.prototype.models_json = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmModelsRes.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmModelsRes.prototype, "_models_json", {
        get: $util.oneOfGetter($oneOfFields = ["models_json"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmModelsRes instance using the specified properties.
     * @function create
     * @memberof P_LlmModelsRes
     * @static
     * @param {P_LlmModelsRes.$Properties=} [properties] Properties to set
     * @returns {P_LlmModelsRes} P_LlmModelsRes instance
     * @type {{
     *   (properties: P_LlmModelsRes.$Shape): P_LlmModelsRes & P_LlmModelsRes.$Shape;
     *   (properties?: P_LlmModelsRes.$Properties): P_LlmModelsRes;
     * }}
     */
    P_LlmModelsRes.create = function(properties) {
        return new P_LlmModelsRes(properties);
    };

    /**
     * Encodes the specified P_LlmModelsRes message. Does not implicitly {@link P_LlmModelsRes.verify|verify} messages.
     * @function encode
     * @memberof P_LlmModelsRes
     * @static
     * @param {P_LlmModelsRes.$Properties} message P_LlmModelsRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmModelsRes.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.req_id);
        if (message.models_json != null && $Object.hasOwnProperty.call(message, "models_json"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.models_json);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmModelsRes message, length delimited. Does not implicitly {@link P_LlmModelsRes.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmModelsRes
     * @static
     * @param {P_LlmModelsRes.$Properties} message P_LlmModelsRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmModelsRes.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmModelsRes message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmModelsRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmModelsRes & P_LlmModelsRes.$Shape} P_LlmModelsRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmModelsRes.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmModelsRes();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.models_json = reader.stringVerify();
                    message._models_json = "models_json";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmModelsRes message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmModelsRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmModelsRes & P_LlmModelsRes.$Shape} P_LlmModelsRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmModelsRes.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmModelsRes message.
     * @function verify
     * @memberof P_LlmModelsRes
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmModelsRes.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        if (message.models_json != null && $Object.hasOwnProperty.call(message, "models_json")) {
            properties._models_json = 1;
            if (!$util.isString(message.models_json))
                return "models_json: string expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmModelsRes message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmModelsRes
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmModelsRes} P_LlmModelsRes
     */
    P_LlmModelsRes.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmModelsRes)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmModelsRes: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmModelsRes();
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        if (object.models_json != null)
            message.models_json = $String(object.models_json);
        return message;
    };

    /**
     * Creates a plain object from a P_LlmModelsRes message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmModelsRes
     * @static
     * @param {P_LlmModelsRes} message P_LlmModelsRes
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmModelsRes.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        if (message.models_json != null && $Object.hasOwnProperty.call(message, "models_json"))
            object.models_json = message.models_json;
        return object;
    };

    /**
     * Converts this P_LlmModelsRes to JSON.
     * @function toJSON
     * @memberof P_LlmModelsRes
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmModelsRes.prototype.toJSON = function() {
        return P_LlmModelsRes.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmModelsRes
     * @function getTypeUrl
     * @memberof P_LlmModelsRes
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmModelsRes.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmModelsRes";
    };

    return P_LlmModelsRes;
})();

export const P_LlmHealthReq = $root.P_LlmHealthReq = (() => {

    /**
     * Properties of a P_LlmHealthReq.
     * @typedef {Object} P_LlmHealthReq.$Properties
     * @property {number|null} [req_id] P_LlmHealthReq req_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmHealthReq.
     * @exports IP_LlmHealthReq
     * @interface IP_LlmHealthReq
     * @augments P_LlmHealthReq.$Properties
     * @deprecated Use P_LlmHealthReq.$Properties instead.
     */

    /**
     * Shape of a P_LlmHealthReq.
     * @typedef {P_LlmHealthReq.$Properties} P_LlmHealthReq.$Shape
     */

    /**
     * Constructs a new P_LlmHealthReq.
     * @exports P_LlmHealthReq
     * @classdesc Represents a P_LlmHealthReq.
     * @constructor
     * @param {P_LlmHealthReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmHealthReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmHealthReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmHealthReq
     * @instance
     */
    P_LlmHealthReq.prototype.req_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmHealthReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmHealthReq instance using the specified properties.
     * @function create
     * @memberof P_LlmHealthReq
     * @static
     * @param {P_LlmHealthReq.$Properties=} [properties] Properties to set
     * @returns {P_LlmHealthReq} P_LlmHealthReq instance
     * @type {{
     *   (properties: P_LlmHealthReq.$Shape): P_LlmHealthReq & P_LlmHealthReq.$Shape;
     *   (properties?: P_LlmHealthReq.$Properties): P_LlmHealthReq;
     * }}
     */
    P_LlmHealthReq.create = function(properties) {
        return new P_LlmHealthReq(properties);
    };

    /**
     * Encodes the specified P_LlmHealthReq message. Does not implicitly {@link P_LlmHealthReq.verify|verify} messages.
     * @function encode
     * @memberof P_LlmHealthReq
     * @static
     * @param {P_LlmHealthReq.$Properties} message P_LlmHealthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmHealthReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.req_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmHealthReq message, length delimited. Does not implicitly {@link P_LlmHealthReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmHealthReq
     * @static
     * @param {P_LlmHealthReq.$Properties} message P_LlmHealthReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmHealthReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmHealthReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmHealthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmHealthReq & P_LlmHealthReq.$Shape} P_LlmHealthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmHealthReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmHealthReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmHealthReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmHealthReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmHealthReq & P_LlmHealthReq.$Shape} P_LlmHealthReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmHealthReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmHealthReq message.
     * @function verify
     * @memberof P_LlmHealthReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmHealthReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmHealthReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmHealthReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmHealthReq} P_LlmHealthReq
     */
    P_LlmHealthReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmHealthReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmHealthReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmHealthReq();
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a P_LlmHealthReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmHealthReq
     * @static
     * @param {P_LlmHealthReq} message P_LlmHealthReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmHealthReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        return object;
    };

    /**
     * Converts this P_LlmHealthReq to JSON.
     * @function toJSON
     * @memberof P_LlmHealthReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmHealthReq.prototype.toJSON = function() {
        return P_LlmHealthReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmHealthReq
     * @function getTypeUrl
     * @memberof P_LlmHealthReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmHealthReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmHealthReq";
    };

    return P_LlmHealthReq;
})();

export const P_LlmHealthRes = $root.P_LlmHealthRes = (() => {

    /**
     * Properties of a P_LlmHealthRes.
     * @typedef {Object} P_LlmHealthRes.$Properties
     * @property {number|null} [req_id] P_LlmHealthRes req_id
     * @property {string|null} [status] P_LlmHealthRes status
     * @property {boolean|null} [running] P_LlmHealthRes running
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LlmHealthRes.
     * @exports IP_LlmHealthRes
     * @interface IP_LlmHealthRes
     * @augments P_LlmHealthRes.$Properties
     * @deprecated Use P_LlmHealthRes.$Properties instead.
     */

    /**
     * Shape of a P_LlmHealthRes.
     * @typedef {P_LlmHealthRes.$Properties} P_LlmHealthRes.$Shape
     */

    /**
     * Constructs a new P_LlmHealthRes.
     * @exports P_LlmHealthRes
     * @classdesc Represents a P_LlmHealthRes.
     * @constructor
     * @param {P_LlmHealthRes.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LlmHealthRes = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LlmHealthRes req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_LlmHealthRes
     * @instance
     */
    P_LlmHealthRes.prototype.req_id = null;

    /**
     * P_LlmHealthRes status.
     * @member {string|null|undefined} status
     * @memberof P_LlmHealthRes
     * @instance
     */
    P_LlmHealthRes.prototype.status = null;

    /**
     * P_LlmHealthRes running.
     * @member {boolean|null|undefined} running
     * @memberof P_LlmHealthRes
     * @instance
     */
    P_LlmHealthRes.prototype.running = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmHealthRes.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmHealthRes.prototype, "_status", {
        get: $util.oneOfGetter($oneOfFields = ["status"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LlmHealthRes.prototype, "_running", {
        get: $util.oneOfGetter($oneOfFields = ["running"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LlmHealthRes instance using the specified properties.
     * @function create
     * @memberof P_LlmHealthRes
     * @static
     * @param {P_LlmHealthRes.$Properties=} [properties] Properties to set
     * @returns {P_LlmHealthRes} P_LlmHealthRes instance
     * @type {{
     *   (properties: P_LlmHealthRes.$Shape): P_LlmHealthRes & P_LlmHealthRes.$Shape;
     *   (properties?: P_LlmHealthRes.$Properties): P_LlmHealthRes;
     * }}
     */
    P_LlmHealthRes.create = function(properties) {
        return new P_LlmHealthRes(properties);
    };

    /**
     * Encodes the specified P_LlmHealthRes message. Does not implicitly {@link P_LlmHealthRes.verify|verify} messages.
     * @function encode
     * @memberof P_LlmHealthRes
     * @static
     * @param {P_LlmHealthRes.$Properties} message P_LlmHealthRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmHealthRes.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.req_id);
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.status);
        if (message.running != null && $Object.hasOwnProperty.call(message, "running"))
            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.running);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LlmHealthRes message, length delimited. Does not implicitly {@link P_LlmHealthRes.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LlmHealthRes
     * @static
     * @param {P_LlmHealthRes.$Properties} message P_LlmHealthRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LlmHealthRes.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LlmHealthRes message from the specified reader or buffer.
     * @function decode
     * @memberof P_LlmHealthRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LlmHealthRes & P_LlmHealthRes.$Shape} P_LlmHealthRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmHealthRes.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LlmHealthRes();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int64();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.status = reader.stringVerify();
                    message._status = "status";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.running = reader.bool();
                    message._running = "running";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LlmHealthRes message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LlmHealthRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LlmHealthRes & P_LlmHealthRes.$Shape} P_LlmHealthRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LlmHealthRes.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LlmHealthRes message.
     * @function verify
     * @memberof P_LlmHealthRes
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LlmHealthRes.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id) && !(message.req_id && $util.isInteger(message.req_id.low) && $util.isInteger(message.req_id.high)))
                return "req_id: integer|Long expected";
        }
        if (message.status != null && $Object.hasOwnProperty.call(message, "status")) {
            properties._status = 1;
            if (!$util.isString(message.status))
                return "status: string expected";
        }
        if (message.running != null && $Object.hasOwnProperty.call(message, "running")) {
            properties._running = 1;
            if (typeof message.running !== "boolean")
                return "running: boolean expected";
        }
        return null;
    };

    /**
     * Creates a P_LlmHealthRes message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LlmHealthRes
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LlmHealthRes} P_LlmHealthRes
     */
    P_LlmHealthRes.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LlmHealthRes)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LlmHealthRes: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LlmHealthRes();
        if (object.req_id != null)
            if ($util.Long)
                message.req_id = $util.Long.fromValue(object.req_id, false);
            else if (typeof object.req_id === "string")
                message.req_id = $parseInt(object.req_id, 10);
            else if (typeof object.req_id === "number")
                message.req_id = object.req_id;
            else if (typeof object.req_id === "object")
                message.req_id = new $util.LongBits(object.req_id.low >>> 0, object.req_id.high >>> 0).toNumber();
        if (object.status != null)
            message.status = $String(object.status);
        if (object.running != null)
            message.running = $Boolean(object.running);
        return message;
    };

    /**
     * Creates a plain object from a P_LlmHealthRes message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LlmHealthRes
     * @static
     * @param {P_LlmHealthRes} message P_LlmHealthRes
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LlmHealthRes.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.req_id = typeof message.req_id === "number" ? $BigInt(message.req_id) : $util.Long.fromBits(message.req_id.low >>> 0, message.req_id.high >>> 0, false).toBigInt();
            else if (typeof message.req_id === "number")
                object.req_id = options.longs === $String ? $String(message.req_id) : message.req_id;
            else
                object.req_id = options.longs === $String ? $util.Long.prototype.toString.call(message.req_id) : options.longs === $Number ? new $util.LongBits(message.req_id.low >>> 0, message.req_id.high >>> 0).toNumber() : message.req_id;
        if (message.status != null && $Object.hasOwnProperty.call(message, "status"))
            object.status = message.status;
        if (message.running != null && $Object.hasOwnProperty.call(message, "running"))
            object.running = message.running;
        return object;
    };

    /**
     * Converts this P_LlmHealthRes to JSON.
     * @function toJSON
     * @memberof P_LlmHealthRes
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LlmHealthRes.prototype.toJSON = function() {
        return P_LlmHealthRes.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LlmHealthRes
     * @function getTypeUrl
     * @memberof P_LlmHealthRes
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LlmHealthRes.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LlmHealthRes";
    };

    return P_LlmHealthRes;
})();

/**
 * PSAVEROLEDATA_C enum.
 * @name PSAVEROLEDATA_C
 * @enum {number}
 * @property {number} PE_SAVE_TYPE_CREATE=0 PE_SAVE_TYPE_CREATE value
 * @property {number} PE_SAVE_TYPE_UPDATE=1 PE_SAVE_TYPE_UPDATE value
 * @property {number} PE_SAVE_TYPE_UPDATE_CACHE=2 PE_SAVE_TYPE_UPDATE_CACHE value
 */
export const PSAVEROLEDATA_C = $root.PSAVEROLEDATA_C = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "PE_SAVE_TYPE_CREATE"] = 0;
    values[valuesById[1] = "PE_SAVE_TYPE_UPDATE"] = 1;
    values[valuesById[2] = "PE_SAVE_TYPE_UPDATE_CACHE"] = 2;
    return values;
})();

export const P_SaveRoleData_C = $root.P_SaveRoleData_C = (() => {

    /**
     * Properties of a P_SaveRoleData_C.
     * @typedef {Object} P_SaveRoleData_C.$Properties
     * @property {number|null} [req_type] P_SaveRoleData_C req_type
     * @property {number|null} [netid] P_SaveRoleData_C netid
     * @property {PB_RoleData.$Properties|null} [role_data] P_SaveRoleData_C role_data
     * @property {number|null} [server_id] P_SaveRoleData_C server_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SaveRoleData_C.
     * @exports IP_SaveRoleData_C
     * @interface IP_SaveRoleData_C
     * @augments P_SaveRoleData_C.$Properties
     * @deprecated Use P_SaveRoleData_C.$Properties instead.
     */

    /**
     * Shape of a P_SaveRoleData_C.
     * @typedef {P_SaveRoleData_C.$Properties} P_SaveRoleData_C.$Shape
     */

    /**
     * Constructs a new P_SaveRoleData_C.
     * @exports P_SaveRoleData_C
     * @classdesc Represents a P_SaveRoleData_C.
     * @constructor
     * @param {P_SaveRoleData_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SaveRoleData_C = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SaveRoleData_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_SaveRoleData_C
     * @instance
     */
    P_SaveRoleData_C.prototype.req_type = null;

    /**
     * P_SaveRoleData_C netid.
     * @member {number|null|undefined} netid
     * @memberof P_SaveRoleData_C
     * @instance
     */
    P_SaveRoleData_C.prototype.netid = null;

    /**
     * P_SaveRoleData_C role_data.
     * @member {PB_RoleData.$Properties|null|undefined} role_data
     * @memberof P_SaveRoleData_C
     * @instance
     */
    P_SaveRoleData_C.prototype.role_data = null;

    /**
     * P_SaveRoleData_C server_id.
     * @member {number|null|undefined} server_id
     * @memberof P_SaveRoleData_C
     * @instance
     */
    P_SaveRoleData_C.prototype.server_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_C.prototype, "_netid", {
        get: $util.oneOfGetter($oneOfFields = ["netid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_C.prototype, "_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_C.prototype, "_server_id", {
        get: $util.oneOfGetter($oneOfFields = ["server_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SaveRoleData_C instance using the specified properties.
     * @function create
     * @memberof P_SaveRoleData_C
     * @static
     * @param {P_SaveRoleData_C.$Properties=} [properties] Properties to set
     * @returns {P_SaveRoleData_C} P_SaveRoleData_C instance
     * @type {{
     *   (properties: P_SaveRoleData_C.$Shape): P_SaveRoleData_C & P_SaveRoleData_C.$Shape;
     *   (properties?: P_SaveRoleData_C.$Properties): P_SaveRoleData_C;
     * }}
     */
    P_SaveRoleData_C.create = function(properties) {
        return new P_SaveRoleData_C(properties);
    };

    /**
     * Encodes the specified P_SaveRoleData_C message. Does not implicitly {@link P_SaveRoleData_C.verify|verify} messages.
     * @function encode
     * @memberof P_SaveRoleData_C
     * @static
     * @param {P_SaveRoleData_C.$Properties} message P_SaveRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveRoleData_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.netid);
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            $root.PB_RoleData.encode(message.role_data, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.server_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SaveRoleData_C message, length delimited. Does not implicitly {@link P_SaveRoleData_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SaveRoleData_C
     * @static
     * @param {P_SaveRoleData_C.$Properties} message P_SaveRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveRoleData_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SaveRoleData_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_SaveRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SaveRoleData_C & P_SaveRoleData_C.$Shape} P_SaveRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveRoleData_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SaveRoleData_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.netid = reader.int32();
                    message._netid = "netid";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.role_data = $root.PB_RoleData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_data);
                    message._role_data = "role_data";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.server_id = reader.int32();
                    message._server_id = "server_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SaveRoleData_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SaveRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SaveRoleData_C & P_SaveRoleData_C.$Shape} P_SaveRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveRoleData_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SaveRoleData_C message.
     * @function verify
     * @memberof P_SaveRoleData_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SaveRoleData_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid")) {
            properties._netid = 1;
            if (!$util.isInteger(message.netid))
                return "netid: integer expected";
        }
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data")) {
            properties._role_data = 1;
            {
                let error = $root.PB_RoleData.verify(message.role_data, _depth + 1);
                if (error)
                    return "role_data." + error;
            }
        }
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id")) {
            properties._server_id = 1;
            if (!$util.isInteger(message.server_id))
                return "server_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_SaveRoleData_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SaveRoleData_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SaveRoleData_C} P_SaveRoleData_C
     */
    P_SaveRoleData_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SaveRoleData_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SaveRoleData_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SaveRoleData_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.netid != null)
            message.netid = object.netid | 0;
        if (object.role_data != null) {
            if (!$util.isObject(object.role_data))
                throw $TypeError(".P_SaveRoleData_C.role_data: object expected");
            message.role_data = $root.PB_RoleData.fromObject(object.role_data, _depth + 1);
        }
        if (object.server_id != null)
            message.server_id = object.server_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_SaveRoleData_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SaveRoleData_C
     * @static
     * @param {P_SaveRoleData_C} message P_SaveRoleData_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SaveRoleData_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            object.netid = message.netid;
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            object.role_data = $root.PB_RoleData.toObject(message.role_data, options, _depth + 1);
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            object.server_id = message.server_id;
        return object;
    };

    /**
     * Converts this P_SaveRoleData_C to JSON.
     * @function toJSON
     * @memberof P_SaveRoleData_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SaveRoleData_C.prototype.toJSON = function() {
        return P_SaveRoleData_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SaveRoleData_C
     * @function getTypeUrl
     * @memberof P_SaveRoleData_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SaveRoleData_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SaveRoleData_C";
    };

    return P_SaveRoleData_C;
})();

export const P_SaveRoleData_S = $root.P_SaveRoleData_S = (() => {

    /**
     * Properties of a P_SaveRoleData_S.
     * @typedef {Object} P_SaveRoleData_S.$Properties
     * @property {number|null} [req_type] P_SaveRoleData_S req_type
     * @property {number|null} [netid] P_SaveRoleData_S netid
     * @property {number|null} [ret] P_SaveRoleData_S ret
     * @property {PB_RoleData.$Properties|null} [create_role_data] P_SaveRoleData_S create_role_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SaveRoleData_S.
     * @exports IP_SaveRoleData_S
     * @interface IP_SaveRoleData_S
     * @augments P_SaveRoleData_S.$Properties
     * @deprecated Use P_SaveRoleData_S.$Properties instead.
     */

    /**
     * Shape of a P_SaveRoleData_S.
     * @typedef {P_SaveRoleData_S.$Properties} P_SaveRoleData_S.$Shape
     */

    /**
     * Constructs a new P_SaveRoleData_S.
     * @exports P_SaveRoleData_S
     * @classdesc Represents a P_SaveRoleData_S.
     * @constructor
     * @param {P_SaveRoleData_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SaveRoleData_S = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SaveRoleData_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_SaveRoleData_S
     * @instance
     */
    P_SaveRoleData_S.prototype.req_type = null;

    /**
     * P_SaveRoleData_S netid.
     * @member {number|null|undefined} netid
     * @memberof P_SaveRoleData_S
     * @instance
     */
    P_SaveRoleData_S.prototype.netid = null;

    /**
     * P_SaveRoleData_S ret.
     * @member {number|null|undefined} ret
     * @memberof P_SaveRoleData_S
     * @instance
     */
    P_SaveRoleData_S.prototype.ret = null;

    /**
     * P_SaveRoleData_S create_role_data.
     * @member {PB_RoleData.$Properties|null|undefined} create_role_data
     * @memberof P_SaveRoleData_S
     * @instance
     */
    P_SaveRoleData_S.prototype.create_role_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_S.prototype, "_netid", {
        get: $util.oneOfGetter($oneOfFields = ["netid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_S.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveRoleData_S.prototype, "_create_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["create_role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SaveRoleData_S instance using the specified properties.
     * @function create
     * @memberof P_SaveRoleData_S
     * @static
     * @param {P_SaveRoleData_S.$Properties=} [properties] Properties to set
     * @returns {P_SaveRoleData_S} P_SaveRoleData_S instance
     * @type {{
     *   (properties: P_SaveRoleData_S.$Shape): P_SaveRoleData_S & P_SaveRoleData_S.$Shape;
     *   (properties?: P_SaveRoleData_S.$Properties): P_SaveRoleData_S;
     * }}
     */
    P_SaveRoleData_S.create = function(properties) {
        return new P_SaveRoleData_S(properties);
    };

    /**
     * Encodes the specified P_SaveRoleData_S message. Does not implicitly {@link P_SaveRoleData_S.verify|verify} messages.
     * @function encode
     * @memberof P_SaveRoleData_S
     * @static
     * @param {P_SaveRoleData_S.$Properties} message P_SaveRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveRoleData_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.netid);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.ret);
        if (message.create_role_data != null && $Object.hasOwnProperty.call(message, "create_role_data"))
            $root.PB_RoleData.encode(message.create_role_data, writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SaveRoleData_S message, length delimited. Does not implicitly {@link P_SaveRoleData_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SaveRoleData_S
     * @static
     * @param {P_SaveRoleData_S.$Properties} message P_SaveRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveRoleData_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SaveRoleData_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_SaveRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SaveRoleData_S & P_SaveRoleData_S.$Shape} P_SaveRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveRoleData_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SaveRoleData_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.netid = reader.int32();
                    message._netid = "netid";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    message.create_role_data = $root.PB_RoleData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.create_role_data);
                    message._create_role_data = "create_role_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SaveRoleData_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SaveRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SaveRoleData_S & P_SaveRoleData_S.$Shape} P_SaveRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveRoleData_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SaveRoleData_S message.
     * @function verify
     * @memberof P_SaveRoleData_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SaveRoleData_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid")) {
            properties._netid = 1;
            if (!$util.isInteger(message.netid))
                return "netid: integer expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.create_role_data != null && $Object.hasOwnProperty.call(message, "create_role_data")) {
            properties._create_role_data = 1;
            {
                let error = $root.PB_RoleData.verify(message.create_role_data, _depth + 1);
                if (error)
                    return "create_role_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SaveRoleData_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SaveRoleData_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SaveRoleData_S} P_SaveRoleData_S
     */
    P_SaveRoleData_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SaveRoleData_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SaveRoleData_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SaveRoleData_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.netid != null)
            message.netid = object.netid | 0;
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.create_role_data != null) {
            if (!$util.isObject(object.create_role_data))
                throw $TypeError(".P_SaveRoleData_S.create_role_data: object expected");
            message.create_role_data = $root.PB_RoleData.fromObject(object.create_role_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SaveRoleData_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SaveRoleData_S
     * @static
     * @param {P_SaveRoleData_S} message P_SaveRoleData_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SaveRoleData_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            object.netid = message.netid;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.create_role_data != null && $Object.hasOwnProperty.call(message, "create_role_data"))
            object.create_role_data = $root.PB_RoleData.toObject(message.create_role_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_SaveRoleData_S to JSON.
     * @function toJSON
     * @memberof P_SaveRoleData_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SaveRoleData_S.prototype.toJSON = function() {
        return P_SaveRoleData_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SaveRoleData_S
     * @function getTypeUrl
     * @memberof P_SaveRoleData_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SaveRoleData_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SaveRoleData_S";
    };

    return P_SaveRoleData_S;
})();

/**
 * PINITROLEDATA_C enum.
 * @name PINITROLEDATA_C
 * @enum {number}
 * @property {number} PE_INIT_TYPE_INIT=0 PE_INIT_TYPE_INIT value
 * @property {number} PE_INIT_TYPE_LOGIN=1 PE_INIT_TYPE_LOGIN value
 * @property {number} PE_INIT_TYPE_FIND=2 PE_INIT_TYPE_FIND value
 */
export const PINITROLEDATA_C = $root.PINITROLEDATA_C = (() => {
    const valuesById = $Object.create(null), values = $Object.create(valuesById);
    values[valuesById[0] = "PE_INIT_TYPE_INIT"] = 0;
    values[valuesById[1] = "PE_INIT_TYPE_LOGIN"] = 1;
    values[valuesById[2] = "PE_INIT_TYPE_FIND"] = 2;
    return values;
})();

export const P_InitRoleData_C = $root.P_InitRoleData_C = (() => {

    /**
     * Properties of a P_InitRoleData_C.
     * @typedef {Object} P_InitRoleData_C.$Properties
     * @property {number|null} [req_type] P_InitRoleData_C req_type
     * @property {number|null} [uid] P_InitRoleData_C uid
     * @property {string|null} [user_name] P_InitRoleData_C user_name
     * @property {number|null} [netid] P_InitRoleData_C netid
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_InitRoleData_C.
     * @exports IP_InitRoleData_C
     * @interface IP_InitRoleData_C
     * @augments P_InitRoleData_C.$Properties
     * @deprecated Use P_InitRoleData_C.$Properties instead.
     */

    /**
     * Shape of a P_InitRoleData_C.
     * @typedef {P_InitRoleData_C.$Properties} P_InitRoleData_C.$Shape
     */

    /**
     * Constructs a new P_InitRoleData_C.
     * @exports P_InitRoleData_C
     * @classdesc Represents a P_InitRoleData_C.
     * @constructor
     * @param {P_InitRoleData_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_InitRoleData_C = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_InitRoleData_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_InitRoleData_C
     * @instance
     */
    P_InitRoleData_C.prototype.req_type = null;

    /**
     * P_InitRoleData_C uid.
     * @member {number|null|undefined} uid
     * @memberof P_InitRoleData_C
     * @instance
     */
    P_InitRoleData_C.prototype.uid = null;

    /**
     * P_InitRoleData_C user_name.
     * @member {string|null|undefined} user_name
     * @memberof P_InitRoleData_C
     * @instance
     */
    P_InitRoleData_C.prototype.user_name = null;

    /**
     * P_InitRoleData_C netid.
     * @member {number|null|undefined} netid
     * @memberof P_InitRoleData_C
     * @instance
     */
    P_InitRoleData_C.prototype.netid = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_C.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_C.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_C.prototype, "_netid", {
        get: $util.oneOfGetter($oneOfFields = ["netid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_InitRoleData_C instance using the specified properties.
     * @function create
     * @memberof P_InitRoleData_C
     * @static
     * @param {P_InitRoleData_C.$Properties=} [properties] Properties to set
     * @returns {P_InitRoleData_C} P_InitRoleData_C instance
     * @type {{
     *   (properties: P_InitRoleData_C.$Shape): P_InitRoleData_C & P_InitRoleData_C.$Shape;
     *   (properties?: P_InitRoleData_C.$Properties): P_InitRoleData_C;
     * }}
     */
    P_InitRoleData_C.create = function(properties) {
        return new P_InitRoleData_C(properties);
    };

    /**
     * Encodes the specified P_InitRoleData_C message. Does not implicitly {@link P_InitRoleData_C.verify|verify} messages.
     * @function encode
     * @memberof P_InitRoleData_C
     * @static
     * @param {P_InitRoleData_C.$Properties} message P_InitRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitRoleData_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.uid);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.netid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_InitRoleData_C message, length delimited. Does not implicitly {@link P_InitRoleData_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_InitRoleData_C
     * @static
     * @param {P_InitRoleData_C.$Properties} message P_InitRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitRoleData_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_InitRoleData_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_InitRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_InitRoleData_C & P_InitRoleData_C.$Shape} P_InitRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitRoleData_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_InitRoleData_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.netid = reader.int32();
                    message._netid = "netid";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_InitRoleData_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_InitRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_InitRoleData_C & P_InitRoleData_C.$Shape} P_InitRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitRoleData_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_InitRoleData_C message.
     * @function verify
     * @memberof P_InitRoleData_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_InitRoleData_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid")) {
            properties._netid = 1;
            if (!$util.isInteger(message.netid))
                return "netid: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_InitRoleData_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_InitRoleData_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_InitRoleData_C} P_InitRoleData_C
     */
    P_InitRoleData_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_InitRoleData_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_InitRoleData_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_InitRoleData_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        if (object.netid != null)
            message.netid = object.netid | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_InitRoleData_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_InitRoleData_C
     * @static
     * @param {P_InitRoleData_C} message P_InitRoleData_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_InitRoleData_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            object.netid = message.netid;
        return object;
    };

    /**
     * Converts this P_InitRoleData_C to JSON.
     * @function toJSON
     * @memberof P_InitRoleData_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_InitRoleData_C.prototype.toJSON = function() {
        return P_InitRoleData_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_InitRoleData_C
     * @function getTypeUrl
     * @memberof P_InitRoleData_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_InitRoleData_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_InitRoleData_C";
    };

    return P_InitRoleData_C;
})();

export const P_InitRoleData_S = $root.P_InitRoleData_S = (() => {

    /**
     * Properties of a P_InitRoleData_S.
     * @typedef {Object} P_InitRoleData_S.$Properties
     * @property {number|null} [req_type] P_InitRoleData_S req_type
     * @property {PB_RoleData.$Properties|null} [role_data] P_InitRoleData_S role_data
     * @property {string|null} [user_name] P_InitRoleData_S user_name
     * @property {number|null} [netid] P_InitRoleData_S netid
     * @property {number|null} [ret] P_InitRoleData_S ret
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_InitRoleData_S.
     * @exports IP_InitRoleData_S
     * @interface IP_InitRoleData_S
     * @augments P_InitRoleData_S.$Properties
     * @deprecated Use P_InitRoleData_S.$Properties instead.
     */

    /**
     * Shape of a P_InitRoleData_S.
     * @typedef {P_InitRoleData_S.$Properties} P_InitRoleData_S.$Shape
     */

    /**
     * Constructs a new P_InitRoleData_S.
     * @exports P_InitRoleData_S
     * @classdesc Represents a P_InitRoleData_S.
     * @constructor
     * @param {P_InitRoleData_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_InitRoleData_S = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_InitRoleData_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_InitRoleData_S
     * @instance
     */
    P_InitRoleData_S.prototype.req_type = null;

    /**
     * P_InitRoleData_S role_data.
     * @member {PB_RoleData.$Properties|null|undefined} role_data
     * @memberof P_InitRoleData_S
     * @instance
     */
    P_InitRoleData_S.prototype.role_data = null;

    /**
     * P_InitRoleData_S user_name.
     * @member {string|null|undefined} user_name
     * @memberof P_InitRoleData_S
     * @instance
     */
    P_InitRoleData_S.prototype.user_name = null;

    /**
     * P_InitRoleData_S netid.
     * @member {number|null|undefined} netid
     * @memberof P_InitRoleData_S
     * @instance
     */
    P_InitRoleData_S.prototype.netid = null;

    /**
     * P_InitRoleData_S ret.
     * @member {number|null|undefined} ret
     * @memberof P_InitRoleData_S
     * @instance
     */
    P_InitRoleData_S.prototype.ret = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_S.prototype, "_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_S.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_S.prototype, "_netid", {
        get: $util.oneOfGetter($oneOfFields = ["netid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitRoleData_S.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_InitRoleData_S instance using the specified properties.
     * @function create
     * @memberof P_InitRoleData_S
     * @static
     * @param {P_InitRoleData_S.$Properties=} [properties] Properties to set
     * @returns {P_InitRoleData_S} P_InitRoleData_S instance
     * @type {{
     *   (properties: P_InitRoleData_S.$Shape): P_InitRoleData_S & P_InitRoleData_S.$Shape;
     *   (properties?: P_InitRoleData_S.$Properties): P_InitRoleData_S;
     * }}
     */
    P_InitRoleData_S.create = function(properties) {
        return new P_InitRoleData_S(properties);
    };

    /**
     * Encodes the specified P_InitRoleData_S message. Does not implicitly {@link P_InitRoleData_S.verify|verify} messages.
     * @function encode
     * @memberof P_InitRoleData_S
     * @static
     * @param {P_InitRoleData_S.$Properties} message P_InitRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitRoleData_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            $root.PB_RoleData.encode(message.role_data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.netid);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.ret);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_InitRoleData_S message, length delimited. Does not implicitly {@link P_InitRoleData_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_InitRoleData_S
     * @static
     * @param {P_InitRoleData_S.$Properties} message P_InitRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitRoleData_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_InitRoleData_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_InitRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_InitRoleData_S & P_InitRoleData_S.$Shape} P_InitRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitRoleData_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_InitRoleData_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.role_data = $root.PB_RoleData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_data);
                    message._role_data = "role_data";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.netid = reader.int32();
                    message._netid = "netid";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_InitRoleData_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_InitRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_InitRoleData_S & P_InitRoleData_S.$Shape} P_InitRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitRoleData_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_InitRoleData_S message.
     * @function verify
     * @memberof P_InitRoleData_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_InitRoleData_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data")) {
            properties._role_data = 1;
            {
                let error = $root.PB_RoleData.verify(message.role_data, _depth + 1);
                if (error)
                    return "role_data." + error;
            }
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid")) {
            properties._netid = 1;
            if (!$util.isInteger(message.netid))
                return "netid: integer expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_InitRoleData_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_InitRoleData_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_InitRoleData_S} P_InitRoleData_S
     */
    P_InitRoleData_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_InitRoleData_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_InitRoleData_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_InitRoleData_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.role_data != null) {
            if (!$util.isObject(object.role_data))
                throw $TypeError(".P_InitRoleData_S.role_data: object expected");
            message.role_data = $root.PB_RoleData.fromObject(object.role_data, _depth + 1);
        }
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        if (object.netid != null)
            message.netid = object.netid | 0;
        if (object.ret != null)
            message.ret = object.ret | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_InitRoleData_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_InitRoleData_S
     * @static
     * @param {P_InitRoleData_S} message P_InitRoleData_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_InitRoleData_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            object.role_data = $root.PB_RoleData.toObject(message.role_data, options, _depth + 1);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        if (message.netid != null && $Object.hasOwnProperty.call(message, "netid"))
            object.netid = message.netid;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        return object;
    };

    /**
     * Converts this P_InitRoleData_S to JSON.
     * @function toJSON
     * @memberof P_InitRoleData_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_InitRoleData_S.prototype.toJSON = function() {
        return P_InitRoleData_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_InitRoleData_S
     * @function getTypeUrl
     * @memberof P_InitRoleData_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_InitRoleData_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_InitRoleData_S";
    };

    return P_InitRoleData_S;
})();

export const P_SaveMultipleRoleData_C = $root.P_SaveMultipleRoleData_C = (() => {

    /**
     * Properties of a P_SaveMultipleRoleData_C.
     * @typedef {Object} P_SaveMultipleRoleData_C.$Properties
     * @property {number|null} [req_type] P_SaveMultipleRoleData_C req_type
     * @property {Array.<P_SaveRoleData_C.$Properties>|null} [datas] P_SaveMultipleRoleData_C datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SaveMultipleRoleData_C.
     * @exports IP_SaveMultipleRoleData_C
     * @interface IP_SaveMultipleRoleData_C
     * @augments P_SaveMultipleRoleData_C.$Properties
     * @deprecated Use P_SaveMultipleRoleData_C.$Properties instead.
     */

    /**
     * Shape of a P_SaveMultipleRoleData_C.
     * @typedef {P_SaveMultipleRoleData_C.$Properties} P_SaveMultipleRoleData_C.$Shape
     */

    /**
     * Constructs a new P_SaveMultipleRoleData_C.
     * @exports P_SaveMultipleRoleData_C
     * @classdesc Represents a P_SaveMultipleRoleData_C.
     * @constructor
     * @param {P_SaveMultipleRoleData_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SaveMultipleRoleData_C = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SaveMultipleRoleData_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_SaveMultipleRoleData_C
     * @instance
     */
    P_SaveMultipleRoleData_C.prototype.req_type = null;

    /**
     * P_SaveMultipleRoleData_C datas.
     * @member {Array.<P_SaveRoleData_C.$Properties>} datas
     * @memberof P_SaveMultipleRoleData_C
     * @instance
     */
    P_SaveMultipleRoleData_C.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveMultipleRoleData_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SaveMultipleRoleData_C instance using the specified properties.
     * @function create
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {P_SaveMultipleRoleData_C.$Properties=} [properties] Properties to set
     * @returns {P_SaveMultipleRoleData_C} P_SaveMultipleRoleData_C instance
     * @type {{
     *   (properties: P_SaveMultipleRoleData_C.$Shape): P_SaveMultipleRoleData_C & P_SaveMultipleRoleData_C.$Shape;
     *   (properties?: P_SaveMultipleRoleData_C.$Properties): P_SaveMultipleRoleData_C;
     * }}
     */
    P_SaveMultipleRoleData_C.create = function(properties) {
        return new P_SaveMultipleRoleData_C(properties);
    };

    /**
     * Encodes the specified P_SaveMultipleRoleData_C message. Does not implicitly {@link P_SaveMultipleRoleData_C.verify|verify} messages.
     * @function encode
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {P_SaveMultipleRoleData_C.$Properties} message P_SaveMultipleRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveMultipleRoleData_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.P_SaveRoleData_C.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SaveMultipleRoleData_C message, length delimited. Does not implicitly {@link P_SaveMultipleRoleData_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {P_SaveMultipleRoleData_C.$Properties} message P_SaveMultipleRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveMultipleRoleData_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SaveMultipleRoleData_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SaveMultipleRoleData_C & P_SaveMultipleRoleData_C.$Shape} P_SaveMultipleRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveMultipleRoleData_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SaveMultipleRoleData_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.P_SaveRoleData_C.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SaveMultipleRoleData_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SaveMultipleRoleData_C & P_SaveMultipleRoleData_C.$Shape} P_SaveMultipleRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveMultipleRoleData_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SaveMultipleRoleData_C message.
     * @function verify
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SaveMultipleRoleData_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.P_SaveRoleData_C.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SaveMultipleRoleData_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SaveMultipleRoleData_C} P_SaveMultipleRoleData_C
     */
    P_SaveMultipleRoleData_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SaveMultipleRoleData_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SaveMultipleRoleData_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SaveMultipleRoleData_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_SaveMultipleRoleData_C.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_SaveMultipleRoleData_C.datas: object expected");
                message.datas[i] = $root.P_SaveRoleData_C.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SaveMultipleRoleData_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {P_SaveMultipleRoleData_C} message P_SaveMultipleRoleData_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SaveMultipleRoleData_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.P_SaveRoleData_C.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_SaveMultipleRoleData_C to JSON.
     * @function toJSON
     * @memberof P_SaveMultipleRoleData_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SaveMultipleRoleData_C.prototype.toJSON = function() {
        return P_SaveMultipleRoleData_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SaveMultipleRoleData_C
     * @function getTypeUrl
     * @memberof P_SaveMultipleRoleData_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SaveMultipleRoleData_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SaveMultipleRoleData_C";
    };

    return P_SaveMultipleRoleData_C;
})();

export const P_SaveMultipleRoleData_S = $root.P_SaveMultipleRoleData_S = (() => {

    /**
     * Properties of a P_SaveMultipleRoleData_S.
     * @typedef {Object} P_SaveMultipleRoleData_S.$Properties
     * @property {number|null} [req_type] P_SaveMultipleRoleData_S req_type
     * @property {Array.<P_SaveRoleData_S.$Properties>|null} [datas] P_SaveMultipleRoleData_S datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SaveMultipleRoleData_S.
     * @exports IP_SaveMultipleRoleData_S
     * @interface IP_SaveMultipleRoleData_S
     * @augments P_SaveMultipleRoleData_S.$Properties
     * @deprecated Use P_SaveMultipleRoleData_S.$Properties instead.
     */

    /**
     * Shape of a P_SaveMultipleRoleData_S.
     * @typedef {P_SaveMultipleRoleData_S.$Properties} P_SaveMultipleRoleData_S.$Shape
     */

    /**
     * Constructs a new P_SaveMultipleRoleData_S.
     * @exports P_SaveMultipleRoleData_S
     * @classdesc Represents a P_SaveMultipleRoleData_S.
     * @constructor
     * @param {P_SaveMultipleRoleData_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SaveMultipleRoleData_S = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SaveMultipleRoleData_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_SaveMultipleRoleData_S
     * @instance
     */
    P_SaveMultipleRoleData_S.prototype.req_type = null;

    /**
     * P_SaveMultipleRoleData_S datas.
     * @member {Array.<P_SaveRoleData_S.$Properties>} datas
     * @memberof P_SaveMultipleRoleData_S
     * @instance
     */
    P_SaveMultipleRoleData_S.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SaveMultipleRoleData_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SaveMultipleRoleData_S instance using the specified properties.
     * @function create
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {P_SaveMultipleRoleData_S.$Properties=} [properties] Properties to set
     * @returns {P_SaveMultipleRoleData_S} P_SaveMultipleRoleData_S instance
     * @type {{
     *   (properties: P_SaveMultipleRoleData_S.$Shape): P_SaveMultipleRoleData_S & P_SaveMultipleRoleData_S.$Shape;
     *   (properties?: P_SaveMultipleRoleData_S.$Properties): P_SaveMultipleRoleData_S;
     * }}
     */
    P_SaveMultipleRoleData_S.create = function(properties) {
        return new P_SaveMultipleRoleData_S(properties);
    };

    /**
     * Encodes the specified P_SaveMultipleRoleData_S message. Does not implicitly {@link P_SaveMultipleRoleData_S.verify|verify} messages.
     * @function encode
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {P_SaveMultipleRoleData_S.$Properties} message P_SaveMultipleRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveMultipleRoleData_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.P_SaveRoleData_S.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SaveMultipleRoleData_S message, length delimited. Does not implicitly {@link P_SaveMultipleRoleData_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {P_SaveMultipleRoleData_S.$Properties} message P_SaveMultipleRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SaveMultipleRoleData_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SaveMultipleRoleData_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SaveMultipleRoleData_S & P_SaveMultipleRoleData_S.$Shape} P_SaveMultipleRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveMultipleRoleData_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SaveMultipleRoleData_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.P_SaveRoleData_S.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SaveMultipleRoleData_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SaveMultipleRoleData_S & P_SaveMultipleRoleData_S.$Shape} P_SaveMultipleRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SaveMultipleRoleData_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SaveMultipleRoleData_S message.
     * @function verify
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SaveMultipleRoleData_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.P_SaveRoleData_S.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SaveMultipleRoleData_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SaveMultipleRoleData_S} P_SaveMultipleRoleData_S
     */
    P_SaveMultipleRoleData_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SaveMultipleRoleData_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SaveMultipleRoleData_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SaveMultipleRoleData_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_SaveMultipleRoleData_S.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_SaveMultipleRoleData_S.datas: object expected");
                message.datas[i] = $root.P_SaveRoleData_S.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SaveMultipleRoleData_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {P_SaveMultipleRoleData_S} message P_SaveMultipleRoleData_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SaveMultipleRoleData_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.P_SaveRoleData_S.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_SaveMultipleRoleData_S to JSON.
     * @function toJSON
     * @memberof P_SaveMultipleRoleData_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SaveMultipleRoleData_S.prototype.toJSON = function() {
        return P_SaveMultipleRoleData_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SaveMultipleRoleData_S
     * @function getTypeUrl
     * @memberof P_SaveMultipleRoleData_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SaveMultipleRoleData_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SaveMultipleRoleData_S";
    };

    return P_SaveMultipleRoleData_S;
})();

export const P_InitMultipleRoleData_C = $root.P_InitMultipleRoleData_C = (() => {

    /**
     * Properties of a P_InitMultipleRoleData_C.
     * @typedef {Object} P_InitMultipleRoleData_C.$Properties
     * @property {number|null} [req_type] P_InitMultipleRoleData_C req_type
     * @property {Array.<P_InitRoleData_C.$Properties>|null} [datas] P_InitMultipleRoleData_C datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_InitMultipleRoleData_C.
     * @exports IP_InitMultipleRoleData_C
     * @interface IP_InitMultipleRoleData_C
     * @augments P_InitMultipleRoleData_C.$Properties
     * @deprecated Use P_InitMultipleRoleData_C.$Properties instead.
     */

    /**
     * Shape of a P_InitMultipleRoleData_C.
     * @typedef {P_InitMultipleRoleData_C.$Properties} P_InitMultipleRoleData_C.$Shape
     */

    /**
     * Constructs a new P_InitMultipleRoleData_C.
     * @exports P_InitMultipleRoleData_C
     * @classdesc Represents a P_InitMultipleRoleData_C.
     * @constructor
     * @param {P_InitMultipleRoleData_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_InitMultipleRoleData_C = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_InitMultipleRoleData_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_InitMultipleRoleData_C
     * @instance
     */
    P_InitMultipleRoleData_C.prototype.req_type = null;

    /**
     * P_InitMultipleRoleData_C datas.
     * @member {Array.<P_InitRoleData_C.$Properties>} datas
     * @memberof P_InitMultipleRoleData_C
     * @instance
     */
    P_InitMultipleRoleData_C.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitMultipleRoleData_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_InitMultipleRoleData_C instance using the specified properties.
     * @function create
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {P_InitMultipleRoleData_C.$Properties=} [properties] Properties to set
     * @returns {P_InitMultipleRoleData_C} P_InitMultipleRoleData_C instance
     * @type {{
     *   (properties: P_InitMultipleRoleData_C.$Shape): P_InitMultipleRoleData_C & P_InitMultipleRoleData_C.$Shape;
     *   (properties?: P_InitMultipleRoleData_C.$Properties): P_InitMultipleRoleData_C;
     * }}
     */
    P_InitMultipleRoleData_C.create = function(properties) {
        return new P_InitMultipleRoleData_C(properties);
    };

    /**
     * Encodes the specified P_InitMultipleRoleData_C message. Does not implicitly {@link P_InitMultipleRoleData_C.verify|verify} messages.
     * @function encode
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {P_InitMultipleRoleData_C.$Properties} message P_InitMultipleRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitMultipleRoleData_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.P_InitRoleData_C.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_InitMultipleRoleData_C message, length delimited. Does not implicitly {@link P_InitMultipleRoleData_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {P_InitMultipleRoleData_C.$Properties} message P_InitMultipleRoleData_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitMultipleRoleData_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_InitMultipleRoleData_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_InitMultipleRoleData_C & P_InitMultipleRoleData_C.$Shape} P_InitMultipleRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitMultipleRoleData_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_InitMultipleRoleData_C();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.P_InitRoleData_C.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_InitMultipleRoleData_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_InitMultipleRoleData_C & P_InitMultipleRoleData_C.$Shape} P_InitMultipleRoleData_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitMultipleRoleData_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_InitMultipleRoleData_C message.
     * @function verify
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_InitMultipleRoleData_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.P_InitRoleData_C.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_InitMultipleRoleData_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_InitMultipleRoleData_C} P_InitMultipleRoleData_C
     */
    P_InitMultipleRoleData_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_InitMultipleRoleData_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_InitMultipleRoleData_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_InitMultipleRoleData_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_InitMultipleRoleData_C.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_InitMultipleRoleData_C.datas: object expected");
                message.datas[i] = $root.P_InitRoleData_C.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_InitMultipleRoleData_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {P_InitMultipleRoleData_C} message P_InitMultipleRoleData_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_InitMultipleRoleData_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.P_InitRoleData_C.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_InitMultipleRoleData_C to JSON.
     * @function toJSON
     * @memberof P_InitMultipleRoleData_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_InitMultipleRoleData_C.prototype.toJSON = function() {
        return P_InitMultipleRoleData_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_InitMultipleRoleData_C
     * @function getTypeUrl
     * @memberof P_InitMultipleRoleData_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_InitMultipleRoleData_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_InitMultipleRoleData_C";
    };

    return P_InitMultipleRoleData_C;
})();

export const P_InitMultipleRoleData_S = $root.P_InitMultipleRoleData_S = (() => {

    /**
     * Properties of a P_InitMultipleRoleData_S.
     * @typedef {Object} P_InitMultipleRoleData_S.$Properties
     * @property {number|null} [req_type] P_InitMultipleRoleData_S req_type
     * @property {Array.<P_InitRoleData_S.$Properties>|null} [datas] P_InitMultipleRoleData_S datas
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_InitMultipleRoleData_S.
     * @exports IP_InitMultipleRoleData_S
     * @interface IP_InitMultipleRoleData_S
     * @augments P_InitMultipleRoleData_S.$Properties
     * @deprecated Use P_InitMultipleRoleData_S.$Properties instead.
     */

    /**
     * Shape of a P_InitMultipleRoleData_S.
     * @typedef {P_InitMultipleRoleData_S.$Properties} P_InitMultipleRoleData_S.$Shape
     */

    /**
     * Constructs a new P_InitMultipleRoleData_S.
     * @exports P_InitMultipleRoleData_S
     * @classdesc Represents a P_InitMultipleRoleData_S.
     * @constructor
     * @param {P_InitMultipleRoleData_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_InitMultipleRoleData_S = function (properties) {
        this.datas = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_InitMultipleRoleData_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_InitMultipleRoleData_S
     * @instance
     */
    P_InitMultipleRoleData_S.prototype.req_type = null;

    /**
     * P_InitMultipleRoleData_S datas.
     * @member {Array.<P_InitRoleData_S.$Properties>} datas
     * @memberof P_InitMultipleRoleData_S
     * @instance
     */
    P_InitMultipleRoleData_S.prototype.datas = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_InitMultipleRoleData_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_InitMultipleRoleData_S instance using the specified properties.
     * @function create
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {P_InitMultipleRoleData_S.$Properties=} [properties] Properties to set
     * @returns {P_InitMultipleRoleData_S} P_InitMultipleRoleData_S instance
     * @type {{
     *   (properties: P_InitMultipleRoleData_S.$Shape): P_InitMultipleRoleData_S & P_InitMultipleRoleData_S.$Shape;
     *   (properties?: P_InitMultipleRoleData_S.$Properties): P_InitMultipleRoleData_S;
     * }}
     */
    P_InitMultipleRoleData_S.create = function(properties) {
        return new P_InitMultipleRoleData_S(properties);
    };

    /**
     * Encodes the specified P_InitMultipleRoleData_S message. Does not implicitly {@link P_InitMultipleRoleData_S.verify|verify} messages.
     * @function encode
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {P_InitMultipleRoleData_S.$Properties} message P_InitMultipleRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitMultipleRoleData_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.datas != null && message.datas.length)
            for (let i = 0; i < message.datas.length; ++i)
                $root.P_InitRoleData_S.encode(message.datas[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_InitMultipleRoleData_S message, length delimited. Does not implicitly {@link P_InitMultipleRoleData_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {P_InitMultipleRoleData_S.$Properties} message P_InitMultipleRoleData_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_InitMultipleRoleData_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_InitMultipleRoleData_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_InitMultipleRoleData_S & P_InitMultipleRoleData_S.$Shape} P_InitMultipleRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitMultipleRoleData_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_InitMultipleRoleData_S();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.datas && message.datas.length))
                        message.datas = [];
                    message.datas.push($root.P_InitRoleData_S.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_InitMultipleRoleData_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_InitMultipleRoleData_S & P_InitMultipleRoleData_S.$Shape} P_InitMultipleRoleData_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_InitMultipleRoleData_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_InitMultipleRoleData_S message.
     * @function verify
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_InitMultipleRoleData_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.datas != null && $Object.hasOwnProperty.call(message, "datas")) {
            if (!$Array.isArray(message.datas))
                return "datas: array expected";
            for (let i = 0; i < message.datas.length; ++i) {
                let error = $root.P_InitRoleData_S.verify(message.datas[i], _depth + 1);
                if (error)
                    return "datas." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_InitMultipleRoleData_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_InitMultipleRoleData_S} P_InitMultipleRoleData_S
     */
    P_InitMultipleRoleData_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_InitMultipleRoleData_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_InitMultipleRoleData_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_InitMultipleRoleData_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.datas) {
            if (!$Array.isArray(object.datas))
                throw $TypeError(".P_InitMultipleRoleData_S.datas: array expected");
            message.datas = $Array(object.datas.length);
            for (let i = 0; i < object.datas.length; ++i) {
                if (!$util.isObject(object.datas[i]))
                    throw $TypeError(".P_InitMultipleRoleData_S.datas: object expected");
                message.datas[i] = $root.P_InitRoleData_S.fromObject(object.datas[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_InitMultipleRoleData_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {P_InitMultipleRoleData_S} message P_InitMultipleRoleData_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_InitMultipleRoleData_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.datas = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.datas && message.datas.length) {
            object.datas = $Array(message.datas.length);
            for (let j = 0; j < message.datas.length; ++j)
                object.datas[j] = $root.P_InitRoleData_S.toObject(message.datas[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_InitMultipleRoleData_S to JSON.
     * @function toJSON
     * @memberof P_InitMultipleRoleData_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_InitMultipleRoleData_S.prototype.toJSON = function() {
        return P_InitMultipleRoleData_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_InitMultipleRoleData_S
     * @function getTypeUrl
     * @memberof P_InitMultipleRoleData_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_InitMultipleRoleData_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_InitMultipleRoleData_S";
    };

    return P_InitMultipleRoleData_S;
})();

export const PB_InnerChatMap = $root.PB_InnerChatMap = (() => {

    /**
     * Properties of a PB_InnerChatMap.
     * @typedef {Object} PB_InnerChatMap.$Properties
     * @property {Object.<string,PB_ChatData.$Properties>|null} [chats] PB_InnerChatMap chats
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_InnerChatMap.
     * @exports IPB_InnerChatMap
     * @interface IPB_InnerChatMap
     * @augments PB_InnerChatMap.$Properties
     * @deprecated Use PB_InnerChatMap.$Properties instead.
     */

    /**
     * Shape of a PB_InnerChatMap.
     * @typedef {PB_InnerChatMap.$Properties} PB_InnerChatMap.$Shape
     */

    /**
     * Constructs a new PB_InnerChatMap.
     * @exports PB_InnerChatMap
     * @classdesc Represents a PB_InnerChatMap.
     * @constructor
     * @param {PB_InnerChatMap.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_InnerChatMap = function (properties) {
        this.chats = {};
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_InnerChatMap chats.
     * @member {Object.<string,PB_ChatData.$Properties>} chats
     * @memberof PB_InnerChatMap
     * @instance
     */
    PB_InnerChatMap.prototype.chats = $util.emptyObject;

    /**
     * Creates a new PB_InnerChatMap instance using the specified properties.
     * @function create
     * @memberof PB_InnerChatMap
     * @static
     * @param {PB_InnerChatMap.$Properties=} [properties] Properties to set
     * @returns {PB_InnerChatMap} PB_InnerChatMap instance
     * @type {{
     *   (properties: PB_InnerChatMap.$Shape): PB_InnerChatMap & PB_InnerChatMap.$Shape;
     *   (properties?: PB_InnerChatMap.$Properties): PB_InnerChatMap;
     * }}
     */
    PB_InnerChatMap.create = function(properties) {
        return new PB_InnerChatMap(properties);
    };

    /**
     * Encodes the specified PB_InnerChatMap message. Does not implicitly {@link PB_InnerChatMap.verify|verify} messages.
     * @function encode
     * @memberof PB_InnerChatMap
     * @static
     * @param {PB_InnerChatMap.$Properties} message PB_InnerChatMap message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InnerChatMap.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.chats != null && $Object.hasOwnProperty.call(message, "chats"))
            for (let keys = $Object.keys(message.chats), i = 0; i < keys.length; ++i) {
                writer.uint32(/* id 1, wireType 2 =*/10).fork().uint32(/* id 1, wireType 0 =*/8).int64($util.longFromKey(keys[i], false));
                $root.PB_ChatData.encode(message.chats[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
            }
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_InnerChatMap message, length delimited. Does not implicitly {@link PB_InnerChatMap.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_InnerChatMap
     * @static
     * @param {PB_InnerChatMap.$Properties} message PB_InnerChatMap message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_InnerChatMap.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_InnerChatMap message from the specified reader or buffer.
     * @function decode
     * @memberof PB_InnerChatMap
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_InnerChatMap & PB_InnerChatMap.$Shape} PB_InnerChatMap
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InnerChatMap.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_InnerChatMap(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    if (message.chats === $util.emptyObject)
                        message.chats = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = 0;
                    value = null;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 0)
                                break;
                            key = reader.int64();
                            continue;
                        case 2:
                            if (wireType !== 2)
                                break;
                            value = $root.PB_ChatData.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    message.chats[typeof key === "object" ? $util.longToHash(key) : key] = value || new $root.PB_ChatData();
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_InnerChatMap message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_InnerChatMap
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_InnerChatMap & PB_InnerChatMap.$Shape} PB_InnerChatMap
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_InnerChatMap.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_InnerChatMap message.
     * @function verify
     * @memberof PB_InnerChatMap
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_InnerChatMap.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.chats != null && $Object.hasOwnProperty.call(message, "chats")) {
            if (!$util.isObject(message.chats))
                return "chats: object expected";
            let key = $Object.keys(message.chats);
            for (let i = 0; i < key.length; ++i) {
                if (!$util.key64Re.test(key[i]))
                    return "chats: integer|Long key{k:int64} expected";
                {
                    let error = $root.PB_ChatData.verify(message.chats[key[i]], _depth + 1);
                    if (error)
                        return "chats." + error;
                }
            }
        }
        return null;
    };

    /**
     * Creates a PB_InnerChatMap message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_InnerChatMap
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_InnerChatMap} PB_InnerChatMap
     */
    PB_InnerChatMap.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_InnerChatMap)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_InnerChatMap: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_InnerChatMap();
        if (object.chats) {
            if (!$util.isObject(object.chats))
                throw $TypeError(".PB_InnerChatMap.chats: object expected");
            message.chats = {};
            for (let keys = $Object.keys(object.chats), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.chats, keys[i]);
                if (!$util.isObject(object.chats[keys[i]]))
                    throw $TypeError(".PB_InnerChatMap.chats: object expected");
                message.chats[keys[i]] = $root.PB_ChatData.fromObject(object.chats[keys[i]], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_InnerChatMap message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_InnerChatMap
     * @static
     * @param {PB_InnerChatMap} message PB_InnerChatMap
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_InnerChatMap.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.objects || options.defaults)
            object.chats = {};
        let keys2;
        if (message.chats && (keys2 = $Object.keys(message.chats)).length) {
            object.chats = {};
            for (let j = 0; j < keys2.length; ++j) {
                let k2 = $util.longFromKey(keys2[j], false).toString();
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.chats, keys2[j]);
                object.chats[k2] = $root.PB_ChatData.toObject(message.chats[keys2[j]], options, _depth + 1);
            }
        }
        return object;
    };

    /**
     * Converts this PB_InnerChatMap to JSON.
     * @function toJSON
     * @memberof PB_InnerChatMap
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_InnerChatMap.prototype.toJSON = function() {
        return PB_InnerChatMap.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_InnerChatMap
     * @function getTypeUrl
     * @memberof PB_InnerChatMap
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_InnerChatMap.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_InnerChatMap";
    };

    return PB_InnerChatMap;
})();

export const P_ChatDb_C = $root.P_ChatDb_C = (() => {

    /**
     * Properties of a P_ChatDb_C.
     * @typedef {Object} P_ChatDb_C.$Properties
     * @property {number|null} [req_type] P_ChatDb_C req_type
     * @property {Object.<string,PB_InnerChatMap.$Properties>|null} [types] P_ChatDb_C types
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ChatDb_C.
     * @exports IP_ChatDb_C
     * @interface IP_ChatDb_C
     * @augments P_ChatDb_C.$Properties
     * @deprecated Use P_ChatDb_C.$Properties instead.
     */

    /**
     * Shape of a P_ChatDb_C.
     * @typedef {P_ChatDb_C.$Properties} P_ChatDb_C.$Shape
     */

    /**
     * Constructs a new P_ChatDb_C.
     * @exports P_ChatDb_C
     * @classdesc Represents a P_ChatDb_C.
     * @constructor
     * @param {P_ChatDb_C.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ChatDb_C = function (properties) {
        this.types = {};
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ChatDb_C req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ChatDb_C
     * @instance
     */
    P_ChatDb_C.prototype.req_type = null;

    /**
     * P_ChatDb_C types.
     * @member {Object.<string,PB_InnerChatMap.$Properties>} types
     * @memberof P_ChatDb_C
     * @instance
     */
    P_ChatDb_C.prototype.types = $util.emptyObject;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ChatDb_C.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ChatDb_C instance using the specified properties.
     * @function create
     * @memberof P_ChatDb_C
     * @static
     * @param {P_ChatDb_C.$Properties=} [properties] Properties to set
     * @returns {P_ChatDb_C} P_ChatDb_C instance
     * @type {{
     *   (properties: P_ChatDb_C.$Shape): P_ChatDb_C & P_ChatDb_C.$Shape;
     *   (properties?: P_ChatDb_C.$Properties): P_ChatDb_C;
     * }}
     */
    P_ChatDb_C.create = function(properties) {
        return new P_ChatDb_C(properties);
    };

    /**
     * Encodes the specified P_ChatDb_C message. Does not implicitly {@link P_ChatDb_C.verify|verify} messages.
     * @function encode
     * @memberof P_ChatDb_C
     * @static
     * @param {P_ChatDb_C.$Properties} message P_ChatDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ChatDb_C.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.types != null && $Object.hasOwnProperty.call(message, "types"))
            for (let keys = $Object.keys(message.types), i = 0; i < keys.length; ++i) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                $root.PB_InnerChatMap.encode(message.types[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
            }
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ChatDb_C message, length delimited. Does not implicitly {@link P_ChatDb_C.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ChatDb_C
     * @static
     * @param {P_ChatDb_C.$Properties} message P_ChatDb_C message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ChatDb_C.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ChatDb_C message from the specified reader or buffer.
     * @function decode
     * @memberof P_ChatDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ChatDb_C & P_ChatDb_C.$Shape} P_ChatDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ChatDb_C.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ChatDb_C(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (message.types === $util.emptyObject)
                        message.types = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = 0;
                    value = null;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 0)
                                break;
                            key = reader.int32();
                            continue;
                        case 2:
                            if (wireType !== 2)
                                break;
                            value = $root.PB_InnerChatMap.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    message.types[key] = value || new $root.PB_InnerChatMap();
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ChatDb_C message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ChatDb_C
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ChatDb_C & P_ChatDb_C.$Shape} P_ChatDb_C
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ChatDb_C.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ChatDb_C message.
     * @function verify
     * @memberof P_ChatDb_C
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ChatDb_C.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.types != null && $Object.hasOwnProperty.call(message, "types")) {
            if (!$util.isObject(message.types))
                return "types: object expected";
            let key = $Object.keys(message.types);
            for (let i = 0; i < key.length; ++i) {
                if (!$util.key32Re.test(key[i]))
                    return "types: integer key{k:int32} expected";
                {
                    let error = $root.PB_InnerChatMap.verify(message.types[key[i]], _depth + 1);
                    if (error)
                        return "types." + error;
                }
            }
        }
        return null;
    };

    /**
     * Creates a P_ChatDb_C message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ChatDb_C
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ChatDb_C} P_ChatDb_C
     */
    P_ChatDb_C.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ChatDb_C)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ChatDb_C: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ChatDb_C();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.types) {
            if (!$util.isObject(object.types))
                throw $TypeError(".P_ChatDb_C.types: object expected");
            message.types = {};
            for (let keys = $Object.keys(object.types), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.types, keys[i]);
                if (!$util.isObject(object.types[keys[i]]))
                    throw $TypeError(".P_ChatDb_C.types: object expected");
                message.types[keys[i]] = $root.PB_InnerChatMap.fromObject(object.types[keys[i]], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ChatDb_C message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ChatDb_C
     * @static
     * @param {P_ChatDb_C} message P_ChatDb_C
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ChatDb_C.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.objects || options.defaults)
            object.types = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        let keys2;
        if (message.types && (keys2 = $Object.keys(message.types)).length) {
            object.types = {};
            for (let j = 0; j < keys2.length; ++j) {
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.types, keys2[j]);
                object.types[keys2[j]] = $root.PB_InnerChatMap.toObject(message.types[keys2[j]], options, _depth + 1);
            }
        }
        return object;
    };

    /**
     * Converts this P_ChatDb_C to JSON.
     * @function toJSON
     * @memberof P_ChatDb_C
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ChatDb_C.prototype.toJSON = function() {
        return P_ChatDb_C.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ChatDb_C
     * @function getTypeUrl
     * @memberof P_ChatDb_C
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ChatDb_C.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ChatDb_C";
    };

    return P_ChatDb_C;
})();

export const P_ChatDb_S = $root.P_ChatDb_S = (() => {

    /**
     * Properties of a P_ChatDb_S.
     * @typedef {Object} P_ChatDb_S.$Properties
     * @property {number|null} [req_type] P_ChatDb_S req_type
     * @property {Object.<string,PB_InnerChatMap.$Properties>|null} [types] P_ChatDb_S types
     * @property {number|null} [save_ret] P_ChatDb_S save_ret
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ChatDb_S.
     * @exports IP_ChatDb_S
     * @interface IP_ChatDb_S
     * @augments P_ChatDb_S.$Properties
     * @deprecated Use P_ChatDb_S.$Properties instead.
     */

    /**
     * Shape of a P_ChatDb_S.
     * @typedef {P_ChatDb_S.$Properties} P_ChatDb_S.$Shape
     */

    /**
     * Constructs a new P_ChatDb_S.
     * @exports P_ChatDb_S
     * @classdesc Represents a P_ChatDb_S.
     * @constructor
     * @param {P_ChatDb_S.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ChatDb_S = function (properties) {
        this.types = {};
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ChatDb_S req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_ChatDb_S
     * @instance
     */
    P_ChatDb_S.prototype.req_type = null;

    /**
     * P_ChatDb_S types.
     * @member {Object.<string,PB_InnerChatMap.$Properties>} types
     * @memberof P_ChatDb_S
     * @instance
     */
    P_ChatDb_S.prototype.types = $util.emptyObject;

    /**
     * P_ChatDb_S save_ret.
     * @member {number|null|undefined} save_ret
     * @memberof P_ChatDb_S
     * @instance
     */
    P_ChatDb_S.prototype.save_ret = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ChatDb_S.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ChatDb_S.prototype, "_save_ret", {
        get: $util.oneOfGetter($oneOfFields = ["save_ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ChatDb_S instance using the specified properties.
     * @function create
     * @memberof P_ChatDb_S
     * @static
     * @param {P_ChatDb_S.$Properties=} [properties] Properties to set
     * @returns {P_ChatDb_S} P_ChatDb_S instance
     * @type {{
     *   (properties: P_ChatDb_S.$Shape): P_ChatDb_S & P_ChatDb_S.$Shape;
     *   (properties?: P_ChatDb_S.$Properties): P_ChatDb_S;
     * }}
     */
    P_ChatDb_S.create = function(properties) {
        return new P_ChatDb_S(properties);
    };

    /**
     * Encodes the specified P_ChatDb_S message. Does not implicitly {@link P_ChatDb_S.verify|verify} messages.
     * @function encode
     * @memberof P_ChatDb_S
     * @static
     * @param {P_ChatDb_S.$Properties} message P_ChatDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ChatDb_S.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.types != null && $Object.hasOwnProperty.call(message, "types"))
            for (let keys = $Object.keys(message.types), i = 0; i < keys.length; ++i) {
                writer.uint32(/* id 2, wireType 2 =*/18).fork().uint32(/* id 1, wireType 0 =*/8).int32(keys[i]);
                $root.PB_InnerChatMap.encode(message.types[keys[i]], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim().ldelim();
            }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.save_ret);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ChatDb_S message, length delimited. Does not implicitly {@link P_ChatDb_S.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ChatDb_S
     * @static
     * @param {P_ChatDb_S.$Properties} message P_ChatDb_S message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ChatDb_S.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ChatDb_S message from the specified reader or buffer.
     * @function decode
     * @memberof P_ChatDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ChatDb_S & P_ChatDb_S.$Shape} P_ChatDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ChatDb_S.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ChatDb_S(), key, value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (message.types === $util.emptyObject)
                        message.types = {};
                    let end2 = reader.uint32() + reader.pos;
                    key = 0;
                    value = null;
                    while (reader.pos < end2) {
                        let tag2 = reader.tag();
                        wireType = tag2 & 7;
                        switch (tag2 >>>= 3) {
                        case 1:
                            if (wireType !== 0)
                                break;
                            key = reader.int32();
                            continue;
                        case 2:
                            if (wireType !== 2)
                                break;
                            value = $root.PB_InnerChatMap.decode(reader, reader.uint32(), $undefined, _depth + 1, value);
                            continue;
                        }
                        reader.skipType(wireType, _depth, tag2);
                    }
                    message.types[key] = value || new $root.PB_InnerChatMap();
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.save_ret = reader.int32();
                    message._save_ret = "save_ret";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ChatDb_S message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ChatDb_S
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ChatDb_S & P_ChatDb_S.$Shape} P_ChatDb_S
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ChatDb_S.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ChatDb_S message.
     * @function verify
     * @memberof P_ChatDb_S
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ChatDb_S.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.types != null && $Object.hasOwnProperty.call(message, "types")) {
            if (!$util.isObject(message.types))
                return "types: object expected";
            let key = $Object.keys(message.types);
            for (let i = 0; i < key.length; ++i) {
                if (!$util.key32Re.test(key[i]))
                    return "types: integer key{k:int32} expected";
                {
                    let error = $root.PB_InnerChatMap.verify(message.types[key[i]], _depth + 1);
                    if (error)
                        return "types." + error;
                }
            }
        }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret")) {
            properties._save_ret = 1;
            if (!$util.isInteger(message.save_ret))
                return "save_ret: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_ChatDb_S message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ChatDb_S
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ChatDb_S} P_ChatDb_S
     */
    P_ChatDb_S.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ChatDb_S)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ChatDb_S: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ChatDb_S();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.types) {
            if (!$util.isObject(object.types))
                throw $TypeError(".P_ChatDb_S.types: object expected");
            message.types = {};
            for (let keys = $Object.keys(object.types), i = 0; i < keys.length; ++i) {
                if (keys[i] === "__proto__")
                    $util.makeProp(message.types, keys[i]);
                if (!$util.isObject(object.types[keys[i]]))
                    throw $TypeError(".P_ChatDb_S.types: object expected");
                message.types[keys[i]] = $root.PB_InnerChatMap.fromObject(object.types[keys[i]], _depth + 1);
            }
        }
        if (object.save_ret != null)
            message.save_ret = object.save_ret | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_ChatDb_S message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ChatDb_S
     * @static
     * @param {P_ChatDb_S} message P_ChatDb_S
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ChatDb_S.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.objects || options.defaults)
            object.types = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        let keys2;
        if (message.types && (keys2 = $Object.keys(message.types)).length) {
            object.types = {};
            for (let j = 0; j < keys2.length; ++j) {
                if (keys2[j] === "__proto__")
                    $util.makeProp(object.types, keys2[j]);
                object.types[keys2[j]] = $root.PB_InnerChatMap.toObject(message.types[keys2[j]], options, _depth + 1);
            }
        }
        if (message.save_ret != null && $Object.hasOwnProperty.call(message, "save_ret"))
            object.save_ret = message.save_ret;
        return object;
    };

    /**
     * Converts this P_ChatDb_S to JSON.
     * @function toJSON
     * @memberof P_ChatDb_S
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ChatDb_S.prototype.toJSON = function() {
        return P_ChatDb_S.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ChatDb_S
     * @function getTypeUrl
     * @memberof P_ChatDb_S
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ChatDb_S.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ChatDb_S";
    };

    return P_ChatDb_S;
})();

export const P_BattleServerRegister = $root.P_BattleServerRegister = (() => {

    /**
     * Properties of a P_BattleServerRegister.
     * @typedef {Object} P_BattleServerRegister.$Properties
     * @property {number|null} [id] P_BattleServerRegister id
     * @property {string|null} [ip] P_BattleServerRegister ip
     * @property {number|null} [port] P_BattleServerRegister port
     * @property {number|null} [weight] P_BattleServerRegister weight
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerRegister.
     * @exports IP_BattleServerRegister
     * @interface IP_BattleServerRegister
     * @augments P_BattleServerRegister.$Properties
     * @deprecated Use P_BattleServerRegister.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerRegister.
     * @typedef {P_BattleServerRegister.$Properties} P_BattleServerRegister.$Shape
     */

    /**
     * Constructs a new P_BattleServerRegister.
     * @exports P_BattleServerRegister
     * @classdesc Represents a P_BattleServerRegister.
     * @constructor
     * @param {P_BattleServerRegister.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerRegister = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerRegister id.
     * @member {number|null|undefined} id
     * @memberof P_BattleServerRegister
     * @instance
     */
    P_BattleServerRegister.prototype.id = null;

    /**
     * P_BattleServerRegister ip.
     * @member {string|null|undefined} ip
     * @memberof P_BattleServerRegister
     * @instance
     */
    P_BattleServerRegister.prototype.ip = null;

    /**
     * P_BattleServerRegister port.
     * @member {number|null|undefined} port
     * @memberof P_BattleServerRegister
     * @instance
     */
    P_BattleServerRegister.prototype.port = null;

    /**
     * P_BattleServerRegister weight.
     * @member {number|null|undefined} weight
     * @memberof P_BattleServerRegister
     * @instance
     */
    P_BattleServerRegister.prototype.weight = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegister.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegister.prototype, "_ip", {
        get: $util.oneOfGetter($oneOfFields = ["ip"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegister.prototype, "_port", {
        get: $util.oneOfGetter($oneOfFields = ["port"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegister.prototype, "_weight", {
        get: $util.oneOfGetter($oneOfFields = ["weight"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerRegister instance using the specified properties.
     * @function create
     * @memberof P_BattleServerRegister
     * @static
     * @param {P_BattleServerRegister.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerRegister} P_BattleServerRegister instance
     * @type {{
     *   (properties: P_BattleServerRegister.$Shape): P_BattleServerRegister & P_BattleServerRegister.$Shape;
     *   (properties?: P_BattleServerRegister.$Properties): P_BattleServerRegister;
     * }}
     */
    P_BattleServerRegister.create = function(properties) {
        return new P_BattleServerRegister(properties);
    };

    /**
     * Encodes the specified P_BattleServerRegister message. Does not implicitly {@link P_BattleServerRegister.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerRegister
     * @static
     * @param {P_BattleServerRegister.$Properties} message P_BattleServerRegister message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerRegister.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.ip);
        if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.port);
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.weight);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerRegister message, length delimited. Does not implicitly {@link P_BattleServerRegister.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerRegister
     * @static
     * @param {P_BattleServerRegister.$Properties} message P_BattleServerRegister message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerRegister.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerRegister message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerRegister
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerRegister & P_BattleServerRegister.$Shape} P_BattleServerRegister
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerRegister.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerRegister();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.ip = reader.stringVerify();
                    message._ip = "ip";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.port = reader.int32();
                    message._port = "port";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.weight = reader.int32();
                    message._weight = "weight";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerRegister message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerRegister
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerRegister & P_BattleServerRegister.$Shape} P_BattleServerRegister
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerRegister.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerRegister message.
     * @function verify
     * @memberof P_BattleServerRegister
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerRegister.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.ip != null && $Object.hasOwnProperty.call(message, "ip")) {
            properties._ip = 1;
            if (!$util.isString(message.ip))
                return "ip: string expected";
        }
        if (message.port != null && $Object.hasOwnProperty.call(message, "port")) {
            properties._port = 1;
            if (!$util.isInteger(message.port))
                return "port: integer expected";
        }
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight")) {
            properties._weight = 1;
            if (!$util.isInteger(message.weight))
                return "weight: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerRegister message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerRegister
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerRegister} P_BattleServerRegister
     */
    P_BattleServerRegister.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerRegister)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerRegister: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerRegister();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.ip != null)
            message.ip = $String(object.ip);
        if (object.port != null)
            message.port = object.port | 0;
        if (object.weight != null)
            message.weight = object.weight | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerRegister message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerRegister
     * @static
     * @param {P_BattleServerRegister} message P_BattleServerRegister
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerRegister.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.ip != null && $Object.hasOwnProperty.call(message, "ip"))
            object.ip = message.ip;
        if (message.port != null && $Object.hasOwnProperty.call(message, "port"))
            object.port = message.port;
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
            object.weight = message.weight;
        return object;
    };

    /**
     * Converts this P_BattleServerRegister to JSON.
     * @function toJSON
     * @memberof P_BattleServerRegister
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerRegister.prototype.toJSON = function() {
        return P_BattleServerRegister.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerRegister
     * @function getTypeUrl
     * @memberof P_BattleServerRegister
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerRegister.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerRegister";
    };

    return P_BattleServerRegister;
})();

export const P_BattleServerHeartbeat = $root.P_BattleServerHeartbeat = (() => {

    /**
     * Properties of a P_BattleServerHeartbeat.
     * @typedef {Object} P_BattleServerHeartbeat.$Properties
     * @property {number|null} [id] P_BattleServerHeartbeat id
     * @property {number|null} [player_count] P_BattleServerHeartbeat player_count
     * @property {number|null} [cpu_usage] P_BattleServerHeartbeat cpu_usage
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerHeartbeat.
     * @exports IP_BattleServerHeartbeat
     * @interface IP_BattleServerHeartbeat
     * @augments P_BattleServerHeartbeat.$Properties
     * @deprecated Use P_BattleServerHeartbeat.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerHeartbeat.
     * @typedef {P_BattleServerHeartbeat.$Properties} P_BattleServerHeartbeat.$Shape
     */

    /**
     * Constructs a new P_BattleServerHeartbeat.
     * @exports P_BattleServerHeartbeat
     * @classdesc Represents a P_BattleServerHeartbeat.
     * @constructor
     * @param {P_BattleServerHeartbeat.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerHeartbeat = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerHeartbeat id.
     * @member {number|null|undefined} id
     * @memberof P_BattleServerHeartbeat
     * @instance
     */
    P_BattleServerHeartbeat.prototype.id = null;

    /**
     * P_BattleServerHeartbeat player_count.
     * @member {number|null|undefined} player_count
     * @memberof P_BattleServerHeartbeat
     * @instance
     */
    P_BattleServerHeartbeat.prototype.player_count = null;

    /**
     * P_BattleServerHeartbeat cpu_usage.
     * @member {number|null|undefined} cpu_usage
     * @memberof P_BattleServerHeartbeat
     * @instance
     */
    P_BattleServerHeartbeat.prototype.cpu_usage = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerHeartbeat.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerHeartbeat.prototype, "_player_count", {
        get: $util.oneOfGetter($oneOfFields = ["player_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerHeartbeat.prototype, "_cpu_usage", {
        get: $util.oneOfGetter($oneOfFields = ["cpu_usage"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerHeartbeat instance using the specified properties.
     * @function create
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {P_BattleServerHeartbeat.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerHeartbeat} P_BattleServerHeartbeat instance
     * @type {{
     *   (properties: P_BattleServerHeartbeat.$Shape): P_BattleServerHeartbeat & P_BattleServerHeartbeat.$Shape;
     *   (properties?: P_BattleServerHeartbeat.$Properties): P_BattleServerHeartbeat;
     * }}
     */
    P_BattleServerHeartbeat.create = function(properties) {
        return new P_BattleServerHeartbeat(properties);
    };

    /**
     * Encodes the specified P_BattleServerHeartbeat message. Does not implicitly {@link P_BattleServerHeartbeat.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {P_BattleServerHeartbeat.$Properties} message P_BattleServerHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerHeartbeat.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.player_count);
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage"))
            writer.uint32(/* id 3, wireType 1 =*/25).double(message.cpu_usage);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerHeartbeat message, length delimited. Does not implicitly {@link P_BattleServerHeartbeat.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {P_BattleServerHeartbeat.$Properties} message P_BattleServerHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerHeartbeat.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerHeartbeat message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerHeartbeat & P_BattleServerHeartbeat.$Shape} P_BattleServerHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerHeartbeat.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerHeartbeat();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.player_count = reader.int32();
                    message._player_count = "player_count";
                    continue;
                }
            case 3: {
                    if (wireType !== 1)
                        break;
                    message.cpu_usage = reader.double();
                    message._cpu_usage = "cpu_usage";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerHeartbeat message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerHeartbeat & P_BattleServerHeartbeat.$Shape} P_BattleServerHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerHeartbeat.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerHeartbeat message.
     * @function verify
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerHeartbeat.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count")) {
            properties._player_count = 1;
            if (!$util.isInteger(message.player_count))
                return "player_count: integer expected";
        }
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage")) {
            properties._cpu_usage = 1;
            if (typeof message.cpu_usage !== "number")
                return "cpu_usage: number expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerHeartbeat message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerHeartbeat} P_BattleServerHeartbeat
     */
    P_BattleServerHeartbeat.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerHeartbeat)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerHeartbeat: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerHeartbeat();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.player_count != null)
            message.player_count = object.player_count | 0;
        if (object.cpu_usage != null)
            message.cpu_usage = $Number(object.cpu_usage);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerHeartbeat message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {P_BattleServerHeartbeat} message P_BattleServerHeartbeat
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerHeartbeat.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count"))
            object.player_count = message.player_count;
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage"))
            object.cpu_usage = options.json && !$isFinite(message.cpu_usage) ? $String(message.cpu_usage) : message.cpu_usage;
        return object;
    };

    /**
     * Converts this P_BattleServerHeartbeat to JSON.
     * @function toJSON
     * @memberof P_BattleServerHeartbeat
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerHeartbeat.prototype.toJSON = function() {
        return P_BattleServerHeartbeat.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerHeartbeat
     * @function getTypeUrl
     * @memberof P_BattleServerHeartbeat
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerHeartbeat.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerHeartbeat";
    };

    return P_BattleServerHeartbeat;
})();

export const P_BattleServerCommand = $root.P_BattleServerCommand = (() => {

    /**
     * Properties of a P_BattleServerCommand.
     * @typedef {Object} P_BattleServerCommand.$Properties
     * @property {number|null} [id] P_BattleServerCommand id
     * @property {string|null} [command] P_BattleServerCommand command
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerCommand.
     * @exports IP_BattleServerCommand
     * @interface IP_BattleServerCommand
     * @augments P_BattleServerCommand.$Properties
     * @deprecated Use P_BattleServerCommand.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerCommand.
     * @typedef {P_BattleServerCommand.$Properties} P_BattleServerCommand.$Shape
     */

    /**
     * Constructs a new P_BattleServerCommand.
     * @exports P_BattleServerCommand
     * @classdesc Represents a P_BattleServerCommand.
     * @constructor
     * @param {P_BattleServerCommand.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerCommand = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerCommand id.
     * @member {number|null|undefined} id
     * @memberof P_BattleServerCommand
     * @instance
     */
    P_BattleServerCommand.prototype.id = null;

    /**
     * P_BattleServerCommand command.
     * @member {string|null|undefined} command
     * @memberof P_BattleServerCommand
     * @instance
     */
    P_BattleServerCommand.prototype.command = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerCommand.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerCommand.prototype, "_command", {
        get: $util.oneOfGetter($oneOfFields = ["command"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerCommand instance using the specified properties.
     * @function create
     * @memberof P_BattleServerCommand
     * @static
     * @param {P_BattleServerCommand.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerCommand} P_BattleServerCommand instance
     * @type {{
     *   (properties: P_BattleServerCommand.$Shape): P_BattleServerCommand & P_BattleServerCommand.$Shape;
     *   (properties?: P_BattleServerCommand.$Properties): P_BattleServerCommand;
     * }}
     */
    P_BattleServerCommand.create = function(properties) {
        return new P_BattleServerCommand(properties);
    };

    /**
     * Encodes the specified P_BattleServerCommand message. Does not implicitly {@link P_BattleServerCommand.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerCommand
     * @static
     * @param {P_BattleServerCommand.$Properties} message P_BattleServerCommand message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerCommand.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.command != null && $Object.hasOwnProperty.call(message, "command"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.command);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerCommand message, length delimited. Does not implicitly {@link P_BattleServerCommand.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerCommand
     * @static
     * @param {P_BattleServerCommand.$Properties} message P_BattleServerCommand message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerCommand.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerCommand message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerCommand
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerCommand & P_BattleServerCommand.$Shape} P_BattleServerCommand
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerCommand.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerCommand();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.command = reader.stringVerify();
                    message._command = "command";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerCommand message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerCommand
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerCommand & P_BattleServerCommand.$Shape} P_BattleServerCommand
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerCommand.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerCommand message.
     * @function verify
     * @memberof P_BattleServerCommand
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerCommand.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.command != null && $Object.hasOwnProperty.call(message, "command")) {
            properties._command = 1;
            if (!$util.isString(message.command))
                return "command: string expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerCommand message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerCommand
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerCommand} P_BattleServerCommand
     */
    P_BattleServerCommand.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerCommand)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerCommand: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerCommand();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.command != null)
            message.command = $String(object.command);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerCommand message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerCommand
     * @static
     * @param {P_BattleServerCommand} message P_BattleServerCommand
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerCommand.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.command != null && $Object.hasOwnProperty.call(message, "command"))
            object.command = message.command;
        return object;
    };

    /**
     * Converts this P_BattleServerCommand to JSON.
     * @function toJSON
     * @memberof P_BattleServerCommand
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerCommand.prototype.toJSON = function() {
        return P_BattleServerCommand.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerCommand
     * @function getTypeUrl
     * @memberof P_BattleServerCommand
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerCommand.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerCommand";
    };

    return P_BattleServerCommand;
})();

export const P_BattleServerStatus = $root.P_BattleServerStatus = (() => {

    /**
     * Properties of a P_BattleServerStatus.
     * @typedef {Object} P_BattleServerStatus.$Properties
     * @property {number|null} [id] P_BattleServerStatus id
     * @property {boolean|null} [alive] P_BattleServerStatus alive
     * @property {number|null} [player_count] P_BattleServerStatus player_count
     * @property {number|null} [cpu_usage] P_BattleServerStatus cpu_usage
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerStatus.
     * @exports IP_BattleServerStatus
     * @interface IP_BattleServerStatus
     * @augments P_BattleServerStatus.$Properties
     * @deprecated Use P_BattleServerStatus.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerStatus.
     * @typedef {P_BattleServerStatus.$Properties} P_BattleServerStatus.$Shape
     */

    /**
     * Constructs a new P_BattleServerStatus.
     * @exports P_BattleServerStatus
     * @classdesc Represents a P_BattleServerStatus.
     * @constructor
     * @param {P_BattleServerStatus.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerStatus = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerStatus id.
     * @member {number|null|undefined} id
     * @memberof P_BattleServerStatus
     * @instance
     */
    P_BattleServerStatus.prototype.id = null;

    /**
     * P_BattleServerStatus alive.
     * @member {boolean|null|undefined} alive
     * @memberof P_BattleServerStatus
     * @instance
     */
    P_BattleServerStatus.prototype.alive = null;

    /**
     * P_BattleServerStatus player_count.
     * @member {number|null|undefined} player_count
     * @memberof P_BattleServerStatus
     * @instance
     */
    P_BattleServerStatus.prototype.player_count = null;

    /**
     * P_BattleServerStatus cpu_usage.
     * @member {number|null|undefined} cpu_usage
     * @memberof P_BattleServerStatus
     * @instance
     */
    P_BattleServerStatus.prototype.cpu_usage = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerStatus.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerStatus.prototype, "_alive", {
        get: $util.oneOfGetter($oneOfFields = ["alive"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerStatus.prototype, "_player_count", {
        get: $util.oneOfGetter($oneOfFields = ["player_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerStatus.prototype, "_cpu_usage", {
        get: $util.oneOfGetter($oneOfFields = ["cpu_usage"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerStatus instance using the specified properties.
     * @function create
     * @memberof P_BattleServerStatus
     * @static
     * @param {P_BattleServerStatus.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerStatus} P_BattleServerStatus instance
     * @type {{
     *   (properties: P_BattleServerStatus.$Shape): P_BattleServerStatus & P_BattleServerStatus.$Shape;
     *   (properties?: P_BattleServerStatus.$Properties): P_BattleServerStatus;
     * }}
     */
    P_BattleServerStatus.create = function(properties) {
        return new P_BattleServerStatus(properties);
    };

    /**
     * Encodes the specified P_BattleServerStatus message. Does not implicitly {@link P_BattleServerStatus.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerStatus
     * @static
     * @param {P_BattleServerStatus.$Properties} message P_BattleServerStatus message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerStatus.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.alive != null && $Object.hasOwnProperty.call(message, "alive"))
            writer.uint32(/* id 2, wireType 0 =*/16).bool(message.alive);
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.player_count);
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage"))
            writer.uint32(/* id 4, wireType 1 =*/33).double(message.cpu_usage);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerStatus message, length delimited. Does not implicitly {@link P_BattleServerStatus.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerStatus
     * @static
     * @param {P_BattleServerStatus.$Properties} message P_BattleServerStatus message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerStatus.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerStatus message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerStatus
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerStatus & P_BattleServerStatus.$Shape} P_BattleServerStatus
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerStatus.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerStatus();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.alive = reader.bool();
                    message._alive = "alive";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.player_count = reader.int32();
                    message._player_count = "player_count";
                    continue;
                }
            case 4: {
                    if (wireType !== 1)
                        break;
                    message.cpu_usage = reader.double();
                    message._cpu_usage = "cpu_usage";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerStatus message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerStatus
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerStatus & P_BattleServerStatus.$Shape} P_BattleServerStatus
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerStatus.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerStatus message.
     * @function verify
     * @memberof P_BattleServerStatus
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerStatus.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.alive != null && $Object.hasOwnProperty.call(message, "alive")) {
            properties._alive = 1;
            if (typeof message.alive !== "boolean")
                return "alive: boolean expected";
        }
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count")) {
            properties._player_count = 1;
            if (!$util.isInteger(message.player_count))
                return "player_count: integer expected";
        }
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage")) {
            properties._cpu_usage = 1;
            if (typeof message.cpu_usage !== "number")
                return "cpu_usage: number expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerStatus message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerStatus
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerStatus} P_BattleServerStatus
     */
    P_BattleServerStatus.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerStatus)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerStatus: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerStatus();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.alive != null)
            message.alive = $Boolean(object.alive);
        if (object.player_count != null)
            message.player_count = object.player_count | 0;
        if (object.cpu_usage != null)
            message.cpu_usage = $Number(object.cpu_usage);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerStatus message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerStatus
     * @static
     * @param {P_BattleServerStatus} message P_BattleServerStatus
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerStatus.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.alive != null && $Object.hasOwnProperty.call(message, "alive"))
            object.alive = message.alive;
        if (message.player_count != null && $Object.hasOwnProperty.call(message, "player_count"))
            object.player_count = message.player_count;
        if (message.cpu_usage != null && $Object.hasOwnProperty.call(message, "cpu_usage"))
            object.cpu_usage = options.json && !$isFinite(message.cpu_usage) ? $String(message.cpu_usage) : message.cpu_usage;
        return object;
    };

    /**
     * Converts this P_BattleServerStatus to JSON.
     * @function toJSON
     * @memberof P_BattleServerStatus
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerStatus.prototype.toJSON = function() {
        return P_BattleServerStatus.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerStatus
     * @function getTypeUrl
     * @memberof P_BattleServerStatus
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerStatus.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerStatus";
    };

    return P_BattleServerStatus;
})();

export const P_BattleServerRegisterTokenAndRoleData = $root.P_BattleServerRegisterTokenAndRoleData = (() => {

    /**
     * Properties of a P_BattleServerRegisterTokenAndRoleData.
     * @typedef {Object} P_BattleServerRegisterTokenAndRoleData.$Properties
     * @property {number|null} [id] P_BattleServerRegisterTokenAndRoleData id
     * @property {PB_RoleData.$Properties|null} [role_data] P_BattleServerRegisterTokenAndRoleData role_data
     * @property {string|null} [token] P_BattleServerRegisterTokenAndRoleData token
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerRegisterTokenAndRoleData.
     * @exports IP_BattleServerRegisterTokenAndRoleData
     * @interface IP_BattleServerRegisterTokenAndRoleData
     * @augments P_BattleServerRegisterTokenAndRoleData.$Properties
     * @deprecated Use P_BattleServerRegisterTokenAndRoleData.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerRegisterTokenAndRoleData.
     * @typedef {P_BattleServerRegisterTokenAndRoleData.$Properties} P_BattleServerRegisterTokenAndRoleData.$Shape
     */

    /**
     * Constructs a new P_BattleServerRegisterTokenAndRoleData.
     * @exports P_BattleServerRegisterTokenAndRoleData
     * @classdesc Represents a P_BattleServerRegisterTokenAndRoleData.
     * @constructor
     * @param {P_BattleServerRegisterTokenAndRoleData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerRegisterTokenAndRoleData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerRegisterTokenAndRoleData id.
     * @member {number|null|undefined} id
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @instance
     */
    P_BattleServerRegisterTokenAndRoleData.prototype.id = null;

    /**
     * P_BattleServerRegisterTokenAndRoleData role_data.
     * @member {PB_RoleData.$Properties|null|undefined} role_data
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @instance
     */
    P_BattleServerRegisterTokenAndRoleData.prototype.role_data = null;

    /**
     * P_BattleServerRegisterTokenAndRoleData token.
     * @member {string|null|undefined} token
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @instance
     */
    P_BattleServerRegisterTokenAndRoleData.prototype.token = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegisterTokenAndRoleData.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegisterTokenAndRoleData.prototype, "_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerRegisterTokenAndRoleData.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerRegisterTokenAndRoleData instance using the specified properties.
     * @function create
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {P_BattleServerRegisterTokenAndRoleData.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerRegisterTokenAndRoleData} P_BattleServerRegisterTokenAndRoleData instance
     * @type {{
     *   (properties: P_BattleServerRegisterTokenAndRoleData.$Shape): P_BattleServerRegisterTokenAndRoleData & P_BattleServerRegisterTokenAndRoleData.$Shape;
     *   (properties?: P_BattleServerRegisterTokenAndRoleData.$Properties): P_BattleServerRegisterTokenAndRoleData;
     * }}
     */
    P_BattleServerRegisterTokenAndRoleData.create = function(properties) {
        return new P_BattleServerRegisterTokenAndRoleData(properties);
    };

    /**
     * Encodes the specified P_BattleServerRegisterTokenAndRoleData message. Does not implicitly {@link P_BattleServerRegisterTokenAndRoleData.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {P_BattleServerRegisterTokenAndRoleData.$Properties} message P_BattleServerRegisterTokenAndRoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerRegisterTokenAndRoleData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            $root.PB_RoleData.encode(message.role_data, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.token);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerRegisterTokenAndRoleData message, length delimited. Does not implicitly {@link P_BattleServerRegisterTokenAndRoleData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {P_BattleServerRegisterTokenAndRoleData.$Properties} message P_BattleServerRegisterTokenAndRoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerRegisterTokenAndRoleData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerRegisterTokenAndRoleData message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerRegisterTokenAndRoleData & P_BattleServerRegisterTokenAndRoleData.$Shape} P_BattleServerRegisterTokenAndRoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerRegisterTokenAndRoleData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerRegisterTokenAndRoleData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.role_data = $root.PB_RoleData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_data);
                    message._role_data = "role_data";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.token = reader.stringVerify();
                    message._token = "token";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerRegisterTokenAndRoleData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerRegisterTokenAndRoleData & P_BattleServerRegisterTokenAndRoleData.$Shape} P_BattleServerRegisterTokenAndRoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerRegisterTokenAndRoleData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerRegisterTokenAndRoleData message.
     * @function verify
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerRegisterTokenAndRoleData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data")) {
            properties._role_data = 1;
            {
                let error = $root.PB_RoleData.verify(message.role_data, _depth + 1);
                if (error)
                    return "role_data." + error;
            }
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isString(message.token))
                return "token: string expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerRegisterTokenAndRoleData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerRegisterTokenAndRoleData} P_BattleServerRegisterTokenAndRoleData
     */
    P_BattleServerRegisterTokenAndRoleData.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerRegisterTokenAndRoleData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerRegisterTokenAndRoleData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerRegisterTokenAndRoleData();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.role_data != null) {
            if (!$util.isObject(object.role_data))
                throw $TypeError(".P_BattleServerRegisterTokenAndRoleData.role_data: object expected");
            message.role_data = $root.PB_RoleData.fromObject(object.role_data, _depth + 1);
        }
        if (object.token != null)
            message.token = $String(object.token);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerRegisterTokenAndRoleData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {P_BattleServerRegisterTokenAndRoleData} message P_BattleServerRegisterTokenAndRoleData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerRegisterTokenAndRoleData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            object.role_data = $root.PB_RoleData.toObject(message.role_data, options, _depth + 1);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        return object;
    };

    /**
     * Converts this P_BattleServerRegisterTokenAndRoleData to JSON.
     * @function toJSON
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerRegisterTokenAndRoleData.prototype.toJSON = function() {
        return P_BattleServerRegisterTokenAndRoleData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerRegisterTokenAndRoleData
     * @function getTypeUrl
     * @memberof P_BattleServerRegisterTokenAndRoleData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerRegisterTokenAndRoleData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerRegisterTokenAndRoleData";
    };

    return P_BattleServerRegisterTokenAndRoleData;
})();

export const P_BattleServerSaveRoleData = $root.P_BattleServerSaveRoleData = (() => {

    /**
     * Properties of a P_BattleServerSaveRoleData.
     * @typedef {Object} P_BattleServerSaveRoleData.$Properties
     * @property {PB_RoleData.$Properties|null} [role_data] P_BattleServerSaveRoleData role_data
     * @property {string|null} [token] P_BattleServerSaveRoleData token
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerSaveRoleData.
     * @exports IP_BattleServerSaveRoleData
     * @interface IP_BattleServerSaveRoleData
     * @augments P_BattleServerSaveRoleData.$Properties
     * @deprecated Use P_BattleServerSaveRoleData.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerSaveRoleData.
     * @typedef {P_BattleServerSaveRoleData.$Properties} P_BattleServerSaveRoleData.$Shape
     */

    /**
     * Constructs a new P_BattleServerSaveRoleData.
     * @exports P_BattleServerSaveRoleData
     * @classdesc Represents a P_BattleServerSaveRoleData.
     * @constructor
     * @param {P_BattleServerSaveRoleData.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerSaveRoleData = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerSaveRoleData role_data.
     * @member {PB_RoleData.$Properties|null|undefined} role_data
     * @memberof P_BattleServerSaveRoleData
     * @instance
     */
    P_BattleServerSaveRoleData.prototype.role_data = null;

    /**
     * P_BattleServerSaveRoleData token.
     * @member {string|null|undefined} token
     * @memberof P_BattleServerSaveRoleData
     * @instance
     */
    P_BattleServerSaveRoleData.prototype.token = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerSaveRoleData.prototype, "_role_data", {
        get: $util.oneOfGetter($oneOfFields = ["role_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerSaveRoleData.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerSaveRoleData instance using the specified properties.
     * @function create
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {P_BattleServerSaveRoleData.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerSaveRoleData} P_BattleServerSaveRoleData instance
     * @type {{
     *   (properties: P_BattleServerSaveRoleData.$Shape): P_BattleServerSaveRoleData & P_BattleServerSaveRoleData.$Shape;
     *   (properties?: P_BattleServerSaveRoleData.$Properties): P_BattleServerSaveRoleData;
     * }}
     */
    P_BattleServerSaveRoleData.create = function(properties) {
        return new P_BattleServerSaveRoleData(properties);
    };

    /**
     * Encodes the specified P_BattleServerSaveRoleData message. Does not implicitly {@link P_BattleServerSaveRoleData.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {P_BattleServerSaveRoleData.$Properties} message P_BattleServerSaveRoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerSaveRoleData.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            $root.PB_RoleData.encode(message.role_data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerSaveRoleData message, length delimited. Does not implicitly {@link P_BattleServerSaveRoleData.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {P_BattleServerSaveRoleData.$Properties} message P_BattleServerSaveRoleData message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerSaveRoleData.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerSaveRoleData message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerSaveRoleData & P_BattleServerSaveRoleData.$Shape} P_BattleServerSaveRoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerSaveRoleData.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerSaveRoleData();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.role_data = $root.PB_RoleData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_data);
                    message._role_data = "role_data";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.token = reader.stringVerify();
                    message._token = "token";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerSaveRoleData message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerSaveRoleData & P_BattleServerSaveRoleData.$Shape} P_BattleServerSaveRoleData
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerSaveRoleData.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerSaveRoleData message.
     * @function verify
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerSaveRoleData.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data")) {
            properties._role_data = 1;
            {
                let error = $root.PB_RoleData.verify(message.role_data, _depth + 1);
                if (error)
                    return "role_data." + error;
            }
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isString(message.token))
                return "token: string expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerSaveRoleData message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerSaveRoleData} P_BattleServerSaveRoleData
     */
    P_BattleServerSaveRoleData.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerSaveRoleData)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerSaveRoleData: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerSaveRoleData();
        if (object.role_data != null) {
            if (!$util.isObject(object.role_data))
                throw $TypeError(".P_BattleServerSaveRoleData.role_data: object expected");
            message.role_data = $root.PB_RoleData.fromObject(object.role_data, _depth + 1);
        }
        if (object.token != null)
            message.token = $String(object.token);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerSaveRoleData message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {P_BattleServerSaveRoleData} message P_BattleServerSaveRoleData
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerSaveRoleData.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.role_data != null && $Object.hasOwnProperty.call(message, "role_data"))
            object.role_data = $root.PB_RoleData.toObject(message.role_data, options, _depth + 1);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        return object;
    };

    /**
     * Converts this P_BattleServerSaveRoleData to JSON.
     * @function toJSON
     * @memberof P_BattleServerSaveRoleData
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerSaveRoleData.prototype.toJSON = function() {
        return P_BattleServerSaveRoleData.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerSaveRoleData
     * @function getTypeUrl
     * @memberof P_BattleServerSaveRoleData
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerSaveRoleData.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerSaveRoleData";
    };

    return P_BattleServerSaveRoleData;
})();

export const P_BattleServerKickOutRole = $root.P_BattleServerKickOutRole = (() => {

    /**
     * Properties of a P_BattleServerKickOutRole.
     * @typedef {Object} P_BattleServerKickOutRole.$Properties
     * @property {number|null} [uid] P_BattleServerKickOutRole uid
     * @property {string|null} [token] P_BattleServerKickOutRole token
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServerKickOutRole.
     * @exports IP_BattleServerKickOutRole
     * @interface IP_BattleServerKickOutRole
     * @augments P_BattleServerKickOutRole.$Properties
     * @deprecated Use P_BattleServerKickOutRole.$Properties instead.
     */

    /**
     * Shape of a P_BattleServerKickOutRole.
     * @typedef {P_BattleServerKickOutRole.$Properties} P_BattleServerKickOutRole.$Shape
     */

    /**
     * Constructs a new P_BattleServerKickOutRole.
     * @exports P_BattleServerKickOutRole
     * @classdesc Represents a P_BattleServerKickOutRole.
     * @constructor
     * @param {P_BattleServerKickOutRole.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServerKickOutRole = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServerKickOutRole uid.
     * @member {number|null|undefined} uid
     * @memberof P_BattleServerKickOutRole
     * @instance
     */
    P_BattleServerKickOutRole.prototype.uid = null;

    /**
     * P_BattleServerKickOutRole token.
     * @member {string|null|undefined} token
     * @memberof P_BattleServerKickOutRole
     * @instance
     */
    P_BattleServerKickOutRole.prototype.token = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerKickOutRole.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServerKickOutRole.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServerKickOutRole instance using the specified properties.
     * @function create
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {P_BattleServerKickOutRole.$Properties=} [properties] Properties to set
     * @returns {P_BattleServerKickOutRole} P_BattleServerKickOutRole instance
     * @type {{
     *   (properties: P_BattleServerKickOutRole.$Shape): P_BattleServerKickOutRole & P_BattleServerKickOutRole.$Shape;
     *   (properties?: P_BattleServerKickOutRole.$Properties): P_BattleServerKickOutRole;
     * }}
     */
    P_BattleServerKickOutRole.create = function(properties) {
        return new P_BattleServerKickOutRole(properties);
    };

    /**
     * Encodes the specified P_BattleServerKickOutRole message. Does not implicitly {@link P_BattleServerKickOutRole.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {P_BattleServerKickOutRole.$Properties} message P_BattleServerKickOutRole message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerKickOutRole.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.uid);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServerKickOutRole message, length delimited. Does not implicitly {@link P_BattleServerKickOutRole.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {P_BattleServerKickOutRole.$Properties} message P_BattleServerKickOutRole message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServerKickOutRole.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServerKickOutRole message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServerKickOutRole & P_BattleServerKickOutRole.$Shape} P_BattleServerKickOutRole
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerKickOutRole.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServerKickOutRole();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.token = reader.stringVerify();
                    message._token = "token";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServerKickOutRole message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServerKickOutRole & P_BattleServerKickOutRole.$Shape} P_BattleServerKickOutRole
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServerKickOutRole.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServerKickOutRole message.
     * @function verify
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServerKickOutRole.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isString(message.token))
                return "token: string expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServerKickOutRole message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServerKickOutRole} P_BattleServerKickOutRole
     */
    P_BattleServerKickOutRole.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServerKickOutRole)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServerKickOutRole: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServerKickOutRole();
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.token != null)
            message.token = $String(object.token);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServerKickOutRole message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {P_BattleServerKickOutRole} message P_BattleServerKickOutRole
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServerKickOutRole.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        return object;
    };

    /**
     * Converts this P_BattleServerKickOutRole to JSON.
     * @function toJSON
     * @memberof P_BattleServerKickOutRole
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServerKickOutRole.prototype.toJSON = function() {
        return P_BattleServerKickOutRole.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServerKickOutRole
     * @function getTypeUrl
     * @memberof P_BattleServerKickOutRole
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServerKickOutRole.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServerKickOutRole";
    };

    return P_BattleServerKickOutRole;
})();

export const P_BattleExecuteReq = $root.P_BattleExecuteReq = (() => {

    /**
     * Properties of a P_BattleExecuteReq.
     * @typedef {Object} P_BattleExecuteReq.$Properties
     * @property {number|null} [req_id] P_BattleExecuteReq req_id
     * @property {PB_BattleReq.$Properties|null} [battle_req] P_BattleExecuteReq battle_req
     * @property {string|null} [report_key] P_BattleExecuteReq report_key
     * @property {number|null} [uid] P_BattleExecuteReq uid
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleExecuteReq.
     * @exports IP_BattleExecuteReq
     * @interface IP_BattleExecuteReq
     * @augments P_BattleExecuteReq.$Properties
     * @deprecated Use P_BattleExecuteReq.$Properties instead.
     */

    /**
     * Shape of a P_BattleExecuteReq.
     * @typedef {P_BattleExecuteReq.$Properties} P_BattleExecuteReq.$Shape
     */

    /**
     * Constructs a new P_BattleExecuteReq.
     * @exports P_BattleExecuteReq
     * @classdesc Represents a P_BattleExecuteReq.
     * @constructor
     * @param {P_BattleExecuteReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleExecuteReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleExecuteReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_BattleExecuteReq
     * @instance
     */
    P_BattleExecuteReq.prototype.req_id = null;

    /**
     * P_BattleExecuteReq battle_req.
     * @member {PB_BattleReq.$Properties|null|undefined} battle_req
     * @memberof P_BattleExecuteReq
     * @instance
     */
    P_BattleExecuteReq.prototype.battle_req = null;

    /**
     * P_BattleExecuteReq report_key.
     * @member {string|null|undefined} report_key
     * @memberof P_BattleExecuteReq
     * @instance
     */
    P_BattleExecuteReq.prototype.report_key = null;

    /**
     * P_BattleExecuteReq uid.
     * @member {number|null|undefined} uid
     * @memberof P_BattleExecuteReq
     * @instance
     */
    P_BattleExecuteReq.prototype.uid = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteReq.prototype, "_battle_req", {
        get: $util.oneOfGetter($oneOfFields = ["battle_req"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteReq.prototype, "_report_key", {
        get: $util.oneOfGetter($oneOfFields = ["report_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteReq.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleExecuteReq instance using the specified properties.
     * @function create
     * @memberof P_BattleExecuteReq
     * @static
     * @param {P_BattleExecuteReq.$Properties=} [properties] Properties to set
     * @returns {P_BattleExecuteReq} P_BattleExecuteReq instance
     * @type {{
     *   (properties: P_BattleExecuteReq.$Shape): P_BattleExecuteReq & P_BattleExecuteReq.$Shape;
     *   (properties?: P_BattleExecuteReq.$Properties): P_BattleExecuteReq;
     * }}
     */
    P_BattleExecuteReq.create = function(properties) {
        return new P_BattleExecuteReq(properties);
    };

    /**
     * Encodes the specified P_BattleExecuteReq message. Does not implicitly {@link P_BattleExecuteReq.verify|verify} messages.
     * @function encode
     * @memberof P_BattleExecuteReq
     * @static
     * @param {P_BattleExecuteReq.$Properties} message P_BattleExecuteReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleExecuteReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_id);
        if (message.battle_req != null && $Object.hasOwnProperty.call(message, "battle_req"))
            $root.PB_BattleReq.encode(message.battle_req, writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.report_key);
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.uid);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleExecuteReq message, length delimited. Does not implicitly {@link P_BattleExecuteReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleExecuteReq
     * @static
     * @param {P_BattleExecuteReq.$Properties} message P_BattleExecuteReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleExecuteReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleExecuteReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleExecuteReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleExecuteReq & P_BattleExecuteReq.$Shape} P_BattleExecuteReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleExecuteReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleExecuteReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.battle_req = $root.PB_BattleReq.decode(reader, reader.uint32(), $undefined, _depth + 1, message.battle_req);
                    message._battle_req = "battle_req";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.report_key = reader.stringVerify();
                    message._report_key = "report_key";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleExecuteReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleExecuteReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleExecuteReq & P_BattleExecuteReq.$Shape} P_BattleExecuteReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleExecuteReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleExecuteReq message.
     * @function verify
     * @memberof P_BattleExecuteReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleExecuteReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        if (message.battle_req != null && $Object.hasOwnProperty.call(message, "battle_req")) {
            properties._battle_req = 1;
            {
                let error = $root.PB_BattleReq.verify(message.battle_req, _depth + 1);
                if (error)
                    return "battle_req." + error;
            }
        }
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key")) {
            properties._report_key = 1;
            if (!$util.isString(message.report_key))
                return "report_key: string expected";
        }
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleExecuteReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleExecuteReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleExecuteReq} P_BattleExecuteReq
     */
    P_BattleExecuteReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleExecuteReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleExecuteReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleExecuteReq();
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        if (object.battle_req != null) {
            if (!$util.isObject(object.battle_req))
                throw $TypeError(".P_BattleExecuteReq.battle_req: object expected");
            message.battle_req = $root.PB_BattleReq.fromObject(object.battle_req, _depth + 1);
        }
        if (object.report_key != null)
            message.report_key = $String(object.report_key);
        if (object.uid != null)
            message.uid = object.uid | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleExecuteReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleExecuteReq
     * @static
     * @param {P_BattleExecuteReq} message P_BattleExecuteReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleExecuteReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        if (message.battle_req != null && $Object.hasOwnProperty.call(message, "battle_req"))
            object.battle_req = $root.PB_BattleReq.toObject(message.battle_req, options, _depth + 1);
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            object.report_key = message.report_key;
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        return object;
    };

    /**
     * Converts this P_BattleExecuteReq to JSON.
     * @function toJSON
     * @memberof P_BattleExecuteReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleExecuteReq.prototype.toJSON = function() {
        return P_BattleExecuteReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleExecuteReq
     * @function getTypeUrl
     * @memberof P_BattleExecuteReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleExecuteReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleExecuteReq";
    };

    return P_BattleExecuteReq;
})();

export const P_BattleExecuteRes = $root.P_BattleExecuteRes = (() => {

    /**
     * Properties of a P_BattleExecuteRes.
     * @typedef {Object} P_BattleExecuteRes.$Properties
     * @property {number|null} [req_id] P_BattleExecuteRes req_id
     * @property {number|null} [ret] P_BattleExecuteRes ret
     * @property {PB_BattleResult.$Properties|null} [battle_result] P_BattleExecuteRes battle_result
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleExecuteRes.
     * @exports IP_BattleExecuteRes
     * @interface IP_BattleExecuteRes
     * @augments P_BattleExecuteRes.$Properties
     * @deprecated Use P_BattleExecuteRes.$Properties instead.
     */

    /**
     * Shape of a P_BattleExecuteRes.
     * @typedef {P_BattleExecuteRes.$Properties} P_BattleExecuteRes.$Shape
     */

    /**
     * Constructs a new P_BattleExecuteRes.
     * @exports P_BattleExecuteRes
     * @classdesc Represents a P_BattleExecuteRes.
     * @constructor
     * @param {P_BattleExecuteRes.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleExecuteRes = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleExecuteRes req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_BattleExecuteRes
     * @instance
     */
    P_BattleExecuteRes.prototype.req_id = null;

    /**
     * P_BattleExecuteRes ret.
     * @member {number|null|undefined} ret
     * @memberof P_BattleExecuteRes
     * @instance
     */
    P_BattleExecuteRes.prototype.ret = null;

    /**
     * P_BattleExecuteRes battle_result.
     * @member {PB_BattleResult.$Properties|null|undefined} battle_result
     * @memberof P_BattleExecuteRes
     * @instance
     */
    P_BattleExecuteRes.prototype.battle_result = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteRes.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteRes.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleExecuteRes.prototype, "_battle_result", {
        get: $util.oneOfGetter($oneOfFields = ["battle_result"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleExecuteRes instance using the specified properties.
     * @function create
     * @memberof P_BattleExecuteRes
     * @static
     * @param {P_BattleExecuteRes.$Properties=} [properties] Properties to set
     * @returns {P_BattleExecuteRes} P_BattleExecuteRes instance
     * @type {{
     *   (properties: P_BattleExecuteRes.$Shape): P_BattleExecuteRes & P_BattleExecuteRes.$Shape;
     *   (properties?: P_BattleExecuteRes.$Properties): P_BattleExecuteRes;
     * }}
     */
    P_BattleExecuteRes.create = function(properties) {
        return new P_BattleExecuteRes(properties);
    };

    /**
     * Encodes the specified P_BattleExecuteRes message. Does not implicitly {@link P_BattleExecuteRes.verify|verify} messages.
     * @function encode
     * @memberof P_BattleExecuteRes
     * @static
     * @param {P_BattleExecuteRes.$Properties} message P_BattleExecuteRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleExecuteRes.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_id);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ret);
        if (message.battle_result != null && $Object.hasOwnProperty.call(message, "battle_result"))
            $root.PB_BattleResult.encode(message.battle_result, writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleExecuteRes message, length delimited. Does not implicitly {@link P_BattleExecuteRes.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleExecuteRes
     * @static
     * @param {P_BattleExecuteRes.$Properties} message P_BattleExecuteRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleExecuteRes.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleExecuteRes message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleExecuteRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleExecuteRes & P_BattleExecuteRes.$Shape} P_BattleExecuteRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleExecuteRes.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleExecuteRes();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.battle_result = $root.PB_BattleResult.decode(reader, reader.uint32(), $undefined, _depth + 1, message.battle_result);
                    message._battle_result = "battle_result";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleExecuteRes message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleExecuteRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleExecuteRes & P_BattleExecuteRes.$Shape} P_BattleExecuteRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleExecuteRes.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleExecuteRes message.
     * @function verify
     * @memberof P_BattleExecuteRes
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleExecuteRes.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.battle_result != null && $Object.hasOwnProperty.call(message, "battle_result")) {
            properties._battle_result = 1;
            {
                let error = $root.PB_BattleResult.verify(message.battle_result, _depth + 1);
                if (error)
                    return "battle_result." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_BattleExecuteRes message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleExecuteRes
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleExecuteRes} P_BattleExecuteRes
     */
    P_BattleExecuteRes.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleExecuteRes)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleExecuteRes: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleExecuteRes();
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.battle_result != null) {
            if (!$util.isObject(object.battle_result))
                throw $TypeError(".P_BattleExecuteRes.battle_result: object expected");
            message.battle_result = $root.PB_BattleResult.fromObject(object.battle_result, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_BattleExecuteRes message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleExecuteRes
     * @static
     * @param {P_BattleExecuteRes} message P_BattleExecuteRes
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleExecuteRes.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.battle_result != null && $Object.hasOwnProperty.call(message, "battle_result"))
            object.battle_result = $root.PB_BattleResult.toObject(message.battle_result, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_BattleExecuteRes to JSON.
     * @function toJSON
     * @memberof P_BattleExecuteRes
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleExecuteRes.prototype.toJSON = function() {
        return P_BattleExecuteRes.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleExecuteRes
     * @function getTypeUrl
     * @memberof P_BattleExecuteRes
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleExecuteRes.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleExecuteRes";
    };

    return P_BattleExecuteRes;
})();

export const P_BattleCancelReq = $root.P_BattleCancelReq = (() => {

    /**
     * Properties of a P_BattleCancelReq.
     * @typedef {Object} P_BattleCancelReq.$Properties
     * @property {number|null} [battle_id] P_BattleCancelReq battle_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleCancelReq.
     * @exports IP_BattleCancelReq
     * @interface IP_BattleCancelReq
     * @augments P_BattleCancelReq.$Properties
     * @deprecated Use P_BattleCancelReq.$Properties instead.
     */

    /**
     * Shape of a P_BattleCancelReq.
     * @typedef {P_BattleCancelReq.$Properties} P_BattleCancelReq.$Shape
     */

    /**
     * Constructs a new P_BattleCancelReq.
     * @exports P_BattleCancelReq
     * @classdesc Represents a P_BattleCancelReq.
     * @constructor
     * @param {P_BattleCancelReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleCancelReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleCancelReq battle_id.
     * @member {number|null|undefined} battle_id
     * @memberof P_BattleCancelReq
     * @instance
     */
    P_BattleCancelReq.prototype.battle_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleCancelReq.prototype, "_battle_id", {
        get: $util.oneOfGetter($oneOfFields = ["battle_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleCancelReq instance using the specified properties.
     * @function create
     * @memberof P_BattleCancelReq
     * @static
     * @param {P_BattleCancelReq.$Properties=} [properties] Properties to set
     * @returns {P_BattleCancelReq} P_BattleCancelReq instance
     * @type {{
     *   (properties: P_BattleCancelReq.$Shape): P_BattleCancelReq & P_BattleCancelReq.$Shape;
     *   (properties?: P_BattleCancelReq.$Properties): P_BattleCancelReq;
     * }}
     */
    P_BattleCancelReq.create = function(properties) {
        return new P_BattleCancelReq(properties);
    };

    /**
     * Encodes the specified P_BattleCancelReq message. Does not implicitly {@link P_BattleCancelReq.verify|verify} messages.
     * @function encode
     * @memberof P_BattleCancelReq
     * @static
     * @param {P_BattleCancelReq.$Properties} message P_BattleCancelReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleCancelReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.battle_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleCancelReq message, length delimited. Does not implicitly {@link P_BattleCancelReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleCancelReq
     * @static
     * @param {P_BattleCancelReq.$Properties} message P_BattleCancelReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleCancelReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleCancelReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleCancelReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleCancelReq & P_BattleCancelReq.$Shape} P_BattleCancelReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleCancelReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleCancelReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.battle_id = reader.int32();
                    message._battle_id = "battle_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleCancelReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleCancelReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleCancelReq & P_BattleCancelReq.$Shape} P_BattleCancelReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleCancelReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleCancelReq message.
     * @function verify
     * @memberof P_BattleCancelReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleCancelReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id")) {
            properties._battle_id = 1;
            if (!$util.isInteger(message.battle_id))
                return "battle_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleCancelReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleCancelReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleCancelReq} P_BattleCancelReq
     */
    P_BattleCancelReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleCancelReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleCancelReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleCancelReq();
        if (object.battle_id != null)
            message.battle_id = object.battle_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleCancelReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleCancelReq
     * @static
     * @param {P_BattleCancelReq} message P_BattleCancelReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleCancelReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            object.battle_id = message.battle_id;
        return object;
    };

    /**
     * Converts this P_BattleCancelReq to JSON.
     * @function toJSON
     * @memberof P_BattleCancelReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleCancelReq.prototype.toJSON = function() {
        return P_BattleCancelReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleCancelReq
     * @function getTypeUrl
     * @memberof P_BattleCancelReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleCancelReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleCancelReq";
    };

    return P_BattleCancelReq;
})();

export const P_BattleQueryReq = $root.P_BattleQueryReq = (() => {

    /**
     * Properties of a P_BattleQueryReq.
     * @typedef {Object} P_BattleQueryReq.$Properties
     * @property {number|null} [req_id] P_BattleQueryReq req_id
     * @property {number|null} [battle_id] P_BattleQueryReq battle_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleQueryReq.
     * @exports IP_BattleQueryReq
     * @interface IP_BattleQueryReq
     * @augments P_BattleQueryReq.$Properties
     * @deprecated Use P_BattleQueryReq.$Properties instead.
     */

    /**
     * Shape of a P_BattleQueryReq.
     * @typedef {P_BattleQueryReq.$Properties} P_BattleQueryReq.$Shape
     */

    /**
     * Constructs a new P_BattleQueryReq.
     * @exports P_BattleQueryReq
     * @classdesc Represents a P_BattleQueryReq.
     * @constructor
     * @param {P_BattleQueryReq.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleQueryReq = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleQueryReq req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_BattleQueryReq
     * @instance
     */
    P_BattleQueryReq.prototype.req_id = null;

    /**
     * P_BattleQueryReq battle_id.
     * @member {number|null|undefined} battle_id
     * @memberof P_BattleQueryReq
     * @instance
     */
    P_BattleQueryReq.prototype.battle_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryReq.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryReq.prototype, "_battle_id", {
        get: $util.oneOfGetter($oneOfFields = ["battle_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleQueryReq instance using the specified properties.
     * @function create
     * @memberof P_BattleQueryReq
     * @static
     * @param {P_BattleQueryReq.$Properties=} [properties] Properties to set
     * @returns {P_BattleQueryReq} P_BattleQueryReq instance
     * @type {{
     *   (properties: P_BattleQueryReq.$Shape): P_BattleQueryReq & P_BattleQueryReq.$Shape;
     *   (properties?: P_BattleQueryReq.$Properties): P_BattleQueryReq;
     * }}
     */
    P_BattleQueryReq.create = function(properties) {
        return new P_BattleQueryReq(properties);
    };

    /**
     * Encodes the specified P_BattleQueryReq message. Does not implicitly {@link P_BattleQueryReq.verify|verify} messages.
     * @function encode
     * @memberof P_BattleQueryReq
     * @static
     * @param {P_BattleQueryReq.$Properties} message P_BattleQueryReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleQueryReq.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_id);
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.battle_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleQueryReq message, length delimited. Does not implicitly {@link P_BattleQueryReq.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleQueryReq
     * @static
     * @param {P_BattleQueryReq.$Properties} message P_BattleQueryReq message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleQueryReq.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleQueryReq message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleQueryReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleQueryReq & P_BattleQueryReq.$Shape} P_BattleQueryReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleQueryReq.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleQueryReq();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.battle_id = reader.int32();
                    message._battle_id = "battle_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleQueryReq message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleQueryReq
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleQueryReq & P_BattleQueryReq.$Shape} P_BattleQueryReq
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleQueryReq.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleQueryReq message.
     * @function verify
     * @memberof P_BattleQueryReq
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleQueryReq.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id")) {
            properties._battle_id = 1;
            if (!$util.isInteger(message.battle_id))
                return "battle_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleQueryReq message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleQueryReq
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleQueryReq} P_BattleQueryReq
     */
    P_BattleQueryReq.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleQueryReq)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleQueryReq: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleQueryReq();
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        if (object.battle_id != null)
            message.battle_id = object.battle_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleQueryReq message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleQueryReq
     * @static
     * @param {P_BattleQueryReq} message P_BattleQueryReq
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleQueryReq.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            object.battle_id = message.battle_id;
        return object;
    };

    /**
     * Converts this P_BattleQueryReq to JSON.
     * @function toJSON
     * @memberof P_BattleQueryReq
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleQueryReq.prototype.toJSON = function() {
        return P_BattleQueryReq.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleQueryReq
     * @function getTypeUrl
     * @memberof P_BattleQueryReq
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleQueryReq.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleQueryReq";
    };

    return P_BattleQueryReq;
})();

export const P_BattleQueryRes = $root.P_BattleQueryRes = (() => {

    /**
     * Properties of a P_BattleQueryRes.
     * @typedef {Object} P_BattleQueryRes.$Properties
     * @property {number|null} [req_id] P_BattleQueryRes req_id
     * @property {number|null} [battle_id] P_BattleQueryRes battle_id
     * @property {PB_BattlePhase|null} [phase] P_BattleQueryRes phase
     * @property {boolean|null} [is_active] P_BattleQueryRes is_active
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleQueryRes.
     * @exports IP_BattleQueryRes
     * @interface IP_BattleQueryRes
     * @augments P_BattleQueryRes.$Properties
     * @deprecated Use P_BattleQueryRes.$Properties instead.
     */

    /**
     * Shape of a P_BattleQueryRes.
     * @typedef {P_BattleQueryRes.$Properties} P_BattleQueryRes.$Shape
     */

    /**
     * Constructs a new P_BattleQueryRes.
     * @exports P_BattleQueryRes
     * @classdesc Represents a P_BattleQueryRes.
     * @constructor
     * @param {P_BattleQueryRes.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleQueryRes = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleQueryRes req_id.
     * @member {number|null|undefined} req_id
     * @memberof P_BattleQueryRes
     * @instance
     */
    P_BattleQueryRes.prototype.req_id = null;

    /**
     * P_BattleQueryRes battle_id.
     * @member {number|null|undefined} battle_id
     * @memberof P_BattleQueryRes
     * @instance
     */
    P_BattleQueryRes.prototype.battle_id = null;

    /**
     * P_BattleQueryRes phase.
     * @member {PB_BattlePhase|null|undefined} phase
     * @memberof P_BattleQueryRes
     * @instance
     */
    P_BattleQueryRes.prototype.phase = null;

    /**
     * P_BattleQueryRes is_active.
     * @member {boolean|null|undefined} is_active
     * @memberof P_BattleQueryRes
     * @instance
     */
    P_BattleQueryRes.prototype.is_active = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryRes.prototype, "_req_id", {
        get: $util.oneOfGetter($oneOfFields = ["req_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryRes.prototype, "_battle_id", {
        get: $util.oneOfGetter($oneOfFields = ["battle_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryRes.prototype, "_phase", {
        get: $util.oneOfGetter($oneOfFields = ["phase"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleQueryRes.prototype, "_is_active", {
        get: $util.oneOfGetter($oneOfFields = ["is_active"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleQueryRes instance using the specified properties.
     * @function create
     * @memberof P_BattleQueryRes
     * @static
     * @param {P_BattleQueryRes.$Properties=} [properties] Properties to set
     * @returns {P_BattleQueryRes} P_BattleQueryRes instance
     * @type {{
     *   (properties: P_BattleQueryRes.$Shape): P_BattleQueryRes & P_BattleQueryRes.$Shape;
     *   (properties?: P_BattleQueryRes.$Properties): P_BattleQueryRes;
     * }}
     */
    P_BattleQueryRes.create = function(properties) {
        return new P_BattleQueryRes(properties);
    };

    /**
     * Encodes the specified P_BattleQueryRes message. Does not implicitly {@link P_BattleQueryRes.verify|verify} messages.
     * @function encode
     * @memberof P_BattleQueryRes
     * @static
     * @param {P_BattleQueryRes.$Properties} message P_BattleQueryRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleQueryRes.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_id);
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.battle_id);
        if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.phase);
        if (message.is_active != null && $Object.hasOwnProperty.call(message, "is_active"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.is_active);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleQueryRes message, length delimited. Does not implicitly {@link P_BattleQueryRes.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleQueryRes
     * @static
     * @param {P_BattleQueryRes.$Properties} message P_BattleQueryRes message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleQueryRes.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleQueryRes message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleQueryRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleQueryRes & P_BattleQueryRes.$Shape} P_BattleQueryRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleQueryRes.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleQueryRes(), value;
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_id = reader.int32();
                    message._req_id = "req_id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.battle_id = reader.int32();
                    message._battle_id = "battle_id";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.phase = reader.int32();
                    message._phase = "phase";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.is_active = reader.bool();
                    message._is_active = "is_active";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleQueryRes message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleQueryRes
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleQueryRes & P_BattleQueryRes.$Shape} P_BattleQueryRes
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleQueryRes.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleQueryRes message.
     * @function verify
     * @memberof P_BattleQueryRes
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleQueryRes.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id")) {
            properties._req_id = 1;
            if (!$util.isInteger(message.req_id))
                return "req_id: integer expected";
        }
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id")) {
            properties._battle_id = 1;
            if (!$util.isInteger(message.battle_id))
                return "battle_id: integer expected";
        }
        if (message.phase != null && $Object.hasOwnProperty.call(message, "phase")) {
            properties._phase = 1;
            if (typeof message.phase !== "number" || (message.phase | 0) !== message.phase)
                return "phase: enum value expected";
        }
        if (message.is_active != null && $Object.hasOwnProperty.call(message, "is_active")) {
            properties._is_active = 1;
            if (typeof message.is_active !== "boolean")
                return "is_active: boolean expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleQueryRes message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleQueryRes
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleQueryRes} P_BattleQueryRes
     */
    P_BattleQueryRes.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleQueryRes)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleQueryRes: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleQueryRes();
        if (object.req_id != null)
            message.req_id = object.req_id | 0;
        if (object.battle_id != null)
            message.battle_id = object.battle_id | 0;
        switch (object.phase) {
        case "BATTLE_PHASE_NONE":
        case 0:
            message.phase = 0;
            break;
        case "BATTLE_PHASE_PREPARING":
        case 1:
            message.phase = 1;
            break;
        case "BATTLE_PHASE_STARTED":
        case 2:
            message.phase = 2;
            break;
        case "BATTLE_PHASE_ENDING":
        case 3:
            message.phase = 3;
            break;
        case "BATTLE_PHASE_FINISHED":
        case 4:
            message.phase = 4;
            break;
        default:
            if (typeof object.phase === "number" && (object.phase | 0) === object.phase)
                message.phase = object.phase;
        }
        if (object.is_active != null)
            message.is_active = $Boolean(object.is_active);
        return message;
    };

    /**
     * Creates a plain object from a P_BattleQueryRes message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleQueryRes
     * @static
     * @param {P_BattleQueryRes} message P_BattleQueryRes
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleQueryRes.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_id != null && $Object.hasOwnProperty.call(message, "req_id"))
            object.req_id = message.req_id;
        if (message.battle_id != null && $Object.hasOwnProperty.call(message, "battle_id"))
            object.battle_id = message.battle_id;
        if (message.phase != null && $Object.hasOwnProperty.call(message, "phase"))
            object.phase = options.enums === $String ? $root.PB_BattlePhase[message.phase] === $undefined ? message.phase : $root.PB_BattlePhase[message.phase] : message.phase;
        if (message.is_active != null && $Object.hasOwnProperty.call(message, "is_active"))
            object.is_active = message.is_active;
        return object;
    };

    /**
     * Converts this P_BattleQueryRes to JSON.
     * @function toJSON
     * @memberof P_BattleQueryRes
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleQueryRes.prototype.toJSON = function() {
        return P_BattleQueryRes.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleQueryRes
     * @function getTypeUrl
     * @memberof P_BattleQueryRes
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleQueryRes.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleQueryRes";
    };

    return P_BattleQueryRes;
})();

export const P_BattleCsServerRegister = $root.P_BattleCsServerRegister = (() => {

    /**
     * Properties of a P_BattleCsServerRegister.
     * @typedef {Object} P_BattleCsServerRegister.$Properties
     * @property {number|null} [id] P_BattleCsServerRegister id
     * @property {number|null} [weight] P_BattleCsServerRegister weight
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleCsServerRegister.
     * @exports IP_BattleCsServerRegister
     * @interface IP_BattleCsServerRegister
     * @augments P_BattleCsServerRegister.$Properties
     * @deprecated Use P_BattleCsServerRegister.$Properties instead.
     */

    /**
     * Shape of a P_BattleCsServerRegister.
     * @typedef {P_BattleCsServerRegister.$Properties} P_BattleCsServerRegister.$Shape
     */

    /**
     * Constructs a new P_BattleCsServerRegister.
     * @exports P_BattleCsServerRegister
     * @classdesc Represents a P_BattleCsServerRegister.
     * @constructor
     * @param {P_BattleCsServerRegister.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleCsServerRegister = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleCsServerRegister id.
     * @member {number|null|undefined} id
     * @memberof P_BattleCsServerRegister
     * @instance
     */
    P_BattleCsServerRegister.prototype.id = null;

    /**
     * P_BattleCsServerRegister weight.
     * @member {number|null|undefined} weight
     * @memberof P_BattleCsServerRegister
     * @instance
     */
    P_BattleCsServerRegister.prototype.weight = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleCsServerRegister.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleCsServerRegister.prototype, "_weight", {
        get: $util.oneOfGetter($oneOfFields = ["weight"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleCsServerRegister instance using the specified properties.
     * @function create
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {P_BattleCsServerRegister.$Properties=} [properties] Properties to set
     * @returns {P_BattleCsServerRegister} P_BattleCsServerRegister instance
     * @type {{
     *   (properties: P_BattleCsServerRegister.$Shape): P_BattleCsServerRegister & P_BattleCsServerRegister.$Shape;
     *   (properties?: P_BattleCsServerRegister.$Properties): P_BattleCsServerRegister;
     * }}
     */
    P_BattleCsServerRegister.create = function(properties) {
        return new P_BattleCsServerRegister(properties);
    };

    /**
     * Encodes the specified P_BattleCsServerRegister message. Does not implicitly {@link P_BattleCsServerRegister.verify|verify} messages.
     * @function encode
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {P_BattleCsServerRegister.$Properties} message P_BattleCsServerRegister message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleCsServerRegister.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.id);
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.weight);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleCsServerRegister message, length delimited. Does not implicitly {@link P_BattleCsServerRegister.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {P_BattleCsServerRegister.$Properties} message P_BattleCsServerRegister message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleCsServerRegister.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleCsServerRegister message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleCsServerRegister & P_BattleCsServerRegister.$Shape} P_BattleCsServerRegister
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleCsServerRegister.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleCsServerRegister();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.weight = reader.int32();
                    message._weight = "weight";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleCsServerRegister message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleCsServerRegister & P_BattleCsServerRegister.$Shape} P_BattleCsServerRegister
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleCsServerRegister.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleCsServerRegister message.
     * @function verify
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleCsServerRegister.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight")) {
            properties._weight = 1;
            if (!$util.isInteger(message.weight))
                return "weight: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleCsServerRegister message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleCsServerRegister} P_BattleCsServerRegister
     */
    P_BattleCsServerRegister.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleCsServerRegister)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleCsServerRegister: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleCsServerRegister();
        if (object.id != null)
            message.id = object.id | 0;
        if (object.weight != null)
            message.weight = object.weight | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleCsServerRegister message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {P_BattleCsServerRegister} message P_BattleCsServerRegister
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleCsServerRegister.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.weight != null && $Object.hasOwnProperty.call(message, "weight"))
            object.weight = message.weight;
        return object;
    };

    /**
     * Converts this P_BattleCsServerRegister to JSON.
     * @function toJSON
     * @memberof P_BattleCsServerRegister
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleCsServerRegister.prototype.toJSON = function() {
        return P_BattleCsServerRegister.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleCsServerRegister
     * @function getTypeUrl
     * @memberof P_BattleCsServerRegister
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleCsServerRegister.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleCsServerRegister";
    };

    return P_BattleCsServerRegister;
})();

export const P_LoginReq_CS = $root.P_LoginReq_CS = (() => {

    /**
     * Properties of a P_LoginReq_CS.
     * @typedef {Object} P_LoginReq_CS.$Properties
     * @property {string|null} [login_user_name] P_LoginReq_CS login_user_name
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LoginReq_CS.
     * @exports IP_LoginReq_CS
     * @interface IP_LoginReq_CS
     * @augments P_LoginReq_CS.$Properties
     * @deprecated Use P_LoginReq_CS.$Properties instead.
     */

    /**
     * Shape of a P_LoginReq_CS.
     * @typedef {P_LoginReq_CS.$Properties} P_LoginReq_CS.$Shape
     */

    /**
     * Constructs a new P_LoginReq_CS.
     * @exports P_LoginReq_CS
     * @classdesc Represents a P_LoginReq_CS.
     * @constructor
     * @param {P_LoginReq_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LoginReq_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LoginReq_CS login_user_name.
     * @member {string|null|undefined} login_user_name
     * @memberof P_LoginReq_CS
     * @instance
     */
    P_LoginReq_CS.prototype.login_user_name = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LoginReq_CS.prototype, "_login_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["login_user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LoginReq_CS instance using the specified properties.
     * @function create
     * @memberof P_LoginReq_CS
     * @static
     * @param {P_LoginReq_CS.$Properties=} [properties] Properties to set
     * @returns {P_LoginReq_CS} P_LoginReq_CS instance
     * @type {{
     *   (properties: P_LoginReq_CS.$Shape): P_LoginReq_CS & P_LoginReq_CS.$Shape;
     *   (properties?: P_LoginReq_CS.$Properties): P_LoginReq_CS;
     * }}
     */
    P_LoginReq_CS.create = function(properties) {
        return new P_LoginReq_CS(properties);
    };

    /**
     * Encodes the specified P_LoginReq_CS message. Does not implicitly {@link P_LoginReq_CS.verify|verify} messages.
     * @function encode
     * @memberof P_LoginReq_CS
     * @static
     * @param {P_LoginReq_CS.$Properties} message P_LoginReq_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LoginReq_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.login_user_name != null && $Object.hasOwnProperty.call(message, "login_user_name"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.login_user_name);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LoginReq_CS message, length delimited. Does not implicitly {@link P_LoginReq_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LoginReq_CS
     * @static
     * @param {P_LoginReq_CS.$Properties} message P_LoginReq_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LoginReq_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LoginReq_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_LoginReq_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LoginReq_CS & P_LoginReq_CS.$Shape} P_LoginReq_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LoginReq_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LoginReq_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.login_user_name = reader.stringVerify();
                    message._login_user_name = "login_user_name";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LoginReq_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LoginReq_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LoginReq_CS & P_LoginReq_CS.$Shape} P_LoginReq_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LoginReq_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LoginReq_CS message.
     * @function verify
     * @memberof P_LoginReq_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LoginReq_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.login_user_name != null && $Object.hasOwnProperty.call(message, "login_user_name")) {
            properties._login_user_name = 1;
            if (!$util.isString(message.login_user_name))
                return "login_user_name: string expected";
        }
        return null;
    };

    /**
     * Creates a P_LoginReq_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LoginReq_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LoginReq_CS} P_LoginReq_CS
     */
    P_LoginReq_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LoginReq_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LoginReq_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LoginReq_CS();
        if (object.login_user_name != null)
            message.login_user_name = $String(object.login_user_name);
        return message;
    };

    /**
     * Creates a plain object from a P_LoginReq_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LoginReq_CS
     * @static
     * @param {P_LoginReq_CS} message P_LoginReq_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LoginReq_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.login_user_name != null && $Object.hasOwnProperty.call(message, "login_user_name"))
            object.login_user_name = message.login_user_name;
        return object;
    };

    /**
     * Converts this P_LoginReq_CS to JSON.
     * @function toJSON
     * @memberof P_LoginReq_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LoginReq_CS.prototype.toJSON = function() {
        return P_LoginReq_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LoginReq_CS
     * @function getTypeUrl
     * @memberof P_LoginReq_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LoginReq_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LoginReq_CS";
    };

    return P_LoginReq_CS;
})();

export const P_LoginReq_SC = $root.P_LoginReq_SC = (() => {

    /**
     * Properties of a P_LoginReq_SC.
     * @typedef {Object} P_LoginReq_SC.$Properties
     * @property {string|null} [error_message] P_LoginReq_SC error_message
     * @property {string|null} [token] P_LoginReq_SC token
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_LoginReq_SC.
     * @exports IP_LoginReq_SC
     * @interface IP_LoginReq_SC
     * @augments P_LoginReq_SC.$Properties
     * @deprecated Use P_LoginReq_SC.$Properties instead.
     */

    /**
     * Shape of a P_LoginReq_SC.
     * @typedef {P_LoginReq_SC.$Properties} P_LoginReq_SC.$Shape
     */

    /**
     * Constructs a new P_LoginReq_SC.
     * @exports P_LoginReq_SC
     * @classdesc Represents a P_LoginReq_SC.
     * @constructor
     * @param {P_LoginReq_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_LoginReq_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_LoginReq_SC error_message.
     * @member {string|null|undefined} error_message
     * @memberof P_LoginReq_SC
     * @instance
     */
    P_LoginReq_SC.prototype.error_message = null;

    /**
     * P_LoginReq_SC token.
     * @member {string|null|undefined} token
     * @memberof P_LoginReq_SC
     * @instance
     */
    P_LoginReq_SC.prototype.token = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LoginReq_SC.prototype, "_error_message", {
        get: $util.oneOfGetter($oneOfFields = ["error_message"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_LoginReq_SC.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_LoginReq_SC instance using the specified properties.
     * @function create
     * @memberof P_LoginReq_SC
     * @static
     * @param {P_LoginReq_SC.$Properties=} [properties] Properties to set
     * @returns {P_LoginReq_SC} P_LoginReq_SC instance
     * @type {{
     *   (properties: P_LoginReq_SC.$Shape): P_LoginReq_SC & P_LoginReq_SC.$Shape;
     *   (properties?: P_LoginReq_SC.$Properties): P_LoginReq_SC;
     * }}
     */
    P_LoginReq_SC.create = function(properties) {
        return new P_LoginReq_SC(properties);
    };

    /**
     * Encodes the specified P_LoginReq_SC message. Does not implicitly {@link P_LoginReq_SC.verify|verify} messages.
     * @function encode
     * @memberof P_LoginReq_SC
     * @static
     * @param {P_LoginReq_SC.$Properties} message P_LoginReq_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LoginReq_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.error_message != null && $Object.hasOwnProperty.call(message, "error_message"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.error_message);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.token);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_LoginReq_SC message, length delimited. Does not implicitly {@link P_LoginReq_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_LoginReq_SC
     * @static
     * @param {P_LoginReq_SC.$Properties} message P_LoginReq_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_LoginReq_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_LoginReq_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_LoginReq_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_LoginReq_SC & P_LoginReq_SC.$Shape} P_LoginReq_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LoginReq_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_LoginReq_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.error_message = reader.stringVerify();
                    message._error_message = "error_message";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.token = reader.stringVerify();
                    message._token = "token";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_LoginReq_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_LoginReq_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_LoginReq_SC & P_LoginReq_SC.$Shape} P_LoginReq_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_LoginReq_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_LoginReq_SC message.
     * @function verify
     * @memberof P_LoginReq_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_LoginReq_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.error_message != null && $Object.hasOwnProperty.call(message, "error_message")) {
            properties._error_message = 1;
            if (!$util.isString(message.error_message))
                return "error_message: string expected";
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isString(message.token))
                return "token: string expected";
        }
        return null;
    };

    /**
     * Creates a P_LoginReq_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_LoginReq_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_LoginReq_SC} P_LoginReq_SC
     */
    P_LoginReq_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_LoginReq_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_LoginReq_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_LoginReq_SC();
        if (object.error_message != null)
            message.error_message = $String(object.error_message);
        if (object.token != null)
            message.token = $String(object.token);
        return message;
    };

    /**
     * Creates a plain object from a P_LoginReq_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_LoginReq_SC
     * @static
     * @param {P_LoginReq_SC} message P_LoginReq_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_LoginReq_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.error_message != null && $Object.hasOwnProperty.call(message, "error_message"))
            object.error_message = message.error_message;
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        return object;
    };

    /**
     * Converts this P_LoginReq_SC to JSON.
     * @function toJSON
     * @memberof P_LoginReq_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_LoginReq_SC.prototype.toJSON = function() {
        return P_LoginReq_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_LoginReq_SC
     * @function getTypeUrl
     * @memberof P_LoginReq_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_LoginReq_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_LoginReq_SC";
    };

    return P_LoginReq_SC;
})();

export const P_GM_CS = $root.P_GM_CS = (() => {

    /**
     * Properties of a P_GM_CS.
     * @typedef {Object} P_GM_CS.$Properties
     * @property {string|null} [params] P_GM_CS params
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GM_CS.
     * @exports IP_GM_CS
     * @interface IP_GM_CS
     * @augments P_GM_CS.$Properties
     * @deprecated Use P_GM_CS.$Properties instead.
     */

    /**
     * Shape of a P_GM_CS.
     * @typedef {P_GM_CS.$Properties} P_GM_CS.$Shape
     */

    /**
     * Constructs a new P_GM_CS.
     * @exports P_GM_CS
     * @classdesc Represents a P_GM_CS.
     * @constructor
     * @param {P_GM_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GM_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GM_CS params.
     * @member {string|null|undefined} params
     * @memberof P_GM_CS
     * @instance
     */
    P_GM_CS.prototype.params = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GM_CS.prototype, "_params", {
        get: $util.oneOfGetter($oneOfFields = ["params"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GM_CS instance using the specified properties.
     * @function create
     * @memberof P_GM_CS
     * @static
     * @param {P_GM_CS.$Properties=} [properties] Properties to set
     * @returns {P_GM_CS} P_GM_CS instance
     * @type {{
     *   (properties: P_GM_CS.$Shape): P_GM_CS & P_GM_CS.$Shape;
     *   (properties?: P_GM_CS.$Properties): P_GM_CS;
     * }}
     */
    P_GM_CS.create = function(properties) {
        return new P_GM_CS(properties);
    };

    /**
     * Encodes the specified P_GM_CS message. Does not implicitly {@link P_GM_CS.verify|verify} messages.
     * @function encode
     * @memberof P_GM_CS
     * @static
     * @param {P_GM_CS.$Properties} message P_GM_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GM_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.params);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GM_CS message, length delimited. Does not implicitly {@link P_GM_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GM_CS
     * @static
     * @param {P_GM_CS.$Properties} message P_GM_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GM_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GM_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_GM_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GM_CS & P_GM_CS.$Shape} P_GM_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GM_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GM_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.params = reader.stringVerify();
                    message._params = "params";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GM_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GM_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GM_CS & P_GM_CS.$Shape} P_GM_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GM_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GM_CS message.
     * @function verify
     * @memberof P_GM_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GM_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
            properties._params = 1;
            if (!$util.isString(message.params))
                return "params: string expected";
        }
        return null;
    };

    /**
     * Creates a P_GM_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GM_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GM_CS} P_GM_CS
     */
    P_GM_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GM_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GM_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GM_CS();
        if (object.params != null)
            message.params = $String(object.params);
        return message;
    };

    /**
     * Creates a plain object from a P_GM_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GM_CS
     * @static
     * @param {P_GM_CS} message P_GM_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GM_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            object.params = message.params;
        return object;
    };

    /**
     * Converts this P_GM_CS to JSON.
     * @function toJSON
     * @memberof P_GM_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GM_CS.prototype.toJSON = function() {
        return P_GM_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GM_CS
     * @function getTypeUrl
     * @memberof P_GM_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GM_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GM_CS";
    };

    return P_GM_CS;
})();

export const P_GM_SC = $root.P_GM_SC = (() => {

    /**
     * Properties of a P_GM_SC.
     * @typedef {Object} P_GM_SC.$Properties
     * @property {string|null} [params] P_GM_SC params
     * @property {number|null} [ret] P_GM_SC ret
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GM_SC.
     * @exports IP_GM_SC
     * @interface IP_GM_SC
     * @augments P_GM_SC.$Properties
     * @deprecated Use P_GM_SC.$Properties instead.
     */

    /**
     * Shape of a P_GM_SC.
     * @typedef {P_GM_SC.$Properties} P_GM_SC.$Shape
     */

    /**
     * Constructs a new P_GM_SC.
     * @exports P_GM_SC
     * @classdesc Represents a P_GM_SC.
     * @constructor
     * @param {P_GM_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GM_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GM_SC params.
     * @member {string|null|undefined} params
     * @memberof P_GM_SC
     * @instance
     */
    P_GM_SC.prototype.params = null;

    /**
     * P_GM_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_GM_SC
     * @instance
     */
    P_GM_SC.prototype.ret = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GM_SC.prototype, "_params", {
        get: $util.oneOfGetter($oneOfFields = ["params"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GM_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GM_SC instance using the specified properties.
     * @function create
     * @memberof P_GM_SC
     * @static
     * @param {P_GM_SC.$Properties=} [properties] Properties to set
     * @returns {P_GM_SC} P_GM_SC instance
     * @type {{
     *   (properties: P_GM_SC.$Shape): P_GM_SC & P_GM_SC.$Shape;
     *   (properties?: P_GM_SC.$Properties): P_GM_SC;
     * }}
     */
    P_GM_SC.create = function(properties) {
        return new P_GM_SC(properties);
    };

    /**
     * Encodes the specified P_GM_SC message. Does not implicitly {@link P_GM_SC.verify|verify} messages.
     * @function encode
     * @memberof P_GM_SC
     * @static
     * @param {P_GM_SC.$Properties} message P_GM_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GM_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            writer.uint32(/* id 1, wireType 2 =*/10).string(message.params);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ret);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GM_SC message, length delimited. Does not implicitly {@link P_GM_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GM_SC
     * @static
     * @param {P_GM_SC.$Properties} message P_GM_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GM_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GM_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_GM_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GM_SC & P_GM_SC.$Shape} P_GM_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GM_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GM_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.params = reader.stringVerify();
                    message._params = "params";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GM_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GM_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GM_SC & P_GM_SC.$Shape} P_GM_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GM_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GM_SC message.
     * @function verify
     * @memberof P_GM_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GM_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
            properties._params = 1;
            if (!$util.isString(message.params))
                return "params: string expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_GM_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GM_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GM_SC} P_GM_SC
     */
    P_GM_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GM_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GM_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GM_SC();
        if (object.params != null)
            message.params = $String(object.params);
        if (object.ret != null)
            message.ret = object.ret | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_GM_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GM_SC
     * @static
     * @param {P_GM_SC} message P_GM_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GM_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            object.params = message.params;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        return object;
    };

    /**
     * Converts this P_GM_SC to JSON.
     * @function toJSON
     * @memberof P_GM_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GM_SC.prototype.toJSON = function() {
        return P_GM_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GM_SC
     * @function getTypeUrl
     * @memberof P_GM_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GM_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GM_SC";
    };

    return P_GM_SC;
})();

export const P_BattleServer_CS = $root.P_BattleServer_CS = (() => {

    /**
     * Properties of a P_BattleServer_CS.
     * @typedef {Object} P_BattleServer_CS.$Properties
     * @property {Array.<number>|null} [param] P_BattleServer_CS param
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServer_CS.
     * @exports IP_BattleServer_CS
     * @interface IP_BattleServer_CS
     * @augments P_BattleServer_CS.$Properties
     * @deprecated Use P_BattleServer_CS.$Properties instead.
     */

    /**
     * Shape of a P_BattleServer_CS.
     * @typedef {P_BattleServer_CS.$Properties} P_BattleServer_CS.$Shape
     */

    /**
     * Constructs a new P_BattleServer_CS.
     * @exports P_BattleServer_CS
     * @classdesc Represents a P_BattleServer_CS.
     * @constructor
     * @param {P_BattleServer_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServer_CS = function (properties) {
        this.param = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServer_CS param.
     * @member {Array.<number>} param
     * @memberof P_BattleServer_CS
     * @instance
     */
    P_BattleServer_CS.prototype.param = $util.emptyArray;

    /**
     * Creates a new P_BattleServer_CS instance using the specified properties.
     * @function create
     * @memberof P_BattleServer_CS
     * @static
     * @param {P_BattleServer_CS.$Properties=} [properties] Properties to set
     * @returns {P_BattleServer_CS} P_BattleServer_CS instance
     * @type {{
     *   (properties: P_BattleServer_CS.$Shape): P_BattleServer_CS & P_BattleServer_CS.$Shape;
     *   (properties?: P_BattleServer_CS.$Properties): P_BattleServer_CS;
     * }}
     */
    P_BattleServer_CS.create = function(properties) {
        return new P_BattleServer_CS(properties);
    };

    /**
     * Encodes the specified P_BattleServer_CS message. Does not implicitly {@link P_BattleServer_CS.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServer_CS
     * @static
     * @param {P_BattleServer_CS.$Properties} message P_BattleServer_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServer_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.param != null && message.param.length)
            writer.uint32(/* id 1, wireType 2 =*/10).int32s(message.param);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServer_CS message, length delimited. Does not implicitly {@link P_BattleServer_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServer_CS
     * @static
     * @param {P_BattleServer_CS.$Properties} message P_BattleServer_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServer_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServer_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServer_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServer_CS & P_BattleServer_CS.$Shape} P_BattleServer_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServer_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServer_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType === 2) {
                        if (!(message.param && message.param.length))
                            message.param = [];
                        reader.int32s(message.param);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.param && message.param.length))
                        message.param = [];
                    message.param.push(reader.int32());
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServer_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServer_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServer_CS & P_BattleServer_CS.$Shape} P_BattleServer_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServer_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServer_CS message.
     * @function verify
     * @memberof P_BattleServer_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServer_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        if (message.param != null && $Object.hasOwnProperty.call(message, "param")) {
            if (!$Array.isArray(message.param))
                return "param: array expected";
            for (let i = 0; i < message.param.length; ++i)
                if (!$util.isInteger(message.param[i]))
                    return "param: integer[] expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServer_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServer_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServer_CS} P_BattleServer_CS
     */
    P_BattleServer_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServer_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServer_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServer_CS();
        if (object.param) {
            if (!$Array.isArray(object.param))
                throw $TypeError(".P_BattleServer_CS.param: array expected");
            message.param = $Array(object.param.length);
            for (let i = 0; i < object.param.length; ++i)
                message.param[i] = object.param[i] | 0;
        }
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServer_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServer_CS
     * @static
     * @param {P_BattleServer_CS} message P_BattleServer_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServer_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.param = [];
        if (message.param && message.param.length) {
            object.param = $Array(message.param.length);
            for (let j = 0; j < message.param.length; ++j)
                object.param[j] = message.param[j];
        }
        return object;
    };

    /**
     * Converts this P_BattleServer_CS to JSON.
     * @function toJSON
     * @memberof P_BattleServer_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServer_CS.prototype.toJSON = function() {
        return P_BattleServer_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServer_CS
     * @function getTypeUrl
     * @memberof P_BattleServer_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServer_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServer_CS";
    };

    return P_BattleServer_CS;
})();

export const P_BattleServer_SC = $root.P_BattleServer_SC = (() => {

    /**
     * Properties of a P_BattleServer_SC.
     * @typedef {Object} P_BattleServer_SC.$Properties
     * @property {number|null} [ret] P_BattleServer_SC ret
     * @property {string|null} [server_ip] P_BattleServer_SC server_ip
     * @property {number|null} [server_port] P_BattleServer_SC server_port
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_BattleServer_SC.
     * @exports IP_BattleServer_SC
     * @interface IP_BattleServer_SC
     * @augments P_BattleServer_SC.$Properties
     * @deprecated Use P_BattleServer_SC.$Properties instead.
     */

    /**
     * Shape of a P_BattleServer_SC.
     * @typedef {P_BattleServer_SC.$Properties} P_BattleServer_SC.$Shape
     */

    /**
     * Constructs a new P_BattleServer_SC.
     * @exports P_BattleServer_SC
     * @classdesc Represents a P_BattleServer_SC.
     * @constructor
     * @param {P_BattleServer_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_BattleServer_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_BattleServer_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_BattleServer_SC
     * @instance
     */
    P_BattleServer_SC.prototype.ret = null;

    /**
     * P_BattleServer_SC server_ip.
     * @member {string|null|undefined} server_ip
     * @memberof P_BattleServer_SC
     * @instance
     */
    P_BattleServer_SC.prototype.server_ip = null;

    /**
     * P_BattleServer_SC server_port.
     * @member {number|null|undefined} server_port
     * @memberof P_BattleServer_SC
     * @instance
     */
    P_BattleServer_SC.prototype.server_port = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServer_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServer_SC.prototype, "_server_ip", {
        get: $util.oneOfGetter($oneOfFields = ["server_ip"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_BattleServer_SC.prototype, "_server_port", {
        get: $util.oneOfGetter($oneOfFields = ["server_port"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_BattleServer_SC instance using the specified properties.
     * @function create
     * @memberof P_BattleServer_SC
     * @static
     * @param {P_BattleServer_SC.$Properties=} [properties] Properties to set
     * @returns {P_BattleServer_SC} P_BattleServer_SC instance
     * @type {{
     *   (properties: P_BattleServer_SC.$Shape): P_BattleServer_SC & P_BattleServer_SC.$Shape;
     *   (properties?: P_BattleServer_SC.$Properties): P_BattleServer_SC;
     * }}
     */
    P_BattleServer_SC.create = function(properties) {
        return new P_BattleServer_SC(properties);
    };

    /**
     * Encodes the specified P_BattleServer_SC message. Does not implicitly {@link P_BattleServer_SC.verify|verify} messages.
     * @function encode
     * @memberof P_BattleServer_SC
     * @static
     * @param {P_BattleServer_SC.$Properties} message P_BattleServer_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServer_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret);
        if (message.server_ip != null && $Object.hasOwnProperty.call(message, "server_ip"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.server_ip);
        if (message.server_port != null && $Object.hasOwnProperty.call(message, "server_port"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.server_port);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_BattleServer_SC message, length delimited. Does not implicitly {@link P_BattleServer_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_BattleServer_SC
     * @static
     * @param {P_BattleServer_SC.$Properties} message P_BattleServer_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_BattleServer_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_BattleServer_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_BattleServer_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_BattleServer_SC & P_BattleServer_SC.$Shape} P_BattleServer_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServer_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_BattleServer_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.server_ip = reader.stringVerify();
                    message._server_ip = "server_ip";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.server_port = reader.int32();
                    message._server_port = "server_port";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_BattleServer_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_BattleServer_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_BattleServer_SC & P_BattleServer_SC.$Shape} P_BattleServer_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_BattleServer_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_BattleServer_SC message.
     * @function verify
     * @memberof P_BattleServer_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_BattleServer_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.server_ip != null && $Object.hasOwnProperty.call(message, "server_ip")) {
            properties._server_ip = 1;
            if (!$util.isString(message.server_ip))
                return "server_ip: string expected";
        }
        if (message.server_port != null && $Object.hasOwnProperty.call(message, "server_port")) {
            properties._server_port = 1;
            if (!$util.isInteger(message.server_port))
                return "server_port: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_BattleServer_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_BattleServer_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_BattleServer_SC} P_BattleServer_SC
     */
    P_BattleServer_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_BattleServer_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_BattleServer_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_BattleServer_SC();
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.server_ip != null)
            message.server_ip = $String(object.server_ip);
        if (object.server_port != null)
            message.server_port = object.server_port | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_BattleServer_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_BattleServer_SC
     * @static
     * @param {P_BattleServer_SC} message P_BattleServer_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_BattleServer_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.server_ip != null && $Object.hasOwnProperty.call(message, "server_ip"))
            object.server_ip = message.server_ip;
        if (message.server_port != null && $Object.hasOwnProperty.call(message, "server_port"))
            object.server_port = message.server_port;
        return object;
    };

    /**
     * Converts this P_BattleServer_SC to JSON.
     * @function toJSON
     * @memberof P_BattleServer_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_BattleServer_SC.prototype.toJSON = function() {
        return P_BattleServer_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_BattleServer_SC
     * @function getTypeUrl
     * @memberof P_BattleServer_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_BattleServer_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_BattleServer_SC";
    };

    return P_BattleServer_SC;
})();

export const P_UserHeartbeat = $root.P_UserHeartbeat = (() => {

    /**
     * Properties of a P_UserHeartbeat.
     * @typedef {Object} P_UserHeartbeat.$Properties
     * @property {number|null} [time] P_UserHeartbeat time
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_UserHeartbeat.
     * @exports IP_UserHeartbeat
     * @interface IP_UserHeartbeat
     * @augments P_UserHeartbeat.$Properties
     * @deprecated Use P_UserHeartbeat.$Properties instead.
     */

    /**
     * Shape of a P_UserHeartbeat.
     * @typedef {P_UserHeartbeat.$Properties} P_UserHeartbeat.$Shape
     */

    /**
     * Constructs a new P_UserHeartbeat.
     * @exports P_UserHeartbeat
     * @classdesc Represents a P_UserHeartbeat.
     * @constructor
     * @param {P_UserHeartbeat.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_UserHeartbeat = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_UserHeartbeat time.
     * @member {number|null|undefined} time
     * @memberof P_UserHeartbeat
     * @instance
     */
    P_UserHeartbeat.prototype.time = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_UserHeartbeat.prototype, "_time", {
        get: $util.oneOfGetter($oneOfFields = ["time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_UserHeartbeat instance using the specified properties.
     * @function create
     * @memberof P_UserHeartbeat
     * @static
     * @param {P_UserHeartbeat.$Properties=} [properties] Properties to set
     * @returns {P_UserHeartbeat} P_UserHeartbeat instance
     * @type {{
     *   (properties: P_UserHeartbeat.$Shape): P_UserHeartbeat & P_UserHeartbeat.$Shape;
     *   (properties?: P_UserHeartbeat.$Properties): P_UserHeartbeat;
     * }}
     */
    P_UserHeartbeat.create = function(properties) {
        return new P_UserHeartbeat(properties);
    };

    /**
     * Encodes the specified P_UserHeartbeat message. Does not implicitly {@link P_UserHeartbeat.verify|verify} messages.
     * @function encode
     * @memberof P_UserHeartbeat
     * @static
     * @param {P_UserHeartbeat.$Properties} message P_UserHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_UserHeartbeat.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.time);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_UserHeartbeat message, length delimited. Does not implicitly {@link P_UserHeartbeat.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_UserHeartbeat
     * @static
     * @param {P_UserHeartbeat.$Properties} message P_UserHeartbeat message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_UserHeartbeat.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_UserHeartbeat message from the specified reader or buffer.
     * @function decode
     * @memberof P_UserHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_UserHeartbeat & P_UserHeartbeat.$Shape} P_UserHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_UserHeartbeat.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_UserHeartbeat();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.time = reader.int64();
                    message._time = "time";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_UserHeartbeat message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_UserHeartbeat
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_UserHeartbeat & P_UserHeartbeat.$Shape} P_UserHeartbeat
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_UserHeartbeat.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_UserHeartbeat message.
     * @function verify
     * @memberof P_UserHeartbeat
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_UserHeartbeat.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.time != null && $Object.hasOwnProperty.call(message, "time")) {
            properties._time = 1;
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a P_UserHeartbeat message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_UserHeartbeat
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_UserHeartbeat} P_UserHeartbeat
     */
    P_UserHeartbeat.fromObject = function (object, _depth) {
        if (object instanceof $root.P_UserHeartbeat)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_UserHeartbeat: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_UserHeartbeat();
        if (object.time != null)
            if ($util.Long)
                message.time = $util.Long.fromValue(object.time, false);
            else if (typeof object.time === "string")
                message.time = $parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
        return message;
    };

    /**
     * Creates a plain object from a P_UserHeartbeat message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_UserHeartbeat
     * @static
     * @param {P_UserHeartbeat} message P_UserHeartbeat
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_UserHeartbeat.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
            else if (typeof message.time === "number")
                object.time = options.longs === $String ? $String(message.time) : message.time;
            else
                object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
        return object;
    };

    /**
     * Converts this P_UserHeartbeat to JSON.
     * @function toJSON
     * @memberof P_UserHeartbeat
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_UserHeartbeat.prototype.toJSON = function() {
        return P_UserHeartbeat.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_UserHeartbeat
     * @function getTypeUrl
     * @memberof P_UserHeartbeat
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_UserHeartbeat.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_UserHeartbeat";
    };

    return P_UserHeartbeat;
})();

export const P_SyncRoleInfoReq_CS = $root.P_SyncRoleInfoReq_CS = (() => {

    /**
     * Properties of a P_SyncRoleInfoReq_CS.
     * @typedef {Object} P_SyncRoleInfoReq_CS.$Properties
     * @property {number|null} [req_type] P_SyncRoleInfoReq_CS req_type
     * @property {number|null} [token] P_SyncRoleInfoReq_CS token
     * @property {string|null} [user_name] P_SyncRoleInfoReq_CS user_name
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncRoleInfoReq_CS.
     * @exports IP_SyncRoleInfoReq_CS
     * @interface IP_SyncRoleInfoReq_CS
     * @augments P_SyncRoleInfoReq_CS.$Properties
     * @deprecated Use P_SyncRoleInfoReq_CS.$Properties instead.
     */

    /**
     * Shape of a P_SyncRoleInfoReq_CS.
     * @typedef {P_SyncRoleInfoReq_CS.$Properties} P_SyncRoleInfoReq_CS.$Shape
     */

    /**
     * Constructs a new P_SyncRoleInfoReq_CS.
     * @exports P_SyncRoleInfoReq_CS
     * @classdesc Represents a P_SyncRoleInfoReq_CS.
     * @constructor
     * @param {P_SyncRoleInfoReq_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncRoleInfoReq_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncRoleInfoReq_CS req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_SyncRoleInfoReq_CS
     * @instance
     */
    P_SyncRoleInfoReq_CS.prototype.req_type = null;

    /**
     * P_SyncRoleInfoReq_CS token.
     * @member {number|null|undefined} token
     * @memberof P_SyncRoleInfoReq_CS
     * @instance
     */
    P_SyncRoleInfoReq_CS.prototype.token = null;

    /**
     * P_SyncRoleInfoReq_CS user_name.
     * @member {string|null|undefined} user_name
     * @memberof P_SyncRoleInfoReq_CS
     * @instance
     */
    P_SyncRoleInfoReq_CS.prototype.user_name = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncRoleInfoReq_CS.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncRoleInfoReq_CS.prototype, "_token", {
        get: $util.oneOfGetter($oneOfFields = ["token"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncRoleInfoReq_CS.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncRoleInfoReq_CS instance using the specified properties.
     * @function create
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {P_SyncRoleInfoReq_CS.$Properties=} [properties] Properties to set
     * @returns {P_SyncRoleInfoReq_CS} P_SyncRoleInfoReq_CS instance
     * @type {{
     *   (properties: P_SyncRoleInfoReq_CS.$Shape): P_SyncRoleInfoReq_CS & P_SyncRoleInfoReq_CS.$Shape;
     *   (properties?: P_SyncRoleInfoReq_CS.$Properties): P_SyncRoleInfoReq_CS;
     * }}
     */
    P_SyncRoleInfoReq_CS.create = function(properties) {
        return new P_SyncRoleInfoReq_CS(properties);
    };

    /**
     * Encodes the specified P_SyncRoleInfoReq_CS message. Does not implicitly {@link P_SyncRoleInfoReq_CS.verify|verify} messages.
     * @function encode
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {P_SyncRoleInfoReq_CS.$Properties} message P_SyncRoleInfoReq_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleInfoReq_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.token);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncRoleInfoReq_CS message, length delimited. Does not implicitly {@link P_SyncRoleInfoReq_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {P_SyncRoleInfoReq_CS.$Properties} message P_SyncRoleInfoReq_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleInfoReq_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncRoleInfoReq_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncRoleInfoReq_CS & P_SyncRoleInfoReq_CS.$Shape} P_SyncRoleInfoReq_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleInfoReq_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncRoleInfoReq_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.token = reader.int32();
                    message._token = "token";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncRoleInfoReq_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncRoleInfoReq_CS & P_SyncRoleInfoReq_CS.$Shape} P_SyncRoleInfoReq_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleInfoReq_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncRoleInfoReq_CS message.
     * @function verify
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncRoleInfoReq_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.token != null && $Object.hasOwnProperty.call(message, "token")) {
            properties._token = 1;
            if (!$util.isInteger(message.token))
                return "token: integer expected";
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        return null;
    };

    /**
     * Creates a P_SyncRoleInfoReq_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncRoleInfoReq_CS} P_SyncRoleInfoReq_CS
     */
    P_SyncRoleInfoReq_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncRoleInfoReq_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncRoleInfoReq_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncRoleInfoReq_CS();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.token != null)
            message.token = object.token | 0;
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        return message;
    };

    /**
     * Creates a plain object from a P_SyncRoleInfoReq_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {P_SyncRoleInfoReq_CS} message P_SyncRoleInfoReq_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncRoleInfoReq_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.token != null && $Object.hasOwnProperty.call(message, "token"))
            object.token = message.token;
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        return object;
    };

    /**
     * Converts this P_SyncRoleInfoReq_CS to JSON.
     * @function toJSON
     * @memberof P_SyncRoleInfoReq_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncRoleInfoReq_CS.prototype.toJSON = function() {
        return P_SyncRoleInfoReq_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncRoleInfoReq_CS
     * @function getTypeUrl
     * @memberof P_SyncRoleInfoReq_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncRoleInfoReq_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncRoleInfoReq_CS";
    };

    return P_SyncRoleInfoReq_CS;
})();

export const P_SyncRoleInfoRet_SC = $root.P_SyncRoleInfoRet_SC = (() => {

    /**
     * Properties of a P_SyncRoleInfoRet_SC.
     * @typedef {Object} P_SyncRoleInfoRet_SC.$Properties
     * @property {PB_RoleBaseInfo.$Properties|null} [role_base_info] P_SyncRoleInfoRet_SC role_base_info
     * @property {number|null} [server_id] P_SyncRoleInfoRet_SC server_id
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncRoleInfoRet_SC.
     * @exports IP_SyncRoleInfoRet_SC
     * @interface IP_SyncRoleInfoRet_SC
     * @augments P_SyncRoleInfoRet_SC.$Properties
     * @deprecated Use P_SyncRoleInfoRet_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncRoleInfoRet_SC.
     * @typedef {P_SyncRoleInfoRet_SC.$Properties} P_SyncRoleInfoRet_SC.$Shape
     */

    /**
     * Constructs a new P_SyncRoleInfoRet_SC.
     * @exports P_SyncRoleInfoRet_SC
     * @classdesc Represents a P_SyncRoleInfoRet_SC.
     * @constructor
     * @param {P_SyncRoleInfoRet_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncRoleInfoRet_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncRoleInfoRet_SC role_base_info.
     * @member {PB_RoleBaseInfo.$Properties|null|undefined} role_base_info
     * @memberof P_SyncRoleInfoRet_SC
     * @instance
     */
    P_SyncRoleInfoRet_SC.prototype.role_base_info = null;

    /**
     * P_SyncRoleInfoRet_SC server_id.
     * @member {number|null|undefined} server_id
     * @memberof P_SyncRoleInfoRet_SC
     * @instance
     */
    P_SyncRoleInfoRet_SC.prototype.server_id = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncRoleInfoRet_SC.prototype, "_role_base_info", {
        get: $util.oneOfGetter($oneOfFields = ["role_base_info"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncRoleInfoRet_SC.prototype, "_server_id", {
        get: $util.oneOfGetter($oneOfFields = ["server_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncRoleInfoRet_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {P_SyncRoleInfoRet_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncRoleInfoRet_SC} P_SyncRoleInfoRet_SC instance
     * @type {{
     *   (properties: P_SyncRoleInfoRet_SC.$Shape): P_SyncRoleInfoRet_SC & P_SyncRoleInfoRet_SC.$Shape;
     *   (properties?: P_SyncRoleInfoRet_SC.$Properties): P_SyncRoleInfoRet_SC;
     * }}
     */
    P_SyncRoleInfoRet_SC.create = function(properties) {
        return new P_SyncRoleInfoRet_SC(properties);
    };

    /**
     * Encodes the specified P_SyncRoleInfoRet_SC message. Does not implicitly {@link P_SyncRoleInfoRet_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {P_SyncRoleInfoRet_SC.$Properties} message P_SyncRoleInfoRet_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleInfoRet_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.role_base_info != null && $Object.hasOwnProperty.call(message, "role_base_info"))
            $root.PB_RoleBaseInfo.encode(message.role_base_info, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.server_id);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncRoleInfoRet_SC message, length delimited. Does not implicitly {@link P_SyncRoleInfoRet_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {P_SyncRoleInfoRet_SC.$Properties} message P_SyncRoleInfoRet_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleInfoRet_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncRoleInfoRet_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncRoleInfoRet_SC & P_SyncRoleInfoRet_SC.$Shape} P_SyncRoleInfoRet_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleInfoRet_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncRoleInfoRet_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.role_base_info = $root.PB_RoleBaseInfo.decode(reader, reader.uint32(), $undefined, _depth + 1, message.role_base_info);
                    message._role_base_info = "role_base_info";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.server_id = reader.int32();
                    message._server_id = "server_id";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncRoleInfoRet_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncRoleInfoRet_SC & P_SyncRoleInfoRet_SC.$Shape} P_SyncRoleInfoRet_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleInfoRet_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncRoleInfoRet_SC message.
     * @function verify
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncRoleInfoRet_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.role_base_info != null && $Object.hasOwnProperty.call(message, "role_base_info")) {
            properties._role_base_info = 1;
            {
                let error = $root.PB_RoleBaseInfo.verify(message.role_base_info, _depth + 1);
                if (error)
                    return "role_base_info." + error;
            }
        }
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id")) {
            properties._server_id = 1;
            if (!$util.isInteger(message.server_id))
                return "server_id: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_SyncRoleInfoRet_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncRoleInfoRet_SC} P_SyncRoleInfoRet_SC
     */
    P_SyncRoleInfoRet_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncRoleInfoRet_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncRoleInfoRet_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncRoleInfoRet_SC();
        if (object.role_base_info != null) {
            if (!$util.isObject(object.role_base_info))
                throw $TypeError(".P_SyncRoleInfoRet_SC.role_base_info: object expected");
            message.role_base_info = $root.PB_RoleBaseInfo.fromObject(object.role_base_info, _depth + 1);
        }
        if (object.server_id != null)
            message.server_id = object.server_id | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_SyncRoleInfoRet_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {P_SyncRoleInfoRet_SC} message P_SyncRoleInfoRet_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncRoleInfoRet_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.role_base_info != null && $Object.hasOwnProperty.call(message, "role_base_info"))
            object.role_base_info = $root.PB_RoleBaseInfo.toObject(message.role_base_info, options, _depth + 1);
        if (message.server_id != null && $Object.hasOwnProperty.call(message, "server_id"))
            object.server_id = message.server_id;
        return object;
    };

    /**
     * Converts this P_SyncRoleInfoRet_SC to JSON.
     * @function toJSON
     * @memberof P_SyncRoleInfoRet_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncRoleInfoRet_SC.prototype.toJSON = function() {
        return P_SyncRoleInfoRet_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncRoleInfoRet_SC
     * @function getTypeUrl
     * @memberof P_SyncRoleInfoRet_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncRoleInfoRet_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncRoleInfoRet_SC";
    };

    return P_SyncRoleInfoRet_SC;
})();

export const P_Chat_CS = $root.P_Chat_CS = (() => {

    /**
     * Properties of a P_Chat_CS.
     * @typedef {Object} P_Chat_CS.$Properties
     * @property {number|null} [req_type] P_Chat_CS req_type
     * @property {number|null} [type] P_Chat_CS type
     * @property {string|null} [chat_msg] P_Chat_CS chat_msg
     * @property {number|null} [begin_id] P_Chat_CS begin_id
     * @property {number|null} [get_count] P_Chat_CS get_count
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Chat_CS.
     * @exports IP_Chat_CS
     * @interface IP_Chat_CS
     * @augments P_Chat_CS.$Properties
     * @deprecated Use P_Chat_CS.$Properties instead.
     */

    /**
     * Shape of a P_Chat_CS.
     * @typedef {P_Chat_CS.$Properties} P_Chat_CS.$Shape
     */

    /**
     * Constructs a new P_Chat_CS.
     * @exports P_Chat_CS
     * @classdesc Represents a P_Chat_CS.
     * @constructor
     * @param {P_Chat_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Chat_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Chat_CS req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_Chat_CS
     * @instance
     */
    P_Chat_CS.prototype.req_type = null;

    /**
     * P_Chat_CS type.
     * @member {number|null|undefined} type
     * @memberof P_Chat_CS
     * @instance
     */
    P_Chat_CS.prototype.type = null;

    /**
     * P_Chat_CS chat_msg.
     * @member {string|null|undefined} chat_msg
     * @memberof P_Chat_CS
     * @instance
     */
    P_Chat_CS.prototype.chat_msg = null;

    /**
     * P_Chat_CS begin_id.
     * @member {number|null|undefined} begin_id
     * @memberof P_Chat_CS
     * @instance
     */
    P_Chat_CS.prototype.begin_id = null;

    /**
     * P_Chat_CS get_count.
     * @member {number|null|undefined} get_count
     * @memberof P_Chat_CS
     * @instance
     */
    P_Chat_CS.prototype.get_count = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_CS.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_CS.prototype, "_type", {
        get: $util.oneOfGetter($oneOfFields = ["type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_CS.prototype, "_chat_msg", {
        get: $util.oneOfGetter($oneOfFields = ["chat_msg"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_CS.prototype, "_begin_id", {
        get: $util.oneOfGetter($oneOfFields = ["begin_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_CS.prototype, "_get_count", {
        get: $util.oneOfGetter($oneOfFields = ["get_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Chat_CS instance using the specified properties.
     * @function create
     * @memberof P_Chat_CS
     * @static
     * @param {P_Chat_CS.$Properties=} [properties] Properties to set
     * @returns {P_Chat_CS} P_Chat_CS instance
     * @type {{
     *   (properties: P_Chat_CS.$Shape): P_Chat_CS & P_Chat_CS.$Shape;
     *   (properties?: P_Chat_CS.$Properties): P_Chat_CS;
     * }}
     */
    P_Chat_CS.create = function(properties) {
        return new P_Chat_CS(properties);
    };

    /**
     * Encodes the specified P_Chat_CS message. Does not implicitly {@link P_Chat_CS.verify|verify} messages.
     * @function encode
     * @memberof P_Chat_CS
     * @static
     * @param {P_Chat_CS.$Properties} message P_Chat_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Chat_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.type);
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.chat_msg);
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id"))
            writer.uint32(/* id 4, wireType 0 =*/32).uint64(message.begin_id);
        if (message.get_count != null && $Object.hasOwnProperty.call(message, "get_count"))
            writer.uint32(/* id 5, wireType 0 =*/40).uint64(message.get_count);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Chat_CS message, length delimited. Does not implicitly {@link P_Chat_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Chat_CS
     * @static
     * @param {P_Chat_CS.$Properties} message P_Chat_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Chat_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Chat_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_Chat_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Chat_CS & P_Chat_CS.$Shape} P_Chat_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Chat_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Chat_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.type = reader.int32();
                    message._type = "type";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.chat_msg = reader.stringVerify();
                    message._chat_msg = "chat_msg";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.begin_id = reader.uint64();
                    message._begin_id = "begin_id";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.get_count = reader.uint64();
                    message._get_count = "get_count";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Chat_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Chat_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Chat_CS & P_Chat_CS.$Shape} P_Chat_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Chat_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Chat_CS message.
     * @function verify
     * @memberof P_Chat_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Chat_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.type != null && $Object.hasOwnProperty.call(message, "type")) {
            properties._type = 1;
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        }
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg")) {
            properties._chat_msg = 1;
            if (!$util.isString(message.chat_msg))
                return "chat_msg: string expected";
        }
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id")) {
            properties._begin_id = 1;
            if (!$util.isInteger(message.begin_id) && !(message.begin_id && $util.isInteger(message.begin_id.low) && $util.isInteger(message.begin_id.high)))
                return "begin_id: integer|Long expected";
        }
        if (message.get_count != null && $Object.hasOwnProperty.call(message, "get_count")) {
            properties._get_count = 1;
            if (!$util.isInteger(message.get_count) && !(message.get_count && $util.isInteger(message.get_count.low) && $util.isInteger(message.get_count.high)))
                return "get_count: integer|Long expected";
        }
        return null;
    };

    /**
     * Creates a P_Chat_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Chat_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Chat_CS} P_Chat_CS
     */
    P_Chat_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Chat_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Chat_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Chat_CS();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.type != null)
            message.type = object.type | 0;
        if (object.chat_msg != null)
            message.chat_msg = $String(object.chat_msg);
        if (object.begin_id != null)
            if ($util.Long)
                message.begin_id = $util.Long.fromValue(object.begin_id, true);
            else if (typeof object.begin_id === "string")
                message.begin_id = $parseInt(object.begin_id, 10);
            else if (typeof object.begin_id === "number")
                message.begin_id = object.begin_id;
            else if (typeof object.begin_id === "object")
                message.begin_id = new $util.LongBits(object.begin_id.low >>> 0, object.begin_id.high >>> 0).toNumber(true);
        if (object.get_count != null)
            if ($util.Long)
                message.get_count = $util.Long.fromValue(object.get_count, true);
            else if (typeof object.get_count === "string")
                message.get_count = $parseInt(object.get_count, 10);
            else if (typeof object.get_count === "number")
                message.get_count = object.get_count;
            else if (typeof object.get_count === "object")
                message.get_count = new $util.LongBits(object.get_count.low >>> 0, object.get_count.high >>> 0).toNumber(true);
        return message;
    };

    /**
     * Creates a plain object from a P_Chat_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Chat_CS
     * @static
     * @param {P_Chat_CS} message P_Chat_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Chat_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
            object.type = message.type;
        if (message.chat_msg != null && $Object.hasOwnProperty.call(message, "chat_msg"))
            object.chat_msg = message.chat_msg;
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.begin_id = typeof message.begin_id === "number" ? $BigInt(message.begin_id) : $util.Long.fromBits(message.begin_id.low >>> 0, message.begin_id.high >>> 0, true).toBigInt();
            else if (typeof message.begin_id === "number")
                object.begin_id = options.longs === $String ? $String(message.begin_id) : message.begin_id;
            else
                object.begin_id = options.longs === $String ? $util.Long.prototype.toString.call(message.begin_id) : options.longs === $Number ? new $util.LongBits(message.begin_id.low >>> 0, message.begin_id.high >>> 0).toNumber(true) : message.begin_id;
        if (message.get_count != null && $Object.hasOwnProperty.call(message, "get_count"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.get_count = typeof message.get_count === "number" ? $BigInt(message.get_count) : $util.Long.fromBits(message.get_count.low >>> 0, message.get_count.high >>> 0, true).toBigInt();
            else if (typeof message.get_count === "number")
                object.get_count = options.longs === $String ? $String(message.get_count) : message.get_count;
            else
                object.get_count = options.longs === $String ? $util.Long.prototype.toString.call(message.get_count) : options.longs === $Number ? new $util.LongBits(message.get_count.low >>> 0, message.get_count.high >>> 0).toNumber(true) : message.get_count;
        return object;
    };

    /**
     * Converts this P_Chat_CS to JSON.
     * @function toJSON
     * @memberof P_Chat_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Chat_CS.prototype.toJSON = function() {
        return P_Chat_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Chat_CS
     * @function getTypeUrl
     * @memberof P_Chat_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Chat_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Chat_CS";
    };

    return P_Chat_CS;
})();

export const P_Chat_SC = $root.P_Chat_SC = (() => {

    /**
     * Properties of a P_Chat_SC.
     * @typedef {Object} P_Chat_SC.$Properties
     * @property {number|null} [type] P_Chat_SC type
     * @property {number|null} [begin_id] P_Chat_SC begin_id
     * @property {number|null} [end_id] P_Chat_SC end_id
     * @property {Array.<PB_ChatData.$Properties>|null} [chat_msgs] P_Chat_SC chat_msgs
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Chat_SC.
     * @exports IP_Chat_SC
     * @interface IP_Chat_SC
     * @augments P_Chat_SC.$Properties
     * @deprecated Use P_Chat_SC.$Properties instead.
     */

    /**
     * Shape of a P_Chat_SC.
     * @typedef {P_Chat_SC.$Properties} P_Chat_SC.$Shape
     */

    /**
     * Constructs a new P_Chat_SC.
     * @exports P_Chat_SC
     * @classdesc Represents a P_Chat_SC.
     * @constructor
     * @param {P_Chat_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Chat_SC = function (properties) {
        this.chat_msgs = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Chat_SC type.
     * @member {number|null|undefined} type
     * @memberof P_Chat_SC
     * @instance
     */
    P_Chat_SC.prototype.type = null;

    /**
     * P_Chat_SC begin_id.
     * @member {number|null|undefined} begin_id
     * @memberof P_Chat_SC
     * @instance
     */
    P_Chat_SC.prototype.begin_id = null;

    /**
     * P_Chat_SC end_id.
     * @member {number|null|undefined} end_id
     * @memberof P_Chat_SC
     * @instance
     */
    P_Chat_SC.prototype.end_id = null;

    /**
     * P_Chat_SC chat_msgs.
     * @member {Array.<PB_ChatData.$Properties>} chat_msgs
     * @memberof P_Chat_SC
     * @instance
     */
    P_Chat_SC.prototype.chat_msgs = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_SC.prototype, "_type", {
        get: $util.oneOfGetter($oneOfFields = ["type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_SC.prototype, "_begin_id", {
        get: $util.oneOfGetter($oneOfFields = ["begin_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Chat_SC.prototype, "_end_id", {
        get: $util.oneOfGetter($oneOfFields = ["end_id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Chat_SC instance using the specified properties.
     * @function create
     * @memberof P_Chat_SC
     * @static
     * @param {P_Chat_SC.$Properties=} [properties] Properties to set
     * @returns {P_Chat_SC} P_Chat_SC instance
     * @type {{
     *   (properties: P_Chat_SC.$Shape): P_Chat_SC & P_Chat_SC.$Shape;
     *   (properties?: P_Chat_SC.$Properties): P_Chat_SC;
     * }}
     */
    P_Chat_SC.create = function(properties) {
        return new P_Chat_SC(properties);
    };

    /**
     * Encodes the specified P_Chat_SC message. Does not implicitly {@link P_Chat_SC.verify|verify} messages.
     * @function encode
     * @memberof P_Chat_SC
     * @static
     * @param {P_Chat_SC.$Properties} message P_Chat_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Chat_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.type);
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id"))
            writer.uint32(/* id 2, wireType 0 =*/16).uint64(message.begin_id);
        if (message.end_id != null && $Object.hasOwnProperty.call(message, "end_id"))
            writer.uint32(/* id 3, wireType 0 =*/24).uint64(message.end_id);
        if (message.chat_msgs != null && message.chat_msgs.length)
            for (let i = 0; i < message.chat_msgs.length; ++i)
                $root.PB_ChatData.encode(message.chat_msgs[i], writer.uint32(/* id 4, wireType 2 =*/34).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Chat_SC message, length delimited. Does not implicitly {@link P_Chat_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Chat_SC
     * @static
     * @param {P_Chat_SC.$Properties} message P_Chat_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Chat_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Chat_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_Chat_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Chat_SC & P_Chat_SC.$Shape} P_Chat_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Chat_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Chat_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.type = reader.int32();
                    message._type = "type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.begin_id = reader.uint64();
                    message._begin_id = "begin_id";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.end_id = reader.uint64();
                    message._end_id = "end_id";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    if (!(message.chat_msgs && message.chat_msgs.length))
                        message.chat_msgs = [];
                    message.chat_msgs.push($root.PB_ChatData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Chat_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Chat_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Chat_SC & P_Chat_SC.$Shape} P_Chat_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Chat_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Chat_SC message.
     * @function verify
     * @memberof P_Chat_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Chat_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.type != null && $Object.hasOwnProperty.call(message, "type")) {
            properties._type = 1;
            if (!$util.isInteger(message.type))
                return "type: integer expected";
        }
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id")) {
            properties._begin_id = 1;
            if (!$util.isInteger(message.begin_id) && !(message.begin_id && $util.isInteger(message.begin_id.low) && $util.isInteger(message.begin_id.high)))
                return "begin_id: integer|Long expected";
        }
        if (message.end_id != null && $Object.hasOwnProperty.call(message, "end_id")) {
            properties._end_id = 1;
            if (!$util.isInteger(message.end_id) && !(message.end_id && $util.isInteger(message.end_id.low) && $util.isInteger(message.end_id.high)))
                return "end_id: integer|Long expected";
        }
        if (message.chat_msgs != null && $Object.hasOwnProperty.call(message, "chat_msgs")) {
            if (!$Array.isArray(message.chat_msgs))
                return "chat_msgs: array expected";
            for (let i = 0; i < message.chat_msgs.length; ++i) {
                let error = $root.PB_ChatData.verify(message.chat_msgs[i], _depth + 1);
                if (error)
                    return "chat_msgs." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_Chat_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Chat_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Chat_SC} P_Chat_SC
     */
    P_Chat_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Chat_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Chat_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Chat_SC();
        if (object.type != null)
            message.type = object.type | 0;
        if (object.begin_id != null)
            if ($util.Long)
                message.begin_id = $util.Long.fromValue(object.begin_id, true);
            else if (typeof object.begin_id === "string")
                message.begin_id = $parseInt(object.begin_id, 10);
            else if (typeof object.begin_id === "number")
                message.begin_id = object.begin_id;
            else if (typeof object.begin_id === "object")
                message.begin_id = new $util.LongBits(object.begin_id.low >>> 0, object.begin_id.high >>> 0).toNumber(true);
        if (object.end_id != null)
            if ($util.Long)
                message.end_id = $util.Long.fromValue(object.end_id, true);
            else if (typeof object.end_id === "string")
                message.end_id = $parseInt(object.end_id, 10);
            else if (typeof object.end_id === "number")
                message.end_id = object.end_id;
            else if (typeof object.end_id === "object")
                message.end_id = new $util.LongBits(object.end_id.low >>> 0, object.end_id.high >>> 0).toNumber(true);
        if (object.chat_msgs) {
            if (!$Array.isArray(object.chat_msgs))
                throw $TypeError(".P_Chat_SC.chat_msgs: array expected");
            message.chat_msgs = $Array(object.chat_msgs.length);
            for (let i = 0; i < object.chat_msgs.length; ++i) {
                if (!$util.isObject(object.chat_msgs[i]))
                    throw $TypeError(".P_Chat_SC.chat_msgs: object expected");
                message.chat_msgs[i] = $root.PB_ChatData.fromObject(object.chat_msgs[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_Chat_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Chat_SC
     * @static
     * @param {P_Chat_SC} message P_Chat_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Chat_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.chat_msgs = [];
        if (message.type != null && $Object.hasOwnProperty.call(message, "type"))
            object.type = message.type;
        if (message.begin_id != null && $Object.hasOwnProperty.call(message, "begin_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.begin_id = typeof message.begin_id === "number" ? $BigInt(message.begin_id) : $util.Long.fromBits(message.begin_id.low >>> 0, message.begin_id.high >>> 0, true).toBigInt();
            else if (typeof message.begin_id === "number")
                object.begin_id = options.longs === $String ? $String(message.begin_id) : message.begin_id;
            else
                object.begin_id = options.longs === $String ? $util.Long.prototype.toString.call(message.begin_id) : options.longs === $Number ? new $util.LongBits(message.begin_id.low >>> 0, message.begin_id.high >>> 0).toNumber(true) : message.begin_id;
        if (message.end_id != null && $Object.hasOwnProperty.call(message, "end_id"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.end_id = typeof message.end_id === "number" ? $BigInt(message.end_id) : $util.Long.fromBits(message.end_id.low >>> 0, message.end_id.high >>> 0, true).toBigInt();
            else if (typeof message.end_id === "number")
                object.end_id = options.longs === $String ? $String(message.end_id) : message.end_id;
            else
                object.end_id = options.longs === $String ? $util.Long.prototype.toString.call(message.end_id) : options.longs === $Number ? new $util.LongBits(message.end_id.low >>> 0, message.end_id.high >>> 0).toNumber(true) : message.end_id;
        if (message.chat_msgs && message.chat_msgs.length) {
            object.chat_msgs = $Array(message.chat_msgs.length);
            for (let j = 0; j < message.chat_msgs.length; ++j)
                object.chat_msgs[j] = $root.PB_ChatData.toObject(message.chat_msgs[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_Chat_SC to JSON.
     * @function toJSON
     * @memberof P_Chat_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Chat_SC.prototype.toJSON = function() {
        return P_Chat_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Chat_SC
     * @function getTypeUrl
     * @memberof P_Chat_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Chat_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Chat_SC";
    };

    return P_Chat_SC;
})();

export const P_GasBattle_CS = $root.P_GasBattle_CS = (() => {

    /**
     * Properties of a P_GasBattle_CS.
     * @typedef {Object} P_GasBattle_CS.$Properties
     * @property {number|null} [req_type] P_GasBattle_CS req_type
     * @property {number|null} [battle_type] P_GasBattle_CS battle_type
     * @property {number|null} [id] P_GasBattle_CS id
     * @property {string|null} [battle_report_key] P_GasBattle_CS battle_report_key
     * @property {string|null} [params] P_GasBattle_CS params
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GasBattle_CS.
     * @exports IP_GasBattle_CS
     * @interface IP_GasBattle_CS
     * @augments P_GasBattle_CS.$Properties
     * @deprecated Use P_GasBattle_CS.$Properties instead.
     */

    /**
     * Shape of a P_GasBattle_CS.
     * @typedef {P_GasBattle_CS.$Properties} P_GasBattle_CS.$Shape
     */

    /**
     * Constructs a new P_GasBattle_CS.
     * @exports P_GasBattle_CS
     * @classdesc Represents a P_GasBattle_CS.
     * @constructor
     * @param {P_GasBattle_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GasBattle_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GasBattle_CS req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_GasBattle_CS
     * @instance
     */
    P_GasBattle_CS.prototype.req_type = null;

    /**
     * P_GasBattle_CS battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof P_GasBattle_CS
     * @instance
     */
    P_GasBattle_CS.prototype.battle_type = null;

    /**
     * P_GasBattle_CS id.
     * @member {number|null|undefined} id
     * @memberof P_GasBattle_CS
     * @instance
     */
    P_GasBattle_CS.prototype.id = null;

    /**
     * P_GasBattle_CS battle_report_key.
     * @member {string|null|undefined} battle_report_key
     * @memberof P_GasBattle_CS
     * @instance
     */
    P_GasBattle_CS.prototype.battle_report_key = null;

    /**
     * P_GasBattle_CS params.
     * @member {string|null|undefined} params
     * @memberof P_GasBattle_CS
     * @instance
     */
    P_GasBattle_CS.prototype.params = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_CS.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_CS.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_CS.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_CS.prototype, "_battle_report_key", {
        get: $util.oneOfGetter($oneOfFields = ["battle_report_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_CS.prototype, "_params", {
        get: $util.oneOfGetter($oneOfFields = ["params"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GasBattle_CS instance using the specified properties.
     * @function create
     * @memberof P_GasBattle_CS
     * @static
     * @param {P_GasBattle_CS.$Properties=} [properties] Properties to set
     * @returns {P_GasBattle_CS} P_GasBattle_CS instance
     * @type {{
     *   (properties: P_GasBattle_CS.$Shape): P_GasBattle_CS & P_GasBattle_CS.$Shape;
     *   (properties?: P_GasBattle_CS.$Properties): P_GasBattle_CS;
     * }}
     */
    P_GasBattle_CS.create = function(properties) {
        return new P_GasBattle_CS(properties);
    };

    /**
     * Encodes the specified P_GasBattle_CS message. Does not implicitly {@link P_GasBattle_CS.verify|verify} messages.
     * @function encode
     * @memberof P_GasBattle_CS
     * @static
     * @param {P_GasBattle_CS.$Properties} message P_GasBattle_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GasBattle_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.battle_type);
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.id);
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key"))
            writer.uint32(/* id 4, wireType 2 =*/34).string(message.battle_report_key);
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            writer.uint32(/* id 10, wireType 2 =*/82).string(message.params);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GasBattle_CS message, length delimited. Does not implicitly {@link P_GasBattle_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GasBattle_CS
     * @static
     * @param {P_GasBattle_CS.$Properties} message P_GasBattle_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GasBattle_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GasBattle_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_GasBattle_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GasBattle_CS & P_GasBattle_CS.$Shape} P_GasBattle_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GasBattle_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GasBattle_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 4: {
                    if (wireType !== 2)
                        break;
                    message.battle_report_key = reader.stringVerify();
                    message._battle_report_key = "battle_report_key";
                    continue;
                }
            case 10: {
                    if (wireType !== 2)
                        break;
                    message.params = reader.stringVerify();
                    message._params = "params";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GasBattle_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GasBattle_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GasBattle_CS & P_GasBattle_CS.$Shape} P_GasBattle_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GasBattle_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GasBattle_CS message.
     * @function verify
     * @memberof P_GasBattle_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GasBattle_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key")) {
            properties._battle_report_key = 1;
            if (!$util.isString(message.battle_report_key))
                return "battle_report_key: string expected";
        }
        if (message.params != null && $Object.hasOwnProperty.call(message, "params")) {
            properties._params = 1;
            if (!$util.isString(message.params))
                return "params: string expected";
        }
        return null;
    };

    /**
     * Creates a P_GasBattle_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GasBattle_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GasBattle_CS} P_GasBattle_CS
     */
    P_GasBattle_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GasBattle_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GasBattle_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GasBattle_CS();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        if (object.id != null)
            message.id = object.id | 0;
        if (object.battle_report_key != null)
            message.battle_report_key = $String(object.battle_report_key);
        if (object.params != null)
            message.params = $String(object.params);
        return message;
    };

    /**
     * Creates a plain object from a P_GasBattle_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GasBattle_CS
     * @static
     * @param {P_GasBattle_CS} message P_GasBattle_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GasBattle_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key"))
            object.battle_report_key = message.battle_report_key;
        if (message.params != null && $Object.hasOwnProperty.call(message, "params"))
            object.params = message.params;
        return object;
    };

    /**
     * Converts this P_GasBattle_CS to JSON.
     * @function toJSON
     * @memberof P_GasBattle_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GasBattle_CS.prototype.toJSON = function() {
        return P_GasBattle_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GasBattle_CS
     * @function getTypeUrl
     * @memberof P_GasBattle_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GasBattle_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GasBattle_CS";
    };

    return P_GasBattle_CS;
})();

export const P_GasBattle_SC = $root.P_GasBattle_SC = (() => {

    /**
     * Properties of a P_GasBattle_SC.
     * @typedef {Object} P_GasBattle_SC.$Properties
     * @property {number|null} [ret_type] P_GasBattle_SC ret_type
     * @property {string|null} [battle_report_key] P_GasBattle_SC battle_report_key
     * @property {string|null} [battle_report] P_GasBattle_SC battle_report
     * @property {number|null} [battle_type] P_GasBattle_SC battle_type
     * @property {number|null} [id] P_GasBattle_SC id
     * @property {number|null} [result_type] P_GasBattle_SC result_type
     * @property {number|null} [score_change] P_GasBattle_SC score_change
     * @property {number|null} [score_after] P_GasBattle_SC score_after
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_GasBattle_SC.
     * @exports IP_GasBattle_SC
     * @interface IP_GasBattle_SC
     * @augments P_GasBattle_SC.$Properties
     * @deprecated Use P_GasBattle_SC.$Properties instead.
     */

    /**
     * Shape of a P_GasBattle_SC.
     * @typedef {P_GasBattle_SC.$Properties} P_GasBattle_SC.$Shape
     */

    /**
     * Constructs a new P_GasBattle_SC.
     * @exports P_GasBattle_SC
     * @classdesc Represents a P_GasBattle_SC.
     * @constructor
     * @param {P_GasBattle_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_GasBattle_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_GasBattle_SC ret_type.
     * @member {number|null|undefined} ret_type
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.ret_type = null;

    /**
     * P_GasBattle_SC battle_report_key.
     * @member {string|null|undefined} battle_report_key
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.battle_report_key = null;

    /**
     * P_GasBattle_SC battle_report.
     * @member {string|null|undefined} battle_report
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.battle_report = null;

    /**
     * P_GasBattle_SC battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.battle_type = null;

    /**
     * P_GasBattle_SC id.
     * @member {number|null|undefined} id
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.id = null;

    /**
     * P_GasBattle_SC result_type.
     * @member {number|null|undefined} result_type
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.result_type = null;

    /**
     * P_GasBattle_SC score_change.
     * @member {number|null|undefined} score_change
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.score_change = null;

    /**
     * P_GasBattle_SC score_after.
     * @member {number|null|undefined} score_after
     * @memberof P_GasBattle_SC
     * @instance
     */
    P_GasBattle_SC.prototype.score_after = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_ret_type", {
        get: $util.oneOfGetter($oneOfFields = ["ret_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_battle_report_key", {
        get: $util.oneOfGetter($oneOfFields = ["battle_report_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_battle_report", {
        get: $util.oneOfGetter($oneOfFields = ["battle_report"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_id", {
        get: $util.oneOfGetter($oneOfFields = ["id"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_result_type", {
        get: $util.oneOfGetter($oneOfFields = ["result_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_score_change", {
        get: $util.oneOfGetter($oneOfFields = ["score_change"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_GasBattle_SC.prototype, "_score_after", {
        get: $util.oneOfGetter($oneOfFields = ["score_after"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_GasBattle_SC instance using the specified properties.
     * @function create
     * @memberof P_GasBattle_SC
     * @static
     * @param {P_GasBattle_SC.$Properties=} [properties] Properties to set
     * @returns {P_GasBattle_SC} P_GasBattle_SC instance
     * @type {{
     *   (properties: P_GasBattle_SC.$Shape): P_GasBattle_SC & P_GasBattle_SC.$Shape;
     *   (properties?: P_GasBattle_SC.$Properties): P_GasBattle_SC;
     * }}
     */
    P_GasBattle_SC.create = function(properties) {
        return new P_GasBattle_SC(properties);
    };

    /**
     * Encodes the specified P_GasBattle_SC message. Does not implicitly {@link P_GasBattle_SC.verify|verify} messages.
     * @function encode
     * @memberof P_GasBattle_SC
     * @static
     * @param {P_GasBattle_SC.$Properties} message P_GasBattle_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GasBattle_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ret_type != null && $Object.hasOwnProperty.call(message, "ret_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret_type);
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.battle_report_key);
        if (message.battle_report != null && $Object.hasOwnProperty.call(message, "battle_report"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.battle_report);
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.battle_type);
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.id);
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type"))
            writer.uint32(/* id 6, wireType 0 =*/48).int32(message.result_type);
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change"))
            writer.uint32(/* id 7, wireType 0 =*/56).int32(message.score_change);
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.score_after);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_GasBattle_SC message, length delimited. Does not implicitly {@link P_GasBattle_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_GasBattle_SC
     * @static
     * @param {P_GasBattle_SC.$Properties} message P_GasBattle_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_GasBattle_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_GasBattle_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_GasBattle_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_GasBattle_SC & P_GasBattle_SC.$Shape} P_GasBattle_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GasBattle_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_GasBattle_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.ret_type = reader.int32();
                    message._ret_type = "ret_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.battle_report_key = reader.stringVerify();
                    message._battle_report_key = "battle_report_key";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.battle_report = reader.stringVerify();
                    message._battle_report = "battle_report";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.id = reader.int32();
                    message._id = "id";
                    continue;
                }
            case 6: {
                    if (wireType !== 0)
                        break;
                    message.result_type = reader.int32();
                    message._result_type = "result_type";
                    continue;
                }
            case 7: {
                    if (wireType !== 0)
                        break;
                    message.score_change = reader.int32();
                    message._score_change = "score_change";
                    continue;
                }
            case 8: {
                    if (wireType !== 0)
                        break;
                    message.score_after = reader.int32();
                    message._score_after = "score_after";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_GasBattle_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_GasBattle_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_GasBattle_SC & P_GasBattle_SC.$Shape} P_GasBattle_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_GasBattle_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_GasBattle_SC message.
     * @function verify
     * @memberof P_GasBattle_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_GasBattle_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.ret_type != null && $Object.hasOwnProperty.call(message, "ret_type")) {
            properties._ret_type = 1;
            if (!$util.isInteger(message.ret_type))
                return "ret_type: integer expected";
        }
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key")) {
            properties._battle_report_key = 1;
            if (!$util.isString(message.battle_report_key))
                return "battle_report_key: string expected";
        }
        if (message.battle_report != null && $Object.hasOwnProperty.call(message, "battle_report")) {
            properties._battle_report = 1;
            if (!$util.isString(message.battle_report))
                return "battle_report: string expected";
        }
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        if (message.id != null && $Object.hasOwnProperty.call(message, "id")) {
            properties._id = 1;
            if (!$util.isInteger(message.id))
                return "id: integer expected";
        }
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type")) {
            properties._result_type = 1;
            if (!$util.isInteger(message.result_type))
                return "result_type: integer expected";
        }
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change")) {
            properties._score_change = 1;
            if (!$util.isInteger(message.score_change))
                return "score_change: integer expected";
        }
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after")) {
            properties._score_after = 1;
            if (!$util.isInteger(message.score_after))
                return "score_after: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_GasBattle_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_GasBattle_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_GasBattle_SC} P_GasBattle_SC
     */
    P_GasBattle_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_GasBattle_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_GasBattle_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_GasBattle_SC();
        if (object.ret_type != null)
            message.ret_type = object.ret_type | 0;
        if (object.battle_report_key != null)
            message.battle_report_key = $String(object.battle_report_key);
        if (object.battle_report != null)
            message.battle_report = $String(object.battle_report);
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        if (object.id != null)
            message.id = object.id | 0;
        if (object.result_type != null)
            message.result_type = object.result_type | 0;
        if (object.score_change != null)
            message.score_change = object.score_change | 0;
        if (object.score_after != null)
            message.score_after = object.score_after | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_GasBattle_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_GasBattle_SC
     * @static
     * @param {P_GasBattle_SC} message P_GasBattle_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_GasBattle_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.ret_type != null && $Object.hasOwnProperty.call(message, "ret_type"))
            object.ret_type = message.ret_type;
        if (message.battle_report_key != null && $Object.hasOwnProperty.call(message, "battle_report_key"))
            object.battle_report_key = message.battle_report_key;
        if (message.battle_report != null && $Object.hasOwnProperty.call(message, "battle_report"))
            object.battle_report = message.battle_report;
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        if (message.id != null && $Object.hasOwnProperty.call(message, "id"))
            object.id = message.id;
        if (message.result_type != null && $Object.hasOwnProperty.call(message, "result_type"))
            object.result_type = message.result_type;
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change"))
            object.score_change = message.score_change;
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after"))
            object.score_after = message.score_after;
        return object;
    };

    /**
     * Converts this P_GasBattle_SC to JSON.
     * @function toJSON
     * @memberof P_GasBattle_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_GasBattle_SC.prototype.toJSON = function() {
        return P_GasBattle_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_GasBattle_SC
     * @function getTypeUrl
     * @memberof P_GasBattle_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_GasBattle_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_GasBattle_SC";
    };

    return P_GasBattle_SC;
})();

export const P_SyncInventoryData_SC = $root.P_SyncInventoryData_SC = (() => {

    /**
     * Properties of a P_SyncInventoryData_SC.
     * @typedef {Object} P_SyncInventoryData_SC.$Properties
     * @property {PB_InventoryData.$Properties|null} [inventory_data] P_SyncInventoryData_SC inventory_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncInventoryData_SC.
     * @exports IP_SyncInventoryData_SC
     * @interface IP_SyncInventoryData_SC
     * @augments P_SyncInventoryData_SC.$Properties
     * @deprecated Use P_SyncInventoryData_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncInventoryData_SC.
     * @typedef {P_SyncInventoryData_SC.$Properties} P_SyncInventoryData_SC.$Shape
     */

    /**
     * Constructs a new P_SyncInventoryData_SC.
     * @exports P_SyncInventoryData_SC
     * @classdesc Represents a P_SyncInventoryData_SC.
     * @constructor
     * @param {P_SyncInventoryData_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncInventoryData_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncInventoryData_SC inventory_data.
     * @member {PB_InventoryData.$Properties|null|undefined} inventory_data
     * @memberof P_SyncInventoryData_SC
     * @instance
     */
    P_SyncInventoryData_SC.prototype.inventory_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncInventoryData_SC.prototype, "_inventory_data", {
        get: $util.oneOfGetter($oneOfFields = ["inventory_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncInventoryData_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {P_SyncInventoryData_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncInventoryData_SC} P_SyncInventoryData_SC instance
     * @type {{
     *   (properties: P_SyncInventoryData_SC.$Shape): P_SyncInventoryData_SC & P_SyncInventoryData_SC.$Shape;
     *   (properties?: P_SyncInventoryData_SC.$Properties): P_SyncInventoryData_SC;
     * }}
     */
    P_SyncInventoryData_SC.create = function(properties) {
        return new P_SyncInventoryData_SC(properties);
    };

    /**
     * Encodes the specified P_SyncInventoryData_SC message. Does not implicitly {@link P_SyncInventoryData_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {P_SyncInventoryData_SC.$Properties} message P_SyncInventoryData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncInventoryData_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.inventory_data != null && $Object.hasOwnProperty.call(message, "inventory_data"))
            $root.PB_InventoryData.encode(message.inventory_data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncInventoryData_SC message, length delimited. Does not implicitly {@link P_SyncInventoryData_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {P_SyncInventoryData_SC.$Properties} message P_SyncInventoryData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncInventoryData_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncInventoryData_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncInventoryData_SC & P_SyncInventoryData_SC.$Shape} P_SyncInventoryData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncInventoryData_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncInventoryData_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.inventory_data = $root.PB_InventoryData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.inventory_data);
                    message._inventory_data = "inventory_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncInventoryData_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncInventoryData_SC & P_SyncInventoryData_SC.$Shape} P_SyncInventoryData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncInventoryData_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncInventoryData_SC message.
     * @function verify
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncInventoryData_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.inventory_data != null && $Object.hasOwnProperty.call(message, "inventory_data")) {
            properties._inventory_data = 1;
            {
                let error = $root.PB_InventoryData.verify(message.inventory_data, _depth + 1);
                if (error)
                    return "inventory_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SyncInventoryData_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncInventoryData_SC} P_SyncInventoryData_SC
     */
    P_SyncInventoryData_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncInventoryData_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncInventoryData_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncInventoryData_SC();
        if (object.inventory_data != null) {
            if (!$util.isObject(object.inventory_data))
                throw $TypeError(".P_SyncInventoryData_SC.inventory_data: object expected");
            message.inventory_data = $root.PB_InventoryData.fromObject(object.inventory_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SyncInventoryData_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {P_SyncInventoryData_SC} message P_SyncInventoryData_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncInventoryData_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.inventory_data != null && $Object.hasOwnProperty.call(message, "inventory_data"))
            object.inventory_data = $root.PB_InventoryData.toObject(message.inventory_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_SyncInventoryData_SC to JSON.
     * @function toJSON
     * @memberof P_SyncInventoryData_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncInventoryData_SC.prototype.toJSON = function() {
        return P_SyncInventoryData_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncInventoryData_SC
     * @function getTypeUrl
     * @memberof P_SyncInventoryData_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncInventoryData_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncInventoryData_SC";
    };

    return P_SyncInventoryData_SC;
})();

export const P_SyncRoleExtraData_SC = $root.P_SyncRoleExtraData_SC = (() => {

    /**
     * Properties of a P_SyncRoleExtraData_SC.
     * @typedef {Object} P_SyncRoleExtraData_SC.$Properties
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncRoleExtraData_SC.
     * @exports IP_SyncRoleExtraData_SC
     * @interface IP_SyncRoleExtraData_SC
     * @augments P_SyncRoleExtraData_SC.$Properties
     * @deprecated Use P_SyncRoleExtraData_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncRoleExtraData_SC.
     * @typedef {P_SyncRoleExtraData_SC.$Properties} P_SyncRoleExtraData_SC.$Shape
     */

    /**
     * Constructs a new P_SyncRoleExtraData_SC.
     * @exports P_SyncRoleExtraData_SC
     * @classdesc Represents a P_SyncRoleExtraData_SC.
     * @constructor
     * @param {P_SyncRoleExtraData_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncRoleExtraData_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * Creates a new P_SyncRoleExtraData_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {P_SyncRoleExtraData_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncRoleExtraData_SC} P_SyncRoleExtraData_SC instance
     * @type {{
     *   (properties: P_SyncRoleExtraData_SC.$Shape): P_SyncRoleExtraData_SC & P_SyncRoleExtraData_SC.$Shape;
     *   (properties?: P_SyncRoleExtraData_SC.$Properties): P_SyncRoleExtraData_SC;
     * }}
     */
    P_SyncRoleExtraData_SC.create = function(properties) {
        return new P_SyncRoleExtraData_SC(properties);
    };

    /**
     * Encodes the specified P_SyncRoleExtraData_SC message. Does not implicitly {@link P_SyncRoleExtraData_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {P_SyncRoleExtraData_SC.$Properties} message P_SyncRoleExtraData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleExtraData_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncRoleExtraData_SC message, length delimited. Does not implicitly {@link P_SyncRoleExtraData_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {P_SyncRoleExtraData_SC.$Properties} message P_SyncRoleExtraData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncRoleExtraData_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncRoleExtraData_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncRoleExtraData_SC & P_SyncRoleExtraData_SC.$Shape} P_SyncRoleExtraData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleExtraData_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncRoleExtraData_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            reader.skipType(tag & 7, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncRoleExtraData_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncRoleExtraData_SC & P_SyncRoleExtraData_SC.$Shape} P_SyncRoleExtraData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncRoleExtraData_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncRoleExtraData_SC message.
     * @function verify
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncRoleExtraData_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        return null;
    };

    /**
     * Creates a P_SyncRoleExtraData_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncRoleExtraData_SC} P_SyncRoleExtraData_SC
     */
    P_SyncRoleExtraData_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncRoleExtraData_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncRoleExtraData_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        return new $root.P_SyncRoleExtraData_SC();
    };

    /**
     * Creates a plain object from a P_SyncRoleExtraData_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {P_SyncRoleExtraData_SC} message P_SyncRoleExtraData_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncRoleExtraData_SC.toObject = function () {
        return {};
    };

    /**
     * Converts this P_SyncRoleExtraData_SC to JSON.
     * @function toJSON
     * @memberof P_SyncRoleExtraData_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncRoleExtraData_SC.prototype.toJSON = function() {
        return P_SyncRoleExtraData_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncRoleExtraData_SC
     * @function getTypeUrl
     * @memberof P_SyncRoleExtraData_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncRoleExtraData_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncRoleExtraData_SC";
    };

    return P_SyncRoleExtraData_SC;
})();

export const P_SyncQuestData_SC = $root.P_SyncQuestData_SC = (() => {

    /**
     * Properties of a P_SyncQuestData_SC.
     * @typedef {Object} P_SyncQuestData_SC.$Properties
     * @property {PB_QuestData.$Properties|null} [quest_data] P_SyncQuestData_SC quest_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncQuestData_SC.
     * @exports IP_SyncQuestData_SC
     * @interface IP_SyncQuestData_SC
     * @augments P_SyncQuestData_SC.$Properties
     * @deprecated Use P_SyncQuestData_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncQuestData_SC.
     * @typedef {P_SyncQuestData_SC.$Properties} P_SyncQuestData_SC.$Shape
     */

    /**
     * Constructs a new P_SyncQuestData_SC.
     * @exports P_SyncQuestData_SC
     * @classdesc Represents a P_SyncQuestData_SC.
     * @constructor
     * @param {P_SyncQuestData_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncQuestData_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncQuestData_SC quest_data.
     * @member {PB_QuestData.$Properties|null|undefined} quest_data
     * @memberof P_SyncQuestData_SC
     * @instance
     */
    P_SyncQuestData_SC.prototype.quest_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncQuestData_SC.prototype, "_quest_data", {
        get: $util.oneOfGetter($oneOfFields = ["quest_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncQuestData_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {P_SyncQuestData_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncQuestData_SC} P_SyncQuestData_SC instance
     * @type {{
     *   (properties: P_SyncQuestData_SC.$Shape): P_SyncQuestData_SC & P_SyncQuestData_SC.$Shape;
     *   (properties?: P_SyncQuestData_SC.$Properties): P_SyncQuestData_SC;
     * }}
     */
    P_SyncQuestData_SC.create = function(properties) {
        return new P_SyncQuestData_SC(properties);
    };

    /**
     * Encodes the specified P_SyncQuestData_SC message. Does not implicitly {@link P_SyncQuestData_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {P_SyncQuestData_SC.$Properties} message P_SyncQuestData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncQuestData_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.quest_data != null && $Object.hasOwnProperty.call(message, "quest_data"))
            $root.PB_QuestData.encode(message.quest_data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncQuestData_SC message, length delimited. Does not implicitly {@link P_SyncQuestData_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {P_SyncQuestData_SC.$Properties} message P_SyncQuestData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncQuestData_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncQuestData_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncQuestData_SC & P_SyncQuestData_SC.$Shape} P_SyncQuestData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncQuestData_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncQuestData_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.quest_data = $root.PB_QuestData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.quest_data);
                    message._quest_data = "quest_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncQuestData_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncQuestData_SC & P_SyncQuestData_SC.$Shape} P_SyncQuestData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncQuestData_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncQuestData_SC message.
     * @function verify
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncQuestData_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.quest_data != null && $Object.hasOwnProperty.call(message, "quest_data")) {
            properties._quest_data = 1;
            {
                let error = $root.PB_QuestData.verify(message.quest_data, _depth + 1);
                if (error)
                    return "quest_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SyncQuestData_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncQuestData_SC} P_SyncQuestData_SC
     */
    P_SyncQuestData_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncQuestData_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncQuestData_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncQuestData_SC();
        if (object.quest_data != null) {
            if (!$util.isObject(object.quest_data))
                throw $TypeError(".P_SyncQuestData_SC.quest_data: object expected");
            message.quest_data = $root.PB_QuestData.fromObject(object.quest_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SyncQuestData_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {P_SyncQuestData_SC} message P_SyncQuestData_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncQuestData_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.quest_data != null && $Object.hasOwnProperty.call(message, "quest_data"))
            object.quest_data = $root.PB_QuestData.toObject(message.quest_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_SyncQuestData_SC to JSON.
     * @function toJSON
     * @memberof P_SyncQuestData_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncQuestData_SC.prototype.toJSON = function() {
        return P_SyncQuestData_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncQuestData_SC
     * @function getTypeUrl
     * @memberof P_SyncQuestData_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncQuestData_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncQuestData_SC";
    };

    return P_SyncQuestData_SC;
})();

export const P_SyncHeroData_SC = $root.P_SyncHeroData_SC = (() => {

    /**
     * Properties of a P_SyncHeroData_SC.
     * @typedef {Object} P_SyncHeroData_SC.$Properties
     * @property {PB_HeroListData.$Properties|null} [hero_data] P_SyncHeroData_SC hero_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncHeroData_SC.
     * @exports IP_SyncHeroData_SC
     * @interface IP_SyncHeroData_SC
     * @augments P_SyncHeroData_SC.$Properties
     * @deprecated Use P_SyncHeroData_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncHeroData_SC.
     * @typedef {P_SyncHeroData_SC.$Properties} P_SyncHeroData_SC.$Shape
     */

    /**
     * Constructs a new P_SyncHeroData_SC.
     * @exports P_SyncHeroData_SC
     * @classdesc Represents a P_SyncHeroData_SC.
     * @constructor
     * @param {P_SyncHeroData_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncHeroData_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncHeroData_SC hero_data.
     * @member {PB_HeroListData.$Properties|null|undefined} hero_data
     * @memberof P_SyncHeroData_SC
     * @instance
     */
    P_SyncHeroData_SC.prototype.hero_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncHeroData_SC.prototype, "_hero_data", {
        get: $util.oneOfGetter($oneOfFields = ["hero_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncHeroData_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {P_SyncHeroData_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncHeroData_SC} P_SyncHeroData_SC instance
     * @type {{
     *   (properties: P_SyncHeroData_SC.$Shape): P_SyncHeroData_SC & P_SyncHeroData_SC.$Shape;
     *   (properties?: P_SyncHeroData_SC.$Properties): P_SyncHeroData_SC;
     * }}
     */
    P_SyncHeroData_SC.create = function(properties) {
        return new P_SyncHeroData_SC(properties);
    };

    /**
     * Encodes the specified P_SyncHeroData_SC message. Does not implicitly {@link P_SyncHeroData_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {P_SyncHeroData_SC.$Properties} message P_SyncHeroData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncHeroData_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.hero_data != null && $Object.hasOwnProperty.call(message, "hero_data"))
            $root.PB_HeroListData.encode(message.hero_data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncHeroData_SC message, length delimited. Does not implicitly {@link P_SyncHeroData_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {P_SyncHeroData_SC.$Properties} message P_SyncHeroData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncHeroData_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncHeroData_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncHeroData_SC & P_SyncHeroData_SC.$Shape} P_SyncHeroData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncHeroData_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncHeroData_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.hero_data = $root.PB_HeroListData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.hero_data);
                    message._hero_data = "hero_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncHeroData_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncHeroData_SC & P_SyncHeroData_SC.$Shape} P_SyncHeroData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncHeroData_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncHeroData_SC message.
     * @function verify
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncHeroData_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.hero_data != null && $Object.hasOwnProperty.call(message, "hero_data")) {
            properties._hero_data = 1;
            {
                let error = $root.PB_HeroListData.verify(message.hero_data, _depth + 1);
                if (error)
                    return "hero_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SyncHeroData_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncHeroData_SC} P_SyncHeroData_SC
     */
    P_SyncHeroData_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncHeroData_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncHeroData_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncHeroData_SC();
        if (object.hero_data != null) {
            if (!$util.isObject(object.hero_data))
                throw $TypeError(".P_SyncHeroData_SC.hero_data: object expected");
            message.hero_data = $root.PB_HeroListData.fromObject(object.hero_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SyncHeroData_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {P_SyncHeroData_SC} message P_SyncHeroData_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncHeroData_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.hero_data != null && $Object.hasOwnProperty.call(message, "hero_data"))
            object.hero_data = $root.PB_HeroListData.toObject(message.hero_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_SyncHeroData_SC to JSON.
     * @function toJSON
     * @memberof P_SyncHeroData_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncHeroData_SC.prototype.toJSON = function() {
        return P_SyncHeroData_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncHeroData_SC
     * @function getTypeUrl
     * @memberof P_SyncHeroData_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncHeroData_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncHeroData_SC";
    };

    return P_SyncHeroData_SC;
})();

export const P_SyncLineupData_SC = $root.P_SyncLineupData_SC = (() => {

    /**
     * Properties of a P_SyncLineupData_SC.
     * @typedef {Object} P_SyncLineupData_SC.$Properties
     * @property {PB_LineupListData.$Properties|null} [lineup_data] P_SyncLineupData_SC lineup_data
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncLineupData_SC.
     * @exports IP_SyncLineupData_SC
     * @interface IP_SyncLineupData_SC
     * @augments P_SyncLineupData_SC.$Properties
     * @deprecated Use P_SyncLineupData_SC.$Properties instead.
     */

    /**
     * Shape of a P_SyncLineupData_SC.
     * @typedef {P_SyncLineupData_SC.$Properties} P_SyncLineupData_SC.$Shape
     */

    /**
     * Constructs a new P_SyncLineupData_SC.
     * @exports P_SyncLineupData_SC
     * @classdesc Represents a P_SyncLineupData_SC.
     * @constructor
     * @param {P_SyncLineupData_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncLineupData_SC = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncLineupData_SC lineup_data.
     * @member {PB_LineupListData.$Properties|null|undefined} lineup_data
     * @memberof P_SyncLineupData_SC
     * @instance
     */
    P_SyncLineupData_SC.prototype.lineup_data = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncLineupData_SC.prototype, "_lineup_data", {
        get: $util.oneOfGetter($oneOfFields = ["lineup_data"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncLineupData_SC instance using the specified properties.
     * @function create
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {P_SyncLineupData_SC.$Properties=} [properties] Properties to set
     * @returns {P_SyncLineupData_SC} P_SyncLineupData_SC instance
     * @type {{
     *   (properties: P_SyncLineupData_SC.$Shape): P_SyncLineupData_SC & P_SyncLineupData_SC.$Shape;
     *   (properties?: P_SyncLineupData_SC.$Properties): P_SyncLineupData_SC;
     * }}
     */
    P_SyncLineupData_SC.create = function(properties) {
        return new P_SyncLineupData_SC(properties);
    };

    /**
     * Encodes the specified P_SyncLineupData_SC message. Does not implicitly {@link P_SyncLineupData_SC.verify|verify} messages.
     * @function encode
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {P_SyncLineupData_SC.$Properties} message P_SyncLineupData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncLineupData_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.lineup_data != null && $Object.hasOwnProperty.call(message, "lineup_data"))
            $root.PB_LineupListData.encode(message.lineup_data, writer.uint32(/* id 1, wireType 2 =*/10).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncLineupData_SC message, length delimited. Does not implicitly {@link P_SyncLineupData_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {P_SyncLineupData_SC.$Properties} message P_SyncLineupData_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncLineupData_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncLineupData_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncLineupData_SC & P_SyncLineupData_SC.$Shape} P_SyncLineupData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncLineupData_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncLineupData_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 2)
                        break;
                    message.lineup_data = $root.PB_LineupListData.decode(reader, reader.uint32(), $undefined, _depth + 1, message.lineup_data);
                    message._lineup_data = "lineup_data";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncLineupData_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncLineupData_SC & P_SyncLineupData_SC.$Shape} P_SyncLineupData_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncLineupData_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncLineupData_SC message.
     * @function verify
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncLineupData_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.lineup_data != null && $Object.hasOwnProperty.call(message, "lineup_data")) {
            properties._lineup_data = 1;
            {
                let error = $root.PB_LineupListData.verify(message.lineup_data, _depth + 1);
                if (error)
                    return "lineup_data." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_SyncLineupData_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncLineupData_SC} P_SyncLineupData_SC
     */
    P_SyncLineupData_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncLineupData_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncLineupData_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncLineupData_SC();
        if (object.lineup_data != null) {
            if (!$util.isObject(object.lineup_data))
                throw $TypeError(".P_SyncLineupData_SC.lineup_data: object expected");
            message.lineup_data = $root.PB_LineupListData.fromObject(object.lineup_data, _depth + 1);
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SyncLineupData_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {P_SyncLineupData_SC} message P_SyncLineupData_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncLineupData_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.lineup_data != null && $Object.hasOwnProperty.call(message, "lineup_data"))
            object.lineup_data = $root.PB_LineupListData.toObject(message.lineup_data, options, _depth + 1);
        return object;
    };

    /**
     * Converts this P_SyncLineupData_SC to JSON.
     * @function toJSON
     * @memberof P_SyncLineupData_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncLineupData_SC.prototype.toJSON = function() {
        return P_SyncLineupData_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncLineupData_SC
     * @function getTypeUrl
     * @memberof P_SyncLineupData_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncLineupData_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncLineupData_SC";
    };

    return P_SyncLineupData_SC;
})();

export const P_SyncLineupUpdate_CS = $root.P_SyncLineupUpdate_CS = (() => {

    /**
     * Properties of a P_SyncLineupUpdate_CS.
     * @typedef {Object} P_SyncLineupUpdate_CS.$Properties
     * @property {number|null} [battle_type] P_SyncLineupUpdate_CS battle_type
     * @property {Array.<number>|null} [hero_ids] P_SyncLineupUpdate_CS hero_ids
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_SyncLineupUpdate_CS.
     * @exports IP_SyncLineupUpdate_CS
     * @interface IP_SyncLineupUpdate_CS
     * @augments P_SyncLineupUpdate_CS.$Properties
     * @deprecated Use P_SyncLineupUpdate_CS.$Properties instead.
     */

    /**
     * Shape of a P_SyncLineupUpdate_CS.
     * @typedef {P_SyncLineupUpdate_CS.$Properties} P_SyncLineupUpdate_CS.$Shape
     */

    /**
     * Constructs a new P_SyncLineupUpdate_CS.
     * @exports P_SyncLineupUpdate_CS
     * @classdesc Represents a P_SyncLineupUpdate_CS.
     * @constructor
     * @param {P_SyncLineupUpdate_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_SyncLineupUpdate_CS = function (properties) {
        this.hero_ids = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_SyncLineupUpdate_CS battle_type.
     * @member {number|null|undefined} battle_type
     * @memberof P_SyncLineupUpdate_CS
     * @instance
     */
    P_SyncLineupUpdate_CS.prototype.battle_type = null;

    /**
     * P_SyncLineupUpdate_CS hero_ids.
     * @member {Array.<number>} hero_ids
     * @memberof P_SyncLineupUpdate_CS
     * @instance
     */
    P_SyncLineupUpdate_CS.prototype.hero_ids = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_SyncLineupUpdate_CS.prototype, "_battle_type", {
        get: $util.oneOfGetter($oneOfFields = ["battle_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_SyncLineupUpdate_CS instance using the specified properties.
     * @function create
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {P_SyncLineupUpdate_CS.$Properties=} [properties] Properties to set
     * @returns {P_SyncLineupUpdate_CS} P_SyncLineupUpdate_CS instance
     * @type {{
     *   (properties: P_SyncLineupUpdate_CS.$Shape): P_SyncLineupUpdate_CS & P_SyncLineupUpdate_CS.$Shape;
     *   (properties?: P_SyncLineupUpdate_CS.$Properties): P_SyncLineupUpdate_CS;
     * }}
     */
    P_SyncLineupUpdate_CS.create = function(properties) {
        return new P_SyncLineupUpdate_CS(properties);
    };

    /**
     * Encodes the specified P_SyncLineupUpdate_CS message. Does not implicitly {@link P_SyncLineupUpdate_CS.verify|verify} messages.
     * @function encode
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {P_SyncLineupUpdate_CS.$Properties} message P_SyncLineupUpdate_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncLineupUpdate_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.battle_type);
        if (message.hero_ids != null && message.hero_ids.length)
            writer.uint32(/* id 2, wireType 2 =*/18).int32s(message.hero_ids);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_SyncLineupUpdate_CS message, length delimited. Does not implicitly {@link P_SyncLineupUpdate_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {P_SyncLineupUpdate_CS.$Properties} message P_SyncLineupUpdate_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_SyncLineupUpdate_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_SyncLineupUpdate_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_SyncLineupUpdate_CS & P_SyncLineupUpdate_CS.$Shape} P_SyncLineupUpdate_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncLineupUpdate_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_SyncLineupUpdate_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.battle_type = reader.int32();
                    message._battle_type = "battle_type";
                    continue;
                }
            case 2: {
                    if (wireType === 2) {
                        if (!(message.hero_ids && message.hero_ids.length))
                            message.hero_ids = [];
                        reader.int32s(message.hero_ids);
                        continue;
                    }
                    if (wireType !== 0)
                        break;
                    if (!(message.hero_ids && message.hero_ids.length))
                        message.hero_ids = [];
                    message.hero_ids.push(reader.int32());
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_SyncLineupUpdate_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_SyncLineupUpdate_CS & P_SyncLineupUpdate_CS.$Shape} P_SyncLineupUpdate_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_SyncLineupUpdate_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_SyncLineupUpdate_CS message.
     * @function verify
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_SyncLineupUpdate_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type")) {
            properties._battle_type = 1;
            if (!$util.isInteger(message.battle_type))
                return "battle_type: integer expected";
        }
        if (message.hero_ids != null && $Object.hasOwnProperty.call(message, "hero_ids")) {
            if (!$Array.isArray(message.hero_ids))
                return "hero_ids: array expected";
            for (let i = 0; i < message.hero_ids.length; ++i)
                if (!$util.isInteger(message.hero_ids[i]))
                    return "hero_ids: integer[] expected";
        }
        return null;
    };

    /**
     * Creates a P_SyncLineupUpdate_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_SyncLineupUpdate_CS} P_SyncLineupUpdate_CS
     */
    P_SyncLineupUpdate_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_SyncLineupUpdate_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_SyncLineupUpdate_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_SyncLineupUpdate_CS();
        if (object.battle_type != null)
            message.battle_type = object.battle_type | 0;
        if (object.hero_ids) {
            if (!$Array.isArray(object.hero_ids))
                throw $TypeError(".P_SyncLineupUpdate_CS.hero_ids: array expected");
            message.hero_ids = $Array(object.hero_ids.length);
            for (let i = 0; i < object.hero_ids.length; ++i)
                message.hero_ids[i] = object.hero_ids[i] | 0;
        }
        return message;
    };

    /**
     * Creates a plain object from a P_SyncLineupUpdate_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {P_SyncLineupUpdate_CS} message P_SyncLineupUpdate_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_SyncLineupUpdate_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.hero_ids = [];
        if (message.battle_type != null && $Object.hasOwnProperty.call(message, "battle_type"))
            object.battle_type = message.battle_type;
        if (message.hero_ids && message.hero_ids.length) {
            object.hero_ids = $Array(message.hero_ids.length);
            for (let j = 0; j < message.hero_ids.length; ++j)
                object.hero_ids[j] = message.hero_ids[j];
        }
        return object;
    };

    /**
     * Converts this P_SyncLineupUpdate_CS to JSON.
     * @function toJSON
     * @memberof P_SyncLineupUpdate_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_SyncLineupUpdate_CS.prototype.toJSON = function() {
        return P_SyncLineupUpdate_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_SyncLineupUpdate_CS
     * @function getTypeUrl
     * @memberof P_SyncLineupUpdate_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_SyncLineupUpdate_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_SyncLineupUpdate_CS";
    };

    return P_SyncLineupUpdate_CS;
})();

export const P_Rank_CS = $root.P_Rank_CS = (() => {

    /**
     * Properties of a P_Rank_CS.
     * @typedef {Object} P_Rank_CS.$Properties
     * @property {number|null} [rank_type] P_Rank_CS rank_type
     * @property {number|null} [rank_offset] P_Rank_CS rank_offset
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Rank_CS.
     * @exports IP_Rank_CS
     * @interface IP_Rank_CS
     * @augments P_Rank_CS.$Properties
     * @deprecated Use P_Rank_CS.$Properties instead.
     */

    /**
     * Shape of a P_Rank_CS.
     * @typedef {P_Rank_CS.$Properties} P_Rank_CS.$Shape
     */

    /**
     * Constructs a new P_Rank_CS.
     * @exports P_Rank_CS
     * @classdesc Represents a P_Rank_CS.
     * @constructor
     * @param {P_Rank_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Rank_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Rank_CS rank_type.
     * @member {number|null|undefined} rank_type
     * @memberof P_Rank_CS
     * @instance
     */
    P_Rank_CS.prototype.rank_type = null;

    /**
     * P_Rank_CS rank_offset.
     * @member {number|null|undefined} rank_offset
     * @memberof P_Rank_CS
     * @instance
     */
    P_Rank_CS.prototype.rank_offset = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_CS.prototype, "_rank_type", {
        get: $util.oneOfGetter($oneOfFields = ["rank_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_CS.prototype, "_rank_offset", {
        get: $util.oneOfGetter($oneOfFields = ["rank_offset"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Rank_CS instance using the specified properties.
     * @function create
     * @memberof P_Rank_CS
     * @static
     * @param {P_Rank_CS.$Properties=} [properties] Properties to set
     * @returns {P_Rank_CS} P_Rank_CS instance
     * @type {{
     *   (properties: P_Rank_CS.$Shape): P_Rank_CS & P_Rank_CS.$Shape;
     *   (properties?: P_Rank_CS.$Properties): P_Rank_CS;
     * }}
     */
    P_Rank_CS.create = function(properties) {
        return new P_Rank_CS(properties);
    };

    /**
     * Encodes the specified P_Rank_CS message. Does not implicitly {@link P_Rank_CS.verify|verify} messages.
     * @function encode
     * @memberof P_Rank_CS
     * @static
     * @param {P_Rank_CS.$Properties} message P_Rank_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Rank_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.rank_type);
        if (message.rank_offset != null && $Object.hasOwnProperty.call(message, "rank_offset"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.rank_offset);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Rank_CS message, length delimited. Does not implicitly {@link P_Rank_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Rank_CS
     * @static
     * @param {P_Rank_CS.$Properties} message P_Rank_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Rank_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Rank_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_Rank_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Rank_CS & P_Rank_CS.$Shape} P_Rank_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Rank_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Rank_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.rank_type = reader.int32();
                    message._rank_type = "rank_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.rank_offset = reader.int32();
                    message._rank_offset = "rank_offset";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Rank_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Rank_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Rank_CS & P_Rank_CS.$Shape} P_Rank_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Rank_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Rank_CS message.
     * @function verify
     * @memberof P_Rank_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Rank_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type")) {
            properties._rank_type = 1;
            if (!$util.isInteger(message.rank_type))
                return "rank_type: integer expected";
        }
        if (message.rank_offset != null && $Object.hasOwnProperty.call(message, "rank_offset")) {
            properties._rank_offset = 1;
            if (!$util.isInteger(message.rank_offset))
                return "rank_offset: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_Rank_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Rank_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Rank_CS} P_Rank_CS
     */
    P_Rank_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Rank_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Rank_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Rank_CS();
        if (object.rank_type != null)
            message.rank_type = object.rank_type | 0;
        if (object.rank_offset != null)
            message.rank_offset = object.rank_offset | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_Rank_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Rank_CS
     * @static
     * @param {P_Rank_CS} message P_Rank_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Rank_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            object.rank_type = message.rank_type;
        if (message.rank_offset != null && $Object.hasOwnProperty.call(message, "rank_offset"))
            object.rank_offset = message.rank_offset;
        return object;
    };

    /**
     * Converts this P_Rank_CS to JSON.
     * @function toJSON
     * @memberof P_Rank_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Rank_CS.prototype.toJSON = function() {
        return P_Rank_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Rank_CS
     * @function getTypeUrl
     * @memberof P_Rank_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Rank_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Rank_CS";
    };

    return P_Rank_CS;
})();

export const P_Rank_SC = $root.P_Rank_SC = (() => {

    /**
     * Properties of a P_Rank_SC.
     * @typedef {Object} P_Rank_SC.$Properties
     * @property {number|null} [rank_type] P_Rank_SC rank_type
     * @property {number|null} [ret] P_Rank_SC ret
     * @property {Array.<PB_RankNode.$Properties>|null} [rank_list] P_Rank_SC rank_list
     * @property {boolean|null} [has_more] P_Rank_SC has_more
     * @property {number|null} [total] P_Rank_SC total
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Rank_SC.
     * @exports IP_Rank_SC
     * @interface IP_Rank_SC
     * @augments P_Rank_SC.$Properties
     * @deprecated Use P_Rank_SC.$Properties instead.
     */

    /**
     * Shape of a P_Rank_SC.
     * @typedef {P_Rank_SC.$Properties} P_Rank_SC.$Shape
     */

    /**
     * Constructs a new P_Rank_SC.
     * @exports P_Rank_SC
     * @classdesc Represents a P_Rank_SC.
     * @constructor
     * @param {P_Rank_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Rank_SC = function (properties) {
        this.rank_list = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Rank_SC rank_type.
     * @member {number|null|undefined} rank_type
     * @memberof P_Rank_SC
     * @instance
     */
    P_Rank_SC.prototype.rank_type = null;

    /**
     * P_Rank_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_Rank_SC
     * @instance
     */
    P_Rank_SC.prototype.ret = null;

    /**
     * P_Rank_SC rank_list.
     * @member {Array.<PB_RankNode.$Properties>} rank_list
     * @memberof P_Rank_SC
     * @instance
     */
    P_Rank_SC.prototype.rank_list = $util.emptyArray;

    /**
     * P_Rank_SC has_more.
     * @member {boolean|null|undefined} has_more
     * @memberof P_Rank_SC
     * @instance
     */
    P_Rank_SC.prototype.has_more = null;

    /**
     * P_Rank_SC total.
     * @member {number|null|undefined} total
     * @memberof P_Rank_SC
     * @instance
     */
    P_Rank_SC.prototype.total = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_SC.prototype, "_rank_type", {
        get: $util.oneOfGetter($oneOfFields = ["rank_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_SC.prototype, "_has_more", {
        get: $util.oneOfGetter($oneOfFields = ["has_more"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Rank_SC.prototype, "_total", {
        get: $util.oneOfGetter($oneOfFields = ["total"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Rank_SC instance using the specified properties.
     * @function create
     * @memberof P_Rank_SC
     * @static
     * @param {P_Rank_SC.$Properties=} [properties] Properties to set
     * @returns {P_Rank_SC} P_Rank_SC instance
     * @type {{
     *   (properties: P_Rank_SC.$Shape): P_Rank_SC & P_Rank_SC.$Shape;
     *   (properties?: P_Rank_SC.$Properties): P_Rank_SC;
     * }}
     */
    P_Rank_SC.create = function(properties) {
        return new P_Rank_SC(properties);
    };

    /**
     * Encodes the specified P_Rank_SC message. Does not implicitly {@link P_Rank_SC.verify|verify} messages.
     * @function encode
     * @memberof P_Rank_SC
     * @static
     * @param {P_Rank_SC.$Properties} message P_Rank_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Rank_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.rank_type);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ret);
        if (message.rank_list != null && message.rank_list.length)
            for (let i = 0; i < message.rank_list.length; ++i)
                $root.PB_RankNode.encode(message.rank_list[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more"))
            writer.uint32(/* id 4, wireType 0 =*/32).bool(message.has_more);
        if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.total);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Rank_SC message, length delimited. Does not implicitly {@link P_Rank_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Rank_SC
     * @static
     * @param {P_Rank_SC.$Properties} message P_Rank_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Rank_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Rank_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_Rank_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Rank_SC & P_Rank_SC.$Shape} P_Rank_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Rank_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Rank_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.rank_type = reader.int32();
                    message._rank_type = "rank_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    if (!(message.rank_list && message.rank_list.length))
                        message.rank_list = [];
                    message.rank_list.push($root.PB_RankNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.has_more = reader.bool();
                    message._has_more = "has_more";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.total = reader.int32();
                    message._total = "total";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Rank_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Rank_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Rank_SC & P_Rank_SC.$Shape} P_Rank_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Rank_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Rank_SC message.
     * @function verify
     * @memberof P_Rank_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Rank_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type")) {
            properties._rank_type = 1;
            if (!$util.isInteger(message.rank_type))
                return "rank_type: integer expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.rank_list != null && $Object.hasOwnProperty.call(message, "rank_list")) {
            if (!$Array.isArray(message.rank_list))
                return "rank_list: array expected";
            for (let i = 0; i < message.rank_list.length; ++i) {
                let error = $root.PB_RankNode.verify(message.rank_list[i], _depth + 1);
                if (error)
                    return "rank_list." + error;
            }
        }
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more")) {
            properties._has_more = 1;
            if (typeof message.has_more !== "boolean")
                return "has_more: boolean expected";
        }
        if (message.total != null && $Object.hasOwnProperty.call(message, "total")) {
            properties._total = 1;
            if (!$util.isInteger(message.total))
                return "total: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_Rank_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Rank_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Rank_SC} P_Rank_SC
     */
    P_Rank_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Rank_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Rank_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Rank_SC();
        if (object.rank_type != null)
            message.rank_type = object.rank_type | 0;
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.rank_list) {
            if (!$Array.isArray(object.rank_list))
                throw $TypeError(".P_Rank_SC.rank_list: array expected");
            message.rank_list = $Array(object.rank_list.length);
            for (let i = 0; i < object.rank_list.length; ++i) {
                if (!$util.isObject(object.rank_list[i]))
                    throw $TypeError(".P_Rank_SC.rank_list: object expected");
                message.rank_list[i] = $root.PB_RankNode.fromObject(object.rank_list[i], _depth + 1);
            }
        }
        if (object.has_more != null)
            message.has_more = $Boolean(object.has_more);
        if (object.total != null)
            message.total = object.total | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_Rank_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Rank_SC
     * @static
     * @param {P_Rank_SC} message P_Rank_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Rank_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.rank_list = [];
        if (message.rank_type != null && $Object.hasOwnProperty.call(message, "rank_type"))
            object.rank_type = message.rank_type;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.rank_list && message.rank_list.length) {
            object.rank_list = $Array(message.rank_list.length);
            for (let j = 0; j < message.rank_list.length; ++j)
                object.rank_list[j] = $root.PB_RankNode.toObject(message.rank_list[j], options, _depth + 1);
        }
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more"))
            object.has_more = message.has_more;
        if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
            object.total = message.total;
        return object;
    };

    /**
     * Converts this P_Rank_SC to JSON.
     * @function toJSON
     * @memberof P_Rank_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Rank_SC.prototype.toJSON = function() {
        return P_Rank_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Rank_SC
     * @function getTypeUrl
     * @memberof P_Rank_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Rank_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Rank_SC";
    };

    return P_Rank_SC;
})();

export const PB_ArenaOpponentInfo = $root.PB_ArenaOpponentInfo = (() => {

    /**
     * Properties of a PB_ArenaOpponentInfo.
     * @typedef {Object} PB_ArenaOpponentInfo.$Properties
     * @property {number|null} [uid] PB_ArenaOpponentInfo uid
     * @property {string|null} [name] PB_ArenaOpponentInfo name
     * @property {string|null} [user_name] PB_ArenaOpponentInfo user_name
     * @property {number|null} [score] PB_ArenaOpponentInfo score
     * @property {Array.<PB_HeroData.$Properties>|null} [heroes] PB_ArenaOpponentInfo heroes
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ArenaOpponentInfo.
     * @exports IPB_ArenaOpponentInfo
     * @interface IPB_ArenaOpponentInfo
     * @augments PB_ArenaOpponentInfo.$Properties
     * @deprecated Use PB_ArenaOpponentInfo.$Properties instead.
     */

    /**
     * Shape of a PB_ArenaOpponentInfo.
     * @typedef {PB_ArenaOpponentInfo.$Properties} PB_ArenaOpponentInfo.$Shape
     */

    /**
     * Constructs a new PB_ArenaOpponentInfo.
     * @exports PB_ArenaOpponentInfo
     * @classdesc Represents a PB_ArenaOpponentInfo.
     * @constructor
     * @param {PB_ArenaOpponentInfo.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ArenaOpponentInfo = function (properties) {
        this.heroes = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ArenaOpponentInfo uid.
     * @member {number|null|undefined} uid
     * @memberof PB_ArenaOpponentInfo
     * @instance
     */
    PB_ArenaOpponentInfo.prototype.uid = null;

    /**
     * PB_ArenaOpponentInfo name.
     * @member {string|null|undefined} name
     * @memberof PB_ArenaOpponentInfo
     * @instance
     */
    PB_ArenaOpponentInfo.prototype.name = null;

    /**
     * PB_ArenaOpponentInfo user_name.
     * @member {string|null|undefined} user_name
     * @memberof PB_ArenaOpponentInfo
     * @instance
     */
    PB_ArenaOpponentInfo.prototype.user_name = null;

    /**
     * PB_ArenaOpponentInfo score.
     * @member {number|null|undefined} score
     * @memberof PB_ArenaOpponentInfo
     * @instance
     */
    PB_ArenaOpponentInfo.prototype.score = null;

    /**
     * PB_ArenaOpponentInfo heroes.
     * @member {Array.<PB_HeroData.$Properties>} heroes
     * @memberof PB_ArenaOpponentInfo
     * @instance
     */
    PB_ArenaOpponentInfo.prototype.heroes = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaOpponentInfo.prototype, "_uid", {
        get: $util.oneOfGetter($oneOfFields = ["uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaOpponentInfo.prototype, "_name", {
        get: $util.oneOfGetter($oneOfFields = ["name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaOpponentInfo.prototype, "_user_name", {
        get: $util.oneOfGetter($oneOfFields = ["user_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaOpponentInfo.prototype, "_score", {
        get: $util.oneOfGetter($oneOfFields = ["score"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ArenaOpponentInfo instance using the specified properties.
     * @function create
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {PB_ArenaOpponentInfo.$Properties=} [properties] Properties to set
     * @returns {PB_ArenaOpponentInfo} PB_ArenaOpponentInfo instance
     * @type {{
     *   (properties: PB_ArenaOpponentInfo.$Shape): PB_ArenaOpponentInfo & PB_ArenaOpponentInfo.$Shape;
     *   (properties?: PB_ArenaOpponentInfo.$Properties): PB_ArenaOpponentInfo;
     * }}
     */
    PB_ArenaOpponentInfo.create = function(properties) {
        return new PB_ArenaOpponentInfo(properties);
    };

    /**
     * Encodes the specified PB_ArenaOpponentInfo message. Does not implicitly {@link PB_ArenaOpponentInfo.verify|verify} messages.
     * @function encode
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {PB_ArenaOpponentInfo.$Properties} message PB_ArenaOpponentInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaOpponentInfo.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.uid);
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.name);
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            writer.uint32(/* id 3, wireType 2 =*/26).string(message.user_name);
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.score);
        if (message.heroes != null && message.heroes.length)
            for (let i = 0; i < message.heroes.length; ++i)
                $root.PB_HeroData.encode(message.heroes[i], writer.uint32(/* id 5, wireType 2 =*/42).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ArenaOpponentInfo message, length delimited. Does not implicitly {@link PB_ArenaOpponentInfo.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {PB_ArenaOpponentInfo.$Properties} message PB_ArenaOpponentInfo message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaOpponentInfo.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ArenaOpponentInfo message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ArenaOpponentInfo & PB_ArenaOpponentInfo.$Shape} PB_ArenaOpponentInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaOpponentInfo.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ArenaOpponentInfo();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.uid = reader.int32();
                    message._uid = "uid";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.name = reader.stringVerify();
                    message._name = "name";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    message.user_name = reader.stringVerify();
                    message._user_name = "user_name";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.score = reader.int32();
                    message._score = "score";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    if (!(message.heroes && message.heroes.length))
                        message.heroes = [];
                    message.heroes.push($root.PB_HeroData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ArenaOpponentInfo message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ArenaOpponentInfo & PB_ArenaOpponentInfo.$Shape} PB_ArenaOpponentInfo
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaOpponentInfo.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ArenaOpponentInfo message.
     * @function verify
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ArenaOpponentInfo.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid")) {
            properties._uid = 1;
            if (!$util.isInteger(message.uid))
                return "uid: integer expected";
        }
        if (message.name != null && $Object.hasOwnProperty.call(message, "name")) {
            properties._name = 1;
            if (!$util.isString(message.name))
                return "name: string expected";
        }
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name")) {
            properties._user_name = 1;
            if (!$util.isString(message.user_name))
                return "user_name: string expected";
        }
        if (message.score != null && $Object.hasOwnProperty.call(message, "score")) {
            properties._score = 1;
            if (!$util.isInteger(message.score))
                return "score: integer expected";
        }
        if (message.heroes != null && $Object.hasOwnProperty.call(message, "heroes")) {
            if (!$Array.isArray(message.heroes))
                return "heroes: array expected";
            for (let i = 0; i < message.heroes.length; ++i) {
                let error = $root.PB_HeroData.verify(message.heroes[i], _depth + 1);
                if (error)
                    return "heroes." + error;
            }
        }
        return null;
    };

    /**
     * Creates a PB_ArenaOpponentInfo message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ArenaOpponentInfo} PB_ArenaOpponentInfo
     */
    PB_ArenaOpponentInfo.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ArenaOpponentInfo)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ArenaOpponentInfo: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ArenaOpponentInfo();
        if (object.uid != null)
            message.uid = object.uid | 0;
        if (object.name != null)
            message.name = $String(object.name);
        if (object.user_name != null)
            message.user_name = $String(object.user_name);
        if (object.score != null)
            message.score = object.score | 0;
        if (object.heroes) {
            if (!$Array.isArray(object.heroes))
                throw $TypeError(".PB_ArenaOpponentInfo.heroes: array expected");
            message.heroes = $Array(object.heroes.length);
            for (let i = 0; i < object.heroes.length; ++i) {
                if (!$util.isObject(object.heroes[i]))
                    throw $TypeError(".PB_ArenaOpponentInfo.heroes: object expected");
                message.heroes[i] = $root.PB_HeroData.fromObject(object.heroes[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a PB_ArenaOpponentInfo message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {PB_ArenaOpponentInfo} message PB_ArenaOpponentInfo
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ArenaOpponentInfo.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.heroes = [];
        if (message.uid != null && $Object.hasOwnProperty.call(message, "uid"))
            object.uid = message.uid;
        if (message.name != null && $Object.hasOwnProperty.call(message, "name"))
            object.name = message.name;
        if (message.user_name != null && $Object.hasOwnProperty.call(message, "user_name"))
            object.user_name = message.user_name;
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            object.score = message.score;
        if (message.heroes && message.heroes.length) {
            object.heroes = $Array(message.heroes.length);
            for (let j = 0; j < message.heroes.length; ++j)
                object.heroes[j] = $root.PB_HeroData.toObject(message.heroes[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this PB_ArenaOpponentInfo to JSON.
     * @function toJSON
     * @memberof PB_ArenaOpponentInfo
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ArenaOpponentInfo.prototype.toJSON = function() {
        return PB_ArenaOpponentInfo.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ArenaOpponentInfo
     * @function getTypeUrl
     * @memberof PB_ArenaOpponentInfo
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ArenaOpponentInfo.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ArenaOpponentInfo";
    };

    return PB_ArenaOpponentInfo;
})();

export const PB_ArenaBattleLog = $root.PB_ArenaBattleLog = (() => {

    /**
     * Properties of a PB_ArenaBattleLog.
     * @typedef {Object} PB_ArenaBattleLog.$Properties
     * @property {number|null} [time] PB_ArenaBattleLog time
     * @property {string|null} [report_key] PB_ArenaBattleLog report_key
     * @property {Array.<PB_HeroData.$Properties>|null} [my_heroes] PB_ArenaBattleLog my_heroes
     * @property {number|null} [opponent_uid] PB_ArenaBattleLog opponent_uid
     * @property {string|null} [opponent_name] PB_ArenaBattleLog opponent_name
     * @property {Array.<PB_HeroData.$Properties>|null} [opponent_heroes] PB_ArenaBattleLog opponent_heroes
     * @property {boolean|null} [is_win] PB_ArenaBattleLog is_win
     * @property {number|null} [score_change] PB_ArenaBattleLog score_change
     * @property {number|null} [score_after] PB_ArenaBattleLog score_after
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a PB_ArenaBattleLog.
     * @exports IPB_ArenaBattleLog
     * @interface IPB_ArenaBattleLog
     * @augments PB_ArenaBattleLog.$Properties
     * @deprecated Use PB_ArenaBattleLog.$Properties instead.
     */

    /**
     * Shape of a PB_ArenaBattleLog.
     * @typedef {PB_ArenaBattleLog.$Properties} PB_ArenaBattleLog.$Shape
     */

    /**
     * Constructs a new PB_ArenaBattleLog.
     * @exports PB_ArenaBattleLog
     * @classdesc Represents a PB_ArenaBattleLog.
     * @constructor
     * @param {PB_ArenaBattleLog.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const PB_ArenaBattleLog = function (properties) {
        this.my_heroes = [];
        this.opponent_heroes = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * PB_ArenaBattleLog time.
     * @member {number|null|undefined} time
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.time = null;

    /**
     * PB_ArenaBattleLog report_key.
     * @member {string|null|undefined} report_key
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.report_key = null;

    /**
     * PB_ArenaBattleLog my_heroes.
     * @member {Array.<PB_HeroData.$Properties>} my_heroes
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.my_heroes = $util.emptyArray;

    /**
     * PB_ArenaBattleLog opponent_uid.
     * @member {number|null|undefined} opponent_uid
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.opponent_uid = null;

    /**
     * PB_ArenaBattleLog opponent_name.
     * @member {string|null|undefined} opponent_name
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.opponent_name = null;

    /**
     * PB_ArenaBattleLog opponent_heroes.
     * @member {Array.<PB_HeroData.$Properties>} opponent_heroes
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.opponent_heroes = $util.emptyArray;

    /**
     * PB_ArenaBattleLog is_win.
     * @member {boolean|null|undefined} is_win
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.is_win = null;

    /**
     * PB_ArenaBattleLog score_change.
     * @member {number|null|undefined} score_change
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.score_change = null;

    /**
     * PB_ArenaBattleLog score_after.
     * @member {number|null|undefined} score_after
     * @memberof PB_ArenaBattleLog
     * @instance
     */
    PB_ArenaBattleLog.prototype.score_after = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_time", {
        get: $util.oneOfGetter($oneOfFields = ["time"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_report_key", {
        get: $util.oneOfGetter($oneOfFields = ["report_key"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_opponent_uid", {
        get: $util.oneOfGetter($oneOfFields = ["opponent_uid"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_opponent_name", {
        get: $util.oneOfGetter($oneOfFields = ["opponent_name"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_is_win", {
        get: $util.oneOfGetter($oneOfFields = ["is_win"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_score_change", {
        get: $util.oneOfGetter($oneOfFields = ["score_change"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(PB_ArenaBattleLog.prototype, "_score_after", {
        get: $util.oneOfGetter($oneOfFields = ["score_after"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new PB_ArenaBattleLog instance using the specified properties.
     * @function create
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {PB_ArenaBattleLog.$Properties=} [properties] Properties to set
     * @returns {PB_ArenaBattleLog} PB_ArenaBattleLog instance
     * @type {{
     *   (properties: PB_ArenaBattleLog.$Shape): PB_ArenaBattleLog & PB_ArenaBattleLog.$Shape;
     *   (properties?: PB_ArenaBattleLog.$Properties): PB_ArenaBattleLog;
     * }}
     */
    PB_ArenaBattleLog.create = function(properties) {
        return new PB_ArenaBattleLog(properties);
    };

    /**
     * Encodes the specified PB_ArenaBattleLog message. Does not implicitly {@link PB_ArenaBattleLog.verify|verify} messages.
     * @function encode
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {PB_ArenaBattleLog.$Properties} message PB_ArenaBattleLog message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaBattleLog.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            writer.uint32(/* id 1, wireType 0 =*/8).int64(message.time);
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            writer.uint32(/* id 2, wireType 2 =*/18).string(message.report_key);
        if (message.my_heroes != null && message.my_heroes.length)
            for (let i = 0; i < message.my_heroes.length; ++i)
                $root.PB_HeroData.encode(message.my_heroes[i], writer.uint32(/* id 3, wireType 2 =*/26).fork(), _depth + 1).ldelim();
        if (message.opponent_uid != null && $Object.hasOwnProperty.call(message, "opponent_uid"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.opponent_uid);
        if (message.opponent_name != null && $Object.hasOwnProperty.call(message, "opponent_name"))
            writer.uint32(/* id 5, wireType 2 =*/42).string(message.opponent_name);
        if (message.opponent_heroes != null && message.opponent_heroes.length)
            for (let i = 0; i < message.opponent_heroes.length; ++i)
                $root.PB_HeroData.encode(message.opponent_heroes[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
        if (message.is_win != null && $Object.hasOwnProperty.call(message, "is_win"))
            writer.uint32(/* id 7, wireType 0 =*/56).bool(message.is_win);
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change"))
            writer.uint32(/* id 8, wireType 0 =*/64).int32(message.score_change);
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after"))
            writer.uint32(/* id 9, wireType 0 =*/72).int32(message.score_after);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified PB_ArenaBattleLog message, length delimited. Does not implicitly {@link PB_ArenaBattleLog.verify|verify} messages.
     * @function encodeDelimited
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {PB_ArenaBattleLog.$Properties} message PB_ArenaBattleLog message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    PB_ArenaBattleLog.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a PB_ArenaBattleLog message from the specified reader or buffer.
     * @function decode
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {PB_ArenaBattleLog & PB_ArenaBattleLog.$Shape} PB_ArenaBattleLog
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaBattleLog.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.PB_ArenaBattleLog();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.time = reader.int64();
                    message._time = "time";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    message.report_key = reader.stringVerify();
                    message._report_key = "report_key";
                    continue;
                }
            case 3: {
                    if (wireType !== 2)
                        break;
                    if (!(message.my_heroes && message.my_heroes.length))
                        message.my_heroes = [];
                    message.my_heroes.push($root.PB_HeroData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.opponent_uid = reader.int32();
                    message._opponent_uid = "opponent_uid";
                    continue;
                }
            case 5: {
                    if (wireType !== 2)
                        break;
                    message.opponent_name = reader.stringVerify();
                    message._opponent_name = "opponent_name";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    if (!(message.opponent_heroes && message.opponent_heroes.length))
                        message.opponent_heroes = [];
                    message.opponent_heroes.push($root.PB_HeroData.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 7: {
                    if (wireType !== 0)
                        break;
                    message.is_win = reader.bool();
                    message._is_win = "is_win";
                    continue;
                }
            case 8: {
                    if (wireType !== 0)
                        break;
                    message.score_change = reader.int32();
                    message._score_change = "score_change";
                    continue;
                }
            case 9: {
                    if (wireType !== 0)
                        break;
                    message.score_after = reader.int32();
                    message._score_after = "score_after";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a PB_ArenaBattleLog message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {PB_ArenaBattleLog & PB_ArenaBattleLog.$Shape} PB_ArenaBattleLog
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    PB_ArenaBattleLog.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a PB_ArenaBattleLog message.
     * @function verify
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    PB_ArenaBattleLog.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.time != null && $Object.hasOwnProperty.call(message, "time")) {
            properties._time = 1;
            if (!$util.isInteger(message.time) && !(message.time && $util.isInteger(message.time.low) && $util.isInteger(message.time.high)))
                return "time: integer|Long expected";
        }
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key")) {
            properties._report_key = 1;
            if (!$util.isString(message.report_key))
                return "report_key: string expected";
        }
        if (message.my_heroes != null && $Object.hasOwnProperty.call(message, "my_heroes")) {
            if (!$Array.isArray(message.my_heroes))
                return "my_heroes: array expected";
            for (let i = 0; i < message.my_heroes.length; ++i) {
                let error = $root.PB_HeroData.verify(message.my_heroes[i], _depth + 1);
                if (error)
                    return "my_heroes." + error;
            }
        }
        if (message.opponent_uid != null && $Object.hasOwnProperty.call(message, "opponent_uid")) {
            properties._opponent_uid = 1;
            if (!$util.isInteger(message.opponent_uid))
                return "opponent_uid: integer expected";
        }
        if (message.opponent_name != null && $Object.hasOwnProperty.call(message, "opponent_name")) {
            properties._opponent_name = 1;
            if (!$util.isString(message.opponent_name))
                return "opponent_name: string expected";
        }
        if (message.opponent_heroes != null && $Object.hasOwnProperty.call(message, "opponent_heroes")) {
            if (!$Array.isArray(message.opponent_heroes))
                return "opponent_heroes: array expected";
            for (let i = 0; i < message.opponent_heroes.length; ++i) {
                let error = $root.PB_HeroData.verify(message.opponent_heroes[i], _depth + 1);
                if (error)
                    return "opponent_heroes." + error;
            }
        }
        if (message.is_win != null && $Object.hasOwnProperty.call(message, "is_win")) {
            properties._is_win = 1;
            if (typeof message.is_win !== "boolean")
                return "is_win: boolean expected";
        }
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change")) {
            properties._score_change = 1;
            if (!$util.isInteger(message.score_change))
                return "score_change: integer expected";
        }
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after")) {
            properties._score_after = 1;
            if (!$util.isInteger(message.score_after))
                return "score_after: integer expected";
        }
        return null;
    };

    /**
     * Creates a PB_ArenaBattleLog message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {PB_ArenaBattleLog} PB_ArenaBattleLog
     */
    PB_ArenaBattleLog.fromObject = function (object, _depth) {
        if (object instanceof $root.PB_ArenaBattleLog)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".PB_ArenaBattleLog: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.PB_ArenaBattleLog();
        if (object.time != null)
            if ($util.Long)
                message.time = $util.Long.fromValue(object.time, false);
            else if (typeof object.time === "string")
                message.time = $parseInt(object.time, 10);
            else if (typeof object.time === "number")
                message.time = object.time;
            else if (typeof object.time === "object")
                message.time = new $util.LongBits(object.time.low >>> 0, object.time.high >>> 0).toNumber();
        if (object.report_key != null)
            message.report_key = $String(object.report_key);
        if (object.my_heroes) {
            if (!$Array.isArray(object.my_heroes))
                throw $TypeError(".PB_ArenaBattleLog.my_heroes: array expected");
            message.my_heroes = $Array(object.my_heroes.length);
            for (let i = 0; i < object.my_heroes.length; ++i) {
                if (!$util.isObject(object.my_heroes[i]))
                    throw $TypeError(".PB_ArenaBattleLog.my_heroes: object expected");
                message.my_heroes[i] = $root.PB_HeroData.fromObject(object.my_heroes[i], _depth + 1);
            }
        }
        if (object.opponent_uid != null)
            message.opponent_uid = object.opponent_uid | 0;
        if (object.opponent_name != null)
            message.opponent_name = $String(object.opponent_name);
        if (object.opponent_heroes) {
            if (!$Array.isArray(object.opponent_heroes))
                throw $TypeError(".PB_ArenaBattleLog.opponent_heroes: array expected");
            message.opponent_heroes = $Array(object.opponent_heroes.length);
            for (let i = 0; i < object.opponent_heroes.length; ++i) {
                if (!$util.isObject(object.opponent_heroes[i]))
                    throw $TypeError(".PB_ArenaBattleLog.opponent_heroes: object expected");
                message.opponent_heroes[i] = $root.PB_HeroData.fromObject(object.opponent_heroes[i], _depth + 1);
            }
        }
        if (object.is_win != null)
            message.is_win = $Boolean(object.is_win);
        if (object.score_change != null)
            message.score_change = object.score_change | 0;
        if (object.score_after != null)
            message.score_after = object.score_after | 0;
        return message;
    };

    /**
     * Creates a plain object from a PB_ArenaBattleLog message. Also converts values to other types if specified.
     * @function toObject
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {PB_ArenaBattleLog} message PB_ArenaBattleLog
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    PB_ArenaBattleLog.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults) {
            object.my_heroes = [];
            object.opponent_heroes = [];
        }
        if (message.time != null && $Object.hasOwnProperty.call(message, "time"))
            if (typeof $BigInt !== "undefined" && options.longs === $BigInt)
                object.time = typeof message.time === "number" ? $BigInt(message.time) : $util.Long.fromBits(message.time.low >>> 0, message.time.high >>> 0, false).toBigInt();
            else if (typeof message.time === "number")
                object.time = options.longs === $String ? $String(message.time) : message.time;
            else
                object.time = options.longs === $String ? $util.Long.prototype.toString.call(message.time) : options.longs === $Number ? new $util.LongBits(message.time.low >>> 0, message.time.high >>> 0).toNumber() : message.time;
        if (message.report_key != null && $Object.hasOwnProperty.call(message, "report_key"))
            object.report_key = message.report_key;
        if (message.my_heroes && message.my_heroes.length) {
            object.my_heroes = $Array(message.my_heroes.length);
            for (let j = 0; j < message.my_heroes.length; ++j)
                object.my_heroes[j] = $root.PB_HeroData.toObject(message.my_heroes[j], options, _depth + 1);
        }
        if (message.opponent_uid != null && $Object.hasOwnProperty.call(message, "opponent_uid"))
            object.opponent_uid = message.opponent_uid;
        if (message.opponent_name != null && $Object.hasOwnProperty.call(message, "opponent_name"))
            object.opponent_name = message.opponent_name;
        if (message.opponent_heroes && message.opponent_heroes.length) {
            object.opponent_heroes = $Array(message.opponent_heroes.length);
            for (let j = 0; j < message.opponent_heroes.length; ++j)
                object.opponent_heroes[j] = $root.PB_HeroData.toObject(message.opponent_heroes[j], options, _depth + 1);
        }
        if (message.is_win != null && $Object.hasOwnProperty.call(message, "is_win"))
            object.is_win = message.is_win;
        if (message.score_change != null && $Object.hasOwnProperty.call(message, "score_change"))
            object.score_change = message.score_change;
        if (message.score_after != null && $Object.hasOwnProperty.call(message, "score_after"))
            object.score_after = message.score_after;
        return object;
    };

    /**
     * Converts this PB_ArenaBattleLog to JSON.
     * @function toJSON
     * @memberof PB_ArenaBattleLog
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    PB_ArenaBattleLog.prototype.toJSON = function() {
        return PB_ArenaBattleLog.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for PB_ArenaBattleLog
     * @function getTypeUrl
     * @memberof PB_ArenaBattleLog
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    PB_ArenaBattleLog.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/PB_ArenaBattleLog";
    };

    return PB_ArenaBattleLog;
})();

export const P_Arena_CS = $root.P_Arena_CS = (() => {

    /**
     * Properties of a P_Arena_CS.
     * @typedef {Object} P_Arena_CS.$Properties
     * @property {number|null} [req_type] P_Arena_CS req_type
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Arena_CS.
     * @exports IP_Arena_CS
     * @interface IP_Arena_CS
     * @augments P_Arena_CS.$Properties
     * @deprecated Use P_Arena_CS.$Properties instead.
     */

    /**
     * Shape of a P_Arena_CS.
     * @typedef {P_Arena_CS.$Properties} P_Arena_CS.$Shape
     */

    /**
     * Constructs a new P_Arena_CS.
     * @exports P_Arena_CS
     * @classdesc Represents a P_Arena_CS.
     * @constructor
     * @param {P_Arena_CS.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Arena_CS = function (properties) {
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Arena_CS req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_Arena_CS
     * @instance
     */
    P_Arena_CS.prototype.req_type = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_CS.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Arena_CS instance using the specified properties.
     * @function create
     * @memberof P_Arena_CS
     * @static
     * @param {P_Arena_CS.$Properties=} [properties] Properties to set
     * @returns {P_Arena_CS} P_Arena_CS instance
     * @type {{
     *   (properties: P_Arena_CS.$Shape): P_Arena_CS & P_Arena_CS.$Shape;
     *   (properties?: P_Arena_CS.$Properties): P_Arena_CS;
     * }}
     */
    P_Arena_CS.create = function(properties) {
        return new P_Arena_CS(properties);
    };

    /**
     * Encodes the specified P_Arena_CS message. Does not implicitly {@link P_Arena_CS.verify|verify} messages.
     * @function encode
     * @memberof P_Arena_CS
     * @static
     * @param {P_Arena_CS.$Properties} message P_Arena_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Arena_CS.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Arena_CS message, length delimited. Does not implicitly {@link P_Arena_CS.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Arena_CS
     * @static
     * @param {P_Arena_CS.$Properties} message P_Arena_CS message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Arena_CS.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Arena_CS message from the specified reader or buffer.
     * @function decode
     * @memberof P_Arena_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Arena_CS & P_Arena_CS.$Shape} P_Arena_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Arena_CS.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Arena_CS();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Arena_CS message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Arena_CS
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Arena_CS & P_Arena_CS.$Shape} P_Arena_CS
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Arena_CS.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Arena_CS message.
     * @function verify
     * @memberof P_Arena_CS
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Arena_CS.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_Arena_CS message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Arena_CS
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Arena_CS} P_Arena_CS
     */
    P_Arena_CS.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Arena_CS)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Arena_CS: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Arena_CS();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_Arena_CS message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Arena_CS
     * @static
     * @param {P_Arena_CS} message P_Arena_CS
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Arena_CS.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        return object;
    };

    /**
     * Converts this P_Arena_CS to JSON.
     * @function toJSON
     * @memberof P_Arena_CS
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Arena_CS.prototype.toJSON = function() {
        return P_Arena_CS.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Arena_CS
     * @function getTypeUrl
     * @memberof P_Arena_CS
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Arena_CS.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Arena_CS";
    };

    return P_Arena_CS;
})();

export const P_Arena_SC = $root.P_Arena_SC = (() => {

    /**
     * Properties of a P_Arena_SC.
     * @typedef {Object} P_Arena_SC.$Properties
     * @property {number|null} [req_type] P_Arena_SC req_type
     * @property {number|null} [ret] P_Arena_SC ret
     * @property {number|null} [score] P_Arena_SC score
     * @property {number|null} [daily_challenge_count] P_Arena_SC daily_challenge_count
     * @property {number|null} [daily_refresh_count] P_Arena_SC daily_refresh_count
     * @property {Array.<PB_ArenaOpponentInfo.$Properties>|null} [opponents] P_Arena_SC opponents
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_Arena_SC.
     * @exports IP_Arena_SC
     * @interface IP_Arena_SC
     * @augments P_Arena_SC.$Properties
     * @deprecated Use P_Arena_SC.$Properties instead.
     */

    /**
     * Shape of a P_Arena_SC.
     * @typedef {P_Arena_SC.$Properties} P_Arena_SC.$Shape
     */

    /**
     * Constructs a new P_Arena_SC.
     * @exports P_Arena_SC
     * @classdesc Represents a P_Arena_SC.
     * @constructor
     * @param {P_Arena_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_Arena_SC = function (properties) {
        this.opponents = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_Arena_SC req_type.
     * @member {number|null|undefined} req_type
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.req_type = null;

    /**
     * P_Arena_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.ret = null;

    /**
     * P_Arena_SC score.
     * @member {number|null|undefined} score
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.score = null;

    /**
     * P_Arena_SC daily_challenge_count.
     * @member {number|null|undefined} daily_challenge_count
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.daily_challenge_count = null;

    /**
     * P_Arena_SC daily_refresh_count.
     * @member {number|null|undefined} daily_refresh_count
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.daily_refresh_count = null;

    /**
     * P_Arena_SC opponents.
     * @member {Array.<PB_ArenaOpponentInfo.$Properties>} opponents
     * @memberof P_Arena_SC
     * @instance
     */
    P_Arena_SC.prototype.opponents = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_SC.prototype, "_req_type", {
        get: $util.oneOfGetter($oneOfFields = ["req_type"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_SC.prototype, "_score", {
        get: $util.oneOfGetter($oneOfFields = ["score"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_SC.prototype, "_daily_challenge_count", {
        get: $util.oneOfGetter($oneOfFields = ["daily_challenge_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_Arena_SC.prototype, "_daily_refresh_count", {
        get: $util.oneOfGetter($oneOfFields = ["daily_refresh_count"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_Arena_SC instance using the specified properties.
     * @function create
     * @memberof P_Arena_SC
     * @static
     * @param {P_Arena_SC.$Properties=} [properties] Properties to set
     * @returns {P_Arena_SC} P_Arena_SC instance
     * @type {{
     *   (properties: P_Arena_SC.$Shape): P_Arena_SC & P_Arena_SC.$Shape;
     *   (properties?: P_Arena_SC.$Properties): P_Arena_SC;
     * }}
     */
    P_Arena_SC.create = function(properties) {
        return new P_Arena_SC(properties);
    };

    /**
     * Encodes the specified P_Arena_SC message. Does not implicitly {@link P_Arena_SC.verify|verify} messages.
     * @function encode
     * @memberof P_Arena_SC
     * @static
     * @param {P_Arena_SC.$Properties} message P_Arena_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Arena_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.req_type);
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 2, wireType 0 =*/16).int32(message.ret);
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            writer.uint32(/* id 3, wireType 0 =*/24).int32(message.score);
        if (message.daily_challenge_count != null && $Object.hasOwnProperty.call(message, "daily_challenge_count"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.daily_challenge_count);
        if (message.daily_refresh_count != null && $Object.hasOwnProperty.call(message, "daily_refresh_count"))
            writer.uint32(/* id 5, wireType 0 =*/40).int32(message.daily_refresh_count);
        if (message.opponents != null && message.opponents.length)
            for (let i = 0; i < message.opponents.length; ++i)
                $root.PB_ArenaOpponentInfo.encode(message.opponents[i], writer.uint32(/* id 6, wireType 2 =*/50).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_Arena_SC message, length delimited. Does not implicitly {@link P_Arena_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_Arena_SC
     * @static
     * @param {P_Arena_SC.$Properties} message P_Arena_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_Arena_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_Arena_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_Arena_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_Arena_SC & P_Arena_SC.$Shape} P_Arena_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Arena_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_Arena_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.req_type = reader.int32();
                    message._req_type = "req_type";
                    continue;
                }
            case 2: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.score = reader.int32();
                    message._score = "score";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.daily_challenge_count = reader.int32();
                    message._daily_challenge_count = "daily_challenge_count";
                    continue;
                }
            case 5: {
                    if (wireType !== 0)
                        break;
                    message.daily_refresh_count = reader.int32();
                    message._daily_refresh_count = "daily_refresh_count";
                    continue;
                }
            case 6: {
                    if (wireType !== 2)
                        break;
                    if (!(message.opponents && message.opponents.length))
                        message.opponents = [];
                    message.opponents.push($root.PB_ArenaOpponentInfo.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_Arena_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_Arena_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_Arena_SC & P_Arena_SC.$Shape} P_Arena_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_Arena_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_Arena_SC message.
     * @function verify
     * @memberof P_Arena_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_Arena_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type")) {
            properties._req_type = 1;
            if (!$util.isInteger(message.req_type))
                return "req_type: integer expected";
        }
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.score != null && $Object.hasOwnProperty.call(message, "score")) {
            properties._score = 1;
            if (!$util.isInteger(message.score))
                return "score: integer expected";
        }
        if (message.daily_challenge_count != null && $Object.hasOwnProperty.call(message, "daily_challenge_count")) {
            properties._daily_challenge_count = 1;
            if (!$util.isInteger(message.daily_challenge_count))
                return "daily_challenge_count: integer expected";
        }
        if (message.daily_refresh_count != null && $Object.hasOwnProperty.call(message, "daily_refresh_count")) {
            properties._daily_refresh_count = 1;
            if (!$util.isInteger(message.daily_refresh_count))
                return "daily_refresh_count: integer expected";
        }
        if (message.opponents != null && $Object.hasOwnProperty.call(message, "opponents")) {
            if (!$Array.isArray(message.opponents))
                return "opponents: array expected";
            for (let i = 0; i < message.opponents.length; ++i) {
                let error = $root.PB_ArenaOpponentInfo.verify(message.opponents[i], _depth + 1);
                if (error)
                    return "opponents." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_Arena_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_Arena_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_Arena_SC} P_Arena_SC
     */
    P_Arena_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_Arena_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_Arena_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_Arena_SC();
        if (object.req_type != null)
            message.req_type = object.req_type | 0;
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.score != null)
            message.score = object.score | 0;
        if (object.daily_challenge_count != null)
            message.daily_challenge_count = object.daily_challenge_count | 0;
        if (object.daily_refresh_count != null)
            message.daily_refresh_count = object.daily_refresh_count | 0;
        if (object.opponents) {
            if (!$Array.isArray(object.opponents))
                throw $TypeError(".P_Arena_SC.opponents: array expected");
            message.opponents = $Array(object.opponents.length);
            for (let i = 0; i < object.opponents.length; ++i) {
                if (!$util.isObject(object.opponents[i]))
                    throw $TypeError(".P_Arena_SC.opponents: object expected");
                message.opponents[i] = $root.PB_ArenaOpponentInfo.fromObject(object.opponents[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_Arena_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_Arena_SC
     * @static
     * @param {P_Arena_SC} message P_Arena_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_Arena_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.opponents = [];
        if (message.req_type != null && $Object.hasOwnProperty.call(message, "req_type"))
            object.req_type = message.req_type;
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.score != null && $Object.hasOwnProperty.call(message, "score"))
            object.score = message.score;
        if (message.daily_challenge_count != null && $Object.hasOwnProperty.call(message, "daily_challenge_count"))
            object.daily_challenge_count = message.daily_challenge_count;
        if (message.daily_refresh_count != null && $Object.hasOwnProperty.call(message, "daily_refresh_count"))
            object.daily_refresh_count = message.daily_refresh_count;
        if (message.opponents && message.opponents.length) {
            object.opponents = $Array(message.opponents.length);
            for (let j = 0; j < message.opponents.length; ++j)
                object.opponents[j] = $root.PB_ArenaOpponentInfo.toObject(message.opponents[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_Arena_SC to JSON.
     * @function toJSON
     * @memberof P_Arena_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_Arena_SC.prototype.toJSON = function() {
        return P_Arena_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_Arena_SC
     * @function getTypeUrl
     * @memberof P_Arena_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_Arena_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_Arena_SC";
    };

    return P_Arena_SC;
})();

export const P_ArenaBattleLog_SC = $root.P_ArenaBattleLog_SC = (() => {

    /**
     * Properties of a P_ArenaBattleLog_SC.
     * @typedef {Object} P_ArenaBattleLog_SC.$Properties
     * @property {number|null} [ret] P_ArenaBattleLog_SC ret
     * @property {Array.<PB_ArenaBattleLog.$Properties>|null} [battle_logs] P_ArenaBattleLog_SC battle_logs
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ArenaBattleLog_SC.
     * @exports IP_ArenaBattleLog_SC
     * @interface IP_ArenaBattleLog_SC
     * @augments P_ArenaBattleLog_SC.$Properties
     * @deprecated Use P_ArenaBattleLog_SC.$Properties instead.
     */

    /**
     * Shape of a P_ArenaBattleLog_SC.
     * @typedef {P_ArenaBattleLog_SC.$Properties} P_ArenaBattleLog_SC.$Shape
     */

    /**
     * Constructs a new P_ArenaBattleLog_SC.
     * @exports P_ArenaBattleLog_SC
     * @classdesc Represents a P_ArenaBattleLog_SC.
     * @constructor
     * @param {P_ArenaBattleLog_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ArenaBattleLog_SC = function (properties) {
        this.battle_logs = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ArenaBattleLog_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_ArenaBattleLog_SC
     * @instance
     */
    P_ArenaBattleLog_SC.prototype.ret = null;

    /**
     * P_ArenaBattleLog_SC battle_logs.
     * @member {Array.<PB_ArenaBattleLog.$Properties>} battle_logs
     * @memberof P_ArenaBattleLog_SC
     * @instance
     */
    P_ArenaBattleLog_SC.prototype.battle_logs = $util.emptyArray;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaBattleLog_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ArenaBattleLog_SC instance using the specified properties.
     * @function create
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {P_ArenaBattleLog_SC.$Properties=} [properties] Properties to set
     * @returns {P_ArenaBattleLog_SC} P_ArenaBattleLog_SC instance
     * @type {{
     *   (properties: P_ArenaBattleLog_SC.$Shape): P_ArenaBattleLog_SC & P_ArenaBattleLog_SC.$Shape;
     *   (properties?: P_ArenaBattleLog_SC.$Properties): P_ArenaBattleLog_SC;
     * }}
     */
    P_ArenaBattleLog_SC.create = function(properties) {
        return new P_ArenaBattleLog_SC(properties);
    };

    /**
     * Encodes the specified P_ArenaBattleLog_SC message. Does not implicitly {@link P_ArenaBattleLog_SC.verify|verify} messages.
     * @function encode
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {P_ArenaBattleLog_SC.$Properties} message P_ArenaBattleLog_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaBattleLog_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret);
        if (message.battle_logs != null && message.battle_logs.length)
            for (let i = 0; i < message.battle_logs.length; ++i)
                $root.PB_ArenaBattleLog.encode(message.battle_logs[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ArenaBattleLog_SC message, length delimited. Does not implicitly {@link P_ArenaBattleLog_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {P_ArenaBattleLog_SC.$Properties} message P_ArenaBattleLog_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaBattleLog_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ArenaBattleLog_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ArenaBattleLog_SC & P_ArenaBattleLog_SC.$Shape} P_ArenaBattleLog_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaBattleLog_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ArenaBattleLog_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.battle_logs && message.battle_logs.length))
                        message.battle_logs = [];
                    message.battle_logs.push($root.PB_ArenaBattleLog.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ArenaBattleLog_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ArenaBattleLog_SC & P_ArenaBattleLog_SC.$Shape} P_ArenaBattleLog_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaBattleLog_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ArenaBattleLog_SC message.
     * @function verify
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ArenaBattleLog_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.battle_logs != null && $Object.hasOwnProperty.call(message, "battle_logs")) {
            if (!$Array.isArray(message.battle_logs))
                return "battle_logs: array expected";
            for (let i = 0; i < message.battle_logs.length; ++i) {
                let error = $root.PB_ArenaBattleLog.verify(message.battle_logs[i], _depth + 1);
                if (error)
                    return "battle_logs." + error;
            }
        }
        return null;
    };

    /**
     * Creates a P_ArenaBattleLog_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ArenaBattleLog_SC} P_ArenaBattleLog_SC
     */
    P_ArenaBattleLog_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ArenaBattleLog_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ArenaBattleLog_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ArenaBattleLog_SC();
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.battle_logs) {
            if (!$Array.isArray(object.battle_logs))
                throw $TypeError(".P_ArenaBattleLog_SC.battle_logs: array expected");
            message.battle_logs = $Array(object.battle_logs.length);
            for (let i = 0; i < object.battle_logs.length; ++i) {
                if (!$util.isObject(object.battle_logs[i]))
                    throw $TypeError(".P_ArenaBattleLog_SC.battle_logs: object expected");
                message.battle_logs[i] = $root.PB_ArenaBattleLog.fromObject(object.battle_logs[i], _depth + 1);
            }
        }
        return message;
    };

    /**
     * Creates a plain object from a P_ArenaBattleLog_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {P_ArenaBattleLog_SC} message P_ArenaBattleLog_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ArenaBattleLog_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.battle_logs = [];
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.battle_logs && message.battle_logs.length) {
            object.battle_logs = $Array(message.battle_logs.length);
            for (let j = 0; j < message.battle_logs.length; ++j)
                object.battle_logs[j] = $root.PB_ArenaBattleLog.toObject(message.battle_logs[j], options, _depth + 1);
        }
        return object;
    };

    /**
     * Converts this P_ArenaBattleLog_SC to JSON.
     * @function toJSON
     * @memberof P_ArenaBattleLog_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ArenaBattleLog_SC.prototype.toJSON = function() {
        return P_ArenaBattleLog_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ArenaBattleLog_SC
     * @function getTypeUrl
     * @memberof P_ArenaBattleLog_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ArenaBattleLog_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ArenaBattleLog_SC";
    };

    return P_ArenaBattleLog_SC;
})();

export const P_ArenaRank_SC = $root.P_ArenaRank_SC = (() => {

    /**
     * Properties of a P_ArenaRank_SC.
     * @typedef {Object} P_ArenaRank_SC.$Properties
     * @property {number|null} [ret] P_ArenaRank_SC ret
     * @property {Array.<PB_ArenaRankNode.$Properties>|null} [rank_list] P_ArenaRank_SC rank_list
     * @property {boolean|null} [has_more] P_ArenaRank_SC has_more
     * @property {number|null} [total] P_ArenaRank_SC total
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */

    /**
     * Properties of a P_ArenaRank_SC.
     * @exports IP_ArenaRank_SC
     * @interface IP_ArenaRank_SC
     * @augments P_ArenaRank_SC.$Properties
     * @deprecated Use P_ArenaRank_SC.$Properties instead.
     */

    /**
     * Shape of a P_ArenaRank_SC.
     * @typedef {P_ArenaRank_SC.$Properties} P_ArenaRank_SC.$Shape
     */

    /**
     * Constructs a new P_ArenaRank_SC.
     * @exports P_ArenaRank_SC
     * @classdesc Represents a P_ArenaRank_SC.
     * @constructor
     * @param {P_ArenaRank_SC.$Properties=} [properties] Properties to set
     * @property {Array.<Uint8Array>} [$unknowns] Unknown fields preserved while decoding when enabled
     */
    const P_ArenaRank_SC = function (properties) {
        this.rank_list = [];
        if (properties)
            for (let keys = $Object.keys(properties), i = 0; i < keys.length; ++i)
                if (properties[keys[i]] != null && keys[i] !== "__proto__")
                    this[keys[i]] = properties[keys[i]];
    };

    /**
     * P_ArenaRank_SC ret.
     * @member {number|null|undefined} ret
     * @memberof P_ArenaRank_SC
     * @instance
     */
    P_ArenaRank_SC.prototype.ret = null;

    /**
     * P_ArenaRank_SC rank_list.
     * @member {Array.<PB_ArenaRankNode.$Properties>} rank_list
     * @memberof P_ArenaRank_SC
     * @instance
     */
    P_ArenaRank_SC.prototype.rank_list = $util.emptyArray;

    /**
     * P_ArenaRank_SC has_more.
     * @member {boolean|null|undefined} has_more
     * @memberof P_ArenaRank_SC
     * @instance
     */
    P_ArenaRank_SC.prototype.has_more = null;

    /**
     * P_ArenaRank_SC total.
     * @member {number|null|undefined} total
     * @memberof P_ArenaRank_SC
     * @instance
     */
    P_ArenaRank_SC.prototype.total = null;

    // OneOf field names bound to virtual getters and setters
    let $oneOfFields;

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaRank_SC.prototype, "_ret", {
        get: $util.oneOfGetter($oneOfFields = ["ret"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaRank_SC.prototype, "_has_more", {
        get: $util.oneOfGetter($oneOfFields = ["has_more"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    // Virtual OneOf for proto3 optional field
    $Object.defineProperty(P_ArenaRank_SC.prototype, "_total", {
        get: $util.oneOfGetter($oneOfFields = ["total"]),
        set: $util.oneOfSetter($oneOfFields)
    });

    /**
     * Creates a new P_ArenaRank_SC instance using the specified properties.
     * @function create
     * @memberof P_ArenaRank_SC
     * @static
     * @param {P_ArenaRank_SC.$Properties=} [properties] Properties to set
     * @returns {P_ArenaRank_SC} P_ArenaRank_SC instance
     * @type {{
     *   (properties: P_ArenaRank_SC.$Shape): P_ArenaRank_SC & P_ArenaRank_SC.$Shape;
     *   (properties?: P_ArenaRank_SC.$Properties): P_ArenaRank_SC;
     * }}
     */
    P_ArenaRank_SC.create = function(properties) {
        return new P_ArenaRank_SC(properties);
    };

    /**
     * Encodes the specified P_ArenaRank_SC message. Does not implicitly {@link P_ArenaRank_SC.verify|verify} messages.
     * @function encode
     * @memberof P_ArenaRank_SC
     * @static
     * @param {P_ArenaRank_SC.$Properties} message P_ArenaRank_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaRank_SC.encode = function (message, writer, _depth) {
        if (!writer)
            writer = $Writer.create();
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            writer.uint32(/* id 1, wireType 0 =*/8).int32(message.ret);
        if (message.rank_list != null && message.rank_list.length)
            for (let i = 0; i < message.rank_list.length; ++i)
                $root.PB_ArenaRankNode.encode(message.rank_list[i], writer.uint32(/* id 2, wireType 2 =*/18).fork(), _depth + 1).ldelim();
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more"))
            writer.uint32(/* id 3, wireType 0 =*/24).bool(message.has_more);
        if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
            writer.uint32(/* id 4, wireType 0 =*/32).int32(message.total);
        if (message.$unknowns != null && $Object.hasOwnProperty.call(message, "$unknowns"))
            for (let i = 0; i < message.$unknowns.length; ++i)
                writer.raw(message.$unknowns[i]);
        return writer;
    };

    /**
     * Encodes the specified P_ArenaRank_SC message, length delimited. Does not implicitly {@link P_ArenaRank_SC.verify|verify} messages.
     * @function encodeDelimited
     * @memberof P_ArenaRank_SC
     * @static
     * @param {P_ArenaRank_SC.$Properties} message P_ArenaRank_SC message or plain object to encode
     * @param {$protobuf.Writer} [writer] Writer to encode to
     * @returns {$protobuf.Writer} Writer
     */
    P_ArenaRank_SC.encodeDelimited = function(message, writer) {
        return this.encode(message, (writer || $Writer.create()).fork()).ldelim();
    };

    /**
     * Decodes a P_ArenaRank_SC message from the specified reader or buffer.
     * @function decode
     * @memberof P_ArenaRank_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @param {number} [length] Message length if known beforehand
     * @returns {P_ArenaRank_SC & P_ArenaRank_SC.$Shape} P_ArenaRank_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaRank_SC.decode = function (reader, length, _end, _depth, _target) {
        if (!(reader instanceof $Reader))
            reader = $Reader.create(reader);
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $Reader.recursionLimit)
            throw $Error("max depth exceeded");
        let end = length === $undefined ? reader.len : reader.pos + length, message = _target || new $root.P_ArenaRank_SC();
        while (reader.pos < end) {
            let start = reader.pos;
            let tag = reader.tag();
            if (tag === _end) {
                _end = $undefined;
                break;
            }
            let wireType = tag & 7;
            switch (tag >>>= 3) {
            case 1: {
                    if (wireType !== 0)
                        break;
                    message.ret = reader.int32();
                    message._ret = "ret";
                    continue;
                }
            case 2: {
                    if (wireType !== 2)
                        break;
                    if (!(message.rank_list && message.rank_list.length))
                        message.rank_list = [];
                    message.rank_list.push($root.PB_ArenaRankNode.decode(reader, reader.uint32(), $undefined, _depth + 1));
                    continue;
                }
            case 3: {
                    if (wireType !== 0)
                        break;
                    message.has_more = reader.bool();
                    message._has_more = "has_more";
                    continue;
                }
            case 4: {
                    if (wireType !== 0)
                        break;
                    message.total = reader.int32();
                    message._total = "total";
                    continue;
                }
            }
            reader.skipType(wireType, _depth, tag);
            if (!reader.discardUnknown) {
                $util.makeProp(message, "$unknowns", false);
                (message.$unknowns || (message.$unknowns = [])).push(reader.raw(start, reader.pos));
            }
        }
        if (_end !== $undefined)
            throw $Error("missing end group");
        return message;
    };

    /**
     * Decodes a P_ArenaRank_SC message from the specified reader or buffer, length delimited.
     * @function decodeDelimited
     * @memberof P_ArenaRank_SC
     * @static
     * @param {$protobuf.Reader|Uint8Array} reader Reader or buffer to decode from
     * @returns {P_ArenaRank_SC & P_ArenaRank_SC.$Shape} P_ArenaRank_SC
     * @throws {Error} If the payload is not a reader or valid buffer
     * @throws {$protobuf.util.ProtocolError} If required fields are missing
     */
    P_ArenaRank_SC.decodeDelimited = function(reader) {
        if (!(reader instanceof $Reader))
            reader = new $Reader(reader);
        return this.decode(reader, reader.uint32());
    };

    /**
     * Verifies a P_ArenaRank_SC message.
     * @function verify
     * @memberof P_ArenaRank_SC
     * @static
     * @param {Object.<string,*>} message Plain object to verify
     * @returns {string|null} `null` if valid, otherwise the reason why it is not
     */
    P_ArenaRank_SC.verify = function (message, _depth) {
        if (typeof message !== "object" || message === null)
            return "object expected";
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            return "max depth exceeded";
        let properties = {};
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret")) {
            properties._ret = 1;
            if (!$util.isInteger(message.ret))
                return "ret: integer expected";
        }
        if (message.rank_list != null && $Object.hasOwnProperty.call(message, "rank_list")) {
            if (!$Array.isArray(message.rank_list))
                return "rank_list: array expected";
            for (let i = 0; i < message.rank_list.length; ++i) {
                let error = $root.PB_ArenaRankNode.verify(message.rank_list[i], _depth + 1);
                if (error)
                    return "rank_list." + error;
            }
        }
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more")) {
            properties._has_more = 1;
            if (typeof message.has_more !== "boolean")
                return "has_more: boolean expected";
        }
        if (message.total != null && $Object.hasOwnProperty.call(message, "total")) {
            properties._total = 1;
            if (!$util.isInteger(message.total))
                return "total: integer expected";
        }
        return null;
    };

    /**
     * Creates a P_ArenaRank_SC message from a plain object. Also converts values to their respective internal types.
     * @function fromObject
     * @memberof P_ArenaRank_SC
     * @static
     * @param {Object.<string,*>} object Plain object
     * @returns {P_ArenaRank_SC} P_ArenaRank_SC
     */
    P_ArenaRank_SC.fromObject = function (object, _depth) {
        if (object instanceof $root.P_ArenaRank_SC)
            return object;
        if (!$util.isObject(object))
            throw $TypeError(".P_ArenaRank_SC: object expected");
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let message = new $root.P_ArenaRank_SC();
        if (object.ret != null)
            message.ret = object.ret | 0;
        if (object.rank_list) {
            if (!$Array.isArray(object.rank_list))
                throw $TypeError(".P_ArenaRank_SC.rank_list: array expected");
            message.rank_list = $Array(object.rank_list.length);
            for (let i = 0; i < object.rank_list.length; ++i) {
                if (!$util.isObject(object.rank_list[i]))
                    throw $TypeError(".P_ArenaRank_SC.rank_list: object expected");
                message.rank_list[i] = $root.PB_ArenaRankNode.fromObject(object.rank_list[i], _depth + 1);
            }
        }
        if (object.has_more != null)
            message.has_more = $Boolean(object.has_more);
        if (object.total != null)
            message.total = object.total | 0;
        return message;
    };

    /**
     * Creates a plain object from a P_ArenaRank_SC message. Also converts values to other types if specified.
     * @function toObject
     * @memberof P_ArenaRank_SC
     * @static
     * @param {P_ArenaRank_SC} message P_ArenaRank_SC
     * @param {$protobuf.IConversionOptions} [options] Conversion options
     * @returns {Object.<string,*>} Plain object
     */
    P_ArenaRank_SC.toObject = function (message, options, _depth) {
        if (!options)
            options = {};
        if (_depth === $undefined)
            _depth = 0;
        if (_depth > $util.recursionLimit)
            throw $Error("max depth exceeded");
        let object = {};
        if (options.arrays || options.defaults)
            object.rank_list = [];
        if (message.ret != null && $Object.hasOwnProperty.call(message, "ret"))
            object.ret = message.ret;
        if (message.rank_list && message.rank_list.length) {
            object.rank_list = $Array(message.rank_list.length);
            for (let j = 0; j < message.rank_list.length; ++j)
                object.rank_list[j] = $root.PB_ArenaRankNode.toObject(message.rank_list[j], options, _depth + 1);
        }
        if (message.has_more != null && $Object.hasOwnProperty.call(message, "has_more"))
            object.has_more = message.has_more;
        if (message.total != null && $Object.hasOwnProperty.call(message, "total"))
            object.total = message.total;
        return object;
    };

    /**
     * Converts this P_ArenaRank_SC to JSON.
     * @function toJSON
     * @memberof P_ArenaRank_SC
     * @instance
     * @returns {Object.<string,*>} JSON object
     */
    P_ArenaRank_SC.prototype.toJSON = function() {
        return P_ArenaRank_SC.toObject(this, $protobuf.util.toJSONOptions);
    };

    /**
     * Gets the type url for P_ArenaRank_SC
     * @function getTypeUrl
     * @memberof P_ArenaRank_SC
     * @static
     * @param {string} [prefix] Custom type url prefix, defaults to `"type.googleapis.com"`
     * @returns {string} The type url
     */
    P_ArenaRank_SC.getTypeUrl = function(prefix) {
        if (prefix === $undefined)
            prefix = "type.googleapis.com";
        return prefix + "/P_ArenaRank_SC";
    };

    return P_ArenaRank_SC;
})();

export {
  $root as default
};
